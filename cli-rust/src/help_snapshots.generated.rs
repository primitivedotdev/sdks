// @generated by scripts/generate-rust-cli-help-snapshots.mjs
// Do not edit by hand.

#[rustfmt::skip]
pub fn lookup(args: &[String]) -> Option<&'static str> {
    let key = args.join("\0");
    match key.as_str() {
        "--help" => Some("Official Primitive CLI: deploy Primitive Functions, send and inspect mail, manage endpoints, all from the terminal. Wraps the @primitivedotdev/sdk runtime client with one-shot commands.\n\nVERSION\n  primitive/0.1.0\n\nUSAGE\n  $ primitive [COMMAND]\n\nTOPICS\n  account\n      Manage your account settings, storage, and webhook secret\n\n  agent\n      Agent signup and authentication API operations\n\n  chat\n      Chat with agents over email. Use `primitive chat <email> <message>` to\n      start, `primitive chat reply <message>` to continue the active chat, or\n      `primitive chat reply <id> <message>` to reply in a specific local chat.\n\n  cli\n      CLI authentication\n\n  deliveries\n      Short aliases for webhook delivery attempts: `primitive deliveries list` and\n      `primitive deliveries replay`.\n\n  domains\n      Claim, verify, manage email domains, and download DNS zone files\n\n  emails\n      List, inspect, and wait for received emails. Prefer task aliases like\n      `primitive emails list`, `primitive emails get`, `primitive emails latest`,\n      `primitive emails wait`, and `primitive emails watch`; generated API names\n      remain available for compatibility.\n\n  endpoints\n      Manage webhook endpoints that receive email events\n\n  filters\n      Manage whitelist and blocklist filter rules\n\n  functions\n      Deploy JavaScript handlers that run on inbound mail. Prefer `primitive\n      functions templates`, `primitive functions init`, `primitive functions\n      deploy`, `primitive functions redeploy`, `primitive functions list`,\n      `primitive functions get`, `primitive functions logs`, and `primitive\n      functions set-secret`; generated API names remain available for\n      compatibility.\n\n  inbox\n      Check inbound email setup and processing readiness. Prefer `primitive inbox\n      setup` for a guided setup path and `primitive inbox status` for the\n      readiness table.\n\n  login\n      Log in to an existing Primitive account\n\n  memories\n      Store durable JSON memories by key. Use `primitive memories\n      set|get|delete|search`; pass `--function <function-id>` for function-scoped\n      memories.\n\n  org\n      Manage org-level (global) resources shared across functions\n\n  otp\n      Authenticate with an emailed one-time code\n\n  payloads\n      Stream-download and decrypt a payload to a file\n\n  payments\n      Collect and pay x402 stablecoin (USDC) payments. Start with `primitive\n      payments register-payout-address`, then `primitive payments\n      create-challenge` to request a payment and `primitive payments pay` to\n      settle one. Settlement is non-custodial; signing happens locally with your\n      wallet key. Manage caps with `primitive payments\n      get-policy|update-spend-policy`.\n\n  registries\n      Create and manage agent registries, publish and resolve agents by handle,\n      and moderate publication requests\n\n  routes\n      Manage recipient routes. Bind an address pattern to an endpoint or function:\n      `primitive routes add alice@d --function <id>`, `primitive routes list`,\n      `primitive routes test <recipient>`, `primitive routes remove <id>`.\n\n  sending\n      Send outbound emails. Prefer `primitive send` for fresh sends and `primitive\n      reply --id <inbound-id>` for replies. Use `primitive domains list` or\n      `primitive inbox status` to find usable sender domains for --from;\n      `primitive sending permissions` lists recipient-scope destinations you may\n      send to.\n\n  sent\n      Short aliases for outbound sent-email history: `primitive sent list` and\n      `primitive sent get`.\n\n  signin\n      Sign in to an existing Primitive account\n\n  signup\n      Start account signup\n\n  templates\n      Browse and install Function templates. Use `primitive templates\n      list-templates`, `primitive templates get-template`, `primitive templates\n      install-template`, and `primitive templates get-template-install`.\n\n  threads\n      Inspect conversation threads spanning received and sent emails. Prefer\n      `primitive threads get --id <thread-id>`; generated API name `primitive\n      threads get-thread --id <thread-id>` remains available.\n\n  wake\n      Schedule and send wake commands to your own functions over signed email, and\n      manage which senders may wake them. Use `primitive wake schedules\n      list|create|get|update|delete|run`, `primitive wake authorizations\n      list|create|update|delete`, and `primitive wake dispatches list`.\n\n  webhook-deliveries\n      View and replay webhook delivery attempts. Prefer `primitive\n      webhook-deliveries list` and `primitive webhook-deliveries replay`.\n\nCOMMANDS\n  autocomplete     Display autocomplete installation instructions.\n  chat             Chat with an agent over email (send and wait for the reply)\n  completion       Output a shell completion script or print setup instructions\n  describe         Describe a single API operation in detail\n  doctor           Check the local environment and live API for common problems\n  help             Display help for primitive.\n  list-operations  List all generated API operations (JSON)\n  login            Log in to an existing account\n  logout           Log out and revoke the saved CLI OAuth grant\n  otp              Start email-code auth\n  reply            Reply to an inbound email\n  search           Search mail (lexical by default; --mode for semantic)\n  semantic-search  Semantic / hybrid / keyword search across received and sent\n                   mail\n  send             Send an email (simplified, agent-friendly)\n  signin           Sign in to an existing account\n  signup           Start account signup\n  whoami           Print the authenticated account (credentials smoke test)\n\n"),
        "--help\x00account" => Some("Manage your account settings, storage, and webhook secret\n\nUSAGE\n  $ primitive account COMMAND\n\nCOMMANDS\n  account get-account            Get account info\n  account get-storage-stats      Get storage usage\n  account get-webhook-secret     Get webhook signing secret\n  account rotate-webhook-secret  Rotate webhook signing secret\n  account show                   Get account info\n  account storage                Get storage usage\n  account update-account         Update account settings\n  account webhook-secret         Get webhook signing secret\n\n"),
        "--help\x00account:get-account" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "--help\x00account:get-storage-stats" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "--help\x00account:get-webhook-secret" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "--help\x00account:rotate-webhook-secret" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "--help\x00account:show" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "--help\x00account:storage" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "--help\x00account:update-account" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "--help\x00account:webhook-secret" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "--help\x00account\x00get-account" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "--help\x00account\x00get-storage-stats" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "--help\x00account\x00get-webhook-secret" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "--help\x00account\x00rotate-webhook-secret" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "--help\x00account\x00show" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "--help\x00account\x00storage" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "--help\x00account\x00update-account" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "--help\x00account\x00webhook-secret" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "--help\x00agent" => Some("Agent signup and authentication API operations\n\nUSAGE\n  $ primitive agent COMMAND\n\nCOMMANDS\n  agent claim                             Start an agent account email claim\n  agent claim-link                        Create a browser claim link\n  agent claim-verify                      Verify an agent account email claim\n  agent create                            Create an emailless agent account\n  agent create-agent-account              Create an emailless agent account\n  agent create-agent-claim-link           Create a browser claim link\n  agent resend-agent-signup-verification  Resend agent signup verification code\n  agent start-agent-claim                 Start an agent account email claim\n  agent start-agent-signup                Start agent account signup\n  agent upgrade                           Upgrade an agent account to developer\n                                          (email confirmation)\n  agent verify-agent-claim                Verify an agent account email claim\n  agent verify-agent-signup               Verify agent signup and create OAuth\n                                          tokens\n\n"),
        "--help\x00agent:claim" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "--help\x00agent:claim-link" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "--help\x00agent:claim-verify" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "--help\x00agent:create" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "--help\x00agent:create-agent-account" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "--help\x00agent:create-agent-claim-link" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "--help\x00agent:resend-agent-signup-verification" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "--help\x00agent:start-agent-claim" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "--help\x00agent:start-agent-signup" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "--help\x00agent:upgrade" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "--help\x00agent:verify-agent-claim" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "--help\x00agent:verify-agent-signup" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "--help\x00agent\x00claim" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "--help\x00agent\x00claim-link" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "--help\x00agent\x00claim-verify" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "--help\x00agent\x00create" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "--help\x00agent\x00create-agent-account" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "--help\x00agent\x00create-agent-claim-link" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "--help\x00agent\x00resend-agent-signup-verification" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "--help\x00agent\x00start-agent-claim" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "--help\x00agent\x00start-agent-signup" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "--help\x00agent\x00upgrade" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "--help\x00agent\x00verify-agent-claim" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "--help\x00agent\x00verify-agent-signup" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "--help\x00autocomplete" => Some("Display autocomplete installation instructions.\n\nUSAGE\n  $ primitive autocomplete [SHELL] [-r]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nFLAGS\n  -r, --refresh-cache  Refresh cache (ignores displaying instructions)\n\nDESCRIPTION\n  Display autocomplete installation instructions.\n\nEXAMPLES\n  $ primitive autocomplete\n\n  $ primitive autocomplete bash\n\n  $ primitive autocomplete zsh\n\n  $ primitive autocomplete powershell\n\n  $ primitive autocomplete --refresh-cache\n\n"),
        "--help\x00autocomplete:create" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "--help\x00autocomplete:script" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "--help\x00autocomplete\x00create" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "--help\x00autocomplete\x00script" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "--help\x00chat" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "--help\x00chat:reply" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "--help\x00chat\x00reply" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "--help\x00cli" => Some("CLI authentication\n\nUSAGE\n  $ primitive cli COMMAND\n\nCOMMANDS\n  cli cli-logout                      Revoke the current CLI OAuth session\n  cli poll-cli-login                  Poll CLI browser login\n  cli resend-cli-signup-verification  Resend CLI signup verification code\n  cli start-cli-login                 Start CLI browser login\n  cli start-cli-signup                Start CLI account signup\n  cli verify-cli-signup               Verify CLI signup and create OAuth session\n\n"),
        "--help\x00cli:cli-logout" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "--help\x00cli:poll-cli-login" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "--help\x00cli:resend-cli-signup-verification" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "--help\x00cli:start-cli-login" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00cli:start-cli-signup" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00cli:verify-cli-signup" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "--help\x00cli\x00cli-logout" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "--help\x00cli\x00poll-cli-login" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "--help\x00cli\x00resend-cli-signup-verification" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "--help\x00cli\x00start-cli-login" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00cli\x00start-cli-signup" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00cli\x00verify-cli-signup" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "--help\x00completion" => Some("Output a shell completion script or print setup instructions\n\nUSAGE\n  $ primitive completion SHELL\n\nARGUMENTS\n  SHELL  (bash|zsh|powershell|fish) Shell type\n\nDESCRIPTION\n  Output a shell completion script or print setup instructions\n\n  Output a sourceable shell completion script, or print setup instructions.\n\n  For fish, and for bash/zsh when the output is piped or redirected (e.g. into a\n  completion file under bash_completion.d or zsh's site-functions), this emits\n  the raw completion script. For bash/zsh in an interactive terminal it prints\n  the human-readable setup instructions instead. This keeps a redirected\n  `primitive completion bash > <file>` safe to source -- the file holds an\n  actual completion function, never instructional prose a shell would choke on.\n\nEXAMPLES\n  $ primitive completion bash >> /etc/bash_completion.d/primitive\n\n  $ primitive completion zsh > /usr/local/share/zsh/site-functions/_primitive\n\n  $ primitive completion fish > ~/.config/fish/completions/primitive.fish\n\n"),
        "--help\x00config" => Some("Manage Primitive CLI request environments\n\nUSAGE\n  $ primitive config [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\nDESCRIPTION\n  Manage Primitive CLI request environments\n\n  Manage local Primitive CLI request environments for API endpoint overrides and\n  request headers.\n\nCOMMANDS\n  config list   List Primitive CLI request environments\n  config reset  Reset Primitive CLI request environments\n  config set    Set a Primitive CLI request environment\n  config use    Switch the active Primitive CLI request environment\n\n"),
        "--help\x00config:list" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "--help\x00config:reset" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "--help\x00config:set" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "--help\x00config:use" => Some("Switch the active Primitive CLI request environment\n\nUSAGE\n  $ primitive config use ENVIRONMENT\n\nARGUMENTS\n  ENVIRONMENT  Environment name to use\n\nDESCRIPTION\n  Switch the active Primitive CLI request environment\n\n  Switch the active Primitive CLI request environment. When this switches to a\n  different environment, the CLI removes saved OAuth credentials so the next\n  authenticated command signs in against the newly active API host.\n\n"),
        "--help\x00config\x00list" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "--help\x00config\x00reset" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "--help\x00config\x00set" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "--help\x00config\x00use" => Some("Switch the active Primitive CLI request environment\n\nUSAGE\n  $ primitive config use ENVIRONMENT\n\nARGUMENTS\n  ENVIRONMENT  Environment name to use\n\nDESCRIPTION\n  Switch the active Primitive CLI request environment\n\n  Switch the active Primitive CLI request environment. When this switches to a\n  different environment, the CLI removes saved OAuth credentials so the next\n  authenticated command signs in against the newly active API host.\n\n"),
        "--help\x00create:autocomplete" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "--help\x00create\x00autocomplete" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "--help\x00deliveries" => Some("Short aliases for webhook delivery attempts: `primitive deliveries list` and `primitive deliveries replay`.\n\nUSAGE\n  $ primitive deliveries COMMAND\n\nCOMMANDS\n  deliveries list    List webhook deliveries\n  deliveries replay  Replay a webhook delivery\n\n"),
        "--help\x00deliveries:list" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "--help\x00deliveries:replay" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "--help\x00deliveries\x00list" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "--help\x00deliveries\x00replay" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "--help\x00describe" => Some("Describe a single API operation in detail\n\nUSAGE\n  $ primitive describe COMMAND\n\nARGUMENTS\n  COMMAND  Command id, alias, or SDK operation name to describe, e.g.\n           `emails:list`, `emails:get-email`, or `getEmail`. Run `primitive\n           list-operations | jq -r '.[] | \"\\(.tagCommand):\\(.command)\n           \\(.operationId)\"'` to enumerate generated operation ids.\n\nDESCRIPTION\n  Describe a single API operation in detail\n\n  Print the full operation manifest entry for a single API command, including\n  the path, request schema, response schema, and per-field descriptions sourced\n  from the OpenAPI spec.\n\n  The manifest entry's `responseSchema` carries the inlined JSON Schema for the\n  operation's 200/201 `data` envelope contents (`$ref`s resolved). Use it to\n  look up what specific response fields mean. Examples:\n\n  # Domain setup records returned by add/verify\n  primitive describe domains:add\n  primitive describe addDomain\n\n  # Which of EmailDetail's sender-shaped fields is canonical?\n  primitive describe emails:get | jq '.responseSchema.properties | keys'\n  primitive describe emails:get | jq -r\n  '.responseSchema.properties.from_email.description'\n\n  # What does each value of SentEmailStatus mean?\n  primitive describe sent:get | jq -r\n  '.responseSchema.properties.status.description'\n\n  `requestSchema` is the same shape for the request body when one exists. For a\n  single field across many operations at once, use `primitive list-operations |\n  jq` instead.\n\nEXAMPLES\n  $ primitive describe addDomain\n\n  $ primitive describe domains:add\n\n  $ primitive describe emails:get\n\n  $ primitive describe sent:get\n\n"),
        "--help\x00doctor" => Some("Check the local environment and live API for common problems\n\nUSAGE\n  $ primitive doctor [--api-key <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n\nDESCRIPTION\n  Check the local environment and live API for common problems\n\n  Run a one-shot environment health check: Node version, proxy env, CLI auth\n  resolution, /account reachability, and verified-domain status. Fails fast on\n  anything that would block other commands and prints actionable hints for each\n  warning or failure.\n\nEXAMPLES\n  $ primitive doctor\n\n  $ primitive doctor --api-key prim_...\n\n"),
        "--help\x00domains" => Some("Claim, verify, manage email domains, and download DNS zone files\n\nUSAGE\n  $ primitive domains COMMAND\n\nCOMMANDS\n  domains add                        Claim a new domain\n  domains add-domain                 Claim a new domain\n  domains delete                     Delete a domain\n  domains delete-domain              Delete a domain\n  domains download-domain-zone-file  Download a DNS zone file for a domain\n  domains list                       List all domains\n  domains list-domains               List all domains\n  domains update                     Update domain settings\n  domains update-domain              Update domain settings\n  domains verify                     Verify domain ownership\n  domains verify-domain              Verify domain ownership\n  domains zone-file                  Download a DNS zone file for a domain\n\n"),
        "--help\x00domains:add" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "--help\x00domains:add-domain" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "--help\x00domains:delete" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "--help\x00domains:delete-domain" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "--help\x00domains:download-domain-zone-file" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "--help\x00domains:list" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "--help\x00domains:list-domains" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "--help\x00domains:update" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "--help\x00domains:update-domain" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "--help\x00domains:verify" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "--help\x00domains:verify-domain" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "--help\x00domains:zone-file" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "--help\x00domains\x00add" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "--help\x00domains\x00add-domain" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "--help\x00domains\x00delete" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "--help\x00domains\x00delete-domain" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "--help\x00domains\x00download-domain-zone-file" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "--help\x00domains\x00list" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "--help\x00domains\x00list-domains" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "--help\x00domains\x00update" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "--help\x00domains\x00update-domain" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "--help\x00domains\x00verify" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "--help\x00domains\x00verify-domain" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "--help\x00domains\x00zone-file" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "--help\x00emails" => Some("List, inspect, and wait for received emails. Prefer task aliases like `primitive emails list`, `primitive emails get`, `primitive emails latest`, `primitive emails wait`, and `primitive emails watch`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive emails COMMAND\n\nCOMMANDS\n  emails conversation           Get the conversation an email belongs to\n  emails delete                 Delete an email\n  emails delete-email           Delete an email\n  emails discard-content        Discard email content\n  emails discard-email-content  Discard email content\n  emails download-attachments   Download email attachments\n  emails download-raw           Download raw email\n  emails download-raw-email     Download raw email\n  emails get                    Get inbound email by id\n  emails get-conversation       Get the conversation an email belongs to\n  emails get-email              Get inbound email by id\n  emails latest                 Show the most recent inbound emails as a compact\n                                table\n  emails list                   List inbound emails\n  emails list-emails            List inbound emails\n  emails replay-email-webhooks  Replay email webhooks\n  emails replay-webhooks        Replay email webhooks\n  emails search                 Search inbound emails\n  emails search-emails          Search inbound emails\n  emails wait                   Wait for matching inbound emails\n  emails watch                  Watch inbound emails with filters\n\n"),
        "--help\x00emails:conversation" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "--help\x00emails:delete" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "--help\x00emails:delete-email" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "--help\x00emails:discard-content" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "--help\x00emails:discard-email-content" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "--help\x00emails:download-attachments" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "--help\x00emails:download-raw" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "--help\x00emails:download-raw-email" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "--help\x00emails:get" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "--help\x00emails:get-conversation" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "--help\x00emails:get-email" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "--help\x00emails:latest" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "--help\x00emails:list" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "--help\x00emails:list-emails" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "--help\x00emails:replay-email-webhooks" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "--help\x00emails:replay-webhooks" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "--help\x00emails:search" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "--help\x00emails:search-emails" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "--help\x00emails:wait" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "--help\x00emails:watch" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "--help\x00emails\x00conversation" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "--help\x00emails\x00delete" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "--help\x00emails\x00delete-email" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "--help\x00emails\x00discard-content" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "--help\x00emails\x00discard-email-content" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "--help\x00emails\x00download-attachments" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "--help\x00emails\x00download-raw" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "--help\x00emails\x00download-raw-email" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "--help\x00emails\x00get" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "--help\x00emails\x00get-conversation" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "--help\x00emails\x00get-email" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "--help\x00emails\x00latest" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "--help\x00emails\x00list" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "--help\x00emails\x00list-emails" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "--help\x00emails\x00replay-email-webhooks" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "--help\x00emails\x00replay-webhooks" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "--help\x00emails\x00search" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "--help\x00emails\x00search-emails" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "--help\x00emails\x00wait" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "--help\x00emails\x00watch" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "--help\x00endpoints" => Some("Manage webhook endpoints that receive email events\n\nUSAGE\n  $ primitive endpoints COMMAND\n\nCOMMANDS\n  endpoints create           Create a webhook endpoint\n  endpoints create-endpoint  Create a webhook endpoint\n  endpoints delete           Delete a webhook endpoint\n  endpoints delete-endpoint  Delete a webhook endpoint\n  endpoints list             List webhook endpoints\n  endpoints list-endpoints   List webhook endpoints\n  endpoints test             Send a test webhook\n  endpoints test-endpoint    Send a test webhook\n  endpoints update           Update a webhook endpoint\n  endpoints update-endpoint  Update a webhook endpoint\n\n"),
        "--help\x00endpoints:create" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00endpoints:create-endpoint" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00endpoints:delete" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "--help\x00endpoints:delete-endpoint" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "--help\x00endpoints:list" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "--help\x00endpoints:list-endpoints" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "--help\x00endpoints:test" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "--help\x00endpoints:test-endpoint" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "--help\x00endpoints:update" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00endpoints:update-endpoint" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00endpoints\x00create" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00endpoints\x00create-endpoint" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00endpoints\x00delete" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "--help\x00endpoints\x00delete-endpoint" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "--help\x00endpoints\x00list" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "--help\x00endpoints\x00list-endpoints" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "--help\x00endpoints\x00test" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "--help\x00endpoints\x00test-endpoint" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "--help\x00endpoints\x00update" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00endpoints\x00update-endpoint" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00filters" => Some("Manage whitelist and blocklist filter rules\n\nUSAGE\n  $ primitive filters COMMAND\n\nCOMMANDS\n  filters create         Create a filter rule\n  filters create-filter  Create a filter rule\n  filters delete         Delete a filter rule\n  filters delete-filter  Delete a filter rule\n  filters list           List filter rules\n  filters list-filters   List filter rules\n  filters update         Update a filter rule\n  filters update-filter  Update a filter rule\n\n"),
        "--help\x00filters:create" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "--help\x00filters:create-filter" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "--help\x00filters:delete" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "--help\x00filters:delete-filter" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "--help\x00filters:list" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "--help\x00filters:list-filters" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "--help\x00filters:update" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "--help\x00filters:update-filter" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "--help\x00filters\x00create" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "--help\x00filters\x00create-filter" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "--help\x00filters\x00delete" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "--help\x00filters\x00delete-filter" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "--help\x00filters\x00list" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "--help\x00filters\x00list-filters" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "--help\x00filters\x00update" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "--help\x00filters\x00update-filter" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "--help\x00functions" => Some("Deploy JavaScript handlers that run on inbound mail. Prefer `primitive functions templates`, `primitive functions init`, `primitive functions deploy`, `primitive functions redeploy`, `primitive functions list`, `primitive functions get`, `primitive functions logs`, and `primitive functions set-secret`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive functions COMMAND\n\nCOMMANDS\n  functions create-function              Deploy a function\n  functions create-function-secret       Create or update a secret\n  functions create-org-secret            Create or update an org secret\n  functions delete                       Delete a function\n  functions delete-function              Delete a function\n  functions delete-function-secret       Delete a secret\n  functions delete-org-secret            Delete an org secret\n  functions delete-secret                Delete a secret\n  functions deploy                       Deploy a new function from a bundled\n                                         handler file\n  functions get                          Get a function\n  functions get-function                 Get a function\n  functions get-function-routing         Get a function's current route binding\n  functions get-function-test-run-trace  Get a function test run trace\n  functions get-org-routing-topology     Get the org's function routing topology\n  functions init                         Scaffold a new Primitive Function\n                                         project ready for functions deploy\n  functions list                         List functions\n  functions list-function-logs           List a function's execution logs\n  functions list-function-secrets        List a function's secrets\n  functions list-functions               List functions\n  functions list-org-secrets             List org-level (global) secrets\n  functions list-secrets                 List a function's secrets\n  functions logs                         List or follow a function's execution\n                                         logs\n  functions redeploy                     Redeploy a function from a bundled\n                                         handler file\n  functions route-get                    Show a function's current route binding\n  functions route-set                    Bind inbound mail to a function\n  functions route-unset                  Unbind any route from a function\n  functions routing-topology             Show the org-wide function routing\n                                         topology\n  functions set-function-route           Bind a route to a function\n  functions set-function-secret          Set a secret by key\n  functions set-org-secret               Set an org secret by key\n  functions set-secret                   Write a function secret (optionally\n                                         redeploying to push it live)\n  functions templates                    List available Primitive Function\n                                         templates\n  functions test                         Trigger a test invocation; with --wait,\n                                         watch it land\n  functions test-function                Trigger a test invocation; with --wait,\n                                         watch it land\n  functions unset-function-route         Unbind any route from a function\n  functions update-function              Update and redeploy a function\n\n"),
        "--help\x00functions:create-function" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "--help\x00functions:create-function-secret" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "--help\x00functions:create-org-secret" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "--help\x00functions:delete" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "--help\x00functions:delete-function" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "--help\x00functions:delete-function-secret" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "--help\x00functions:delete-org-secret" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "--help\x00functions:delete-secret" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "--help\x00functions:deploy" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "--help\x00functions:get" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "--help\x00functions:get-function" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "--help\x00functions:get-function-routing" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "--help\x00functions:get-function-test-run-trace" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "--help\x00functions:get-org-routing-topology" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "--help\x00functions:init" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "--help\x00functions:list" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "--help\x00functions:list-function-logs" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "--help\x00functions:list-function-secrets" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "--help\x00functions:list-functions" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "--help\x00functions:list-org-secrets" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "--help\x00functions:list-secrets" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "--help\x00functions:logs" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "--help\x00functions:redeploy" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "--help\x00functions:route-get" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "--help\x00functions:route-set" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "--help\x00functions:route-unset" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "--help\x00functions:routing-topology" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "--help\x00functions:set-function-route" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00functions:set-function-secret" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "--help\x00functions:set-org-secret" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "--help\x00functions:set-secret" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "--help\x00functions:templates" => Some("List available Primitive Function templates\n\nUSAGE\n  $ primitive functions templates [--json]\n\nGLOBAL FLAGS\n  --json  Format output as json.\n\nDESCRIPTION\n  List available Primitive Function templates\n\n  List Primitive Function templates available to `primitive functions init`.\n\n  The default table is optimized for quick terminal discovery. Use\n  --json when an agent or script needs stable metadata for searching,\n  ranking, or choosing a template programmatically.\n\nEXAMPLES\n  $ primitive functions templates\n\n  $ primitive functions templates --json\n\n  $ primitive functions init my-fn --template email-reply\n\n"),
        "--help\x00functions:test" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "--help\x00functions:test-function" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "--help\x00functions:unset-function-route" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "--help\x00functions:update-function" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "--help\x00functions\x00create-function" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "--help\x00functions\x00create-function-secret" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "--help\x00functions\x00create-org-secret" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "--help\x00functions\x00delete" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "--help\x00functions\x00delete-function" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "--help\x00functions\x00delete-function-secret" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "--help\x00functions\x00delete-org-secret" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "--help\x00functions\x00delete-secret" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "--help\x00functions\x00deploy" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "--help\x00functions\x00get" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "--help\x00functions\x00get-function" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "--help\x00functions\x00get-function-routing" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "--help\x00functions\x00get-function-test-run-trace" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "--help\x00functions\x00get-org-routing-topology" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "--help\x00functions\x00init" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "--help\x00functions\x00list" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "--help\x00functions\x00list-function-logs" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "--help\x00functions\x00list-function-secrets" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "--help\x00functions\x00list-functions" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "--help\x00functions\x00list-org-secrets" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "--help\x00functions\x00list-secrets" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "--help\x00functions\x00logs" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "--help\x00functions\x00redeploy" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "--help\x00functions\x00route-get" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "--help\x00functions\x00route-set" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "--help\x00functions\x00route-unset" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "--help\x00functions\x00routing-topology" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "--help\x00functions\x00set-function-route" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00functions\x00set-function-secret" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "--help\x00functions\x00set-org-secret" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "--help\x00functions\x00set-secret" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "--help\x00functions\x00templates" => Some("List available Primitive Function templates\n\nUSAGE\n  $ primitive functions templates [--json]\n\nGLOBAL FLAGS\n  --json  Format output as json.\n\nDESCRIPTION\n  List available Primitive Function templates\n\n  List Primitive Function templates available to `primitive functions init`.\n\n  The default table is optimized for quick terminal discovery. Use\n  --json when an agent or script needs stable metadata for searching,\n  ranking, or choosing a template programmatically.\n\nEXAMPLES\n  $ primitive functions templates\n\n  $ primitive functions templates --json\n\n  $ primitive functions init my-fn --template email-reply\n\n"),
        "--help\x00functions\x00test" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "--help\x00functions\x00test-function" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "--help\x00functions\x00unset-function-route" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "--help\x00functions\x00update-function" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "--help\x00inbox" => Some("Check inbound email setup and processing readiness. Prefer `primitive inbox setup` for a guided setup path and `primitive inbox status` for the readiness table.\n\nUSAGE\n  $ primitive inbox COMMAND\n\nCOMMANDS\n  inbox get-inbox-status  Show inbound email readiness\n  inbox setup             Guide inbound email setup\n  inbox status            Show inbound email readiness\n\n"),
        "--help\x00inbox:get-inbox-status" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "--help\x00inbox:setup" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "--help\x00inbox:status" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "--help\x00inbox\x00get-inbox-status" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "--help\x00inbox\x00setup" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "--help\x00inbox\x00status" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "--help\x00list-operations" => Some("List all generated API operations (JSON)\n\nUSAGE\n  $ primitive list-operations\n\nDESCRIPTION\n  List all generated API operations (JSON)\n\n  List all generated API operations as JSON. Useful for piping to `jq` to\n  discover available commands, their request/response schemas, and per-field\n  descriptions. For inspecting a single operation in detail, prefer `primitive\n  describe <command-or-operation-name>`.\n\n"),
        "--help\x00login" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "--help\x00login:browser" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "--help\x00login:confirm" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00login:otp" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "--help\x00login:otp:confirm" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00login:otp:resend" => Some("Resend OTP login code\n\nUSAGE\n  $ primitive login otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP login code\n\n  Resend the verification code for a pending OTP login.\n\nEXAMPLES\n  $ primitive login otp resend user@example.com\n\n"),
        "--help\x00login:resend" => Some("Resend email-code login code\n\nUSAGE\n  $ primitive login resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code login code\n\n  Resend the verification code for a pending email-code login.\n\nEXAMPLES\n  $ primitive login resend user@example.com\n\n"),
        "--help\x00login\x00browser" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "--help\x00login\x00confirm" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00login\x00otp" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "--help\x00login\x00otp\x00confirm" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00login\x00otp\x00resend" => Some("Resend OTP login code\n\nUSAGE\n  $ primitive login otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP login code\n\n  Resend the verification code for a pending OTP login.\n\nEXAMPLES\n  $ primitive login otp resend user@example.com\n\n"),
        "--help\x00login\x00resend" => Some("Resend email-code login code\n\nUSAGE\n  $ primitive login resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code login code\n\n  Resend the verification code for a pending email-code login.\n\nEXAMPLES\n  $ primitive login resend user@example.com\n\n"),
        "--help\x00logout" => Some("Log out and revoke the saved CLI OAuth grant\n\nUSAGE\n  $ primitive logout [-f]\n\nFLAGS\n  -f, --force  Remove local CLI credentials, pending email-code auth state, and\n               any credential lock without revoking the server OAuth grant\n\nDESCRIPTION\n  Log out and revoke the saved CLI OAuth grant\n\n  Log out by revoking the saved Primitive CLI OAuth grant and deleting local\n  credentials. Use --force to remove local credentials, pending email-code auth\n  state, and stale credential locks without contacting Primitive.\n\nEXAMPLES\n  $ primitive logout\n\n  $ primitive logout --force\n\n"),
        "--help\x00memories" => Some("Store durable JSON memories by key. Use `primitive memories set|get|delete|search`; pass `--function <function-id>` for function-scoped memories.\n\nUSAGE\n  $ primitive memories COMMAND\n\nCOMMANDS\n  memories delete           Delete a memory\n  memories delete-memory    Delete a memory\n  memories get              Get a memory\n  memories get-memory       Get a memory\n  memories search           Search memories\n  memories search-memories  Search memories\n  memories set              Set a memory\n  memories set-memory       Set a memory\n\n"),
        "--help\x00memories:delete" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "--help\x00memories:delete-memory" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "--help\x00memories:get" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "--help\x00memories:get-memory" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "--help\x00memories:search" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "--help\x00memories:search-memories" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "--help\x00memories:set" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "--help\x00memories:set-memory" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "--help\x00memories\x00delete" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "--help\x00memories\x00delete-memory" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "--help\x00memories\x00get" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "--help\x00memories\x00get-memory" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "--help\x00memories\x00search" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "--help\x00memories\x00search-memories" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "--help\x00memories\x00set" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "--help\x00memories\x00set-memory" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "--help\x00org" => Some("Manage org-level (global) resources shared across functions\n\nUSAGE\n  $ primitive org COMMAND\n\nTOPICS\n  org secrets  Global secrets shared across every function. Use `primitive org\n               secrets list|set|remove`. Changes land in a function on its next\n               redeploy; a function secret of the same name overrides the\n               global.\n\n"),
        "--help\x00org:secrets" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "--help\x00org:secrets:delete" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "--help\x00org:secrets:list" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "--help\x00org:secrets:remove" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "--help\x00org:secrets:set" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "--help\x00org\x00secrets" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "--help\x00org\x00secrets\x00delete" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "--help\x00org\x00secrets\x00list" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "--help\x00org\x00secrets\x00remove" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "--help\x00org\x00secrets\x00set" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "--help\x00otp" => Some("Start email-code auth\n\nUSAGE\n  $ primitive otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start email-code auth\n\n  Start email-code authentication, send a verification code, and save the\n  pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive otp confirm user@example.com 123456\n\nCOMMANDS\n  otp confirm  Confirm email-code auth\n  otp resend   Resend email-code auth code\n\n"),
        "--help\x00otp:confirm" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00otp:resend" => Some("Resend email-code auth code\n\nUSAGE\n  $ primitive otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code auth code\n\n  Resend the verification code for pending email-code authentication.\n\nEXAMPLES\n  $ primitive otp resend user@example.com\n\n"),
        "--help\x00otp\x00confirm" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00otp\x00resend" => Some("Resend email-code auth code\n\nUSAGE\n  $ primitive otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code auth code\n\n  Resend the verification code for pending email-code authentication.\n\nEXAMPLES\n  $ primitive otp resend user@example.com\n\n"),
        "--help\x00payloads" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads COMMAND\n\nCOMMANDS\n  payloads pull  Stream-download and decrypt a payload to a file\n  payloads push  Stream-upload a file as an encrypted payload\n\n"),
        "--help\x00payloads:pull" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "--help\x00payloads:push" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "--help\x00payloads\x00pull" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "--help\x00payloads\x00push" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "--help\x00payments" => Some("Collect and pay x402 stablecoin (USDC) payments. Start with `primitive payments register-payout-address`, then `primitive payments create-challenge` to request a payment and `primitive payments pay` to settle one. Settlement is non-custodial; signing happens locally with your wallet key. Manage caps with `primitive payments get-policy|update-spend-policy`.\n\nUSAGE\n  $ primitive payments COMMAND\n\nCOMMANDS\n  payments challenge-from-email     Print the email x402 challenge object\n                                    derived from a received payment-request\n                                    email\n  payments charge                   Request an x402 payment (create a challenge)\n  payments create-challenge         Create a payment challenge\n  payments create-email-challenge   Create an email-native payment challenge\n  payments get-challenge            Get a payment challenge\n  payments get-spend-policy         Get your spend policy\n  payments list-declined-payments   List declined payments\n  payments list-payout-addresses    List payout addresses\n  payments pay                      Sign and settle an x402 payment challenge\n  payments pay-challenge            Sign and settle an x402 payment challenge\n  payments pay-email                Sign an email x402 challenge and send the\n                                    signed interaction.json (one step; derives\n                                    the challenge from --in-reply-to)\n  payments pay-email-step           Sign an email x402 challenge into a\n                                    payment-step envelope\n  payments register-payout          Register a payout address for receiving x402\n                                    payments\n  payments register-payout-address  Register a payout address for receiving x402\n                                    payments\n  payments update-spend-policy      Update your spend policy\n\n"),
        "--help\x00payments:challenge-from-email" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "--help\x00payments:charge" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "--help\x00payments:create-challenge" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "--help\x00payments:create-email-challenge" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "--help\x00payments:get-challenge" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "--help\x00payments:get-spend-policy" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "--help\x00payments:list-declined-payments" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "--help\x00payments:list-payout-addresses" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "--help\x00payments:pay" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "--help\x00payments:pay-challenge" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "--help\x00payments:pay-email" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "--help\x00payments:pay-email-step" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "--help\x00payments:register-payout" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "--help\x00payments:register-payout-address" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "--help\x00payments:update-spend-policy" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00payments\x00challenge-from-email" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "--help\x00payments\x00charge" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "--help\x00payments\x00create-challenge" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "--help\x00payments\x00create-email-challenge" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "--help\x00payments\x00get-challenge" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "--help\x00payments\x00get-spend-policy" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "--help\x00payments\x00list-declined-payments" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "--help\x00payments\x00list-payout-addresses" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "--help\x00payments\x00pay" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "--help\x00payments\x00pay-challenge" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "--help\x00payments\x00pay-email" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "--help\x00payments\x00pay-email-step" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "--help\x00payments\x00register-payout" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "--help\x00payments\x00register-payout-address" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "--help\x00payments\x00update-spend-policy" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00registries" => Some("Create and manage agent registries, publish and resolve agents by handle, and moderate publication requests\n\nUSAGE\n  $ primitive registries COMMAND\n\nCOMMANDS\n  registries agent                    Get an agent's public profile by address\n  registries agents                   List agents in a registry\n  registries create                   Create a registry\n  registries create-registry          Create a registry\n  registries decide                   Approve or reject a publication request\n  registries decide-registry-request  Approve or reject a publication request\n  registries define                   Define an agent identity\n  registries define-agent             Define an agent identity\n  registries delete-registry          Delete a registry you own\n  registries get                      Get a public registry's metadata\n  registries get-agent                Get an agent's public profile by address\n  registries get-registry             Get a public registry's metadata\n  registries list                     List the registries you own\n  registries list-registries          List the registries you own\n  registries list-registry-agents     List agents in a registry\n  registries list-registry-requests   List pending publication requests\n  registries publish                  Publish an agent into a registry\n  registries publish-agent            Publish an agent into a registry\n  registries requests                 List pending publication requests\n  registries resolve                  Resolve a registry handle to its agent\n  registries resolve-registry-handle  Resolve a registry handle to its agent\n  registries unpublish                Unpublish an agent from a registry\n  registries unpublish-agent          Unpublish an agent from a registry\n  registries update                   Update a registry you own\n  registries update-registry          Update a registry you own\n\n"),
        "--help\x00registries:agent" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "--help\x00registries:agents" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "--help\x00registries:create" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "--help\x00registries:create-registry" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "--help\x00registries:decide" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "--help\x00registries:decide-registry-request" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "--help\x00registries:define" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00registries:define-agent" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00registries:delete-registry" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "--help\x00registries:get" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "--help\x00registries:get-agent" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "--help\x00registries:get-registry" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "--help\x00registries:list" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "--help\x00registries:list-registries" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "--help\x00registries:list-registry-agents" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "--help\x00registries:list-registry-requests" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "--help\x00registries:publish" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00registries:publish-agent" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00registries:requests" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "--help\x00registries:resolve" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "--help\x00registries:resolve-registry-handle" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "--help\x00registries:unpublish" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "--help\x00registries:unpublish-agent" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "--help\x00registries:update" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "--help\x00registries:update-registry" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "--help\x00registries\x00agent" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "--help\x00registries\x00agents" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "--help\x00registries\x00create" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "--help\x00registries\x00create-registry" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "--help\x00registries\x00decide" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "--help\x00registries\x00decide-registry-request" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "--help\x00registries\x00define" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00registries\x00define-agent" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00registries\x00delete-registry" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "--help\x00registries\x00get" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "--help\x00registries\x00get-agent" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "--help\x00registries\x00get-registry" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "--help\x00registries\x00list" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "--help\x00registries\x00list-registries" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "--help\x00registries\x00list-registry-agents" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "--help\x00registries\x00list-registry-requests" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "--help\x00registries\x00publish" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00registries\x00publish-agent" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00registries\x00requests" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "--help\x00registries\x00resolve" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "--help\x00registries\x00resolve-registry-handle" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "--help\x00registries\x00unpublish" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "--help\x00registries\x00unpublish-agent" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "--help\x00registries\x00update" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "--help\x00registries\x00update-registry" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "--help\x00reply" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "--help\x00routes" => Some("Manage recipient routes. Bind an address pattern to an endpoint or function: `primitive routes add alice@d --function <id>`, `primitive routes list`, `primitive routes test <recipient>`, `primitive routes remove <id>`.\n\nUSAGE\n  $ primitive routes COMMAND\n\nCOMMANDS\n  routes add             Add a recipient route\n  routes create-route    Create a recipient route\n  routes delete-route    Delete a recipient route\n  routes list            List recipient routes\n  routes list-routes     List recipient routes\n  routes remove          Remove a recipient route\n  routes reorder         Reorder recipient routes\n  routes reorder-routes  Reorder recipient routes\n  routes simulate-route  Simulate routing for a recipient\n  routes test            Simulate routing for a recipient\n  routes update          Update a recipient route\n  routes update-route    Update a recipient route\n\n"),
        "--help\x00routes:add" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "--help\x00routes:create-route" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "--help\x00routes:delete-route" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "--help\x00routes:list" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "--help\x00routes:list-routes" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "--help\x00routes:remove" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "--help\x00routes:reorder" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "--help\x00routes:reorder-routes" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00routes:simulate-route" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "--help\x00routes:test" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "--help\x00routes:update" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "--help\x00routes:update-route" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "--help\x00routes\x00add" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "--help\x00routes\x00create-route" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "--help\x00routes\x00delete-route" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "--help\x00routes\x00list" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "--help\x00routes\x00list-routes" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "--help\x00routes\x00remove" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "--help\x00routes\x00reorder" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "--help\x00routes\x00reorder-routes" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00routes\x00simulate-route" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "--help\x00routes\x00test" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "--help\x00routes\x00update" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "--help\x00routes\x00update-route" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "--help\x00script:autocomplete" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "--help\x00script\x00autocomplete" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "--help\x00search" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "--help\x00search:semantic-search" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "--help\x00search\x00semantic-search" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "--help\x00semantic-search" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "--help\x00send" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "--help\x00sending" => Some("Send outbound emails. Prefer `primitive send` for fresh sends and `primitive reply --id <inbound-id>` for replies. Use `primitive domains list` or `primitive inbox status` to find usable sender domains for --from; `primitive sending permissions` lists recipient-scope destinations you may send to.\n\nUSAGE\n  $ primitive sending COMMAND\n\nCOMMANDS\n  sending get                   Get a sent email by id\n  sending get-send-permissions  List send-permission rules\n  sending get-sent-email        Get a sent email by id\n  sending list                  List outbound sent emails\n  sending list-sent-emails      List outbound sent emails\n  sending permissions           List send-permission rules\n  sending reply                 Reply to an inbound email\n  sending reply-to-email        Reply to an inbound email\n  sending send                  Send outbound email\n  sending send-email            Send outbound email\n\n"),
        "--help\x00sending:get" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "--help\x00sending:get-send-permissions" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "--help\x00sending:get-sent-email" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "--help\x00sending:list" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "--help\x00sending:list-sent-emails" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "--help\x00sending:permissions" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "--help\x00sending:reply" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00sending:reply-to-email" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00sending:send" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "--help\x00sending:send-email" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "--help\x00sending\x00get" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "--help\x00sending\x00get-send-permissions" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "--help\x00sending\x00get-sent-email" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "--help\x00sending\x00list" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "--help\x00sending\x00list-sent-emails" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "--help\x00sending\x00permissions" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "--help\x00sending\x00reply" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00sending\x00reply-to-email" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00sending\x00send" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "--help\x00sending\x00send-email" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "--help\x00sent" => Some("Short aliases for outbound sent-email history: `primitive sent list` and `primitive sent get`.\n\nUSAGE\n  $ primitive sent COMMAND\n\nCOMMANDS\n  sent get   Get a sent email by id\n  sent list  List outbound sent emails\n\n"),
        "--help\x00sent:get" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "--help\x00sent:list" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "--help\x00sent\x00get" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "--help\x00sent\x00list" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "--help\x00signin" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "--help\x00signin:browser" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "--help\x00signin:confirm" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00signin:otp" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "--help\x00signin:otp:confirm" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00signin:otp:resend" => Some("Resend OTP sign-in code\n\nUSAGE\n  $ primitive signin otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP sign-in code\n\n  Resend the verification code for a pending OTP sign-in.\n\nEXAMPLES\n  $ primitive signin otp resend user@example.com\n\n"),
        "--help\x00signin:resend" => Some("Resend email-code sign-in code\n\nUSAGE\n  $ primitive signin resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code sign-in code\n\n  Resend the verification code for a pending email-code sign-in.\n\nEXAMPLES\n  $ primitive signin resend user@example.com\n\n"),
        "--help\x00signin\x00browser" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "--help\x00signin\x00confirm" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00signin\x00otp" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "--help\x00signin\x00otp\x00confirm" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "--help\x00signin\x00otp\x00resend" => Some("Resend OTP sign-in code\n\nUSAGE\n  $ primitive signin otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP sign-in code\n\n  Resend the verification code for a pending OTP sign-in.\n\nEXAMPLES\n  $ primitive signin otp resend user@example.com\n\n"),
        "--help\x00signin\x00resend" => Some("Resend email-code sign-in code\n\nUSAGE\n  $ primitive signin resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code sign-in code\n\n  Resend the verification code for a pending email-code sign-in.\n\nEXAMPLES\n  $ primitive signin resend user@example.com\n\n"),
        "--help\x00signup" => Some("Start account signup\n\nUSAGE\n  $ primitive signup [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign up\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Start account signup\n\n  Start a Primitive account signup, send an email verification code, and save a\n  pending signup token locally.\n\nEXAMPLES\n  $ primitive signup user@example.com\n\n  $ primitive signup user@example.com --accept-terms\n\n  $ primitive signup user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signup confirm user@example.com 123456\n\nCOMMANDS\n  signup confirm      Confirm account signup\n  signup interactive  Run interactive account signup\n  signup resend       Resend signup verification code\n  signup status       Show pending signup status\n\n"),
        "--help\x00signup:confirm" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "--help\x00signup:interactive" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "--help\x00signup:resend" => Some("Resend signup verification code\n\nUSAGE\n  $ primitive signup resend [EMAIL]\n\nARGUMENTS\n  [EMAIL]  Email address used to start signup. Defaults to the saved pending\n           signup.\n\nDESCRIPTION\n  Resend signup verification code\n\n  Resend the verification code for a pending signup.\n\nEXAMPLES\n  $ primitive signup resend\n\n  $ primitive signup resend user@example.com\n\n"),
        "--help\x00signup:status" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "--help\x00signup\x00confirm" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "--help\x00signup\x00interactive" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "--help\x00signup\x00resend" => Some("Resend signup verification code\n\nUSAGE\n  $ primitive signup resend [EMAIL]\n\nARGUMENTS\n  [EMAIL]  Email address used to start signup. Defaults to the saved pending\n           signup.\n\nDESCRIPTION\n  Resend signup verification code\n\n  Resend the verification code for a pending signup.\n\nEXAMPLES\n  $ primitive signup resend\n\n  $ primitive signup resend user@example.com\n\n"),
        "--help\x00signup\x00status" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "--help\x00templates" => Some("Browse and install Function templates. Use `primitive templates list-templates`, `primitive templates get-template`, `primitive templates install-template`, and `primitive templates get-template-install`.\n\nUSAGE\n  $ primitive templates COMMAND\n\nCOMMANDS\n  templates get-template          Get a function template\n  templates get-template-install  Get template install status\n  templates install-template      Install a function template\n  templates list-templates        List function templates\n\n"),
        "--help\x00templates:get-template" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "--help\x00templates:get-template-install" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "--help\x00templates:install-template" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00templates:list-templates" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "--help\x00templates\x00get-template" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "--help\x00templates\x00get-template-install" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "--help\x00templates\x00install-template" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00templates\x00list-templates" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "--help\x00threads" => Some("Inspect conversation threads spanning received and sent emails. Prefer `primitive threads get --id <thread-id>`; generated API name `primitive threads get-thread --id <thread-id>` remains available.\n\nUSAGE\n  $ primitive threads COMMAND\n\nCOMMANDS\n  threads get         Get a conversation thread by id\n  threads get-thread  Get a conversation thread by id\n\n"),
        "--help\x00threads:get" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "--help\x00threads:get-thread" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "--help\x00threads\x00get" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "--help\x00threads\x00get-thread" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "--help\x00wake" => Some("Schedule and send wake commands to your own functions over signed email, and manage which senders may wake them. Use `primitive wake schedules list|create|get|update|delete|run`, `primitive wake authorizations list|create|update|delete`, and `primitive wake dispatches list`.\n\nUSAGE\n  $ primitive wake COMMAND\n\nTOPICS\n  wake authorizations  Create a wake authorization\n  wake dispatches      List recent wake dispatches\n  wake schedules       Create a wake schedule\n\nCOMMANDS\n  wake create-wake-authorization  Create a wake authorization\n  wake create-wake-schedule       Create a wake schedule\n  wake delete-wake-authorization  Delete a wake authorization\n  wake delete-wake-schedule       Delete a wake schedule\n  wake get-wake-schedule          Get a wake schedule\n  wake list-wake-authorizations   List wake authorizations\n  wake list-wake-dispatches       List recent wake dispatches\n  wake list-wake-schedules        List wake schedules\n  wake run-wake-schedule          Run a wake schedule now\n  wake update-wake-authorization  Update a wake authorization\n  wake update-wake-schedule       Update a wake schedule\n\n"),
        "--help\x00wake:authorizations" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "--help\x00wake:authorizations:create" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "--help\x00wake:authorizations:delete" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "--help\x00wake:authorizations:list" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "--help\x00wake:authorizations:update" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "--help\x00wake:create-wake-authorization" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00wake:create-wake-schedule" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00wake:delete-wake-authorization" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "--help\x00wake:delete-wake-schedule" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "--help\x00wake:dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "--help\x00wake:dispatches:list" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "--help\x00wake:get-wake-schedule" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "--help\x00wake:list-wake-authorizations" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "--help\x00wake:list-wake-dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "--help\x00wake:list-wake-schedules" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "--help\x00wake:run-wake-schedule" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "--help\x00wake:schedules" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "--help\x00wake:schedules:create" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "--help\x00wake:schedules:delete" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "--help\x00wake:schedules:get" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "--help\x00wake:schedules:list" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "--help\x00wake:schedules:run" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "--help\x00wake:schedules:update" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "--help\x00wake:update-wake-authorization" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "--help\x00wake:update-wake-schedule" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00wake\x00authorizations" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "--help\x00wake\x00authorizations\x00create" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "--help\x00wake\x00authorizations\x00delete" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "--help\x00wake\x00authorizations\x00list" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "--help\x00wake\x00authorizations\x00update" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "--help\x00wake\x00create-wake-authorization" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00wake\x00create-wake-schedule" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00wake\x00delete-wake-authorization" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "--help\x00wake\x00delete-wake-schedule" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "--help\x00wake\x00dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "--help\x00wake\x00dispatches\x00list" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "--help\x00wake\x00get-wake-schedule" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "--help\x00wake\x00list-wake-authorizations" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "--help\x00wake\x00list-wake-dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "--help\x00wake\x00list-wake-schedules" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "--help\x00wake\x00run-wake-schedule" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "--help\x00wake\x00schedules" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "--help\x00wake\x00schedules\x00create" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "--help\x00wake\x00schedules\x00delete" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "--help\x00wake\x00schedules\x00get" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "--help\x00wake\x00schedules\x00list" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "--help\x00wake\x00schedules\x00run" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "--help\x00wake\x00schedules\x00update" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "--help\x00wake\x00update-wake-authorization" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "--help\x00wake\x00update-wake-schedule" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "--help\x00webhook-deliveries" => Some("View and replay webhook delivery attempts. Prefer `primitive webhook-deliveries list` and `primitive webhook-deliveries replay`.\n\nUSAGE\n  $ primitive webhook-deliveries COMMAND\n\nCOMMANDS\n  webhook-deliveries list             List webhook deliveries\n  webhook-deliveries list-deliveries  List webhook deliveries\n  webhook-deliveries replay           Replay a webhook delivery\n  webhook-deliveries replay-delivery  Replay a webhook delivery\n\n"),
        "--help\x00webhook-deliveries:list" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "--help\x00webhook-deliveries:list-deliveries" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "--help\x00webhook-deliveries:replay" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "--help\x00webhook-deliveries:replay-delivery" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "--help\x00webhook-deliveries\x00list" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "--help\x00webhook-deliveries\x00list-deliveries" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "--help\x00webhook-deliveries\x00replay" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "--help\x00webhook-deliveries\x00replay-delivery" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "--help\x00whoami" => Some("Print the authenticated account (credentials smoke test)\n\nUSAGE\n  $ primitive whoami [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the full account JSON response. Default output hides\n                     setup and billing internals.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the authenticated account (credentials smoke test)\n\n  Print the account currently authenticated by saved OAuth credentials or an\n  explicit API key. Useful as a credentials smoke test: confirms auth is live\n  and shows which account it belongs to.\n\n  The default output is a concise human summary. Pass --json only when a script\n  intentionally needs the full /account response.\n\nEXAMPLES\n  $ primitive whoami\n\n  $ primitive whoami --api-key prim_...\n\n  $ primitive whoami --json | jq .id\n\n"),
        "account" => Some("Manage your account settings, storage, and webhook secret\n\nUSAGE\n  $ primitive account COMMAND\n\nCOMMANDS\n  account get-account            Get account info\n  account get-storage-stats      Get storage usage\n  account get-webhook-secret     Get webhook signing secret\n  account rotate-webhook-secret  Rotate webhook signing secret\n  account show                   Get account info\n  account storage                Get storage usage\n  account update-account         Update account settings\n  account webhook-secret         Get webhook signing secret\n\n"),
        "account\x00--help" => Some("Manage your account settings, storage, and webhook secret\n\nUSAGE\n  $ primitive account COMMAND\n\nCOMMANDS\n  account get-account            Get account info\n  account get-storage-stats      Get storage usage\n  account get-webhook-secret     Get webhook signing secret\n  account rotate-webhook-secret  Rotate webhook signing secret\n  account show                   Get account info\n  account storage                Get storage usage\n  account update-account         Update account settings\n  account webhook-secret         Get webhook signing secret\n\n"),
        "account\x00-h" => Some("Manage your account settings, storage, and webhook secret\n\nUSAGE\n  $ primitive account COMMAND\n\nCOMMANDS\n  account get-account            Get account info\n  account get-storage-stats      Get storage usage\n  account get-webhook-secret     Get webhook signing secret\n  account rotate-webhook-secret  Rotate webhook signing secret\n  account show                   Get account info\n  account storage                Get storage usage\n  account update-account         Update account settings\n  account webhook-secret         Get webhook signing secret\n\n"),
        "account:get-account\x00--api-key\x00prim_test\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:get-account\x00--envelope\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:get-account\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:get-account\x00--json\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:get-account\x00--time\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:get-storage-stats\x00--api-key\x00prim_test\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:get-storage-stats\x00--envelope\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:get-storage-stats\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:get-storage-stats\x00--json\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:get-storage-stats\x00--time\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:get-webhook-secret\x00--api-key\x00prim_test\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account:get-webhook-secret\x00--envelope\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account:get-webhook-secret\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account:get-webhook-secret\x00--json\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account:get-webhook-secret\x00--time\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account:rotate-webhook-secret\x00--api-key\x00prim_test\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account:rotate-webhook-secret\x00--envelope\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account:rotate-webhook-secret\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account:rotate-webhook-secret\x00--json\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account:rotate-webhook-secret\x00--time\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account:show\x00--api-key\x00prim_test\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:show\x00--envelope\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:show\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:show\x00--json\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:show\x00--time\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account:storage\x00--api-key\x00prim_test\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:storage\x00--envelope\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:storage\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:storage\x00--json\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:storage\x00--time\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account:update-account\x00--api-key\x00prim_test\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:update-account\x00--body-file\x00value\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:update-account\x00--discard-content-on-webhook-confirmed\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:update-account\x00--envelope\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:update-account\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:update-account\x00--json\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:update-account\x00--no-discard-content-on-webhook-confirmed\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:update-account\x00--raw-body\x00value\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:update-account\x00--spam-threshold\x00value\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:update-account\x00--time\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account:webhook-secret\x00--api-key\x00prim_test\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account:webhook-secret\x00--envelope\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account:webhook-secret\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account:webhook-secret\x00--json\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account:webhook-secret\x00--time\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00get-account\x00--api-key\x00prim_test\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00get-account\x00--envelope\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00get-account\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00get-account\x00--json\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00get-account\x00--time\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00get-storage-stats\x00--api-key\x00prim_test\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00get-storage-stats\x00--envelope\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00get-storage-stats\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00get-storage-stats\x00--json\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00get-storage-stats\x00--time\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00get-webhook-secret\x00--api-key\x00prim_test\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00get-webhook-secret\x00--envelope\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00get-webhook-secret\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00get-webhook-secret\x00--json\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00get-webhook-secret\x00--time\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00rotate-webhook-secret\x00--api-key\x00prim_test\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account\x00rotate-webhook-secret\x00--envelope\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account\x00rotate-webhook-secret\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account\x00rotate-webhook-secret\x00--json\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account\x00rotate-webhook-secret\x00--time\x00--help" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "account\x00show\x00--api-key\x00prim_test\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00show\x00--envelope\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00show\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00show\x00--json\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00show\x00--time\x00--help" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "account\x00storage\x00--api-key\x00prim_test\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00storage\x00--envelope\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00storage\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00storage\x00--json\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00storage\x00--time\x00--help" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "account\x00update-account\x00--api-key\x00prim_test\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00update-account\x00--body-file\x00value\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00update-account\x00--discard-content-on-webhook-confirmed\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00update-account\x00--envelope\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00update-account\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00update-account\x00--json\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00update-account\x00--no-discard-content-on-webhook-confirmed\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00update-account\x00--raw-body\x00value\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00update-account\x00--spam-threshold\x00value\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00update-account\x00--time\x00--help" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "account\x00webhook-secret\x00--api-key\x00prim_test\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00webhook-secret\x00--envelope\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00webhook-secret\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00webhook-secret\x00--json\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "account\x00webhook-secret\x00--time\x00--help" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "agent" => Some("Agent signup and authentication API operations\n\nUSAGE\n  $ primitive agent COMMAND\n\nCOMMANDS\n  agent claim                             Start an agent account email claim\n  agent claim-link                        Create a browser claim link\n  agent claim-verify                      Verify an agent account email claim\n  agent create                            Create an emailless agent account\n  agent create-agent-account              Create an emailless agent account\n  agent create-agent-claim-link           Create a browser claim link\n  agent resend-agent-signup-verification  Resend agent signup verification code\n  agent start-agent-claim                 Start an agent account email claim\n  agent start-agent-signup                Start agent account signup\n  agent upgrade                           Upgrade an agent account to developer\n                                          (email confirmation)\n  agent verify-agent-claim                Verify an agent account email claim\n  agent verify-agent-signup               Verify agent signup and create OAuth\n                                          tokens\n\n"),
        "agent\x00--help" => Some("Agent signup and authentication API operations\n\nUSAGE\n  $ primitive agent COMMAND\n\nCOMMANDS\n  agent claim                             Start an agent account email claim\n  agent claim-link                        Create a browser claim link\n  agent claim-verify                      Verify an agent account email claim\n  agent create                            Create an emailless agent account\n  agent create-agent-account              Create an emailless agent account\n  agent create-agent-claim-link           Create a browser claim link\n  agent resend-agent-signup-verification  Resend agent signup verification code\n  agent start-agent-claim                 Start an agent account email claim\n  agent start-agent-signup                Start agent account signup\n  agent upgrade                           Upgrade an agent account to developer\n                                          (email confirmation)\n  agent verify-agent-claim                Verify an agent account email claim\n  agent verify-agent-signup               Verify agent signup and create OAuth\n                                          tokens\n\n"),
        "agent\x00-h" => Some("Agent signup and authentication API operations\n\nUSAGE\n  $ primitive agent COMMAND\n\nCOMMANDS\n  agent claim                             Start an agent account email claim\n  agent claim-link                        Create a browser claim link\n  agent claim-verify                      Verify an agent account email claim\n  agent create                            Create an emailless agent account\n  agent create-agent-account              Create an emailless agent account\n  agent create-agent-claim-link           Create a browser claim link\n  agent resend-agent-signup-verification  Resend agent signup verification code\n  agent start-agent-claim                 Start an agent account email claim\n  agent start-agent-signup                Start agent account signup\n  agent upgrade                           Upgrade an agent account to developer\n                                          (email confirmation)\n  agent verify-agent-claim                Verify an agent account email claim\n  agent verify-agent-signup               Verify agent signup and create OAuth\n                                          tokens\n\n"),
        "agent:claim\x00--api-key\x00prim_test\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:claim\x00--body-file\x00value\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:claim\x00--email\x00user@example.com\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:claim\x00--envelope\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:claim\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:claim\x00--json\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:claim\x00--raw-body\x00value\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:claim\x00--time\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:claim-link\x00--api-key\x00prim_test\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:claim-link\x00--body-file\x00value\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:claim-link\x00--envelope\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:claim-link\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:claim-link\x00--json\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:claim-link\x00--raw-body\x00value\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:claim-link\x00--time\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:claim-verify\x00--api-key\x00prim_test\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:claim-verify\x00--body-file\x00value\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:claim-verify\x00--envelope\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:claim-verify\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:claim-verify\x00--json\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:claim-verify\x00--raw-body\x00value\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:claim-verify\x00--time\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:claim-verify\x00--verification-code\x00123456\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:create\x00--api-key\x00prim_test\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create\x00--body-file\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create\x00--device-name\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create\x00--envelope\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create\x00--json\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create\x00--no-terms-accepted\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create\x00--raw-body\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create\x00--terms-accepted\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create\x00--time\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--api-key\x00prim_test\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--body-file\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--device-name\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--envelope\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--json\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--no-terms-accepted\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--raw-body\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--terms-accepted\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-account\x00--time\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent:create-agent-claim-link\x00--api-key\x00prim_test\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:create-agent-claim-link\x00--body-file\x00value\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:create-agent-claim-link\x00--envelope\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:create-agent-claim-link\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:create-agent-claim-link\x00--json\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:create-agent-claim-link\x00--raw-body\x00value\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:create-agent-claim-link\x00--time\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent:resend-agent-signup-verification\x00--api-key\x00prim_test\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent:resend-agent-signup-verification\x00--body-file\x00value\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent:resend-agent-signup-verification\x00--envelope\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent:resend-agent-signup-verification\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent:resend-agent-signup-verification\x00--json\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent:resend-agent-signup-verification\x00--raw-body\x00value\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent:resend-agent-signup-verification\x00--signup-token\x00value\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent:resend-agent-signup-verification\x00--time\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent:start-agent-claim\x00--api-key\x00prim_test\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:start-agent-claim\x00--body-file\x00value\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:start-agent-claim\x00--email\x00user@example.com\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:start-agent-claim\x00--envelope\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:start-agent-claim\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:start-agent-claim\x00--json\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:start-agent-claim\x00--raw-body\x00value\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:start-agent-claim\x00--time\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent:start-agent-signup\x00--api-key\x00prim_test\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--body-file\x00value\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--device-name\x00value\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--email\x00user@example.com\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--envelope\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--json\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--no-terms-accepted\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--raw-body\x00value\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--signup-code\x00123456\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--terms-accepted\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:start-agent-signup\x00--time\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent:upgrade\x00--api-base-url\x00value\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent:upgrade\x00--api-key\x00prim_test\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent:upgrade\x00--code\x00123456\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent:upgrade\x00--email\x00user@example.com\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent:upgrade\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent:verify-agent-claim\x00--api-key\x00prim_test\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:verify-agent-claim\x00--body-file\x00value\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:verify-agent-claim\x00--envelope\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:verify-agent-claim\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:verify-agent-claim\x00--json\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:verify-agent-claim\x00--raw-body\x00value\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:verify-agent-claim\x00--time\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:verify-agent-claim\x00--verification-code\x00123456\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent:verify-agent-signup\x00--api-key\x00prim_test\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--body-file\x00value\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--code\x00123456\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--envelope\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--json\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--org-id\x00fn_123\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--raw-body\x00value\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--signup-token\x00value\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--time\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent:verify-agent-signup\x00--verification-code\x00123456\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00claim\x00--api-key\x00prim_test\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00claim\x00--body-file\x00value\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00claim\x00--email\x00user@example.com\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00claim\x00--envelope\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00claim\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00claim\x00--json\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00claim\x00--raw-body\x00value\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00claim\x00--time\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00claim-link\x00--api-key\x00prim_test\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00claim-link\x00--body-file\x00value\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00claim-link\x00--envelope\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00claim-link\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00claim-link\x00--json\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00claim-link\x00--raw-body\x00value\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00claim-link\x00--time\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00claim-verify\x00--api-key\x00prim_test\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00claim-verify\x00--body-file\x00value\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00claim-verify\x00--envelope\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00claim-verify\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00claim-verify\x00--json\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00claim-verify\x00--raw-body\x00value\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00claim-verify\x00--time\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00claim-verify\x00--verification-code\x00123456\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00create\x00--api-key\x00prim_test\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create\x00--body-file\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create\x00--device-name\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create\x00--envelope\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create\x00--json\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create\x00--no-terms-accepted\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create\x00--raw-body\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create\x00--terms-accepted\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create\x00--time\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--api-key\x00prim_test\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--body-file\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--device-name\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--envelope\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--json\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--no-terms-accepted\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--raw-body\x00value\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--terms-accepted\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-account\x00--time\x00--help" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "agent\x00create-agent-claim-link\x00--api-key\x00prim_test\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00create-agent-claim-link\x00--body-file\x00value\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00create-agent-claim-link\x00--envelope\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00create-agent-claim-link\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00create-agent-claim-link\x00--json\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00create-agent-claim-link\x00--raw-body\x00value\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00create-agent-claim-link\x00--time\x00--help" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "agent\x00resend-agent-signup-verification\x00--api-key\x00prim_test\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent\x00resend-agent-signup-verification\x00--body-file\x00value\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent\x00resend-agent-signup-verification\x00--envelope\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent\x00resend-agent-signup-verification\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent\x00resend-agent-signup-verification\x00--json\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent\x00resend-agent-signup-verification\x00--raw-body\x00value\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent\x00resend-agent-signup-verification\x00--signup-token\x00value\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent\x00resend-agent-signup-verification\x00--time\x00--help" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "agent\x00start-agent-claim\x00--api-key\x00prim_test\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00start-agent-claim\x00--body-file\x00value\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00start-agent-claim\x00--email\x00user@example.com\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00start-agent-claim\x00--envelope\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00start-agent-claim\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00start-agent-claim\x00--json\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00start-agent-claim\x00--raw-body\x00value\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00start-agent-claim\x00--time\x00--help" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "agent\x00start-agent-signup\x00--api-key\x00prim_test\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--body-file\x00value\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--device-name\x00value\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--email\x00user@example.com\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--envelope\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--json\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--no-terms-accepted\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--raw-body\x00value\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--signup-code\x00123456\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--terms-accepted\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00start-agent-signup\x00--time\x00--help" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "agent\x00upgrade\x00--api-base-url\x00value\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent\x00upgrade\x00--api-key\x00prim_test\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent\x00upgrade\x00--code\x00123456\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent\x00upgrade\x00--email\x00user@example.com\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent\x00upgrade\x00--help" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "agent\x00verify-agent-claim\x00--api-key\x00prim_test\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00verify-agent-claim\x00--body-file\x00value\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00verify-agent-claim\x00--envelope\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00verify-agent-claim\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00verify-agent-claim\x00--json\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00verify-agent-claim\x00--raw-body\x00value\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00verify-agent-claim\x00--time\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00verify-agent-claim\x00--verification-code\x00123456\x00--help" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "agent\x00verify-agent-signup\x00--api-key\x00prim_test\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--body-file\x00value\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--code\x00123456\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--envelope\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--json\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--org-id\x00fn_123\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--raw-body\x00value\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--signup-token\x00value\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--time\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "agent\x00verify-agent-signup\x00--verification-code\x00123456\x00--help" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "autocomplete\x00--help" => Some("Display autocomplete installation instructions.\n\nUSAGE\n  $ primitive autocomplete [SHELL] [-r]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nFLAGS\n  -r, --refresh-cache  Refresh cache (ignores displaying instructions)\n\nDESCRIPTION\n  Display autocomplete installation instructions.\n\nEXAMPLES\n  $ primitive autocomplete\n\n  $ primitive autocomplete bash\n\n  $ primitive autocomplete zsh\n\n  $ primitive autocomplete powershell\n\n  $ primitive autocomplete --refresh-cache\n\n"),
        "autocomplete\x00--refresh-cache\x00--help" => Some("Display autocomplete installation instructions.\n\nUSAGE\n  $ primitive autocomplete [SHELL] [-r]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nFLAGS\n  -r, --refresh-cache  Refresh cache (ignores displaying instructions)\n\nDESCRIPTION\n  Display autocomplete installation instructions.\n\nEXAMPLES\n  $ primitive autocomplete\n\n  $ primitive autocomplete bash\n\n  $ primitive autocomplete zsh\n\n  $ primitive autocomplete powershell\n\n  $ primitive autocomplete --refresh-cache\n\n"),
        "autocomplete\x00-r\x00--help" => Some("Display autocomplete installation instructions.\n\nUSAGE\n  $ primitive autocomplete [SHELL] [-r]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nFLAGS\n  -r, --refresh-cache  Refresh cache (ignores displaying instructions)\n\nDESCRIPTION\n  Display autocomplete installation instructions.\n\nEXAMPLES\n  $ primitive autocomplete\n\n  $ primitive autocomplete bash\n\n  $ primitive autocomplete zsh\n\n  $ primitive autocomplete powershell\n\n  $ primitive autocomplete --refresh-cache\n\n"),
        "autocomplete:create\x00--help" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "autocomplete:script\x00--help" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "autocomplete:script\x00bash\x00--help" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "autocomplete\x00bash\x00--help" => Some("Display autocomplete installation instructions.\n\nUSAGE\n  $ primitive autocomplete [SHELL] [-r]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nFLAGS\n  -r, --refresh-cache  Refresh cache (ignores displaying instructions)\n\nDESCRIPTION\n  Display autocomplete installation instructions.\n\nEXAMPLES\n  $ primitive autocomplete\n\n  $ primitive autocomplete bash\n\n  $ primitive autocomplete zsh\n\n  $ primitive autocomplete powershell\n\n  $ primitive autocomplete --refresh-cache\n\n"),
        "autocomplete\x00create\x00--help" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "autocomplete\x00script\x00--help" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "autocomplete\x00script\x00bash\x00--help" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "chat\x00--api-key\x00prim_test\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--attachment\x00value\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--from\x00user@example.com\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--in-reply-to\x00value\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--interval\x00value\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--json\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--quiet\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--reply-to-email-id\x00user@example.com\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--reply\x00value\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--strict-only\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--strict-phase-seconds\x001\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--time\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00--timeout\x001\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat\x00-a\x00value\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "chat:reply\x00--api-key\x00prim_test\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--attachment\x00value\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--id\x00fn_123\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--interval\x00value\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--json\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--quiet\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--strict-only\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--strict-phase-seconds\x001\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--time\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00--timeout\x001\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00-a\x00value\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat:reply\x00value\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--api-key\x00prim_test\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--attachment\x00value\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--id\x00fn_123\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--interval\x00value\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--json\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--quiet\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--strict-only\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--strict-phase-seconds\x001\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--time\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00--timeout\x001\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00-a\x00value\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00reply\x00value\x00--help" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "chat\x00user@example.com\x00--help" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "cli" => Some("CLI authentication\n\nUSAGE\n  $ primitive cli COMMAND\n\nCOMMANDS\n  cli cli-logout                      Revoke the current CLI OAuth session\n  cli poll-cli-login                  Poll CLI browser login\n  cli resend-cli-signup-verification  Resend CLI signup verification code\n  cli start-cli-login                 Start CLI browser login\n  cli start-cli-signup                Start CLI account signup\n  cli verify-cli-signup               Verify CLI signup and create OAuth session\n\n"),
        "cli\x00--help" => Some("CLI authentication\n\nUSAGE\n  $ primitive cli COMMAND\n\nCOMMANDS\n  cli cli-logout                      Revoke the current CLI OAuth session\n  cli poll-cli-login                  Poll CLI browser login\n  cli resend-cli-signup-verification  Resend CLI signup verification code\n  cli start-cli-login                 Start CLI browser login\n  cli start-cli-signup                Start CLI account signup\n  cli verify-cli-signup               Verify CLI signup and create OAuth session\n\n"),
        "cli\x00-h" => Some("CLI authentication\n\nUSAGE\n  $ primitive cli COMMAND\n\nCOMMANDS\n  cli cli-logout                      Revoke the current CLI OAuth session\n  cli poll-cli-login                  Poll CLI browser login\n  cli resend-cli-signup-verification  Resend CLI signup verification code\n  cli start-cli-login                 Start CLI browser login\n  cli start-cli-signup                Start CLI account signup\n  cli verify-cli-signup               Verify CLI signup and create OAuth session\n\n"),
        "cli:cli-logout\x00--api-key\x00prim_test\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli:cli-logout\x00--body-file\x00value\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli:cli-logout\x00--envelope\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli:cli-logout\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli:cli-logout\x00--json\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli:cli-logout\x00--key-id\x00fn_123\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli:cli-logout\x00--raw-body\x00value\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli:cli-logout\x00--time\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli:poll-cli-login\x00--api-key\x00prim_test\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli:poll-cli-login\x00--body-file\x00value\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli:poll-cli-login\x00--device-code\x00123456\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli:poll-cli-login\x00--envelope\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli:poll-cli-login\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli:poll-cli-login\x00--json\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli:poll-cli-login\x00--raw-body\x00value\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli:poll-cli-login\x00--time\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli:resend-cli-signup-verification\x00--api-key\x00prim_test\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli:resend-cli-signup-verification\x00--body-file\x00value\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli:resend-cli-signup-verification\x00--envelope\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli:resend-cli-signup-verification\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli:resend-cli-signup-verification\x00--json\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli:resend-cli-signup-verification\x00--raw-body\x00value\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli:resend-cli-signup-verification\x00--signup-token\x00value\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli:resend-cli-signup-verification\x00--time\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli:start-cli-login\x00--api-key\x00prim_test\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-login\x00--body-file\x00value\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-login\x00--device-name\x00value\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-login\x00--envelope\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-login\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-login\x00--json\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-login\x00--raw-body\x00value\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-login\x00--time\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--api-key\x00prim_test\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--body-file\x00value\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--device-name\x00value\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--email\x00user@example.com\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--envelope\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--json\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--no-terms-accepted\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--raw-body\x00value\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--signup-code\x00123456\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--terms-accepted\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:start-cli-signup\x00--time\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli:verify-cli-signup\x00--api-key\x00prim_test\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli:verify-cli-signup\x00--body-file\x00value\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli:verify-cli-signup\x00--envelope\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli:verify-cli-signup\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli:verify-cli-signup\x00--json\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli:verify-cli-signup\x00--password\x00value\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli:verify-cli-signup\x00--raw-body\x00value\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli:verify-cli-signup\x00--signup-token\x00value\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli:verify-cli-signup\x00--time\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli:verify-cli-signup\x00--verification-code\x00123456\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00cli-logout\x00--api-key\x00prim_test\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli\x00cli-logout\x00--body-file\x00value\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli\x00cli-logout\x00--envelope\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli\x00cli-logout\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli\x00cli-logout\x00--json\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli\x00cli-logout\x00--key-id\x00fn_123\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli\x00cli-logout\x00--raw-body\x00value\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli\x00cli-logout\x00--time\x00--help" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "cli\x00poll-cli-login\x00--api-key\x00prim_test\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli\x00poll-cli-login\x00--body-file\x00value\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli\x00poll-cli-login\x00--device-code\x00123456\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli\x00poll-cli-login\x00--envelope\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli\x00poll-cli-login\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli\x00poll-cli-login\x00--json\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli\x00poll-cli-login\x00--raw-body\x00value\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli\x00poll-cli-login\x00--time\x00--help" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "cli\x00resend-cli-signup-verification\x00--api-key\x00prim_test\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli\x00resend-cli-signup-verification\x00--body-file\x00value\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli\x00resend-cli-signup-verification\x00--envelope\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli\x00resend-cli-signup-verification\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli\x00resend-cli-signup-verification\x00--json\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli\x00resend-cli-signup-verification\x00--raw-body\x00value\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli\x00resend-cli-signup-verification\x00--signup-token\x00value\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli\x00resend-cli-signup-verification\x00--time\x00--help" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "cli\x00start-cli-login\x00--api-key\x00prim_test\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-login\x00--body-file\x00value\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-login\x00--device-name\x00value\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-login\x00--envelope\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-login\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-login\x00--json\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-login\x00--raw-body\x00value\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-login\x00--time\x00--help" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--api-key\x00prim_test\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--body-file\x00value\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--device-name\x00value\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--email\x00user@example.com\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--envelope\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--json\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--no-terms-accepted\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--raw-body\x00value\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--signup-code\x00123456\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--terms-accepted\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00start-cli-signup\x00--time\x00--help" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "cli\x00verify-cli-signup\x00--api-key\x00prim_test\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00verify-cli-signup\x00--body-file\x00value\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00verify-cli-signup\x00--envelope\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00verify-cli-signup\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00verify-cli-signup\x00--json\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00verify-cli-signup\x00--password\x00value\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00verify-cli-signup\x00--raw-body\x00value\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00verify-cli-signup\x00--signup-token\x00value\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00verify-cli-signup\x00--time\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "cli\x00verify-cli-signup\x00--verification-code\x00123456\x00--help" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "completion\x00--help" => Some("Output a shell completion script or print setup instructions\n\nUSAGE\n  $ primitive completion SHELL\n\nARGUMENTS\n  SHELL  (bash|zsh|powershell|fish) Shell type\n\nDESCRIPTION\n  Output a shell completion script or print setup instructions\n\n  Output a sourceable shell completion script, or print setup instructions.\n\n  For fish, and for bash/zsh when the output is piped or redirected (e.g. into a\n  completion file under bash_completion.d or zsh's site-functions), this emits\n  the raw completion script. For bash/zsh in an interactive terminal it prints\n  the human-readable setup instructions instead. This keeps a redirected\n  `primitive completion bash > <file>` safe to source -- the file holds an\n  actual completion function, never instructional prose a shell would choke on.\n\nEXAMPLES\n  $ primitive completion bash >> /etc/bash_completion.d/primitive\n\n  $ primitive completion zsh > /usr/local/share/zsh/site-functions/_primitive\n\n  $ primitive completion fish > ~/.config/fish/completions/primitive.fish\n\n"),
        "completion\x00bash\x00--help" => Some("Output a shell completion script or print setup instructions\n\nUSAGE\n  $ primitive completion SHELL\n\nARGUMENTS\n  SHELL  (bash|zsh|powershell|fish) Shell type\n\nDESCRIPTION\n  Output a shell completion script or print setup instructions\n\n  Output a sourceable shell completion script, or print setup instructions.\n\n  For fish, and for bash/zsh when the output is piped or redirected (e.g. into a\n  completion file under bash_completion.d or zsh's site-functions), this emits\n  the raw completion script. For bash/zsh in an interactive terminal it prints\n  the human-readable setup instructions instead. This keeps a redirected\n  `primitive completion bash > <file>` safe to source -- the file holds an\n  actual completion function, never instructional prose a shell would choke on.\n\nEXAMPLES\n  $ primitive completion bash >> /etc/bash_completion.d/primitive\n\n  $ primitive completion zsh > /usr/local/share/zsh/site-functions/_primitive\n\n  $ primitive completion fish > ~/.config/fish/completions/primitive.fish\n\n"),
        "config\x00--help" => Some("Manage Primitive CLI request environments\n\nUSAGE\n  $ primitive config [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\nDESCRIPTION\n  Manage Primitive CLI request environments\n\n  Manage local Primitive CLI request environments for API endpoint overrides and\n  request headers.\n\nCOMMANDS\n  config list   List Primitive CLI request environments\n  config reset  Reset Primitive CLI request environments\n  config set    Set a Primitive CLI request environment\n  config use    Switch the active Primitive CLI request environment\n\n"),
        "config\x00--json\x00--help" => Some("Manage Primitive CLI request environments\n\nUSAGE\n  $ primitive config [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\nDESCRIPTION\n  Manage Primitive CLI request environments\n\n  Manage local Primitive CLI request environments for API endpoint overrides and\n  request headers.\n\nCOMMANDS\n  config list   List Primitive CLI request environments\n  config reset  Reset Primitive CLI request environments\n  config set    Set a Primitive CLI request environment\n  config use    Switch the active Primitive CLI request environment\n\n"),
        "config\x00--show-secrets\x00--help" => Some("Manage Primitive CLI request environments\n\nUSAGE\n  $ primitive config [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\nDESCRIPTION\n  Manage Primitive CLI request environments\n\n  Manage local Primitive CLI request environments for API endpoint overrides and\n  request headers.\n\nCOMMANDS\n  config list   List Primitive CLI request environments\n  config reset  Reset Primitive CLI request environments\n  config set    Set a Primitive CLI request environment\n  config use    Switch the active Primitive CLI request environment\n\n"),
        "config:list\x00--help" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "config:list\x00--json\x00--help" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "config:list\x00--show-secrets\x00--help" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "config:reset\x00--environment\x00value\x00--help" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "config:reset\x00--help" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "config:reset\x00-e\x00value\x00--help" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "config:set\x00--api-base-url\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config:set\x00--environment\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config:set\x00--header\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config:set\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config:set\x00--unset-header\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config:set\x00-e\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config:use\x00--help" => Some("Switch the active Primitive CLI request environment\n\nUSAGE\n  $ primitive config use ENVIRONMENT\n\nARGUMENTS\n  ENVIRONMENT  Environment name to use\n\nDESCRIPTION\n  Switch the active Primitive CLI request environment\n\n  Switch the active Primitive CLI request environment. When this switches to a\n  different environment, the CLI removes saved OAuth credentials so the next\n  authenticated command signs in against the newly active API host.\n\n"),
        "config:use\x00value\x00--help" => Some("Switch the active Primitive CLI request environment\n\nUSAGE\n  $ primitive config use ENVIRONMENT\n\nARGUMENTS\n  ENVIRONMENT  Environment name to use\n\nDESCRIPTION\n  Switch the active Primitive CLI request environment\n\n  Switch the active Primitive CLI request environment. When this switches to a\n  different environment, the CLI removes saved OAuth credentials so the next\n  authenticated command signs in against the newly active API host.\n\n"),
        "config\x00list\x00--help" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "config\x00list\x00--json\x00--help" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "config\x00list\x00--show-secrets\x00--help" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "config\x00reset\x00--environment\x00value\x00--help" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "config\x00reset\x00--help" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "config\x00reset\x00-e\x00value\x00--help" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "config\x00set\x00--api-base-url\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config\x00set\x00--environment\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config\x00set\x00--header\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config\x00set\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config\x00set\x00--unset-header\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config\x00set\x00-e\x00value\x00--help" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "config\x00use\x00--help" => Some("Switch the active Primitive CLI request environment\n\nUSAGE\n  $ primitive config use ENVIRONMENT\n\nARGUMENTS\n  ENVIRONMENT  Environment name to use\n\nDESCRIPTION\n  Switch the active Primitive CLI request environment\n\n  Switch the active Primitive CLI request environment. When this switches to a\n  different environment, the CLI removes saved OAuth credentials so the next\n  authenticated command signs in against the newly active API host.\n\n"),
        "config\x00use\x00value\x00--help" => Some("Switch the active Primitive CLI request environment\n\nUSAGE\n  $ primitive config use ENVIRONMENT\n\nARGUMENTS\n  ENVIRONMENT  Environment name to use\n\nDESCRIPTION\n  Switch the active Primitive CLI request environment\n\n  Switch the active Primitive CLI request environment. When this switches to a\n  different environment, the CLI removes saved OAuth credentials so the next\n  authenticated command signs in against the newly active API host.\n\n"),
        "create:autocomplete\x00--help" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "create\x00autocomplete\x00--help" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "deliveries" => Some("Short aliases for webhook delivery attempts: `primitive deliveries list` and `primitive deliveries replay`.\n\nUSAGE\n  $ primitive deliveries COMMAND\n\nCOMMANDS\n  deliveries list    List webhook deliveries\n  deliveries replay  Replay a webhook delivery\n\n"),
        "deliveries\x00--help" => Some("Short aliases for webhook delivery attempts: `primitive deliveries list` and `primitive deliveries replay`.\n\nUSAGE\n  $ primitive deliveries COMMAND\n\nCOMMANDS\n  deliveries list    List webhook deliveries\n  deliveries replay  Replay a webhook delivery\n\n"),
        "deliveries\x00-h" => Some("Short aliases for webhook delivery attempts: `primitive deliveries list` and `primitive deliveries replay`.\n\nUSAGE\n  $ primitive deliveries COMMAND\n\nCOMMANDS\n  deliveries list    List webhook deliveries\n  deliveries replay  Replay a webhook delivery\n\n"),
        "deliveries:list\x00--api-key\x00prim_test\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--cursor\x00value\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--email-id\x00user@example.com\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--envelope\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--json\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--limit\x001\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--status\x00pending\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:list\x00--time\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries:replay\x00--api-key\x00prim_test\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries:replay\x00--envelope\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries:replay\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries:replay\x00--id\x00fn_123\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries:replay\x00--json\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries:replay\x00--time\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries\x00list\x00--api-key\x00prim_test\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--cursor\x00value\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--email-id\x00user@example.com\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--envelope\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--json\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--limit\x001\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--status\x00pending\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00list\x00--time\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "deliveries\x00replay\x00--api-key\x00prim_test\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries\x00replay\x00--envelope\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries\x00replay\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries\x00replay\x00--id\x00fn_123\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries\x00replay\x00--json\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "deliveries\x00replay\x00--time\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "describe\x00--help" => Some("Describe a single API operation in detail\n\nUSAGE\n  $ primitive describe COMMAND\n\nARGUMENTS\n  COMMAND  Command id, alias, or SDK operation name to describe, e.g.\n           `emails:list`, `emails:get-email`, or `getEmail`. Run `primitive\n           list-operations | jq -r '.[] | \"\\(.tagCommand):\\(.command)\n           \\(.operationId)\"'` to enumerate generated operation ids.\n\nDESCRIPTION\n  Describe a single API operation in detail\n\n  Print the full operation manifest entry for a single API command, including\n  the path, request schema, response schema, and per-field descriptions sourced\n  from the OpenAPI spec.\n\n  The manifest entry's `responseSchema` carries the inlined JSON Schema for the\n  operation's 200/201 `data` envelope contents (`$ref`s resolved). Use it to\n  look up what specific response fields mean. Examples:\n\n  # Domain setup records returned by add/verify\n  primitive describe domains:add\n  primitive describe addDomain\n\n  # Which of EmailDetail's sender-shaped fields is canonical?\n  primitive describe emails:get | jq '.responseSchema.properties | keys'\n  primitive describe emails:get | jq -r\n  '.responseSchema.properties.from_email.description'\n\n  # What does each value of SentEmailStatus mean?\n  primitive describe sent:get | jq -r\n  '.responseSchema.properties.status.description'\n\n  `requestSchema` is the same shape for the request body when one exists. For a\n  single field across many operations at once, use `primitive list-operations |\n  jq` instead.\n\nEXAMPLES\n  $ primitive describe addDomain\n\n  $ primitive describe domains:add\n\n  $ primitive describe emails:get\n\n  $ primitive describe sent:get\n\n"),
        "describe\x00send\x00--help" => Some("Describe a single API operation in detail\n\nUSAGE\n  $ primitive describe COMMAND\n\nARGUMENTS\n  COMMAND  Command id, alias, or SDK operation name to describe, e.g.\n           `emails:list`, `emails:get-email`, or `getEmail`. Run `primitive\n           list-operations | jq -r '.[] | \"\\(.tagCommand):\\(.command)\n           \\(.operationId)\"'` to enumerate generated operation ids.\n\nDESCRIPTION\n  Describe a single API operation in detail\n\n  Print the full operation manifest entry for a single API command, including\n  the path, request schema, response schema, and per-field descriptions sourced\n  from the OpenAPI spec.\n\n  The manifest entry's `responseSchema` carries the inlined JSON Schema for the\n  operation's 200/201 `data` envelope contents (`$ref`s resolved). Use it to\n  look up what specific response fields mean. Examples:\n\n  # Domain setup records returned by add/verify\n  primitive describe domains:add\n  primitive describe addDomain\n\n  # Which of EmailDetail's sender-shaped fields is canonical?\n  primitive describe emails:get | jq '.responseSchema.properties | keys'\n  primitive describe emails:get | jq -r\n  '.responseSchema.properties.from_email.description'\n\n  # What does each value of SentEmailStatus mean?\n  primitive describe sent:get | jq -r\n  '.responseSchema.properties.status.description'\n\n  `requestSchema` is the same shape for the request body when one exists. For a\n  single field across many operations at once, use `primitive list-operations |\n  jq` instead.\n\nEXAMPLES\n  $ primitive describe addDomain\n\n  $ primitive describe domains:add\n\n  $ primitive describe emails:get\n\n  $ primitive describe sent:get\n\n"),
        "doctor\x00--api-key\x00prim_test\x00--help" => Some("Check the local environment and live API for common problems\n\nUSAGE\n  $ primitive doctor [--api-key <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n\nDESCRIPTION\n  Check the local environment and live API for common problems\n\n  Run a one-shot environment health check: Node version, proxy env, CLI auth\n  resolution, /account reachability, and verified-domain status. Fails fast on\n  anything that would block other commands and prints actionable hints for each\n  warning or failure.\n\nEXAMPLES\n  $ primitive doctor\n\n  $ primitive doctor --api-key prim_...\n\n"),
        "doctor\x00--help" => Some("Check the local environment and live API for common problems\n\nUSAGE\n  $ primitive doctor [--api-key <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n\nDESCRIPTION\n  Check the local environment and live API for common problems\n\n  Run a one-shot environment health check: Node version, proxy env, CLI auth\n  resolution, /account reachability, and verified-domain status. Fails fast on\n  anything that would block other commands and prints actionable hints for each\n  warning or failure.\n\nEXAMPLES\n  $ primitive doctor\n\n  $ primitive doctor --api-key prim_...\n\n"),
        "domains" => Some("Claim, verify, manage email domains, and download DNS zone files\n\nUSAGE\n  $ primitive domains COMMAND\n\nCOMMANDS\n  domains add                        Claim a new domain\n  domains add-domain                 Claim a new domain\n  domains delete                     Delete a domain\n  domains delete-domain              Delete a domain\n  domains download-domain-zone-file  Download a DNS zone file for a domain\n  domains list                       List all domains\n  domains list-domains               List all domains\n  domains update                     Update domain settings\n  domains update-domain              Update domain settings\n  domains verify                     Verify domain ownership\n  domains verify-domain              Verify domain ownership\n  domains zone-file                  Download a DNS zone file for a domain\n\n"),
        "domains\x00--help" => Some("Claim, verify, manage email domains, and download DNS zone files\n\nUSAGE\n  $ primitive domains COMMAND\n\nCOMMANDS\n  domains add                        Claim a new domain\n  domains add-domain                 Claim a new domain\n  domains delete                     Delete a domain\n  domains delete-domain              Delete a domain\n  domains download-domain-zone-file  Download a DNS zone file for a domain\n  domains list                       List all domains\n  domains list-domains               List all domains\n  domains update                     Update domain settings\n  domains update-domain              Update domain settings\n  domains verify                     Verify domain ownership\n  domains verify-domain              Verify domain ownership\n  domains zone-file                  Download a DNS zone file for a domain\n\n"),
        "domains\x00-h" => Some("Claim, verify, manage email domains, and download DNS zone files\n\nUSAGE\n  $ primitive domains COMMAND\n\nCOMMANDS\n  domains add                        Claim a new domain\n  domains add-domain                 Claim a new domain\n  domains delete                     Delete a domain\n  domains delete-domain              Delete a domain\n  domains download-domain-zone-file  Download a DNS zone file for a domain\n  domains list                       List all domains\n  domains list-domains               List all domains\n  domains update                     Update domain settings\n  domains update-domain              Update domain settings\n  domains verify                     Verify domain ownership\n  domains verify-domain              Verify domain ownership\n  domains zone-file                  Download a DNS zone file for a domain\n\n"),
        "domains:add\x00--api-key\x00prim_test\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--body-file\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--confirmed\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--domain\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--envelope\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--json\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--no-confirmed\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--no-outbound\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--outbound\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--raw-body\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add\x00--time\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--api-key\x00prim_test\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--body-file\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--confirmed\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--domain\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--envelope\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--json\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--no-confirmed\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--no-outbound\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--outbound\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--raw-body\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:add-domain\x00--time\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains:delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete\x00--envelope\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete\x00--id\x00fn_123\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete\x00--json\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete\x00--time\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete-domain\x00--api-key\x00prim_test\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete-domain\x00--envelope\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete-domain\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete-domain\x00--id\x00fn_123\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete-domain\x00--json\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:delete-domain\x00--time\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains:download-domain-zone-file\x00--api-key\x00prim_test\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:download-domain-zone-file\x00--domain\x00value\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:download-domain-zone-file\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:download-domain-zone-file\x00--id\x00fn_123\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:download-domain-zone-file\x00--outbound-only\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:download-domain-zone-file\x00--output\x00help-sweep.tmp\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:download-domain-zone-file\x00--time\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:download-domain-zone-file\x00-o\x00help-sweep.tmp\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:list\x00--api-key\x00prim_test\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:list\x00--envelope\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:list\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:list\x00--json\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:list\x00--time\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:list-domains\x00--api-key\x00prim_test\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:list-domains\x00--envelope\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:list-domains\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:list-domains\x00--json\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:list-domains\x00--time\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains:update\x00--api-key\x00prim_test\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--body-file\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--envelope\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--id\x00fn_123\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--is-active\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--json\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--no-is-active\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--raw-body\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--spam-threshold\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update\x00--time\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--api-key\x00prim_test\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--body-file\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--envelope\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--id\x00fn_123\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--is-active\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--json\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--no-is-active\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--raw-body\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--spam-threshold\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:update-domain\x00--time\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains:verify\x00--api-key\x00prim_test\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify\x00--envelope\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify\x00--id\x00fn_123\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify\x00--json\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify\x00--time\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify-domain\x00--api-key\x00prim_test\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify-domain\x00--envelope\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify-domain\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify-domain\x00--id\x00fn_123\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify-domain\x00--json\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:verify-domain\x00--time\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains:zone-file\x00--api-key\x00prim_test\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:zone-file\x00--domain\x00value\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:zone-file\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:zone-file\x00--id\x00fn_123\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:zone-file\x00--outbound-only\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:zone-file\x00--output\x00help-sweep.tmp\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:zone-file\x00--time\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains:zone-file\x00-o\x00help-sweep.tmp\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00add\x00--api-key\x00prim_test\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--body-file\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--confirmed\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--domain\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--envelope\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--json\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--no-confirmed\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--no-outbound\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--outbound\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--raw-body\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add\x00--time\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--api-key\x00prim_test\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--body-file\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--confirmed\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--domain\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--envelope\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--json\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--no-confirmed\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--no-outbound\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--outbound\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--raw-body\x00value\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00add-domain\x00--time\x00--help" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "domains\x00delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete\x00--envelope\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete\x00--id\x00fn_123\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete\x00--json\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete\x00--time\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete-domain\x00--api-key\x00prim_test\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete-domain\x00--envelope\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete-domain\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete-domain\x00--id\x00fn_123\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete-domain\x00--json\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00delete-domain\x00--time\x00--help" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "domains\x00download-domain-zone-file\x00--api-key\x00prim_test\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00download-domain-zone-file\x00--domain\x00value\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00download-domain-zone-file\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00download-domain-zone-file\x00--id\x00fn_123\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00download-domain-zone-file\x00--outbound-only\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00download-domain-zone-file\x00--output\x00help-sweep.tmp\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00download-domain-zone-file\x00--time\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00download-domain-zone-file\x00-o\x00help-sweep.tmp\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00list\x00--api-key\x00prim_test\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00list\x00--envelope\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00list\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00list\x00--json\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00list\x00--time\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00list-domains\x00--api-key\x00prim_test\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00list-domains\x00--envelope\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00list-domains\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00list-domains\x00--json\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00list-domains\x00--time\x00--help" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "domains\x00update\x00--api-key\x00prim_test\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--body-file\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--envelope\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--id\x00fn_123\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--is-active\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--json\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--no-is-active\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--raw-body\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--spam-threshold\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update\x00--time\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--api-key\x00prim_test\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--body-file\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--envelope\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--id\x00fn_123\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--is-active\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--json\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--no-is-active\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--raw-body\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--spam-threshold\x00value\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00update-domain\x00--time\x00--help" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "domains\x00verify\x00--api-key\x00prim_test\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify\x00--envelope\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify\x00--id\x00fn_123\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify\x00--json\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify\x00--time\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify-domain\x00--api-key\x00prim_test\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify-domain\x00--envelope\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify-domain\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify-domain\x00--id\x00fn_123\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify-domain\x00--json\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00verify-domain\x00--time\x00--help" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "domains\x00zone-file\x00--api-key\x00prim_test\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00zone-file\x00--domain\x00value\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00zone-file\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00zone-file\x00--id\x00fn_123\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00zone-file\x00--outbound-only\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00zone-file\x00--output\x00help-sweep.tmp\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00zone-file\x00--time\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "domains\x00zone-file\x00-o\x00help-sweep.tmp\x00--help" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "emails" => Some("List, inspect, and wait for received emails. Prefer task aliases like `primitive emails list`, `primitive emails get`, `primitive emails latest`, `primitive emails wait`, and `primitive emails watch`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive emails COMMAND\n\nCOMMANDS\n  emails conversation           Get the conversation an email belongs to\n  emails delete                 Delete an email\n  emails delete-email           Delete an email\n  emails discard-content        Discard email content\n  emails discard-email-content  Discard email content\n  emails download-attachments   Download email attachments\n  emails download-raw           Download raw email\n  emails download-raw-email     Download raw email\n  emails get                    Get inbound email by id\n  emails get-conversation       Get the conversation an email belongs to\n  emails get-email              Get inbound email by id\n  emails latest                 Show the most recent inbound emails as a compact\n                                table\n  emails list                   List inbound emails\n  emails list-emails            List inbound emails\n  emails replay-email-webhooks  Replay email webhooks\n  emails replay-webhooks        Replay email webhooks\n  emails search                 Search inbound emails\n  emails search-emails          Search inbound emails\n  emails wait                   Wait for matching inbound emails\n  emails watch                  Watch inbound emails with filters\n\n"),
        "emails\x00--help" => Some("List, inspect, and wait for received emails. Prefer task aliases like `primitive emails list`, `primitive emails get`, `primitive emails latest`, `primitive emails wait`, and `primitive emails watch`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive emails COMMAND\n\nCOMMANDS\n  emails conversation           Get the conversation an email belongs to\n  emails delete                 Delete an email\n  emails delete-email           Delete an email\n  emails discard-content        Discard email content\n  emails discard-email-content  Discard email content\n  emails download-attachments   Download email attachments\n  emails download-raw           Download raw email\n  emails download-raw-email     Download raw email\n  emails get                    Get inbound email by id\n  emails get-conversation       Get the conversation an email belongs to\n  emails get-email              Get inbound email by id\n  emails latest                 Show the most recent inbound emails as a compact\n                                table\n  emails list                   List inbound emails\n  emails list-emails            List inbound emails\n  emails replay-email-webhooks  Replay email webhooks\n  emails replay-webhooks        Replay email webhooks\n  emails search                 Search inbound emails\n  emails search-emails          Search inbound emails\n  emails wait                   Wait for matching inbound emails\n  emails watch                  Watch inbound emails with filters\n\n"),
        "emails\x00-h" => Some("List, inspect, and wait for received emails. Prefer task aliases like `primitive emails list`, `primitive emails get`, `primitive emails latest`, `primitive emails wait`, and `primitive emails watch`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive emails COMMAND\n\nCOMMANDS\n  emails conversation           Get the conversation an email belongs to\n  emails delete                 Delete an email\n  emails delete-email           Delete an email\n  emails discard-content        Discard email content\n  emails discard-email-content  Discard email content\n  emails download-attachments   Download email attachments\n  emails download-raw           Download raw email\n  emails download-raw-email     Download raw email\n  emails get                    Get inbound email by id\n  emails get-conversation       Get the conversation an email belongs to\n  emails get-email              Get inbound email by id\n  emails latest                 Show the most recent inbound emails as a compact\n                                table\n  emails list                   List inbound emails\n  emails list-emails            List inbound emails\n  emails replay-email-webhooks  Replay email webhooks\n  emails replay-webhooks        Replay email webhooks\n  emails search                 Search inbound emails\n  emails search-emails          Search inbound emails\n  emails wait                   Wait for matching inbound emails\n  emails watch                  Watch inbound emails with filters\n\n"),
        "emails:conversation\x00--api-key\x00prim_test\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:conversation\x00--envelope\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:conversation\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:conversation\x00--id\x00fn_123\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:conversation\x00--json\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:conversation\x00--time\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:delete\x00--api-key\x00prim_test\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete\x00--envelope\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete\x00--id\x00fn_123\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete\x00--json\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete\x00--time\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete-email\x00--api-key\x00prim_test\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete-email\x00--envelope\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete-email\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete-email\x00--id\x00fn_123\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete-email\x00--json\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:delete-email\x00--time\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails:discard-content\x00--api-key\x00prim_test\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-content\x00--envelope\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-content\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-content\x00--id\x00fn_123\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-content\x00--json\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-content\x00--time\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-email-content\x00--api-key\x00prim_test\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-email-content\x00--envelope\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-email-content\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-email-content\x00--id\x00fn_123\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-email-content\x00--json\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:discard-email-content\x00--time\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails:download-attachments\x00--api-key\x00prim_test\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails:download-attachments\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails:download-attachments\x00--id\x00fn_123\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails:download-attachments\x00--output\x00help-sweep.tmp\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails:download-attachments\x00--time\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails:download-attachments\x00--token\x00value\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails:download-raw\x00--api-key\x00prim_test\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw\x00--id\x00fn_123\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw\x00--output\x00help-sweep.tmp\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw\x00--time\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw\x00--token\x00value\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw-email\x00--api-key\x00prim_test\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw-email\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw-email\x00--id\x00fn_123\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw-email\x00--output\x00help-sweep.tmp\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw-email\x00--time\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:download-raw-email\x00--token\x00value\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails:get\x00--api-key\x00prim_test\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get\x00--envelope\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get\x00--id\x00fn_123\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get\x00--json\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get\x00--time\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get-conversation\x00--api-key\x00prim_test\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:get-conversation\x00--envelope\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:get-conversation\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:get-conversation\x00--id\x00fn_123\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:get-conversation\x00--json\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:get-conversation\x00--time\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails:get-email\x00--api-key\x00prim_test\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get-email\x00--envelope\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get-email\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get-email\x00--id\x00fn_123\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get-email\x00--json\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:get-email\x00--time\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails:latest\x00--api-key\x00prim_test\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails:latest\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails:latest\x00--json\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails:latest\x00--limit\x001\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails:latest\x00--time\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails:list\x00--api-key\x00prim_test\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--cursor\x00value\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--domain-id\x00fn_123\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--envelope\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--json\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--limit\x001\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--search\x00hello\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--since\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--status\x00value\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--time\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list\x00--wait\x001\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--api-key\x00prim_test\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--cursor\x00value\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--domain-id\x00fn_123\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--envelope\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--json\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--limit\x001\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--search\x00hello\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--since\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--status\x00value\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--time\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:list-emails\x00--wait\x001\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails:replay-email-webhooks\x00--api-key\x00prim_test\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-email-webhooks\x00--envelope\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-email-webhooks\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-email-webhooks\x00--id\x00fn_123\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-email-webhooks\x00--json\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-email-webhooks\x00--time\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-webhooks\x00--api-key\x00prim_test\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-webhooks\x00--envelope\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-webhooks\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-webhooks\x00--id\x00fn_123\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-webhooks\x00--json\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:replay-webhooks\x00--time\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails:search\x00--api-key\x00prim_test\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--body\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--cursor\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--domain-id\x00fn_123\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--envelope\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--from\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--has-attachment\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--include-facets\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--json\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--limit\x001\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--q\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--reply-to-sent-email-id\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--snippet\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--sort\x00relevance\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--spam-score-gte\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--spam-score-lt\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--status\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--subject\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--time\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search\x00--to\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--api-key\x00prim_test\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--body\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--cursor\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--domain-id\x00fn_123\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--envelope\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--from\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--has-attachment\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--include-facets\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--json\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--limit\x001\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--q\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--reply-to-sent-email-id\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--snippet\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--sort\x00relevance\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--spam-score-gte\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--spam-score-lt\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--status\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--subject\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--time\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:search-emails\x00--to\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails:wait\x00--api-key\x00prim_test\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--body\x00hello\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--domain-id\x00fn_123\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--domain\x00value\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--from\x00user@example.com\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--has-attachment\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--include-existing\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--interval\x00value\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--number\x001\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--page-size\x001\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--q\x00hello\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--reply-to-sent-email-id\x00user@example.com\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--since\x002026-01-01T00:00:00Z\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--spam-score-gte\x00value\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--spam-score-lt\x00value\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--subject\x00hello\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--table\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--timeout\x001\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00--to\x00user@example.com\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:wait\x00-n\x001\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails:watch\x00--api-key\x00prim_test\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--body\x00hello\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--domain-id\x00fn_123\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--domain\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--from\x00user@example.com\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--has-attachment\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--include-existing\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--interval\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--jsonl\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--number\x001\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--page-size\x001\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--q\x00hello\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--seconds\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--since\x002026-01-01T00:00:00Z\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--spam-score-gte\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--spam-score-lt\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--subject\x00hello\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails:watch\x00--to\x00user@example.com\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00conversation\x00--api-key\x00prim_test\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00conversation\x00--envelope\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00conversation\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00conversation\x00--id\x00fn_123\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00conversation\x00--json\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00conversation\x00--time\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00delete\x00--api-key\x00prim_test\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete\x00--envelope\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete\x00--id\x00fn_123\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete\x00--json\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete\x00--time\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete-email\x00--api-key\x00prim_test\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete-email\x00--envelope\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete-email\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete-email\x00--id\x00fn_123\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete-email\x00--json\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00delete-email\x00--time\x00--help" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "emails\x00discard-content\x00--api-key\x00prim_test\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-content\x00--envelope\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-content\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-content\x00--id\x00fn_123\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-content\x00--json\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-content\x00--time\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-email-content\x00--api-key\x00prim_test\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-email-content\x00--envelope\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-email-content\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-email-content\x00--id\x00fn_123\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-email-content\x00--json\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00discard-email-content\x00--time\x00--help" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "emails\x00download-attachments\x00--api-key\x00prim_test\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails\x00download-attachments\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails\x00download-attachments\x00--id\x00fn_123\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails\x00download-attachments\x00--output\x00help-sweep.tmp\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails\x00download-attachments\x00--time\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails\x00download-attachments\x00--token\x00value\x00--help" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "emails\x00download-raw\x00--api-key\x00prim_test\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw\x00--id\x00fn_123\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw\x00--output\x00help-sweep.tmp\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw\x00--time\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw\x00--token\x00value\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw-email\x00--api-key\x00prim_test\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw-email\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw-email\x00--id\x00fn_123\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw-email\x00--output\x00help-sweep.tmp\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw-email\x00--time\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00download-raw-email\x00--token\x00value\x00--help" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "emails\x00get\x00--api-key\x00prim_test\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get\x00--envelope\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get\x00--id\x00fn_123\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get\x00--json\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get\x00--time\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get-conversation\x00--api-key\x00prim_test\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00get-conversation\x00--envelope\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00get-conversation\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00get-conversation\x00--id\x00fn_123\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00get-conversation\x00--json\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00get-conversation\x00--time\x00--help" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "emails\x00get-email\x00--api-key\x00prim_test\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get-email\x00--envelope\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get-email\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get-email\x00--id\x00fn_123\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get-email\x00--json\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00get-email\x00--time\x00--help" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "emails\x00latest\x00--api-key\x00prim_test\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails\x00latest\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails\x00latest\x00--json\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails\x00latest\x00--limit\x001\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails\x00latest\x00--time\x00--help" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "emails\x00list\x00--api-key\x00prim_test\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--cursor\x00value\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--domain-id\x00fn_123\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--envelope\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--json\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--limit\x001\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--search\x00hello\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--since\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--status\x00value\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--time\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list\x00--wait\x001\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--api-key\x00prim_test\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--cursor\x00value\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--domain-id\x00fn_123\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--envelope\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--json\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--limit\x001\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--search\x00hello\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--since\x002026-01-01T00:00:00Z\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--status\x00value\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--time\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00list-emails\x00--wait\x001\x00--help" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "emails\x00replay-email-webhooks\x00--api-key\x00prim_test\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-email-webhooks\x00--envelope\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-email-webhooks\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-email-webhooks\x00--id\x00fn_123\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-email-webhooks\x00--json\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-email-webhooks\x00--time\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-webhooks\x00--api-key\x00prim_test\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-webhooks\x00--envelope\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-webhooks\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-webhooks\x00--id\x00fn_123\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-webhooks\x00--json\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00replay-webhooks\x00--time\x00--help" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "emails\x00search\x00--api-key\x00prim_test\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--body\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--cursor\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--domain-id\x00fn_123\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--envelope\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--from\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--has-attachment\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--include-facets\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--json\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--limit\x001\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--q\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--reply-to-sent-email-id\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--snippet\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--sort\x00relevance\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--spam-score-gte\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--spam-score-lt\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--status\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--subject\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--time\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search\x00--to\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--api-key\x00prim_test\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--body\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--cursor\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--domain-id\x00fn_123\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--envelope\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--from\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--has-attachment\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--include-facets\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--json\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--limit\x001\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--q\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--reply-to-sent-email-id\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--snippet\x00true\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--sort\x00relevance\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--spam-score-gte\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--spam-score-lt\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--status\x00value\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--subject\x00hello\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--time\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00search-emails\x00--to\x00user@example.com\x00--help" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "emails\x00wait\x00--api-key\x00prim_test\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--body\x00hello\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--domain-id\x00fn_123\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--domain\x00value\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--from\x00user@example.com\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--has-attachment\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--include-existing\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--interval\x00value\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--number\x001\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--page-size\x001\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--q\x00hello\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--reply-to-sent-email-id\x00user@example.com\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--since\x002026-01-01T00:00:00Z\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--spam-score-gte\x00value\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--spam-score-lt\x00value\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--subject\x00hello\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--table\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--timeout\x001\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00--to\x00user@example.com\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00wait\x00-n\x001\x00--help" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "emails\x00watch\x00--api-key\x00prim_test\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--body\x00hello\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--domain-id\x00fn_123\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--domain\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--from\x00user@example.com\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--has-attachment\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--include-existing\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--interval\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--jsonl\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--number\x001\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--page-size\x001\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--q\x00hello\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--seconds\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--since\x002026-01-01T00:00:00Z\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--spam-score-gte\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--spam-score-lt\x00value\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--subject\x00hello\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "emails\x00watch\x00--to\x00user@example.com\x00--help" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "endpoints" => Some("Manage webhook endpoints that receive email events\n\nUSAGE\n  $ primitive endpoints COMMAND\n\nCOMMANDS\n  endpoints create           Create a webhook endpoint\n  endpoints create-endpoint  Create a webhook endpoint\n  endpoints delete           Delete a webhook endpoint\n  endpoints delete-endpoint  Delete a webhook endpoint\n  endpoints list             List webhook endpoints\n  endpoints list-endpoints   List webhook endpoints\n  endpoints test             Send a test webhook\n  endpoints test-endpoint    Send a test webhook\n  endpoints update           Update a webhook endpoint\n  endpoints update-endpoint  Update a webhook endpoint\n\n"),
        "endpoints\x00--help" => Some("Manage webhook endpoints that receive email events\n\nUSAGE\n  $ primitive endpoints COMMAND\n\nCOMMANDS\n  endpoints create           Create a webhook endpoint\n  endpoints create-endpoint  Create a webhook endpoint\n  endpoints delete           Delete a webhook endpoint\n  endpoints delete-endpoint  Delete a webhook endpoint\n  endpoints list             List webhook endpoints\n  endpoints list-endpoints   List webhook endpoints\n  endpoints test             Send a test webhook\n  endpoints test-endpoint    Send a test webhook\n  endpoints update           Update a webhook endpoint\n  endpoints update-endpoint  Update a webhook endpoint\n\n"),
        "endpoints\x00-h" => Some("Manage webhook endpoints that receive email events\n\nUSAGE\n  $ primitive endpoints COMMAND\n\nCOMMANDS\n  endpoints create           Create a webhook endpoint\n  endpoints create-endpoint  Create a webhook endpoint\n  endpoints delete           Delete a webhook endpoint\n  endpoints delete-endpoint  Delete a webhook endpoint\n  endpoints list             List webhook endpoints\n  endpoints list-endpoints   List webhook endpoints\n  endpoints test             Send a test webhook\n  endpoints test-endpoint    Send a test webhook\n  endpoints update           Update a webhook endpoint\n  endpoints update-endpoint  Update a webhook endpoint\n\n"),
        "endpoints:create\x00--api-key\x00prim_test\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--body-file\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--domain-id\x00fn_123\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--enabled\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--envelope\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--function-id\x00fn_123\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--is-route-target\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--json\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--kind\x00http\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--no-enabled\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--no-is-route-target\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--raw-body\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--time\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create\x00--url\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--api-key\x00prim_test\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--body-file\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--domain-id\x00fn_123\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--enabled\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--envelope\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--function-id\x00fn_123\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--is-route-target\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--json\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--kind\x00http\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--no-enabled\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--no-is-route-target\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--raw-body\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--time\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:create-endpoint\x00--url\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete\x00--envelope\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete\x00--id\x00fn_123\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete\x00--json\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete\x00--time\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete-endpoint\x00--api-key\x00prim_test\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete-endpoint\x00--envelope\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete-endpoint\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete-endpoint\x00--id\x00fn_123\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete-endpoint\x00--json\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:delete-endpoint\x00--time\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints:list\x00--api-key\x00prim_test\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:list\x00--envelope\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:list\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:list\x00--json\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:list\x00--time\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:list-endpoints\x00--api-key\x00prim_test\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:list-endpoints\x00--envelope\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:list-endpoints\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:list-endpoints\x00--json\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:list-endpoints\x00--time\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints:test\x00--api-key\x00prim_test\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test\x00--envelope\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test\x00--id\x00fn_123\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test\x00--json\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test\x00--time\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test-endpoint\x00--api-key\x00prim_test\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test-endpoint\x00--envelope\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test-endpoint\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test-endpoint\x00--id\x00fn_123\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test-endpoint\x00--json\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:test-endpoint\x00--time\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints:update\x00--api-key\x00prim_test\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--body-file\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--domain-id\x00fn_123\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--enabled\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--envelope\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--id\x00fn_123\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--json\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--no-enabled\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--raw-body\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--time\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update\x00--url\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--api-key\x00prim_test\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--body-file\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--domain-id\x00fn_123\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--enabled\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--envelope\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--id\x00fn_123\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--json\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--no-enabled\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--raw-body\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--time\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints:update-endpoint\x00--url\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--api-key\x00prim_test\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--body-file\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--domain-id\x00fn_123\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--enabled\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--envelope\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--function-id\x00fn_123\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--is-route-target\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--json\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--kind\x00http\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--no-enabled\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--no-is-route-target\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--raw-body\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--time\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create\x00--url\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--api-key\x00prim_test\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--body-file\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--domain-id\x00fn_123\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--enabled\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--envelope\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--function-id\x00fn_123\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--is-route-target\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--json\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--kind\x00http\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--no-enabled\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--no-is-route-target\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--raw-body\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--time\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00create-endpoint\x00--url\x00value\x00--help" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete\x00--envelope\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete\x00--id\x00fn_123\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete\x00--json\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete\x00--time\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete-endpoint\x00--api-key\x00prim_test\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete-endpoint\x00--envelope\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete-endpoint\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete-endpoint\x00--id\x00fn_123\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete-endpoint\x00--json\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00delete-endpoint\x00--time\x00--help" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "endpoints\x00list\x00--api-key\x00prim_test\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00list\x00--envelope\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00list\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00list\x00--json\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00list\x00--time\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00list-endpoints\x00--api-key\x00prim_test\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00list-endpoints\x00--envelope\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00list-endpoints\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00list-endpoints\x00--json\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00list-endpoints\x00--time\x00--help" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "endpoints\x00test\x00--api-key\x00prim_test\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test\x00--envelope\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test\x00--id\x00fn_123\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test\x00--json\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test\x00--time\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test-endpoint\x00--api-key\x00prim_test\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test-endpoint\x00--envelope\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test-endpoint\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test-endpoint\x00--id\x00fn_123\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test-endpoint\x00--json\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00test-endpoint\x00--time\x00--help" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "endpoints\x00update\x00--api-key\x00prim_test\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--body-file\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--domain-id\x00fn_123\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--enabled\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--envelope\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--id\x00fn_123\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--json\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--no-enabled\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--raw-body\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--time\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update\x00--url\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--api-key\x00prim_test\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--body-file\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--domain-id\x00fn_123\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--enabled\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--envelope\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--id\x00fn_123\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--json\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--no-enabled\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--raw-body\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--time\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "endpoints\x00update-endpoint\x00--url\x00value\x00--help" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "filters" => Some("Manage whitelist and blocklist filter rules\n\nUSAGE\n  $ primitive filters COMMAND\n\nCOMMANDS\n  filters create         Create a filter rule\n  filters create-filter  Create a filter rule\n  filters delete         Delete a filter rule\n  filters delete-filter  Delete a filter rule\n  filters list           List filter rules\n  filters list-filters   List filter rules\n  filters update         Update a filter rule\n  filters update-filter  Update a filter rule\n\n"),
        "filters\x00--help" => Some("Manage whitelist and blocklist filter rules\n\nUSAGE\n  $ primitive filters COMMAND\n\nCOMMANDS\n  filters create         Create a filter rule\n  filters create-filter  Create a filter rule\n  filters delete         Delete a filter rule\n  filters delete-filter  Delete a filter rule\n  filters list           List filter rules\n  filters list-filters   List filter rules\n  filters update         Update a filter rule\n  filters update-filter  Update a filter rule\n\n"),
        "filters\x00-h" => Some("Manage whitelist and blocklist filter rules\n\nUSAGE\n  $ primitive filters COMMAND\n\nCOMMANDS\n  filters create         Create a filter rule\n  filters create-filter  Create a filter rule\n  filters delete         Delete a filter rule\n  filters delete-filter  Delete a filter rule\n  filters list           List filter rules\n  filters list-filters   List filter rules\n  filters update         Update a filter rule\n  filters update-filter  Update a filter rule\n\n"),
        "filters:create\x00--api-key\x00prim_test\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create\x00--body-file\x00value\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create\x00--domain-id\x00fn_123\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create\x00--envelope\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create\x00--json\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create\x00--pattern\x00b@example.com\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create\x00--raw-body\x00value\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create\x00--time\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create\x00--type\x00whitelist\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--api-key\x00prim_test\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--body-file\x00value\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--domain-id\x00fn_123\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--envelope\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--json\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--pattern\x00b@example.com\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--raw-body\x00value\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--time\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:create-filter\x00--type\x00whitelist\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters:delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete\x00--envelope\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete\x00--id\x00fn_123\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete\x00--json\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete\x00--time\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete-filter\x00--api-key\x00prim_test\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete-filter\x00--envelope\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete-filter\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete-filter\x00--id\x00fn_123\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete-filter\x00--json\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:delete-filter\x00--time\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters:list\x00--api-key\x00prim_test\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:list\x00--envelope\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:list\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:list\x00--json\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:list\x00--time\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:list-filters\x00--api-key\x00prim_test\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:list-filters\x00--envelope\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:list-filters\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:list-filters\x00--json\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:list-filters\x00--time\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters:update\x00--api-key\x00prim_test\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update\x00--body-file\x00value\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update\x00--enabled\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update\x00--envelope\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update\x00--id\x00fn_123\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update\x00--json\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update\x00--no-enabled\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update\x00--raw-body\x00value\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update\x00--time\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--api-key\x00prim_test\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--body-file\x00value\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--enabled\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--envelope\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--id\x00fn_123\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--json\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--no-enabled\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--raw-body\x00value\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters:update-filter\x00--time\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00create\x00--api-key\x00prim_test\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create\x00--body-file\x00value\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create\x00--domain-id\x00fn_123\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create\x00--envelope\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create\x00--json\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create\x00--pattern\x00b@example.com\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create\x00--raw-body\x00value\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create\x00--time\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create\x00--type\x00whitelist\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--api-key\x00prim_test\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--body-file\x00value\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--domain-id\x00fn_123\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--envelope\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--json\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--pattern\x00b@example.com\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--raw-body\x00value\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--time\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00create-filter\x00--type\x00whitelist\x00--help" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "filters\x00delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete\x00--envelope\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete\x00--id\x00fn_123\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete\x00--json\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete\x00--time\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete-filter\x00--api-key\x00prim_test\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete-filter\x00--envelope\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete-filter\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete-filter\x00--id\x00fn_123\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete-filter\x00--json\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00delete-filter\x00--time\x00--help" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "filters\x00list\x00--api-key\x00prim_test\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00list\x00--envelope\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00list\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00list\x00--json\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00list\x00--time\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00list-filters\x00--api-key\x00prim_test\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00list-filters\x00--envelope\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00list-filters\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00list-filters\x00--json\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00list-filters\x00--time\x00--help" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "filters\x00update\x00--api-key\x00prim_test\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update\x00--body-file\x00value\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update\x00--enabled\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update\x00--envelope\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update\x00--id\x00fn_123\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update\x00--json\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update\x00--no-enabled\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update\x00--raw-body\x00value\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update\x00--time\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--api-key\x00prim_test\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--body-file\x00value\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--enabled\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--envelope\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--id\x00fn_123\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--json\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--no-enabled\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--raw-body\x00value\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "filters\x00update-filter\x00--time\x00--help" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "functions" => Some("Deploy JavaScript handlers that run on inbound mail. Prefer `primitive functions templates`, `primitive functions init`, `primitive functions deploy`, `primitive functions redeploy`, `primitive functions list`, `primitive functions get`, `primitive functions logs`, and `primitive functions set-secret`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive functions COMMAND\n\nCOMMANDS\n  functions create-function              Deploy a function\n  functions create-function-secret       Create or update a secret\n  functions create-org-secret            Create or update an org secret\n  functions delete                       Delete a function\n  functions delete-function              Delete a function\n  functions delete-function-secret       Delete a secret\n  functions delete-org-secret            Delete an org secret\n  functions delete-secret                Delete a secret\n  functions deploy                       Deploy a new function from a bundled\n                                         handler file\n  functions get                          Get a function\n  functions get-function                 Get a function\n  functions get-function-routing         Get a function's current route binding\n  functions get-function-test-run-trace  Get a function test run trace\n  functions get-org-routing-topology     Get the org's function routing topology\n  functions init                         Scaffold a new Primitive Function\n                                         project ready for functions deploy\n  functions list                         List functions\n  functions list-function-logs           List a function's execution logs\n  functions list-function-secrets        List a function's secrets\n  functions list-functions               List functions\n  functions list-org-secrets             List org-level (global) secrets\n  functions list-secrets                 List a function's secrets\n  functions logs                         List or follow a function's execution\n                                         logs\n  functions redeploy                     Redeploy a function from a bundled\n                                         handler file\n  functions route-get                    Show a function's current route binding\n  functions route-set                    Bind inbound mail to a function\n  functions route-unset                  Unbind any route from a function\n  functions routing-topology             Show the org-wide function routing\n                                         topology\n  functions set-function-route           Bind a route to a function\n  functions set-function-secret          Set a secret by key\n  functions set-org-secret               Set an org secret by key\n  functions set-secret                   Write a function secret (optionally\n                                         redeploying to push it live)\n  functions templates                    List available Primitive Function\n                                         templates\n  functions test                         Trigger a test invocation; with --wait,\n                                         watch it land\n  functions test-function                Trigger a test invocation; with --wait,\n                                         watch it land\n  functions unset-function-route         Unbind any route from a function\n  functions update-function              Update and redeploy a function\n\n"),
        "functions\x00--help" => Some("Deploy JavaScript handlers that run on inbound mail. Prefer `primitive functions templates`, `primitive functions init`, `primitive functions deploy`, `primitive functions redeploy`, `primitive functions list`, `primitive functions get`, `primitive functions logs`, and `primitive functions set-secret`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive functions COMMAND\n\nCOMMANDS\n  functions create-function              Deploy a function\n  functions create-function-secret       Create or update a secret\n  functions create-org-secret            Create or update an org secret\n  functions delete                       Delete a function\n  functions delete-function              Delete a function\n  functions delete-function-secret       Delete a secret\n  functions delete-org-secret            Delete an org secret\n  functions delete-secret                Delete a secret\n  functions deploy                       Deploy a new function from a bundled\n                                         handler file\n  functions get                          Get a function\n  functions get-function                 Get a function\n  functions get-function-routing         Get a function's current route binding\n  functions get-function-test-run-trace  Get a function test run trace\n  functions get-org-routing-topology     Get the org's function routing topology\n  functions init                         Scaffold a new Primitive Function\n                                         project ready for functions deploy\n  functions list                         List functions\n  functions list-function-logs           List a function's execution logs\n  functions list-function-secrets        List a function's secrets\n  functions list-functions               List functions\n  functions list-org-secrets             List org-level (global) secrets\n  functions list-secrets                 List a function's secrets\n  functions logs                         List or follow a function's execution\n                                         logs\n  functions redeploy                     Redeploy a function from a bundled\n                                         handler file\n  functions route-get                    Show a function's current route binding\n  functions route-set                    Bind inbound mail to a function\n  functions route-unset                  Unbind any route from a function\n  functions routing-topology             Show the org-wide function routing\n                                         topology\n  functions set-function-route           Bind a route to a function\n  functions set-function-secret          Set a secret by key\n  functions set-org-secret               Set an org secret by key\n  functions set-secret                   Write a function secret (optionally\n                                         redeploying to push it live)\n  functions templates                    List available Primitive Function\n                                         templates\n  functions test                         Trigger a test invocation; with --wait,\n                                         watch it land\n  functions test-function                Trigger a test invocation; with --wait,\n                                         watch it land\n  functions unset-function-route         Unbind any route from a function\n  functions update-function              Update and redeploy a function\n\n"),
        "functions\x00-h" => Some("Deploy JavaScript handlers that run on inbound mail. Prefer `primitive functions templates`, `primitive functions init`, `primitive functions deploy`, `primitive functions redeploy`, `primitive functions list`, `primitive functions get`, `primitive functions logs`, and `primitive functions set-secret`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive functions COMMAND\n\nCOMMANDS\n  functions create-function              Deploy a function\n  functions create-function-secret       Create or update a secret\n  functions create-org-secret            Create or update an org secret\n  functions delete                       Delete a function\n  functions delete-function              Delete a function\n  functions delete-function-secret       Delete a secret\n  functions delete-org-secret            Delete an org secret\n  functions delete-secret                Delete a secret\n  functions deploy                       Deploy a new function from a bundled\n                                         handler file\n  functions get                          Get a function\n  functions get-function                 Get a function\n  functions get-function-routing         Get a function's current route binding\n  functions get-function-test-run-trace  Get a function test run trace\n  functions get-org-routing-topology     Get the org's function routing topology\n  functions init                         Scaffold a new Primitive Function\n                                         project ready for functions deploy\n  functions list                         List functions\n  functions list-function-logs           List a function's execution logs\n  functions list-function-secrets        List a function's secrets\n  functions list-functions               List functions\n  functions list-org-secrets             List org-level (global) secrets\n  functions list-secrets                 List a function's secrets\n  functions logs                         List or follow a function's execution\n                                         logs\n  functions redeploy                     Redeploy a function from a bundled\n                                         handler file\n  functions route-get                    Show a function's current route binding\n  functions route-set                    Bind inbound mail to a function\n  functions route-unset                  Unbind any route from a function\n  functions routing-topology             Show the org-wide function routing\n                                         topology\n  functions set-function-route           Bind a route to a function\n  functions set-function-secret          Set a secret by key\n  functions set-org-secret               Set an org secret by key\n  functions set-secret                   Write a function secret (optionally\n                                         redeploying to push it live)\n  functions templates                    List available Primitive Function\n                                         templates\n  functions test                         Trigger a test invocation; with --wait,\n                                         watch it land\n  functions test-function                Trigger a test invocation; with --wait,\n                                         watch it land\n  functions unset-function-route         Unbind any route from a function\n  functions update-function              Update and redeploy a function\n\n"),
        "functions:create-function\x00--api-key\x00prim_test\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function\x00--body-file\x00value\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function\x00--code\x00123456\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function\x00--envelope\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function\x00--json\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function\x00--name\x00help-name\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function\x00--raw-body\x00value\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function\x00--sourcemap\x00value\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function\x00--time\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--api-key\x00prim_test\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--body-file\x00value\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--envelope\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--id\x00fn_123\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--json\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--key\x00help-key\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--raw-body\x00value\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--time\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-function-secret\x00--value\x00hello\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:create-org-secret\x00--api-key\x00prim_test\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions:create-org-secret\x00--body-file\x00value\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions:create-org-secret\x00--envelope\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions:create-org-secret\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions:create-org-secret\x00--json\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions:create-org-secret\x00--key\x00help-key\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions:create-org-secret\x00--raw-body\x00value\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions:create-org-secret\x00--time\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions:create-org-secret\x00--value\x00hello\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions:delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete\x00--envelope\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete\x00--id\x00fn_123\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete\x00--json\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete\x00--time\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete-function\x00--api-key\x00prim_test\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete-function\x00--envelope\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete-function\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete-function\x00--id\x00fn_123\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete-function\x00--json\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete-function\x00--time\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions:delete-function-secret\x00--api-key\x00prim_test\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-function-secret\x00--envelope\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-function-secret\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-function-secret\x00--id\x00fn_123\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-function-secret\x00--json\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-function-secret\x00--key\x00help-key\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-function-secret\x00--time\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-org-secret\x00--api-key\x00prim_test\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions:delete-org-secret\x00--envelope\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions:delete-org-secret\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions:delete-org-secret\x00--json\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions:delete-org-secret\x00--key\x00help-key\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions:delete-org-secret\x00--time\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions:delete-secret\x00--api-key\x00prim_test\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-secret\x00--envelope\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-secret\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-secret\x00--id\x00fn_123\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-secret\x00--json\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-secret\x00--key\x00help-key\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:delete-secret\x00--time\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions:deploy\x00--api-key\x00prim_test\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--file\x00help-sweep.tmp\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--name\x00help-name\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--poll-interval\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--secret-from-env-file\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--secret-from-env\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--secret-from-file\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--secret-from-stdin\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--secret\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--source-map-file\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--source\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--time\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--timeout\x001\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:deploy\x00--wait\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:get\x00--api-key\x00prim_test\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get\x00--envelope\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get\x00--id\x00fn_123\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get\x00--json\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get\x00--time\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get-function\x00--api-key\x00prim_test\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get-function\x00--envelope\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get-function\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get-function\x00--id\x00fn_123\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get-function\x00--json\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get-function\x00--time\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions:get-function-routing\x00--api-key\x00prim_test\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions:get-function-routing\x00--envelope\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions:get-function-routing\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions:get-function-routing\x00--id\x00fn_123\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions:get-function-routing\x00--json\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions:get-function-routing\x00--time\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions:get-function-test-run-trace\x00--api-key\x00prim_test\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions:get-function-test-run-trace\x00--envelope\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions:get-function-test-run-trace\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions:get-function-test-run-trace\x00--id\x00fn_123\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions:get-function-test-run-trace\x00--json\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions:get-function-test-run-trace\x00--run-id\x00fn_123\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions:get-function-test-run-trace\x00--time\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions:get-org-routing-topology\x00--api-key\x00prim_test\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions:get-org-routing-topology\x00--envelope\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions:get-org-routing-topology\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions:get-org-routing-topology\x00--json\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions:get-org-routing-topology\x00--time\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions:init\x00--help" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "functions:init\x00--out-dir\x00value\x00--help" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "functions:init\x00--template\x00email-reply\x00--help" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "functions:init\x00help-name\x00--help" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "functions:list\x00--api-key\x00prim_test\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list\x00--envelope\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list\x00--json\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list\x00--time\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list-function-logs\x00--api-key\x00prim_test\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions:list-function-logs\x00--cursor\x00value\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions:list-function-logs\x00--envelope\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions:list-function-logs\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions:list-function-logs\x00--id\x00fn_123\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions:list-function-logs\x00--json\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions:list-function-logs\x00--limit\x001\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions:list-function-logs\x00--time\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions:list-function-secrets\x00--api-key\x00prim_test\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-function-secrets\x00--envelope\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-function-secrets\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-function-secrets\x00--id\x00fn_123\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-function-secrets\x00--json\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-function-secrets\x00--time\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-functions\x00--api-key\x00prim_test\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list-functions\x00--envelope\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list-functions\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list-functions\x00--json\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list-functions\x00--time\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions:list-org-secrets\x00--api-key\x00prim_test\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions:list-org-secrets\x00--envelope\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions:list-org-secrets\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions:list-org-secrets\x00--json\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions:list-org-secrets\x00--time\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions:list-secrets\x00--api-key\x00prim_test\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-secrets\x00--envelope\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-secrets\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-secrets\x00--id\x00fn_123\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-secrets\x00--json\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:list-secrets\x00--time\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions:logs\x00--api-key\x00prim_test\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:logs\x00--cursor\x00value\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:logs\x00--follow\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:logs\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:logs\x00--id\x00fn_123\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:logs\x00--jsonl\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:logs\x00--limit\x001\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:logs\x00--poll-interval\x00value\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:logs\x00--time\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:logs\x00-f\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions:redeploy\x00--api-key\x00prim_test\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--file\x00help-sweep.tmp\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--id\x00fn_123\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--poll-interval\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--secret-from-env-file\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--secret-from-env\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--secret-from-file\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--secret-from-stdin\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--secret\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--source-map-file\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--time\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--timeout\x001\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:redeploy\x00--wait\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions:route-get\x00--api-key\x00prim_test\x00--help" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "functions:route-get\x00--help" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "functions:route-get\x00--id\x00fn_123\x00--help" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "functions:route-get\x00--time\x00--help" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "functions:route-set\x00--api-key\x00prim_test\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions:route-set\x00--domain\x00value\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions:route-set\x00--fallback\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions:route-set\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions:route-set\x00--id\x00fn_123\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions:route-set\x00--takeover\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions:route-set\x00--time\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions:route-unset\x00--api-key\x00prim_test\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "functions:route-unset\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "functions:route-unset\x00--id\x00fn_123\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "functions:route-unset\x00--time\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "functions:routing-topology\x00--api-key\x00prim_test\x00--help" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "functions:routing-topology\x00--help" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "functions:routing-topology\x00--time\x00--help" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "functions:set-function-route\x00--api-key\x00prim_test\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-route\x00--body-file\x00value\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-route\x00--envelope\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-route\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-route\x00--id\x00fn_123\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-route\x00--json\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-route\x00--no-takeover\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-route\x00--raw-body\x00value\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-route\x00--takeover\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-route\x00--time\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions:set-function-secret\x00--api-key\x00prim_test\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-function-secret\x00--body-file\x00value\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-function-secret\x00--envelope\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-function-secret\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-function-secret\x00--id\x00fn_123\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-function-secret\x00--json\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-function-secret\x00--key\x00help-key\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-function-secret\x00--raw-body\x00value\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-function-secret\x00--time\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-function-secret\x00--value\x00hello\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions:set-org-secret\x00--api-key\x00prim_test\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions:set-org-secret\x00--body-file\x00value\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions:set-org-secret\x00--envelope\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions:set-org-secret\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions:set-org-secret\x00--json\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions:set-org-secret\x00--key\x00help-key\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions:set-org-secret\x00--raw-body\x00value\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions:set-org-secret\x00--time\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions:set-org-secret\x00--value\x00hello\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions:set-secret\x00--api-key\x00prim_test\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--id\x00fn_123\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--key\x00help-key\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--redeploy\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--stdin\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--time\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--value-file\x00value\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--value-from-env-file\x00value\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--value-from-env\x00value\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:set-secret\x00--value\x00hello\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions:templates\x00--help" => Some("List available Primitive Function templates\n\nUSAGE\n  $ primitive functions templates [--json]\n\nGLOBAL FLAGS\n  --json  Format output as json.\n\nDESCRIPTION\n  List available Primitive Function templates\n\n  List Primitive Function templates available to `primitive functions init`.\n\n  The default table is optimized for quick terminal discovery. Use\n  --json when an agent or script needs stable metadata for searching,\n  ranking, or choosing a template programmatically.\n\nEXAMPLES\n  $ primitive functions templates\n\n  $ primitive functions templates --json\n\n  $ primitive functions init my-fn --template email-reply\n\n"),
        "functions:test\x00--api-key\x00prim_test\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test\x00--id\x00fn_123\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test\x00--local-part\x00value\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test\x00--poll-interval\x00value\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test\x00--show-sends\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test\x00--time\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test\x00--timeout\x001\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test\x00--wait\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test-function\x00--api-key\x00prim_test\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test-function\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test-function\x00--id\x00fn_123\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test-function\x00--local-part\x00value\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test-function\x00--poll-interval\x00value\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test-function\x00--show-sends\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test-function\x00--time\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test-function\x00--timeout\x001\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:test-function\x00--wait\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions:unset-function-route\x00--api-key\x00prim_test\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions:unset-function-route\x00--envelope\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions:unset-function-route\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions:unset-function-route\x00--id\x00fn_123\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions:unset-function-route\x00--json\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions:unset-function-route\x00--time\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions:update-function\x00--api-key\x00prim_test\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:update-function\x00--body-file\x00value\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:update-function\x00--code\x00123456\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:update-function\x00--envelope\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:update-function\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:update-function\x00--id\x00fn_123\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:update-function\x00--json\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:update-function\x00--raw-body\x00value\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:update-function\x00--sourcemap\x00value\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions:update-function\x00--time\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--api-key\x00prim_test\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--body-file\x00value\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--code\x00123456\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--envelope\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--json\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--name\x00help-name\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--raw-body\x00value\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--sourcemap\x00value\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function\x00--time\x00--help" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--api-key\x00prim_test\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--body-file\x00value\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--envelope\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--id\x00fn_123\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--json\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--key\x00help-key\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--raw-body\x00value\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--time\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-function-secret\x00--value\x00hello\x00--help" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00create-org-secret\x00--api-key\x00prim_test\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions\x00create-org-secret\x00--body-file\x00value\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions\x00create-org-secret\x00--envelope\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions\x00create-org-secret\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions\x00create-org-secret\x00--json\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions\x00create-org-secret\x00--key\x00help-key\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions\x00create-org-secret\x00--raw-body\x00value\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions\x00create-org-secret\x00--time\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions\x00create-org-secret\x00--value\x00hello\x00--help" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "functions\x00delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete\x00--envelope\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete\x00--id\x00fn_123\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete\x00--json\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete\x00--time\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete-function\x00--api-key\x00prim_test\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete-function\x00--envelope\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete-function\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete-function\x00--id\x00fn_123\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete-function\x00--json\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete-function\x00--time\x00--help" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "functions\x00delete-function-secret\x00--api-key\x00prim_test\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-function-secret\x00--envelope\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-function-secret\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-function-secret\x00--id\x00fn_123\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-function-secret\x00--json\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-function-secret\x00--key\x00help-key\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-function-secret\x00--time\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-org-secret\x00--api-key\x00prim_test\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions\x00delete-org-secret\x00--envelope\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions\x00delete-org-secret\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions\x00delete-org-secret\x00--json\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions\x00delete-org-secret\x00--key\x00help-key\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions\x00delete-org-secret\x00--time\x00--help" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "functions\x00delete-secret\x00--api-key\x00prim_test\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-secret\x00--envelope\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-secret\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-secret\x00--id\x00fn_123\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-secret\x00--json\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-secret\x00--key\x00help-key\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00delete-secret\x00--time\x00--help" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "functions\x00deploy\x00--api-key\x00prim_test\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--file\x00help-sweep.tmp\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--name\x00help-name\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--poll-interval\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--secret-from-env-file\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--secret-from-env\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--secret-from-file\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--secret-from-stdin\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--secret\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--source-map-file\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--source\x00value\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--time\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--timeout\x001\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00deploy\x00--wait\x00--help" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00get\x00--api-key\x00prim_test\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get\x00--envelope\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get\x00--id\x00fn_123\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get\x00--json\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get\x00--time\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get-function\x00--api-key\x00prim_test\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get-function\x00--envelope\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get-function\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get-function\x00--id\x00fn_123\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get-function\x00--json\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get-function\x00--time\x00--help" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "functions\x00get-function-routing\x00--api-key\x00prim_test\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions\x00get-function-routing\x00--envelope\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions\x00get-function-routing\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions\x00get-function-routing\x00--id\x00fn_123\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions\x00get-function-routing\x00--json\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions\x00get-function-routing\x00--time\x00--help" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "functions\x00get-function-test-run-trace\x00--api-key\x00prim_test\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions\x00get-function-test-run-trace\x00--envelope\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions\x00get-function-test-run-trace\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions\x00get-function-test-run-trace\x00--id\x00fn_123\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions\x00get-function-test-run-trace\x00--json\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions\x00get-function-test-run-trace\x00--run-id\x00fn_123\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions\x00get-function-test-run-trace\x00--time\x00--help" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "functions\x00get-org-routing-topology\x00--api-key\x00prim_test\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions\x00get-org-routing-topology\x00--envelope\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions\x00get-org-routing-topology\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions\x00get-org-routing-topology\x00--json\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions\x00get-org-routing-topology\x00--time\x00--help" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "functions\x00init\x00--help" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "functions\x00init\x00--out-dir\x00value\x00--help" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "functions\x00init\x00--template\x00email-reply\x00--help" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "functions\x00init\x00help-name\x00--help" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "functions\x00list\x00--api-key\x00prim_test\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list\x00--envelope\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list\x00--json\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list\x00--time\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list-function-logs\x00--api-key\x00prim_test\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions\x00list-function-logs\x00--cursor\x00value\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions\x00list-function-logs\x00--envelope\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions\x00list-function-logs\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions\x00list-function-logs\x00--id\x00fn_123\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions\x00list-function-logs\x00--json\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions\x00list-function-logs\x00--limit\x001\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions\x00list-function-logs\x00--time\x00--help" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "functions\x00list-function-secrets\x00--api-key\x00prim_test\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-function-secrets\x00--envelope\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-function-secrets\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-function-secrets\x00--id\x00fn_123\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-function-secrets\x00--json\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-function-secrets\x00--time\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-functions\x00--api-key\x00prim_test\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list-functions\x00--envelope\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list-functions\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list-functions\x00--json\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list-functions\x00--time\x00--help" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "functions\x00list-org-secrets\x00--api-key\x00prim_test\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions\x00list-org-secrets\x00--envelope\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions\x00list-org-secrets\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions\x00list-org-secrets\x00--json\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions\x00list-org-secrets\x00--time\x00--help" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "functions\x00list-secrets\x00--api-key\x00prim_test\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-secrets\x00--envelope\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-secrets\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-secrets\x00--id\x00fn_123\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-secrets\x00--json\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00list-secrets\x00--time\x00--help" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "functions\x00logs\x00--api-key\x00prim_test\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00logs\x00--cursor\x00value\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00logs\x00--follow\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00logs\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00logs\x00--id\x00fn_123\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00logs\x00--jsonl\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00logs\x00--limit\x001\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00logs\x00--poll-interval\x00value\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00logs\x00--time\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00logs\x00-f\x00--help" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "functions\x00redeploy\x00--api-key\x00prim_test\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--file\x00help-sweep.tmp\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--id\x00fn_123\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--poll-interval\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--secret-from-env-file\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--secret-from-env\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--secret-from-file\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--secret-from-stdin\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--secret\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--source-map-file\x00value\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--time\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--timeout\x001\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00redeploy\x00--wait\x00--help" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "functions\x00route-get\x00--api-key\x00prim_test\x00--help" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "functions\x00route-get\x00--help" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "functions\x00route-get\x00--id\x00fn_123\x00--help" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "functions\x00route-get\x00--time\x00--help" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "functions\x00route-set\x00--api-key\x00prim_test\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions\x00route-set\x00--domain\x00value\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions\x00route-set\x00--fallback\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions\x00route-set\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions\x00route-set\x00--id\x00fn_123\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions\x00route-set\x00--takeover\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions\x00route-set\x00--time\x00--help" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "functions\x00route-unset\x00--api-key\x00prim_test\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "functions\x00route-unset\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "functions\x00route-unset\x00--id\x00fn_123\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "functions\x00route-unset\x00--time\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "functions\x00routing-topology\x00--api-key\x00prim_test\x00--help" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "functions\x00routing-topology\x00--help" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "functions\x00routing-topology\x00--time\x00--help" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "functions\x00set-function-route\x00--api-key\x00prim_test\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-route\x00--body-file\x00value\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-route\x00--envelope\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-route\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-route\x00--id\x00fn_123\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-route\x00--json\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-route\x00--no-takeover\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-route\x00--raw-body\x00value\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-route\x00--takeover\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-route\x00--time\x00--help" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "functions\x00set-function-secret\x00--api-key\x00prim_test\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-function-secret\x00--body-file\x00value\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-function-secret\x00--envelope\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-function-secret\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-function-secret\x00--id\x00fn_123\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-function-secret\x00--json\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-function-secret\x00--key\x00help-key\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-function-secret\x00--raw-body\x00value\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-function-secret\x00--time\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-function-secret\x00--value\x00hello\x00--help" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "functions\x00set-org-secret\x00--api-key\x00prim_test\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions\x00set-org-secret\x00--body-file\x00value\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions\x00set-org-secret\x00--envelope\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions\x00set-org-secret\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions\x00set-org-secret\x00--json\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions\x00set-org-secret\x00--key\x00help-key\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions\x00set-org-secret\x00--raw-body\x00value\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions\x00set-org-secret\x00--time\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions\x00set-org-secret\x00--value\x00hello\x00--help" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "functions\x00set-secret\x00--api-key\x00prim_test\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--id\x00fn_123\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--key\x00help-key\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--redeploy\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--stdin\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--time\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--value-file\x00value\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--value-from-env-file\x00value\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--value-from-env\x00value\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00set-secret\x00--value\x00hello\x00--help" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "functions\x00templates\x00--help" => Some("List available Primitive Function templates\n\nUSAGE\n  $ primitive functions templates [--json]\n\nGLOBAL FLAGS\n  --json  Format output as json.\n\nDESCRIPTION\n  List available Primitive Function templates\n\n  List Primitive Function templates available to `primitive functions init`.\n\n  The default table is optimized for quick terminal discovery. Use\n  --json when an agent or script needs stable metadata for searching,\n  ranking, or choosing a template programmatically.\n\nEXAMPLES\n  $ primitive functions templates\n\n  $ primitive functions templates --json\n\n  $ primitive functions init my-fn --template email-reply\n\n"),
        "functions\x00test\x00--api-key\x00prim_test\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test\x00--id\x00fn_123\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test\x00--local-part\x00value\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test\x00--poll-interval\x00value\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test\x00--show-sends\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test\x00--time\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test\x00--timeout\x001\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test\x00--wait\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test-function\x00--api-key\x00prim_test\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test-function\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test-function\x00--id\x00fn_123\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test-function\x00--local-part\x00value\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test-function\x00--poll-interval\x00value\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test-function\x00--show-sends\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test-function\x00--time\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test-function\x00--timeout\x001\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00test-function\x00--wait\x00--help" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "functions\x00unset-function-route\x00--api-key\x00prim_test\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions\x00unset-function-route\x00--envelope\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions\x00unset-function-route\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions\x00unset-function-route\x00--id\x00fn_123\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions\x00unset-function-route\x00--json\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions\x00unset-function-route\x00--time\x00--help" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "functions\x00update-function\x00--api-key\x00prim_test\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00update-function\x00--body-file\x00value\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00update-function\x00--code\x00123456\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00update-function\x00--envelope\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00update-function\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00update-function\x00--id\x00fn_123\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00update-function\x00--json\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00update-function\x00--raw-body\x00value\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00update-function\x00--sourcemap\x00value\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "functions\x00update-function\x00--time\x00--help" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "help" => Some("Official Primitive CLI: deploy Primitive Functions, send and inspect mail, manage endpoints, all from the terminal. Wraps the @primitivedotdev/sdk runtime client with one-shot commands.\n\nVERSION\n  primitive/0.1.0\n\nUSAGE\n  $ primitive [COMMAND]\n\nTOPICS\n  account\n      Manage your account settings, storage, and webhook secret\n\n  agent\n      Agent signup and authentication API operations\n\n  chat\n      Chat with agents over email. Use `primitive chat <email> <message>` to\n      start, `primitive chat reply <message>` to continue the active chat, or\n      `primitive chat reply <id> <message>` to reply in a specific local chat.\n\n  cli\n      CLI authentication\n\n  deliveries\n      Short aliases for webhook delivery attempts: `primitive deliveries list` and\n      `primitive deliveries replay`.\n\n  domains\n      Claim, verify, manage email domains, and download DNS zone files\n\n  emails\n      List, inspect, and wait for received emails. Prefer task aliases like\n      `primitive emails list`, `primitive emails get`, `primitive emails latest`,\n      `primitive emails wait`, and `primitive emails watch`; generated API names\n      remain available for compatibility.\n\n  endpoints\n      Manage webhook endpoints that receive email events\n\n  filters\n      Manage whitelist and blocklist filter rules\n\n  functions\n      Deploy JavaScript handlers that run on inbound mail. Prefer `primitive\n      functions templates`, `primitive functions init`, `primitive functions\n      deploy`, `primitive functions redeploy`, `primitive functions list`,\n      `primitive functions get`, `primitive functions logs`, and `primitive\n      functions set-secret`; generated API names remain available for\n      compatibility.\n\n  inbox\n      Check inbound email setup and processing readiness. Prefer `primitive inbox\n      setup` for a guided setup path and `primitive inbox status` for the\n      readiness table.\n\n  login\n      Log in to an existing Primitive account\n\n  memories\n      Store durable JSON memories by key. Use `primitive memories\n      set|get|delete|search`; pass `--function <function-id>` for function-scoped\n      memories.\n\n  org\n      Manage org-level (global) resources shared across functions\n\n  otp\n      Authenticate with an emailed one-time code\n\n  payloads\n      Stream-download and decrypt a payload to a file\n\n  payments\n      Collect and pay x402 stablecoin (USDC) payments. Start with `primitive\n      payments register-payout-address`, then `primitive payments\n      create-challenge` to request a payment and `primitive payments pay` to\n      settle one. Settlement is non-custodial; signing happens locally with your\n      wallet key. Manage caps with `primitive payments\n      get-policy|update-spend-policy`.\n\n  registries\n      Create and manage agent registries, publish and resolve agents by handle,\n      and moderate publication requests\n\n  routes\n      Manage recipient routes. Bind an address pattern to an endpoint or function:\n      `primitive routes add alice@d --function <id>`, `primitive routes list`,\n      `primitive routes test <recipient>`, `primitive routes remove <id>`.\n\n  sending\n      Send outbound emails. Prefer `primitive send` for fresh sends and `primitive\n      reply --id <inbound-id>` for replies. Use `primitive domains list` or\n      `primitive inbox status` to find usable sender domains for --from;\n      `primitive sending permissions` lists recipient-scope destinations you may\n      send to.\n\n  sent\n      Short aliases for outbound sent-email history: `primitive sent list` and\n      `primitive sent get`.\n\n  signin\n      Sign in to an existing Primitive account\n\n  signup\n      Start account signup\n\n  templates\n      Browse and install Function templates. Use `primitive templates\n      list-templates`, `primitive templates get-template`, `primitive templates\n      install-template`, and `primitive templates get-template-install`.\n\n  threads\n      Inspect conversation threads spanning received and sent emails. Prefer\n      `primitive threads get --id <thread-id>`; generated API name `primitive\n      threads get-thread --id <thread-id>` remains available.\n\n  wake\n      Schedule and send wake commands to your own functions over signed email, and\n      manage which senders may wake them. Use `primitive wake schedules\n      list|create|get|update|delete|run`, `primitive wake authorizations\n      list|create|update|delete`, and `primitive wake dispatches list`.\n\n  webhook-deliveries\n      View and replay webhook delivery attempts. Prefer `primitive\n      webhook-deliveries list` and `primitive webhook-deliveries replay`.\n\nCOMMANDS\n  autocomplete     Display autocomplete installation instructions.\n  chat             Chat with an agent over email (send and wait for the reply)\n  completion       Output a shell completion script or print setup instructions\n  describe         Describe a single API operation in detail\n  doctor           Check the local environment and live API for common problems\n  help             Display help for primitive.\n  list-operations  List all generated API operations (JSON)\n  login            Log in to an existing account\n  logout           Log out and revoke the saved CLI OAuth grant\n  otp              Start email-code auth\n  reply            Reply to an inbound email\n  search           Search mail (lexical by default; --mode for semantic)\n  semantic-search  Semantic / hybrid / keyword search across received and sent\n                   mail\n  send             Send an email (simplified, agent-friendly)\n  signin           Sign in to an existing account\n  signup           Start account signup\n  whoami           Print the authenticated account (credentials smoke test)\n\n"),
        "help\x00--help" => Some("Official Primitive CLI: deploy Primitive Functions, send and inspect mail, manage endpoints, all from the terminal. Wraps the @primitivedotdev/sdk runtime client with one-shot commands.\n\nVERSION\n  primitive/0.1.0\n\nUSAGE\n  $ primitive [COMMAND]\n\nTOPICS\n  account\n      Manage your account settings, storage, and webhook secret\n\n  agent\n      Agent signup and authentication API operations\n\n  chat\n      Chat with agents over email. Use `primitive chat <email> <message>` to\n      start, `primitive chat reply <message>` to continue the active chat, or\n      `primitive chat reply <id> <message>` to reply in a specific local chat.\n\n  cli\n      CLI authentication\n\n  deliveries\n      Short aliases for webhook delivery attempts: `primitive deliveries list` and\n      `primitive deliveries replay`.\n\n  domains\n      Claim, verify, manage email domains, and download DNS zone files\n\n  emails\n      List, inspect, and wait for received emails. Prefer task aliases like\n      `primitive emails list`, `primitive emails get`, `primitive emails latest`,\n      `primitive emails wait`, and `primitive emails watch`; generated API names\n      remain available for compatibility.\n\n  endpoints\n      Manage webhook endpoints that receive email events\n\n  filters\n      Manage whitelist and blocklist filter rules\n\n  functions\n      Deploy JavaScript handlers that run on inbound mail. Prefer `primitive\n      functions templates`, `primitive functions init`, `primitive functions\n      deploy`, `primitive functions redeploy`, `primitive functions list`,\n      `primitive functions get`, `primitive functions logs`, and `primitive\n      functions set-secret`; generated API names remain available for\n      compatibility.\n\n  inbox\n      Check inbound email setup and processing readiness. Prefer `primitive inbox\n      setup` for a guided setup path and `primitive inbox status` for the\n      readiness table.\n\n  login\n      Log in to an existing Primitive account\n\n  memories\n      Store durable JSON memories by key. Use `primitive memories\n      set|get|delete|search`; pass `--function <function-id>` for function-scoped\n      memories.\n\n  org\n      Manage org-level (global) resources shared across functions\n\n  otp\n      Authenticate with an emailed one-time code\n\n  payloads\n      Stream-download and decrypt a payload to a file\n\n  payments\n      Collect and pay x402 stablecoin (USDC) payments. Start with `primitive\n      payments register-payout-address`, then `primitive payments\n      create-challenge` to request a payment and `primitive payments pay` to\n      settle one. Settlement is non-custodial; signing happens locally with your\n      wallet key. Manage caps with `primitive payments\n      get-policy|update-spend-policy`.\n\n  registries\n      Create and manage agent registries, publish and resolve agents by handle,\n      and moderate publication requests\n\n  routes\n      Manage recipient routes. Bind an address pattern to an endpoint or function:\n      `primitive routes add alice@d --function <id>`, `primitive routes list`,\n      `primitive routes test <recipient>`, `primitive routes remove <id>`.\n\n  sending\n      Send outbound emails. Prefer `primitive send` for fresh sends and `primitive\n      reply --id <inbound-id>` for replies. Use `primitive domains list` or\n      `primitive inbox status` to find usable sender domains for --from;\n      `primitive sending permissions` lists recipient-scope destinations you may\n      send to.\n\n  sent\n      Short aliases for outbound sent-email history: `primitive sent list` and\n      `primitive sent get`.\n\n  signin\n      Sign in to an existing Primitive account\n\n  signup\n      Start account signup\n\n  templates\n      Browse and install Function templates. Use `primitive templates\n      list-templates`, `primitive templates get-template`, `primitive templates\n      install-template`, and `primitive templates get-template-install`.\n\n  threads\n      Inspect conversation threads spanning received and sent emails. Prefer\n      `primitive threads get --id <thread-id>`; generated API name `primitive\n      threads get-thread --id <thread-id>` remains available.\n\n  wake\n      Schedule and send wake commands to your own functions over signed email, and\n      manage which senders may wake them. Use `primitive wake schedules\n      list|create|get|update|delete|run`, `primitive wake authorizations\n      list|create|update|delete`, and `primitive wake dispatches list`.\n\n  webhook-deliveries\n      View and replay webhook delivery attempts. Prefer `primitive\n      webhook-deliveries list` and `primitive webhook-deliveries replay`.\n\nCOMMANDS\n  autocomplete     Display autocomplete installation instructions.\n  chat             Chat with an agent over email (send and wait for the reply)\n  completion       Output a shell completion script or print setup instructions\n  describe         Describe a single API operation in detail\n  doctor           Check the local environment and live API for common problems\n  help             Display help for primitive.\n  list-operations  List all generated API operations (JSON)\n  login            Log in to an existing account\n  logout           Log out and revoke the saved CLI OAuth grant\n  otp              Start email-code auth\n  reply            Reply to an inbound email\n  search           Search mail (lexical by default; --mode for semantic)\n  semantic-search  Semantic / hybrid / keyword search across received and sent\n                   mail\n  send             Send an email (simplified, agent-friendly)\n  signin           Sign in to an existing account\n  signup           Start account signup\n  whoami           Print the authenticated account (credentials smoke test)\n\n"),
        "help\x00account" => Some("Manage your account settings, storage, and webhook secret\n\nUSAGE\n  $ primitive account COMMAND\n\nCOMMANDS\n  account get-account            Get account info\n  account get-storage-stats      Get storage usage\n  account get-webhook-secret     Get webhook signing secret\n  account rotate-webhook-secret  Rotate webhook signing secret\n  account show                   Get account info\n  account storage                Get storage usage\n  account update-account         Update account settings\n  account webhook-secret         Get webhook signing secret\n\n"),
        "help\x00account:get-account" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "help\x00account:get-storage-stats" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "help\x00account:get-webhook-secret" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "help\x00account:rotate-webhook-secret" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "help\x00account:show" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "help\x00account:storage" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "help\x00account:update-account" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "help\x00account:webhook-secret" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "help\x00account\x00get-account" => Some("Get account info\n\nUSAGE\n  $ primitive account get-account [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "help\x00account\x00get-storage-stats" => Some("Get storage usage\n\nUSAGE\n  $ primitive account get-storage-stats [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "help\x00account\x00get-webhook-secret" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account get-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "help\x00account\x00rotate-webhook-secret" => Some("Rotate webhook signing secret\n\nUSAGE\n  $ primitive account rotate-webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Rotate webhook signing secret\n\n  Generates a new webhook signing secret, replacing the current one.\n  Rate limited to once per 60 minutes.\n\n\n"),
        "help\x00account\x00show" => Some("Get account info\n\nUSAGE\n  $ primitive account show [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get account info\n\n  GET /account\n\n"),
        "help\x00account\x00storage" => Some("Get storage usage\n\nUSAGE\n  $ primitive account storage [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get storage usage\n\n  GET /account/storage\n\n"),
        "help\x00account\x00update-account" => Some("Update account settings\n\nUSAGE\n  $ primitive account update-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--discard-content-on-webhook-confirmed] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --[no-]discard-content-on-webhook-confirmed\n      Whether to discard email content after the webhook endpoint confirms\n      receipt.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --spam-threshold=<value>\n      Global spam score threshold (0-15). Emails scoring above this are rejected.\n      Set to null to disable.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update account settings\n\n  PATCH /account\n\n"),
        "help\x00account\x00webhook-secret" => Some("Get webhook signing secret\n\nUSAGE\n  $ primitive account webhook-secret [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get webhook signing secret\n\n  Returns the webhook signing secret for your account. If no\n  secret exists yet, one is generated automatically on first\n  access.\n\n  Signing is account-scoped, not per-endpoint. Every webhook\n  delivery from any of your registered endpoints is signed\n  with this single secret. Rotate via\n  `POST /account/webhook-secret/rotate`.\n\n  **Secret format**: the returned string looks base64-shaped\n  (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.\n  Use it AS-IS as a UTF-8 string when computing HMAC over a\n  delivery body. Base64-decoding before HMAC will silently\n  produce mismatched signatures.\n\n  See the API-level \"Webhook signing\" section for the full\n  wire format (header name, signed string shape, hash algo,\n  tolerance) including a language-agnostic verification\n  recipe.\n\n\n"),
        "help\x00agent" => Some("Agent signup and authentication API operations\n\nUSAGE\n  $ primitive agent COMMAND\n\nCOMMANDS\n  agent claim                             Start an agent account email claim\n  agent claim-link                        Create a browser claim link\n  agent claim-verify                      Verify an agent account email claim\n  agent create                            Create an emailless agent account\n  agent create-agent-account              Create an emailless agent account\n  agent create-agent-claim-link           Create a browser claim link\n  agent resend-agent-signup-verification  Resend agent signup verification code\n  agent start-agent-claim                 Start an agent account email claim\n  agent start-agent-signup                Start agent account signup\n  agent upgrade                           Upgrade an agent account to developer\n                                          (email confirmation)\n  agent verify-agent-claim                Verify an agent account email claim\n  agent verify-agent-signup               Verify agent signup and create OAuth\n                                          tokens\n\n"),
        "help\x00agent:claim" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "help\x00agent:claim-link" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "help\x00agent:claim-verify" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "help\x00agent:create" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "help\x00agent:create-agent-account" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "help\x00agent:create-agent-claim-link" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "help\x00agent:resend-agent-signup-verification" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "help\x00agent:start-agent-claim" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "help\x00agent:start-agent-signup" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "help\x00agent:upgrade" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "help\x00agent:verify-agent-claim" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "help\x00agent:verify-agent-signup" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "help\x00agent\x00claim" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "help\x00agent\x00claim-link" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "help\x00agent\x00claim-verify" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent claim-verify [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "help\x00agent\x00create" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "help\x00agent\x00create-agent-account" => Some("Create an emailless agent account\n\nUSAGE\n  $ primitive agent create-agent-account [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--terms-accepted]\n    [--device-name <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Optional label for the device or agent creating the\n                         account.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --[no-]terms-accepted  Must be true to accept the Terms of Service and Privacy\n                         Policy.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create an emailless agent account\n\n  Creates an emailless agent account without authentication and returns a\n  one-time API key (prefixed `prim_`) plus a provisioned managed inbox.\n  The account is on the `agent` plan: reply-only (it can send only to\n  addresses that have already sent it authenticated mail) with tight send\n  limits. Use the returned `api_key` as a Bearer token on later calls. The\n  account can be upgraded to a full developer account by confirming an\n  email through the claim flow. This endpoint does not require an API key.\n\n\n"),
        "help\x00agent\x00create-agent-claim-link" => Some("Create a browser claim link\n\nUSAGE\n  $ primitive agent create-agent-claim-link [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Create a browser claim link\n\n  Mints an opaque, single-use link an agent can hand to a human to\n  complete the email-confirmation upgrade in a browser. Authenticated by\n  the agent's own API key. `claim_url` is null when the API host cannot\n  resolve a web origin to build the link.\n\n\n"),
        "help\x00agent\x00resend-agent-signup-verification" => Some("Resend agent signup verification code\n\nUSAGE\n  $ primitive agent resend-agent-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend agent signup verification code\n\n  Sends a new email verification code for a pending agent signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "help\x00agent\x00start-agent-claim" => Some("Start an agent account email claim\n\nUSAGE\n  $ primitive agent start-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --email=<value>      Email to confirm. Must not already belong to a Primitive\n                       account.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Start an agent account email claim\n\n  Begins upgrading an emailless `agent` account into a full `developer`\n  account by confirming an email address. Authenticated by the agent's own\n  API key (the org is taken from the credential). Sends a verification\n  code to the supplied email and returns the claim session id plus resend\n  timing. Submit the code to `/agent/claim/verify` to complete the\n  upgrade. Confirming an email that already belongs to a Primitive account\n  is rejected.\n\n\n"),
        "help\x00agent\x00start-agent-signup" => Some("Start agent account signup\n\nUSAGE\n  $ primitive agent start-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created agent\n                         OAuth session\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start agent account signup\n\n  Starts an agent-native signup session. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: pass --terms-accepted, and optionally --signup-code <code> if you have\n  one. Capture the signup_token from the response and feed it to `primitive\n  agent verify-agent-signup --signup-token <token> --verification-code\n  <6-digit-code>` (the verify flag accepts --code as an alias). The high-level\n  `primitive signup <email>` command walks an interactive user through both\n  steps with friendlier prompts.\n\n"),
        "help\x00agent\x00upgrade" => Some("Upgrade an agent account to developer (email confirmation)\n\nUSAGE\n  $ primitive agent upgrade [--email <value>] [--code <value>] [--api-key\n    <value>] [--api-base-url <value>]\n\nFLAGS\n  --api-base-url=<value>  Override the API base URL.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Agent API key (defaults to\n                          PRIMITIVE_API_KEY or saved credentials).\n  --code=<value>          Verification code from the email. Prompted if omitted.\n  --email=<value>         Email to confirm. Prompted if omitted.\n\nDESCRIPTION\n  Upgrade an agent account to developer (email confirmation)\n\n  Upgrade an emailless agent account to a full developer account by confirming\n  an email. Authenticated by the agent's own API key (PRIMITIVE_API_KEY).\n\nEXAMPLES\n  $ primitive agent upgrade --email you@example.com\n\n"),
        "help\x00agent\x00verify-agent-claim" => Some("Verify an agent account email claim\n\nUSAGE\n  $ primitive agent verify-agent-claim [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--verification-code\n    <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  The verification code emailed by the claim start\n                               step.\n\nDESCRIPTION\n  Verify an agent account email claim\n\n  Confirms the verification code emailed by `/agent/claim/start` and\n  upgrades the account to the `developer` plan. The org id, API key, and\n  managed inbox all carry over; the send cap lifts. Authenticated by the\n  agent's own API key.\n\n\n"),
        "help\x00agent\x00verify-agent-signup" => Some("Verify agent signup and create OAuth tokens\n\nUSAGE\n  $ primitive agent verify-agent-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--org-id <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --org-id=<value>             Optional workspace id to target when the verified\n                               email already belongs to multiple workspaces\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify agent signup and create OAuth tokens\n\n  Verifies the email code for an agent signup session and creates\n  the account when needed. When the session was started with a\n  `signup_code`, the reserved code is redeemed; sessions started\n  without a code skip the redemption step. An org-scoped OAuth\n  session for CLI authentication is minted and the raw tokens are\n  returned exactly once. For existing users, the optional `org_id`\n  selects which accessible workspace should receive the new\n  session (no signup-code redemption is performed for existing\n  users regardless of how the session was started).\n\n\n  Tip: pass --verification-code <code> (or --code; both work). The response\n  carries OAuth tokens but not your assigned inbox domain; run `primitive\n  domains list` (or `primitive whoami`) after success to see the managed\n  *.primitive.email address that routes to this account.\n\n"),
        "help\x00autocomplete" => Some("Display autocomplete installation instructions.\n\nUSAGE\n  $ primitive autocomplete [SHELL] [-r]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nFLAGS\n  -r, --refresh-cache  Refresh cache (ignores displaying instructions)\n\nDESCRIPTION\n  Display autocomplete installation instructions.\n\nEXAMPLES\n  $ primitive autocomplete\n\n  $ primitive autocomplete bash\n\n  $ primitive autocomplete zsh\n\n  $ primitive autocomplete powershell\n\n  $ primitive autocomplete --refresh-cache\n\n"),
        "help\x00autocomplete:create" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "help\x00autocomplete:script" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "help\x00autocomplete\x00create" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "help\x00autocomplete\x00script" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "help\x00chat" => Some("Chat with an agent over email (send and wait for the reply)\n\nUSAGE\n  $ primitive chat RECIPIENT [MESSAGE] [--api-key <value>] [--from\n    <value>] [--reply <value>] [--reply-to-email-id <value>] [--in-reply-to\n    <value>] [-a <value>...] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [--interval <value>]\n    [--time]\n\nARGUMENTS\n  RECIPIENT  Address to chat with (e.g. help@agent.acme.dev).\n  [MESSAGE]  Message body. If omitted, read from stdin.\n\nFLAGS\n  -a, --attachment=<value>...\n      Attach a file to this chat message. Repeat --attachment to attach multiple\n      files.\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to PRIMITIVE_API_KEY or\n      saved `primitive signin` credentials)\n\n  --from=<value>\n      Sender address. Defaults to agent@<your-first-verified-outbound-domain>.\n\n  --in-reply-to=<value>\n      Raw Message-Id of the parent email to thread a new send against. Prefer\n      --reply-to-email-id with --reply when continuing an inbound email stored by\n      Primitive.\n\n  --interval=<value>\n      [default: 2] Seconds between polls while waiting for the reply.\n\n  --json\n      Emit a structured JSON envelope { sent, reply, response_body,\n      response_body_format, match, follow_up_commands } on stdout instead of the\n      human-readable transcript.\n\n  --quiet\n      Suppress stderr progress updates while sending and waiting. Errors and\n      recovery commands are still written to stderr.\n\n  --reply=<value>\n      Reply body. Continues the latest inbound email from the recipient to your\n      sender address; pass --reply-to-email-id for an exact thread.\n\n  --reply-to-email-id=<value>\n      Inbound email id to continue exactly. Uses Primitive's reply endpoint, so\n      recipient, subject, and threading headers are derived from the inbound\n      email.\n\n  --strict-only\n      Disable the time-window fallback. Only accept inbounds whose threading\n      headers (In-Reply-To / References) resolve to this send. Use when matching\n      the wrong reply is worse than timing out.\n\n  --strict-phase-seconds=<value>\n      [default: 60] Seconds to wait in strict-threading mode (filter by\n      reply_to_sent_email_id) before falling back to time-window matching. Set to\n      the full --timeout to disable the fallback; --strict-only is the explicit\n      way to do that.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait for a reply before exiting non-zero; 0 waits\n      forever.\n\nDESCRIPTION\n  Chat with an agent over email (send and wait for the reply)\n\n  Send a message to an address and wait for the reply.\n\n  This is the first-party verb for talking to agents that live behind\n  email addresses. `primitive send` is transport (fire-and-forget);\n  `primitive chat` is semantic (send + wait for the threaded reply).\n\n  The message body can be given as the second positional argument or\n  piped via stdin. The default output confirms the reply was received,\n  prints exchange metadata, shows the response body, and lists helpful\n  follow-up commands as templates. The default transcript is for humans;\n  agents and scripts should pass --json for parse-safe output.\n\n  To continue an existing chat, pass --reply '<message>'. By default,\n  the CLI replies to the latest inbound email from the recipient to\n  your sender address. For exact continuation, pass\n  --reply-to-email-id <inbound-email-id>. Reply mode uses Primitive's\n  reply endpoint, so the reply subject and threading headers are\n  derived from the inbound email instead of copied into CLI flags.\n  Successful chat turns also save an active local chat, so the next\n  follow-up can be sent with `primitive chat reply '<message>'`.\n\n  --json emits a structured envelope with both sides of the exchange,\n  a direct response_body field, match details, and follow-up command\n  metadata such as kind, argv, placeholders, and requires_message.\n\n  Matching the reply: chat first waits in strict threading mode by\n  filtering inbound mail with reply_to_sent_email_id=<sent id>. If\n  no strict match arrives before the strict phase ends, and\n  --strict-only is not set, it falls back to a weaker sender/time\n  window match: from=<recipient>, to=<sender>, and since=<send time>.\n  The fallback can catch clients that strip threading headers, but it\n  is less exact than strict matching. Use --strict-only when matching\n  the wrong reply is worse than timing out. Progress is written to\n  stderr while the CLI waits. Exits non-zero on timeout and prints\n  recovery commands when the send succeeded but no reply was returned.\n\nEXAMPLES\n  $ primitive chat help@agent.acme.dev 'how do I rotate my API key?'\n\n  cat error.log | primitive chat help@agent.acme.dev\n\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing'\n\n  $ primitive chat help@agent.acme.dev --reply 'one more thing' --reply-to-email-id <inbound-email-id>\n\n  $ primitive chat help@agent.acme.dev 'can you review this?' --attachment ./report.pdf\n\n  $ primitive chat help@agent.acme.dev 'follow up question' --json\n\n  $ primitive chat help@agent.acme.dev 'one more thing' --timeout 300\n\nCOMMANDS\n  chat reply  Reply in the active chat\n\n"),
        "help\x00chat:reply" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "help\x00chat\x00reply" => Some("Reply in the active chat\n\nUSAGE\n  $ primitive chat reply [IDORMESSAGE] [MESSAGE] [--api-key <value>]\n    [--id <value>] [--json] [--quiet] [--timeout <value>]\n    [--strict-phase-seconds <value>] [--strict-only] [-a <value>...] [--interval\n    <value>] [--time]\n\nARGUMENTS\n  [IDORMESSAGE]  Reply body, or a local chat id when followed by a separate\n                 message.\n  [MESSAGE]      Reply body when the first positional argument is an id.\n\nFLAGS\n  -a, --attachment=<value>...         Attach a file to the reply. Repeat\n                                      --attachment to attach multiple files.\n      --api-key=<value>               [env: PRIMITIVE_API_KEY] Primitive API key\n                                      (defaults to PRIMITIVE_API_KEY or saved\n                                      `primitive signin` credentials)\n      --id=<value>                    Local chat id to reply in. Omit to use the\n                                      most recent active chat.\n      --interval=<value>              Seconds between polls while waiting for\n                                      the reply.\n      --json                          Emit a structured JSON envelope { sent,\n                                      reply, response_body,\n                                      response_body_format, match,\n                                      follow_up_commands } on stdout instead of\n                                      the human-readable transcript.\n      --quiet                         Suppress stderr progress updates while\n                                      sending and waiting. Errors and recovery\n                                      commands are still written to stderr.\n      --strict-only                   Disable the time-window fallback. If the\n                                      active chat was saved from a strict match,\n                                      this is already the default.\n      --strict-phase-seconds=<value>  Seconds to wait in strict-threading mode\n                                      before falling back. Defaults to the\n                                      active chat's last setting.\n      --time                          Print the wall-clock duration of this\n                                      command to stderr after it completes (e.g.\n                                      `[time: 1.34s]`). Useful for measuring\n                                      `--wait` send latency, comparing CLI\n                                      overhead, or capturing timing in scripts.\n      --timeout=<value>               Seconds to wait for a reply before exiting\n                                      non-zero. Defaults to the active chat's\n                                      last timeout.\n\nDESCRIPTION\n  Reply in the active chat\n\n  Reply in the active chat.\n\n  A successful `primitive chat <email> <message>` saves the latest\n  inbound reply as a local chat and makes it active. Use\n  `primitive chat reply <message>` for the active chat, or\n  `primitive chat reply <local-id> <message>` / `--id <local-id>`\n  for a specific local chat. The command uses Primitive's real reply\n  endpoint against the stored inbound email id, so the recipient,\n  subject, and threading headers are derived server-side from the\n  thread.\n\n  If no chat is open, start one with `primitive chat <email> '<message>'`.\n  For explicit control, use `primitive chat <email> --reply '<message>'\n  --reply-to-email-id <inbound-email-id>`.\n\nEXAMPLES\n  $ primitive chat reply 'one more thing'\n\n  $ primitive chat reply 0 'one more thing'\n\n  $ primitive chat reply --id 0 'one more thing'\n\n  $ primitive chat reply 'see attached' --attachment ./report.pdf\n\n  cat follow-up.txt | primitive chat reply\n\n"),
        "help\x00cli" => Some("CLI authentication\n\nUSAGE\n  $ primitive cli COMMAND\n\nCOMMANDS\n  cli cli-logout                      Revoke the current CLI OAuth session\n  cli poll-cli-login                  Poll CLI browser login\n  cli resend-cli-signup-verification  Resend CLI signup verification code\n  cli start-cli-login                 Start CLI browser login\n  cli start-cli-signup                Start CLI account signup\n  cli verify-cli-signup               Verify CLI signup and create OAuth session\n\n"),
        "help\x00cli:cli-logout" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "help\x00cli:poll-cli-login" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "help\x00cli:resend-cli-signup-verification" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "help\x00cli:start-cli-login" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00cli:start-cli-signup" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00cli:verify-cli-signup" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "help\x00cli\x00cli-logout" => Some("Revoke the current CLI OAuth session\n\nUSAGE\n  $ primitive cli cli-logout [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key-id=<value>     Optional id guard; when provided it must match the\n                       authenticated OAuth grant id or API key id\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Revoke the current CLI OAuth session\n\n  Revokes the OAuth grant used to authenticate the request. API-key\n  authenticated legacy logout requests succeed without deleting server API\n  keys so old local CLI state can be cleared safely.\n\n\n"),
        "help\x00cli\x00poll-cli-login" => Some("Poll CLI browser login\n\nUSAGE\n  $ primitive cli poll-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-code\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-code=<value>  device_code\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Poll CLI browser login\n\n  Polls a CLI login session until the browser approval either succeeds,\n  is denied, expires, or is polled too quickly. The OAuth token set is\n  created only after approval and is returned exactly once.\n\n\n"),
        "help\x00cli\x00resend-cli-signup-verification" => Some("Resend CLI signup verification code\n\nUSAGE\n  $ primitive cli resend-cli-signup-verification [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --signup-token=<value>  signup_token\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Resend CLI signup verification code\n\n  Sends a new email verification code for a pending CLI signup session.\n  This endpoint does not require an API key.\n\n\n"),
        "help\x00cli\x00start-cli-login" => Some("Start CLI browser login\n\nUSAGE\n  $ primitive cli start-cli-login [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--device-name\n    <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name shown during browser\n                         approval\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI browser login\n\n  Starts a browser-assisted CLI login session. The response includes a\n  device code for polling and a user code that the user approves in the\n  browser. This endpoint does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the login session;\n  serialized JSON mus...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00cli\x00start-cli-signup" => Some("Start CLI account signup\n\nUSAGE\n  $ primitive cli start-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--email <value>]\n    [--terms-accepted] [--device-name <value>] [--signup-code <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --device-name=<value>  Human-readable device name used for the created CLI\n                         OAuth grant\n  --email=<value>        email\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --signup-code=<value>  Optional signup code. Omit if you do not have one.\n  --[no-]terms-accepted  Must be true to confirm acceptance of Primitive's Terms\n                         of Service and Privacy Policy\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Start CLI account signup\n\n  Starts a terminal-native CLI signup. `signup_code` is optional;\n  omit it to sign up without one. The API creates a pending signup\n  session, sends an email verification code, and returns an opaque\n  signup token used by the resend and verify steps. This endpoint\n  does not require an API key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  metadata  object  Optional client metadata stored with the signup session;\n  serialized JSON mu...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00cli\x00verify-cli-signup" => Some("Verify CLI signup and create OAuth session\n\nUSAGE\n  $ primitive cli verify-cli-signup [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--signup-token\n    <value>] [--verification-code <value>] [--password <value>]\n\nFLAGS\n  --api-key=<value>            [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n  --body-file=<value>          Path to a JSON file used as the request body.\n                               Same role as --raw-body for callers passing a\n                               saved payload.\n  --envelope                   Print the full response envelope, including\n                               pagination metadata such as meta.cursor. Defaults\n                               to printing only the data payload for backward\n                               compatibility.\n  --json                       Accepted for consistency with task-focused\n                               commands. Generated API commands already print\n                               JSON by default.\n  --password=<value>           password\n  --raw-body=<value>           Full request body as raw JSON. Escape hatch for\n                               nested or complex fields (e.g. arrays); prefer\n                               per-field flags (e.g. --to, --from, --body-text)\n                               when available.\n  --signup-token=<value>       signup_token\n  --time                       Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n  --verification-code=<value>  verification_code\n\nDESCRIPTION\n  Verify CLI signup and create OAuth session\n\n  Verifies the email code for a CLI signup session and creates the\n  account. When the session was started with a `signup_code`, the\n  reserved code is redeemed; sessions started without a code skip\n  the redemption step. Either way an org-scoped OAuth CLI session\n  is created and the token set is returned exactly once. This\n  endpoint does not require an API key.\n\n\n"),
        "help\x00completion" => Some("Output a shell completion script or print setup instructions\n\nUSAGE\n  $ primitive completion SHELL\n\nARGUMENTS\n  SHELL  (bash|zsh|powershell|fish) Shell type\n\nDESCRIPTION\n  Output a shell completion script or print setup instructions\n\n  Output a sourceable shell completion script, or print setup instructions.\n\n  For fish, and for bash/zsh when the output is piped or redirected (e.g. into a\n  completion file under bash_completion.d or zsh's site-functions), this emits\n  the raw completion script. For bash/zsh in an interactive terminal it prints\n  the human-readable setup instructions instead. This keeps a redirected\n  `primitive completion bash > <file>` safe to source -- the file holds an\n  actual completion function, never instructional prose a shell would choke on.\n\nEXAMPLES\n  $ primitive completion bash >> /etc/bash_completion.d/primitive\n\n  $ primitive completion zsh > /usr/local/share/zsh/site-functions/_primitive\n\n  $ primitive completion fish > ~/.config/fish/completions/primitive.fish\n\n"),
        "help\x00config" => Some("Manage Primitive CLI request environments\n\nUSAGE\n  $ primitive config [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\nDESCRIPTION\n  Manage Primitive CLI request environments\n\n  Manage local Primitive CLI request environments for API endpoint overrides and\n  request headers.\n\nCOMMANDS\n  config list   List Primitive CLI request environments\n  config reset  Reset Primitive CLI request environments\n  config set    Set a Primitive CLI request environment\n  config use    Switch the active Primitive CLI request environment\n\n"),
        "help\x00config:list" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "help\x00config:reset" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "help\x00config:set" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "help\x00config:use" => Some("Switch the active Primitive CLI request environment\n\nUSAGE\n  $ primitive config use ENVIRONMENT\n\nARGUMENTS\n  ENVIRONMENT  Environment name to use\n\nDESCRIPTION\n  Switch the active Primitive CLI request environment\n\n  Switch the active Primitive CLI request environment. When this switches to a\n  different environment, the CLI removes saved OAuth credentials so the next\n  authenticated command signs in against the newly active API host.\n\n"),
        "help\x00config\x00list" => Some("List Primitive CLI request environments\n\nUSAGE\n  $ primitive config list [--json] [--show-secrets]\n\nFLAGS\n  --json          Print JSON\n  --show-secrets  Show header values instead of redacting them\n\n"),
        "help\x00config\x00reset" => Some("Reset Primitive CLI request environments\n\nUSAGE\n  $ primitive config reset [-e <value>]\n\nFLAGS\n  -e, --environment=<value>  Only remove one environment\n\n"),
        "help\x00config\x00set" => Some("Set a Primitive CLI request environment\n\nUSAGE\n  $ primitive config set [-e <value>] [--api-base-url <value>] [--header\n    <value>...] [--unset-header <value>...]\n\nFLAGS\n  -e, --environment=<value>      Environment name to create or update. Defaults\n                                 to the active environment, or default when none\n                                 is active.\n      --api-base-url=<value>     API base URL\n      --header=<value>...        Request header in name=value form. Repeatable.\n      --unset-header=<value>...  Request header name to remove. Repeatable.\n\n"),
        "help\x00config\x00use" => Some("Switch the active Primitive CLI request environment\n\nUSAGE\n  $ primitive config use ENVIRONMENT\n\nARGUMENTS\n  ENVIRONMENT  Environment name to use\n\nDESCRIPTION\n  Switch the active Primitive CLI request environment\n\n  Switch the active Primitive CLI request environment. When this switches to a\n  different environment, the CLI removes saved OAuth credentials so the next\n  authenticated command signs in against the newly active API host.\n\n"),
        "help\x00create:autocomplete" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "help\x00create\x00autocomplete" => Some("create autocomplete setup scripts and completion functions\n\nUSAGE\n  $ primitive autocomplete create\n\nDESCRIPTION\n  create autocomplete setup scripts and completion functions\n\n"),
        "help\x00deliveries" => Some("Short aliases for webhook delivery attempts: `primitive deliveries list` and `primitive deliveries replay`.\n\nUSAGE\n  $ primitive deliveries COMMAND\n\nCOMMANDS\n  deliveries list    List webhook deliveries\n  deliveries replay  Replay a webhook delivery\n\n"),
        "help\x00deliveries:list" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "help\x00deliveries:replay" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "help\x00deliveries\x00list" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "help\x00deliveries\x00replay" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "help\x00describe" => Some("Describe a single API operation in detail\n\nUSAGE\n  $ primitive describe COMMAND\n\nARGUMENTS\n  COMMAND  Command id, alias, or SDK operation name to describe, e.g.\n           `emails:list`, `emails:get-email`, or `getEmail`. Run `primitive\n           list-operations | jq -r '.[] | \"\\(.tagCommand):\\(.command)\n           \\(.operationId)\"'` to enumerate generated operation ids.\n\nDESCRIPTION\n  Describe a single API operation in detail\n\n  Print the full operation manifest entry for a single API command, including\n  the path, request schema, response schema, and per-field descriptions sourced\n  from the OpenAPI spec.\n\n  The manifest entry's `responseSchema` carries the inlined JSON Schema for the\n  operation's 200/201 `data` envelope contents (`$ref`s resolved). Use it to\n  look up what specific response fields mean. Examples:\n\n  # Domain setup records returned by add/verify\n  primitive describe domains:add\n  primitive describe addDomain\n\n  # Which of EmailDetail's sender-shaped fields is canonical?\n  primitive describe emails:get | jq '.responseSchema.properties | keys'\n  primitive describe emails:get | jq -r\n  '.responseSchema.properties.from_email.description'\n\n  # What does each value of SentEmailStatus mean?\n  primitive describe sent:get | jq -r\n  '.responseSchema.properties.status.description'\n\n  `requestSchema` is the same shape for the request body when one exists. For a\n  single field across many operations at once, use `primitive list-operations |\n  jq` instead.\n\nEXAMPLES\n  $ primitive describe addDomain\n\n  $ primitive describe domains:add\n\n  $ primitive describe emails:get\n\n  $ primitive describe sent:get\n\n"),
        "help\x00doctor" => Some("Check the local environment and live API for common problems\n\nUSAGE\n  $ primitive doctor [--api-key <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n\nDESCRIPTION\n  Check the local environment and live API for common problems\n\n  Run a one-shot environment health check: Node version, proxy env, CLI auth\n  resolution, /account reachability, and verified-domain status. Fails fast on\n  anything that would block other commands and prints actionable hints for each\n  warning or failure.\n\nEXAMPLES\n  $ primitive doctor\n\n  $ primitive doctor --api-key prim_...\n\n"),
        "help\x00domains" => Some("Claim, verify, manage email domains, and download DNS zone files\n\nUSAGE\n  $ primitive domains COMMAND\n\nCOMMANDS\n  domains add                        Claim a new domain\n  domains add-domain                 Claim a new domain\n  domains delete                     Delete a domain\n  domains delete-domain              Delete a domain\n  domains download-domain-zone-file  Download a DNS zone file for a domain\n  domains list                       List all domains\n  domains list-domains               List all domains\n  domains update                     Update domain settings\n  domains update-domain              Update domain settings\n  domains verify                     Verify domain ownership\n  domains verify-domain              Verify domain ownership\n  domains zone-file                  Download a DNS zone file for a domain\n\n"),
        "help\x00domains:add" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "help\x00domains:add-domain" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "help\x00domains:delete" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "help\x00domains:delete-domain" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "help\x00domains:download-domain-zone-file" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "help\x00domains:list" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "help\x00domains:list-domains" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "help\x00domains:update" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "help\x00domains:update-domain" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "help\x00domains:verify" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "help\x00domains:verify-domain" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "help\x00domains:zone-file" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "help\x00domains\x00add" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "help\x00domains\x00add-domain" => Some("Claim a new domain\n\nUSAGE\n  $ primitive domains add-domain [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain <value>]\n    [--confirmed] [--outbound]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]confirmed     Set to true to confirm replacing an existing mailbox\n                       provider after an mx_conflict response.\n  --domain=<value>     The domain name to claim (e.g. \"example.com\")\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --[no-]outbound      Deprecated and ignored. Outbound DNS is provisioned for\n                       every new domain claim.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Claim a new domain\n\n  Creates an unverified domain claim and returns the exact\n  DNS records to publish in `dns_records`. Publish those\n  records before calling the verify endpoint. To give users\n  an importable DNS file, call `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: after this returns a domain id, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` when the user wants an importable DNS zone\n  file.\n\n"),
        "help\x00domains\x00delete" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "help\x00domains\x00delete-domain" => Some("Delete a domain\n\nUSAGE\n  $ primitive domains delete-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a domain\n\n  Deletes a verified or unverified domain claim.\n\n"),
        "help\x00domains\x00download-domain-zone-file" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains download-domain-zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "help\x00domains\x00list" => Some("List all domains\n\nUSAGE\n  $ primitive domains list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "help\x00domains\x00list-domains" => Some("List all domains\n\nUSAGE\n  $ primitive domains list-domains [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List all domains\n\n  Returns all verified and unverified domains for your organization,\n  sorted by creation date (newest first). Each domain includes a\n  `verified` boolean to distinguish between the two states.\n\n\n"),
        "help\x00domains\x00update" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "help\x00domains\x00update-domain" => Some("Update domain settings\n\nUSAGE\n  $ primitive domains update-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--is-active] [--spam-threshold <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --id=<value>              (required) Resource UUID\n  --[no-]is-active          Whether the domain accepts incoming emails\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --spam-threshold=<value>  Per-domain spam threshold override (Pro plan\n                            required)\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n\nDESCRIPTION\n  Update domain settings\n\n  Update a verified domain's settings. Only verified domains can be\n  updated. Per-domain spam thresholds require a Pro plan.\n\n\n"),
        "help\x00domains\x00verify" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "help\x00domains\x00verify-domain" => Some("Verify domain ownership\n\nUSAGE\n  $ primitive domains verify-domain --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Verify domain ownership\n\n  Checks DNS records required for inbound routing, ownership,\n  and outbound authentication: MX, ownership TXT, SPF, DKIM,\n  DMARC, and TLS-RPT.\n  On success, the domain is promoted from unverified to verified.\n  On failure, returns which checks passed and which failed,\n  plus the exact DNS records still expected. To give users\n  an importable DNS file for missing records, call\n  `downloadDomainZoneFile` or run\n  `primitive domains zone-file --id <domain-id>`.\n\n\n  Tip: if DNS is still missing, run `primitive domains zone-file --id\n  <domain-id> --output <domain>.zone` to give the user an importable DNS zone\n  file.\n\n"),
        "help\x00domains\x00zone-file" => Some("Download a DNS zone file for a domain\n\nUSAGE\n  $ primitive domains zone-file [--api-key <value>] [--domain <value>] [--id\n    <value>] [-o <value>] [--outbound-only] [--time]\n\nFLAGS\n  -o, --output=<value>   Write the zone file to this path instead of stdout.\n                         Defaults to stdout.\n      --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n      --domain=<value>   Domain name to look up before downloading its zone\n                         file. Prefer --id when you have the domain id from\n                         `primitive domains add`.\n      --id=<value>       Domain id returned by `primitive domains add` or\n                         `primitive domains list`.\n      --outbound-only    Include only outbound DNS records. Verified domains\n                         default to outbound-only at the API; this flag is\n                         explicit for agents and scripts.\n      --time             Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Download a DNS zone file for a domain\n\n  Download a BIND-format DNS zone file for a domain claim.\n\n  Use this when a DNS provider supports zone-file import and you want to publish\n  all required records at once instead of copying each record manually. The file\n  is generated by the Primitive API, matching the dashboard download flow.\n\n  Agents: after claiming a domain, tell users they can run `primitive domains\n  zone-file --id <domain-id> --output <domain>.zone` to get an importable DNS\n  zone file.\n\nEXAMPLES\n  $ primitive domains zone-file --id <domain-id>\n\n  $ primitive domains zone-file --id <domain-id> --output example.com.zone\n\n  $ primitive domains zone-file --domain example.com --output example.com.zone\n\n  $ primitive domains zone-file --id <domain-id> --outbound-only\n\n"),
        "help\x00emails" => Some("List, inspect, and wait for received emails. Prefer task aliases like `primitive emails list`, `primitive emails get`, `primitive emails latest`, `primitive emails wait`, and `primitive emails watch`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive emails COMMAND\n\nCOMMANDS\n  emails conversation           Get the conversation an email belongs to\n  emails delete                 Delete an email\n  emails delete-email           Delete an email\n  emails discard-content        Discard email content\n  emails discard-email-content  Discard email content\n  emails download-attachments   Download email attachments\n  emails download-raw           Download raw email\n  emails download-raw-email     Download raw email\n  emails get                    Get inbound email by id\n  emails get-conversation       Get the conversation an email belongs to\n  emails get-email              Get inbound email by id\n  emails latest                 Show the most recent inbound emails as a compact\n                                table\n  emails list                   List inbound emails\n  emails list-emails            List inbound emails\n  emails replay-email-webhooks  Replay email webhooks\n  emails replay-webhooks        Replay email webhooks\n  emails search                 Search inbound emails\n  emails search-emails          Search inbound emails\n  emails wait                   Wait for matching inbound emails\n  emails watch                  Watch inbound emails with filters\n\n"),
        "help\x00emails:conversation" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "help\x00emails:delete" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "help\x00emails:delete-email" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "help\x00emails:discard-content" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "help\x00emails:discard-email-content" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "help\x00emails:download-attachments" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "help\x00emails:download-raw" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "help\x00emails:download-raw-email" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "help\x00emails:get" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "help\x00emails:get-conversation" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "help\x00emails:get-email" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "help\x00emails:latest" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "help\x00emails:list" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "help\x00emails:list-emails" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "help\x00emails:replay-email-webhooks" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "help\x00emails:replay-webhooks" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "help\x00emails:search" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "help\x00emails:search-emails" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "help\x00emails:wait" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "help\x00emails:watch" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "help\x00emails\x00conversation" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "help\x00emails\x00delete" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "help\x00emails\x00delete-email" => Some("Delete an email\n\nUSAGE\n  $ primitive emails delete-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an email\n\n  DELETE /emails/{id}\n\n"),
        "help\x00emails\x00discard-content" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "help\x00emails\x00discard-email-content" => Some("Discard email content\n\nUSAGE\n  $ primitive emails discard-email-content --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Discard email content\n\n  Permanently deletes the email's raw bytes, parsed body (text + HTML),\n  and attachments while preserving metadata (sender, recipient,\n  subject, timestamps, hashes, attachment manifest) for audit logs.\n  Idempotent: a second call returns success with\n  `already_discarded: true` and does no work.\n\n  **Gated** on the customer's discard-content opt-in (managed in the\n  dashboard at Settings > Webhooks). When the toggle is off, this\n  endpoint returns `403` with code `discard_not_enabled` and a\n  message pointing the human at the dashboard. There is intentionally\n  no API to flip this toggle. Opting in to a destructive,\n  non-reversible operation must be a deliberate human click in the\n  UI.\n\n\n"),
        "help\x00emails\x00download-attachments" => Some("Download email attachments\n\nUSAGE\n  $ primitive emails download-attachments --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download email attachments\n\n  Downloads all attachments as a gzip-compressed tar archive.\n  Authenticates via a signed download token (provided in webhook\n  payloads) or a valid session.\n\n\n"),
        "help\x00emails\x00download-raw" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "help\x00emails\x00download-raw-email" => Some("Download raw email\n\nUSAGE\n  $ primitive emails download-raw-email --id <value> [--api-key <value>] [--time]\n    [--token <value>] [--output <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Resource UUID\n  --output=<value>   Write binary response bytes to a file\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n  --token=<value>    Signed download token from webhook payload\n\nDESCRIPTION\n  Download raw email\n\n  Downloads the raw RFC 822 email file (.eml). Authenticates via\n  a signed download token (provided in webhook payloads) or a\n  valid session.\n\n\n"),
        "help\x00emails\x00get" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "help\x00emails\x00get-conversation" => Some("Get the conversation an email belongs to\n\nUSAGE\n  $ primitive emails get-conversation --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the conversation an email belongs to\n\n  Returns the full conversation the given inbound email belongs\n  to, as ordered, ready-to-prompt turns WITH bodies. It resolves\n  the thread from the email and returns every message oldest-first,\n  so an agent that received an email can pass `messages` straight\n  to a chat model in one call instead of walking `/threads/{id}`\n  plus `/emails/{id}` and `/sent-emails/{id}` per message.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and a\n  derived `role`: `inbound` -> `user`, `outbound` -> `assistant`\n  (your own prior replies). The role mapping assumes the caller\n  owns the outbound side, which is the agent-reply case this exists\n  for. If the email has no thread yet (a brand-new message), the\n  conversation is just that one message as a single user turn.\n\n  The message list is capped; check `truncated` to detect when\n  older messages were omitted. Consecutive same-role turns are not\n  merged here; that normalization is model-specific and left to the\n  caller.\n\n\n"),
        "help\x00emails\x00get-email" => Some("Get inbound email by id\n\nUSAGE\n  $ primitive emails get-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get inbound email by id\n\n  Returns the full record for an inbound email received at one\n  of your verified domains, including the parsed text and HTML\n  bodies, threading metadata, SMTP envelope detail, webhook\n  delivery state, and a `replies` array for any outbound sends\n  recorded as replies to this inbound.\n\n  For listing inbound emails (with cursor pagination, status\n  and date filters, and free-text search), use\n  `/emails`. Outbound (sent) email records are NOT returned\n  here; use `/sent-emails/{id}` for those.\n\n  The response carries four sender-shaped fields whose\n  meanings overlap. `from_email` is the canonical \"who sent\n  this\" field for most use cases (parsed bare address from\n  the `From:` header, with a `sender` fallback). `from_header`\n  is the raw header including any display name. `sender` and\n  `smtp_mail_from` both carry the SMTP envelope MAIL FROM\n  (return-path) and are equal by construction; `sender` is\n  the older field name retained for compatibility. See\n  `primitive describe emails:get | jq '.responseSchema.properties'`\n  for per-field detail.\n\n\n"),
        "help\x00emails\x00latest" => Some("Show the most recent inbound emails as a compact table\n\nUSAGE\n  $ primitive emails latest [--api-key <value>] [--limit <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the raw response envelope (with full UUIDs and meta)\n                     as JSON on STDOUT instead of the text table. Useful for\n                     piping into `jq`, capturing ids for follow-up commands, or\n                     scripting.\n  --limit=<value>    [default: 10] Number of rows to print (1-100, default 10).\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the most recent inbound emails as a compact table\n\n  Print the N most recent inbound emails as a one-line-per-row text table.\n  Designed for quick triage and visual scanning. For programmatic access, use\n  `primitive emails list` (full JSON envelope, cursor pagination, filters) or\n  pass `--json` here for the same raw shape without pagination/filters.\n\n  ID display is TTY-aware. When STDOUT is a terminal, the table truncates each\n  row's id to the first 8 characters for readability. When STDOUT is piped or\n  redirected (the row stream is being consumed by another command), the full\n  UUID is printed so the id can be fed straight back into `emails:get-email`,\n  `emails:delete-email`, etc. without a separate `--json` round-trip.\n\n  Output streams: the column header line is written to STDERR so the row data on\n  STDOUT stays grep/awk-friendly. `--json` writes everything (including the\n  envelope) to STDOUT and is equivalent to running `emails list --limit N` for\n  the same N.\n\nEXAMPLES\n  $ primitive emails latest\n\n  $ primitive emails latest --limit 25\n\n  $ primitive emails latest | head -1 | awk '{print $1}'  # full UUID since piped\n\n  $ primitive emails latest --json | jq '.data[0].id'\n\n"),
        "help\x00emails\x00list" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "help\x00emails\x00list-emails" => Some("List inbound emails\n\nUSAGE\n  $ primitive emails list-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--domain-id <value>]\n    [--status <value>] [--search <value>] [--date-from <value>] [--date-to\n    <value>] [--since <value>] [--wait <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Filter emails created on or after this timestamp\n\n  --date-to=<value>\n      Filter emails created on or before this timestamp\n\n  --domain-id=<value>\n      Filter by domain ID\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --search=<value>\n      Search subject, sender, and recipient (case-insensitive)\n\n  --since=<value>\n      Forward-tail cursor. Returns rows that became visible AFTER this\n      cursor, oldest-first, so a caller can stream new inbound mail by\n      re-passing the cursor from each response. Mutually exclusive with\n      `cursor` (which pages history newest-first). Pass the `meta.cursor`\n      from the previous `since` response; an empty page means caught up.\n\n  --status=<value>\n      Filter inbound rows by lifecycle status. See `EmailStatus`\n      for what each value means. Note that the webhook delivery\n      state is a SEPARATE lifecycle on the same row; filter by\n      `webhook_status` semantics is not currently supported on\n      this endpoint.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait=<value>\n      Long-poll: hold the request up to this many seconds waiting for new\n      mail past `since`, returning as soon as any arrives (or an empty\n      page when the wait elapses). Requires `since`. Omitted means no wait\n      (returns immediately); the server treats an absent value as 0. NOT\n      given an OpenAPI `default` on purpose: a default makes some\n      generators (e.g. openapi-python-client) send `wait=0` on every call,\n      which then fails the `wait` requires `since` check for plain history\n      listings.\n\nDESCRIPTION\n  List inbound emails\n\n  Returns a paginated list of INBOUND emails received at your\n  verified domains. Outbound messages sent via /send-mail are\n  not included; this endpoint is the inbox view, not a\n  unified send/receive history.\n\n  Supports filtering by domain, status, date range, and\n  free-text search across subject, sender, and recipient\n  fields.\n\n  For a compact text-table summary of the most recent N\n  inbounds (no filters, no cursor pagination), the CLI ships\n  `primitive emails latest` as a one-line-per-email shortcut.\n  It's TTY-aware so id columns are full UUIDs when piped, and\n  a `--json` flag returns the same envelope this endpoint\n  does. Use whichever fits the call site.\n\n\n"),
        "help\x00emails\x00replay-email-webhooks" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-email-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "help\x00emails\x00replay-webhooks" => Some("Replay email webhooks\n\nUSAGE\n  $ primitive emails replay-webhooks --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay email webhooks\n\n  Re-delivers the webhook payload for this email to all active\n  endpoints matching the email's domain. Rate limited per-email\n  (short cooldown between successive replays of the same email)\n  and per-org (burst + sustained windows), sharing an org-wide\n  budget with delivery replays.\n\n\n"),
        "help\x00emails\x00search" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "help\x00emails\x00search-emails" => Some("Search inbound emails\n\nUSAGE\n  $ primitive emails search-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--from <value>] [--to <value>] [--subject\n    <value>] [--body <value>] [--domain-id <value>] [--reply-to-sent-email-id\n    <value>] [--status <value>] [--date-from <value>] [--date-to <value>]\n    [--has-attachment true|false] [--spam-score-lt <value>] [--spam-score-gte\n    <value>] [--sort relevance|received_at_desc|received_at_asc] [--cursor\n    <value>] [--limit <value>] [--snippet true|false] [--include-facets\n    true|false]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text search restricted to the parsed text body.\n\n  --cursor=<value>\n      Opaque pagination cursor from a previous search response.\n\n  --date-from=<value>\n      Filter emails received on or after this timestamp.\n\n  --date-to=<value>\n      Filter emails received on or before this timestamp.\n\n  --domain-id=<value>\n      Filter by domain ID.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Filter by sender address or sender domain.\n\n  --has-attachment=<option>\n      Filter by whether the email has one or more attachments.\n      <options: true|false>\n\n  --include-facets=<option>\n      Include facet counts for sender, domain, status, and attachment presence.\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --q=<value>\n      Full-text search DSL query.\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are replies to a specific\n      outbound send. The value is a `sent_emails.id` (UUID). At\n      inbound ingest, Primitive matches the parsed In-Reply-To\n      header (or References as a fallback) against\n      `sent_emails.message_id` in the same org and records the\n      resolved id on `emails.reply_to_sent_email_id`. This filter\n      is the strict-threading lookup behind `primitive chat` and\n      any UI that wants to show the inbound reply to a given\n      send. NULL on inbound that isn't a threaded reply to one\n      of your sends, so existing emails received before this\n      ingestion landed will not match.\n\n  --snippet=<option>\n      Include subject/body highlight snippets when text search is active.\n      <options: true|false>\n\n  --sort=<option>\n      Sort mode. Defaults to relevance when a text query is present,\n      otherwise `received_at_desc`.\n\n      <options: relevance|received_at_desc|received_at_asc>\n\n  --spam-score-gte=<value>\n      Filter to emails with spam score greater than or equal to this value.\n\n  --spam-score-lt=<value>\n      Filter to emails with spam score below this value.\n\n  --status=<value>\n      Filter by inbound email lifecycle status.\n\n  --subject=<value>\n      Full-text search restricted to the subject field.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Filter by recipient address or recipient domain.\n\nDESCRIPTION\n  Search inbound emails\n\n  Searches inbound emails with structured filters and optional\n  full-text matching across parsed email fields. This endpoint is\n  optimized for filtered inbox views and CLI polling workflows:\n  callers that only need new accepted mail can pass\n  `sort=received_at_asc`, `snippet=false`, `include_facets=false`,\n  and a `date_from` timestamp.\n\n  `q`, `subject`, and `body` use the same English full-text index\n  as the web inbox search. Structured filters such as `from`, `to`,\n  `domain_id`, status, attachment presence, and spam score bounds\n  are combined with the text query.\n\n\n"),
        "help\x00emails\x00wait" => Some("Wait for matching inbound emails\n\nUSAGE\n  $ primitive emails wait [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [-n <value>] [--page-size <value>]\n    [--q <value>] [--reply-to-sent-email-id <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--table] [--timeout <value>] [--to <value>]\n\nFLAGS\n  -n, --number=<value>\n      [default: 1] Exit successfully after this many matching emails\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Full-text body filter\n\n  --domain=<value>\n      Filter by inbound email domain\n\n  --domain-id=<value>\n      Filter by domain UUID\n\n  --from=<value>\n      Filter by sender address or domain\n\n  --has-attachment\n      Only match emails with one or more attachments\n\n  --include-existing\n      Start from existing matching emails instead of only new arrivals\n\n  --interval=<value>\n      [default: 2] Seconds to wait between empty polls\n\n  --page-size=<value>\n      [default: 50] Emails to fetch per poll (1-100)\n\n  --q=<value>\n      Full-text search DSL query\n\n  --reply-to-sent-email-id=<value>\n      Filter to inbound emails that are threaded replies to a specific outbound\n      send (UUID from a /v1/send-mail response). Combine with --to and --since for\n      the strictest version of the wait-for-reply pattern.\n\n  --since=<value>\n      Only match emails received on or after this date/time\n\n  --spam-score-gte=<value>\n      Only match emails with spam score greater than or equal to this value\n\n  --spam-score-lt=<value>\n      Only match emails with spam score below this value\n\n  --subject=<value>\n      Full-text subject filter\n\n  --table\n      Print a human-readable table instead of JSONL\n\n  --timeout=<value>\n      [default: 300] Seconds to wait before exiting nonzero; 0 waits forever\n\n  --to=<value>\n      Filter by recipient address or domain\n\nDESCRIPTION\n  Wait for matching inbound emails\n\n  Poll until matching inbound emails arrive, printing each match as it is found.\n\nEXAMPLES\n  $ primitive emails wait --to test@example.com\n\n  $ primitive emails wait --subject verify --number 5 --timeout 120\n\n  $ primitive emails wait --q 'domain:example.com' --table\n\n"),
        "help\x00emails\x00watch" => Some("Watch inbound emails with filters\n\nUSAGE\n  $ primitive emails watch [--api-key <value>] [--body <value>] [--domain\n    <value>] [--domain-id <value>] [--from <value>] [--has-attachment]\n    [--include-existing] [--interval <value>] [--jsonl] [--number <value>]\n    [--page-size <value>] [--q <value>] [--seconds <value>] [--since <value>]\n    [--spam-score-gte <value>] [--spam-score-lt <value>] [--subject <value>]\n    [--to <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body=<value>            Full-text body filter\n  --domain=<value>          Filter by inbound email domain\n  --domain-id=<value>       Filter by domain UUID\n  --from=<value>            Filter by sender address or domain\n  --has-attachment          Only show emails with one or more attachments\n  --include-existing        Start from existing matching emails instead of only\n                            new arrivals\n  --interval=<value>        [default: 2] Seconds to wait between empty polls\n  --jsonl                   Print each email as one JSON object per line\n  --number=<value>          Exit after printing this many matching emails\n  --page-size=<value>       [default: 50] Emails to fetch per poll (1-100)\n  --q=<value>               Full-text search DSL query\n  --seconds=<value>         Exit after this many seconds\n  --since=<value>           Only show emails received on or after this date/time\n  --spam-score-gte=<value>  Only show emails with spam score greater than or\n                            equal to this value\n  --spam-score-lt=<value>   Only show emails with spam score below this value\n  --subject=<value>         Full-text subject filter\n  --to=<value>              Filter by recipient address or domain\n\nDESCRIPTION\n  Watch inbound emails with filters\n\n  Poll for new inbound emails and print matching messages as they arrive.\n\nEXAMPLES\n  $ primitive emails watch --to support@example.com\n\n  $ primitive emails watch --subject verify --seconds 300\n\n  $ primitive emails watch --number 20 --jsonl\n\n"),
        "help\x00endpoints" => Some("Manage webhook endpoints that receive email events\n\nUSAGE\n  $ primitive endpoints COMMAND\n\nCOMMANDS\n  endpoints create           Create a webhook endpoint\n  endpoints create-endpoint  Create a webhook endpoint\n  endpoints delete           Delete a webhook endpoint\n  endpoints delete-endpoint  Delete a webhook endpoint\n  endpoints list             List webhook endpoints\n  endpoints list-endpoints   List webhook endpoints\n  endpoints test             Send a test webhook\n  endpoints test-endpoint    Send a test webhook\n  endpoints update           Update a webhook endpoint\n  endpoints update-endpoint  Update a webhook endpoint\n\n"),
        "help\x00endpoints:create" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00endpoints:create-endpoint" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00endpoints:delete" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "help\x00endpoints:delete-endpoint" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "help\x00endpoints:list" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "help\x00endpoints:list-endpoints" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "help\x00endpoints:test" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "help\x00endpoints:test-endpoint" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "help\x00endpoints:update" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00endpoints:update-endpoint" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00endpoints\x00create" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00endpoints\x00create-endpoint" => Some("Create a webhook endpoint\n\nUSAGE\n  $ primitive endpoints create-endpoint [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--domain-id\n    <value>] [--enabled] [--function-id <value>] [--is-route-target] [--kind\n    http|function] [--url <value>]\n\nFLAGS\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --domain-id=<value>     Restrict to emails from a specific domain\n  --[no-]enabled          Whether the endpoint is active\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --function-id=<value>   The Function to invoke. Required when kind is\n                          function.\n  --[no-]is-route-target  Create this endpoint as a route-target: reachable only\n                          via an explicit recipient route, never a domain's\n                          default destination, and exempt from the\n                          one-endpoint-per-domain rule.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --kind=<option>         http: deliver to a webhook URL (provide url).\n                          function: invoke a Primitive Function (provide\n                          function_id, omit url).\n                          <options: http|function>\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --url=<value>           The webhook URL to deliver events to. Required when\n                          kind is http; omit for function endpoints.\n\nDESCRIPTION\n  Create a webhook endpoint\n\n  Creates a new webhook endpoint. If a deactivated endpoint\n  with the same URL and domain exists, it is reactivated\n  instead. Subject to plan limits on the number of active\n  endpoints.\n\n  **Signing is account-scoped, not per-endpoint.** This call\n  does not return any signing material; every endpoint on the\n  account uses the same webhook secret, fetched via\n  `GET /account/webhook-secret`. See the API-level \"Webhook\n  signing\" section for the full wire format (header name,\n  signed string, hash algo, secret format, tolerance) and a\n  language-agnostic verification recipe.\n\n  After creating the endpoint, fire a test delivery against\n  it via `POST /endpoints/{id}/test` to confirm your verifier\n  accepts the signature.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object  Endpoint-specific filtering rules\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00endpoints\x00delete" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "help\x00endpoints\x00delete-endpoint" => Some("Delete a webhook endpoint\n\nUSAGE\n  $ primitive endpoints delete-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a webhook endpoint\n\n  Soft-deletes a webhook endpoint. The endpoint will no longer\n  receive webhook deliveries.\n\n\n"),
        "help\x00endpoints\x00list" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "help\x00endpoints\x00list-endpoints" => Some("List webhook endpoints\n\nUSAGE\n  $ primitive endpoints list-endpoints [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List webhook endpoints\n\n  Returns all active (non-deleted) webhook endpoints.\n\n"),
        "help\x00endpoints\x00test" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "help\x00endpoints\x00test-endpoint" => Some("Send a test webhook\n\nUSAGE\n  $ primitive endpoints test-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Send a test webhook\n\n  Sends a sample `email.received` event to the endpoint. The request\n  includes SSRF protection (private IP rejection and DNS pinning).\n  Rate limited to 4 per minute and 30 per hour (non-exempt).\n  Successful deliveries and verified-domain endpoints are exempt\n  from the rate limit.\n\n\n"),
        "help\x00endpoints\x00update" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00endpoints\x00update-endpoint" => Some("Update a webhook endpoint\n\nUSAGE\n  $ primitive endpoints update-endpoint --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--url <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  domain_id\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --url=<value>        New webhook URL (triggers endpoint rotation)\n\nDESCRIPTION\n  Update a webhook endpoint\n\n  Updates an active webhook endpoint. If the URL is changed, the old\n  endpoint is deactivated and a new one is created (or an existing\n  deactivated endpoint with the new URL is reactivated).\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  rules  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00filters" => Some("Manage whitelist and blocklist filter rules\n\nUSAGE\n  $ primitive filters COMMAND\n\nCOMMANDS\n  filters create         Create a filter rule\n  filters create-filter  Create a filter rule\n  filters delete         Delete a filter rule\n  filters delete-filter  Delete a filter rule\n  filters list           List filter rules\n  filters list-filters   List filter rules\n  filters update         Update a filter rule\n  filters update-filter  Update a filter rule\n\n"),
        "help\x00filters:create" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "help\x00filters:create-filter" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "help\x00filters:delete" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "help\x00filters:delete-filter" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "help\x00filters:list" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "help\x00filters:list-filters" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "help\x00filters:update" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "help\x00filters:update-filter" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "help\x00filters\x00create" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "help\x00filters\x00create-filter" => Some("Create a filter rule\n\nUSAGE\n  $ primitive filters create-filter [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--pattern <value>]\n    [--type whitelist|blocklist] [--domain-id <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain-id=<value>  Restrict filter to a specific domain (Pro plan required)\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --pattern=<value>    Email address or pattern to filter\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --type=<option>      type\n                       <options: whitelist|blocklist>\n\nDESCRIPTION\n  Create a filter rule\n\n  Creates a new whitelist or blocklist filter. Per-domain filters\n  require a Pro plan. Patterns are stored as lowercase.\n\n\n"),
        "help\x00filters\x00delete" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "help\x00filters\x00delete-filter" => Some("Delete a filter rule\n\nUSAGE\n  $ primitive filters delete-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a filter rule\n\n  DELETE /filters/{id}\n\n"),
        "help\x00filters\x00list" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "help\x00filters\x00list-filters" => Some("List filter rules\n\nUSAGE\n  $ primitive filters list-filters [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List filter rules\n\n  Returns all whitelist and blocklist filter rules.\n\n"),
        "help\x00filters\x00update" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "help\x00filters\x00update-filter" => Some("Update a filter rule\n\nUSAGE\n  $ primitive filters update-filter --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a filter rule\n\n  Toggle a filter's enabled state.\n\n"),
        "help\x00functions" => Some("Deploy JavaScript handlers that run on inbound mail. Prefer `primitive functions templates`, `primitive functions init`, `primitive functions deploy`, `primitive functions redeploy`, `primitive functions list`, `primitive functions get`, `primitive functions logs`, and `primitive functions set-secret`; generated API names remain available for compatibility.\n\nUSAGE\n  $ primitive functions COMMAND\n\nCOMMANDS\n  functions create-function              Deploy a function\n  functions create-function-secret       Create or update a secret\n  functions create-org-secret            Create or update an org secret\n  functions delete                       Delete a function\n  functions delete-function              Delete a function\n  functions delete-function-secret       Delete a secret\n  functions delete-org-secret            Delete an org secret\n  functions delete-secret                Delete a secret\n  functions deploy                       Deploy a new function from a bundled\n                                         handler file\n  functions get                          Get a function\n  functions get-function                 Get a function\n  functions get-function-routing         Get a function's current route binding\n  functions get-function-test-run-trace  Get a function test run trace\n  functions get-org-routing-topology     Get the org's function routing topology\n  functions init                         Scaffold a new Primitive Function\n                                         project ready for functions deploy\n  functions list                         List functions\n  functions list-function-logs           List a function's execution logs\n  functions list-function-secrets        List a function's secrets\n  functions list-functions               List functions\n  functions list-org-secrets             List org-level (global) secrets\n  functions list-secrets                 List a function's secrets\n  functions logs                         List or follow a function's execution\n                                         logs\n  functions redeploy                     Redeploy a function from a bundled\n                                         handler file\n  functions route-get                    Show a function's current route binding\n  functions route-set                    Bind inbound mail to a function\n  functions route-unset                  Unbind any route from a function\n  functions routing-topology             Show the org-wide function routing\n                                         topology\n  functions set-function-route           Bind a route to a function\n  functions set-function-secret          Set a secret by key\n  functions set-org-secret               Set an org secret by key\n  functions set-secret                   Write a function secret (optionally\n                                         redeploying to push it live)\n  functions templates                    List available Primitive Function\n                                         templates\n  functions test                         Trigger a test invocation; with --wait,\n                                         watch it land\n  functions test-function                Trigger a test invocation; with --wait,\n                                         watch it land\n  functions unset-function-route         Unbind any route from a function\n  functions update-function              Update and redeploy a function\n\n"),
        "help\x00functions:create-function" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "help\x00functions:create-function-secret" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "help\x00functions:create-org-secret" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "help\x00functions:delete" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "help\x00functions:delete-function" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "help\x00functions:delete-function-secret" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "help\x00functions:delete-org-secret" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "help\x00functions:delete-secret" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "help\x00functions:deploy" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "help\x00functions:get" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "help\x00functions:get-function" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "help\x00functions:get-function-routing" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "help\x00functions:get-function-test-run-trace" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "help\x00functions:get-org-routing-topology" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "help\x00functions:init" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "help\x00functions:list" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "help\x00functions:list-function-logs" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "help\x00functions:list-function-secrets" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "help\x00functions:list-functions" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "help\x00functions:list-org-secrets" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "help\x00functions:list-secrets" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "help\x00functions:logs" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "help\x00functions:redeploy" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "help\x00functions:route-get" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "help\x00functions:route-set" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "help\x00functions:route-unset" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "help\x00functions:routing-topology" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "help\x00functions:set-function-route" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00functions:set-function-secret" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "help\x00functions:set-org-secret" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "help\x00functions:set-secret" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "help\x00functions:templates" => Some("List available Primitive Function templates\n\nUSAGE\n  $ primitive functions templates [--json]\n\nGLOBAL FLAGS\n  --json  Format output as json.\n\nDESCRIPTION\n  List available Primitive Function templates\n\n  List Primitive Function templates available to `primitive functions init`.\n\n  The default table is optimized for quick terminal discovery. Use\n  --json when an agent or script needs stable metadata for searching,\n  ranking, or choosing a template programmatically.\n\nEXAMPLES\n  $ primitive functions templates\n\n  $ primitive functions templates --json\n\n  $ primitive functions init my-fn --template email-reply\n\n"),
        "help\x00functions:test" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "help\x00functions:test-function" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "help\x00functions:unset-function-route" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "help\x00functions:update-function" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "help\x00functions\x00create-function" => Some("Deploy a function\n\nUSAGE\n  $ primitive functions create-function [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--code <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       Pre-built handler as a single ESM module. Up to 1 MiB\n                       UTF-8. Must export a default `{ async fetch(req, env,\n                       ctx) { ... } }` object. Provide either `code` or `files`,\n                       not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --name=<value>       Slug-style name. Lowercase letters, digits, hyphens, and\n                       underscores. 1 to 64 characters. Must be unique within\n                       the org; a 409 is returned on collision.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  Optional source map for the bundle. Up to 5 MiB UTF-8.\n                       Stored with the deployment attempt and sent to the\n                       runtime to symbolicate stack traces in the function's\n                       logs. Only valid with `code`.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Deploy a function\n\n  Creates and deploys a new function. The handler must be a single\n  ESM module whose default export is an object with an async\n  `fetch(request, env)` method (Workers-style). Primitive signs\n  each delivery and forwards the `Primitive-Signature` header to\n  the handler. Verify the raw request body with\n  `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification\n  the request body parses to a webhook event whose `event` field is\n  `email.received` for normal inbound mail, or a machine-mail type\n  (`email.bounced`, `email.tls_report`, `email.dmarc_report`,\n  `email.dmarc_failure`) for bounces and reports. Code is bundled\n  before being uploaded; ship a single self-contained file rather\n  than relying on external imports.\n\n  **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`\n  (optional) is capped at 5 MiB UTF-8, stored with each deployment\n  attempt, and sent to the runtime so stack traces can resolve to\n  original source files.\n\n  **Routing.** On successful deploy, the function code is live\n  in the runtime, but inbound mail will not reach it until at\n  least one route is bound. Routes are managed from the Primitive\n  dashboard. A `deploy_status` of `deployed` means the script is\n  installed, not that the function is receiving mail. The\n  internal runtime URL is not returned by the API and is not a\n  customer-facing integration surface.\n\n  **Secrets.** New functions ship with the managed secrets\n  (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,\n  `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via\n  `POST /functions/{id}/secrets`; secret writes only land in the\n  running handler on the next redeploy.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents (for ex...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions deploy --name <name> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "help\x00functions\x00create-function-secret" => Some("Create or update a secret\n\nUSAGE\n  $ primitive functions create-function-secret --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--key\n    <value>] [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys (e.g.\n                       PRIMITIVE_WEBHOOK_SECRET, PRIMITIVE_API_KEY, and\n                       PRIMITIVE_API_BASE_URL) are reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update a secret\n\n  Idempotent insert-or-update keyed on `(function_id, key)`.\n  Returns 201 the first time the key is set, 200 on subsequent\n  updates. Values are encrypted at rest and only become visible\n  to the running handler on the next deploy (`PUT /functions/{id}`\n  with the existing code is sufficient to refresh bindings).\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "help\x00functions\x00create-org-secret" => Some("Create or update an org secret\n\nUSAGE\n  $ primitive functions create-org-secret [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--key <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        Uppercase letters, digits, and underscores. Must start\n                       with a letter or underscore. System-managed keys are\n                       reserved.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      Secret value, up to 4096 UTF-8 bytes. Encrypted at rest.\n                       Never returned by any read endpoint.\n\nDESCRIPTION\n  Create or update an org secret\n\n  Idempotent insert-or-update keyed on `(org_id, key)`. Returns\n  201 the first time the key is set, 200 on subsequent updates.\n  Values are encrypted at rest. A changed value lands in a\n  function only on that function's next deploy.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,\n  digits, underscores; first character is a letter or\n  underscore). Values are at most 4096 UTF-8 bytes. System-\n  managed keys are reserved and rejected.\n\n\n"),
        "help\x00functions\x00delete" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "help\x00functions\x00delete-function" => Some("Delete a function\n\nUSAGE\n  $ primitive functions delete-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a function\n\n  Soft-deletes the function row, removes the script from the edge\n  runtime, and deactivates any route bound to this function so no\n  further inbound mail is delivered. Past deploy history,\n  invocations, and logs are retained.\n\n  Returns 502 if the runtime delete fails partway; the function\n  row stays in place and the call is safe to retry until it\n  succeeds.\n\n\n"),
        "help\x00functions\x00delete-function-secret" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "help\x00functions\x00delete-org-secret" => Some("Delete an org secret\n\nUSAGE\n  $ primitive functions delete-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete an org secret\n\n  Removes the org secret. Functions keep the previous value until\n  each is redeployed. Returns 404 if the key did not exist.\n\n\n"),
        "help\x00functions\x00delete-secret" => Some("Delete a secret\n\nUSAGE\n  $ primitive functions delete-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --key=<value>      (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a secret\n\n  Removes the secret. The binding stays live in the running\n  handler until the next deploy refreshes the binding set\n  (`PUT /functions/{id}` with the existing code is sufficient).\n  Returns 404 if the key did not exist. Managed system keys\n  cannot be deleted.\n\n\n"),
        "help\x00functions\x00deploy" => Some("Deploy a new function from a bundled handler file\n\nUSAGE\n  $ primitive functions deploy --name <value> [--api-key <value>] [--file\n    <value>] [--source <value>] [--source-map-file <value>] [--secret\n    <value>...] [--secret-from-env <value>...] [--secret-from-file <value>...]\n    [--secret-from-env-file <value>...] [--secret-from-stdin <value>] [--wait]\n    [--timeout <value>] [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      Path to the bundled ESM handler file (single self-contained module). Loaded\n      as the `code` body field. Exactly one of --file or --source is required.\n\n  --name=<value>\n      (required) Slug-style name. Lowercase letters, digits, hyphens, underscores.\n      1-64 chars. Must be unique within the org.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to seed on the deployed function. Repeatable. KEY must\n      match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only the first `=` is\n      treated as a delimiter). Each KEY may only appear once per command. Passing\n      one or more --secret flags fans out the deploy to create-function,\n      set-secret per pair, then a final redeploy so the running handler picks up\n      the bindings. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and seed on the deployed function.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and seed on the deployed\n      function. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and seed on the deployed function.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and seed on the deployed function. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source=<value>\n      Path to a project directory (containing package.json and src/) to deploy via\n      managed build: the source is uploaded and the server installs dependencies,\n      bundles for the Workers runtime, and deploys. Idempotent by name (creates\n      the function, or redeploys it if --name already exists), so it is safe to\n      run on every push. Exactly one of --file or --source is required.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Stored with the deployment\n      attempt and used to symbolicate stack traces in function logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Deploy a new function from a bundled handler file\n\n  Deploy a new function from a bundled handler file. Agent-grade shortcut for\n  functions:create-function.\n\n  Reads the bundle off disk (--file) instead of forcing the caller to\n  serialize the source into a JSON body. Use the underlying operation\n  `functions:create-function` if you need the full flag surface\n  (raw-body JSON, etc.).\n\n  Pass secret source flags to seed bindings in the same command. Keys\n  must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore).\n\n  With one or more secrets the deploy fans out to multiple API calls.\n  For --file (and for --source when no function with the given name\n  exists yet): create-function, set-secret per pair, then a final\n  update-function so the running handler picks up the bindings. For\n  --source against an existing function name: the create-function step\n  is replaced by an id lookup, then set-secret per pair, then a single\n  update-function that binds the new code and the new secret env in\n  one step (avoiding an intermediate redeploy that would briefly run\n  the new code with the previous secret bindings).\n\n  If a secret write fails before the final redeploy, the function row\n  carries whatever bindings landed but the running handler has NOT yet\n  picked them up. Re-run `primitive functions set-secret` for the\n  missing keys, then re-run `primitive functions deploy` (or\n  `functions redeploy`) to push them live. Safe sources: --secret-from-env KEY\n  reads process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions deploy --name forwarder --file ./bundle.js\n\n  $ primitive functions deploy --name triage --source ./triage-agent\n\n  $ primitive functions deploy --name triage --source . --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --wait\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions deploy --name forwarder --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-env-file .env.local:OWNER_EMAIL\n\n  $ primitive functions deploy --name triage --source . --secret-from-env ANTHROPIC_API_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions deploy --name forwarder --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "help\x00functions\x00get" => Some("Get a function\n\nUSAGE\n  $ primitive functions get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "help\x00functions\x00get-function" => Some("Get a function\n\nUSAGE\n  $ primitive functions get-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function\n\n  Returns the full record for a function, including its current\n  source code and the deploy status / error from the most recent\n  deploy attempt.\n\n\n"),
        "help\x00functions\x00get-function-routing" => Some("Get a function's current route binding\n\nUSAGE\n  $ primitive functions get-function-routing --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function's current route binding\n\n  Returns the endpoint binding for the function, or null when no\n  route is currently bound. The binding identifies whether the\n  function receives mail for a specific domain (scoped) or for any\n  active domain that has no scoped binding (fallback).\n\n\n"),
        "help\x00functions\x00get-function-test-run-trace" => Some("Get a function test run trace\n\nUSAGE\n  $ primitive functions get-function-test-run-trace --id <value> --run-id <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --run-id=<value>   (required) Function test run id returned by POST\n                     /functions/{id}/test.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function test run trace\n\n  Returns the current end-to-end trace for a function test run.\n  The trace is intentionally partial while the test is still in\n  flight: callers can poll this endpoint and watch it fill in\n  from send -> inbound -> webhook deliveries -> outbound\n  requests, logs, and replies.\n\n\n"),
        "help\x00functions\x00get-org-routing-topology" => Some("Get the org's function routing topology\n\nUSAGE\n  $ primitive functions get-org-routing-topology [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get the org's function routing topology\n\n  Returns a single snapshot of how inbound mail is routed across\n  this org's active domains and functions: which active domain has\n  which function bound, the org's fallback function (if any), and\n  every deployed function with no route bound. Use this to answer\n  \"which of my functions actually receive mail?\" diagnostically.\n\n\n"),
        "help\x00functions\x00init" => Some("Scaffold a new Primitive Function project ready for functions deploy\n\nUSAGE\n  $ primitive functions init NAME [--out-dir <value>] [--template\n    email-reply]\n\nARGUMENTS\n  NAME  Function name. Lowercase letters, digits, hyphens, underscores. 1-63\n        chars. Used as the directory name (when --out-dir is not set) and as the\n        package.json name.\n\nFLAGS\n  --out-dir=<value>    Directory to scaffold into. Defaults to ./<name>/. Must\n                       not already exist.\n  --template=<option>  [default: email-reply] Function template id. Run\n                       `primitive functions templates` to list templates.\n                       <options: email-reply>\n\nDESCRIPTION\n  Scaffold a new Primitive Function project ready for functions deploy\n\n  Scaffold a new Primitive Function project from a Primitive-owned template.\n\n  The scaffolded handler imports `createPrimitiveClient` from\n  `@primitivedotdev/sdk/api` and demonstrates the canonical pattern:\n  parse the email.received event, send a reply via the SDK, return a\n  JSON envelope. The build script uses esbuild's JS API and emits\n  ./dist/handler.js, ready to hand to `primitive functions deploy --file`.\n\n  Refuses to overwrite an existing directory. Use --out-dir to pick a\n  different target path than ./<name>/. Run `primitive functions templates`\n  to inspect available templates.\n\nEXAMPLES\n  $ primitive functions init my-fn\n\n  $ primitive functions init my-fn --template email-reply\n\n  $ primitive functions init my-fn --out-dir ./functions/my-fn\n\n"),
        "help\x00functions\x00list" => Some("List functions\n\nUSAGE\n  $ primitive functions list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "help\x00functions\x00list-function-logs" => Some("List a function's execution logs\n\nUSAGE\n  $ primitive functions list-function-logs --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Opaque pagination cursor from a previous response's\n                     `next_cursor`. Omit on the first call.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return. Clamped to\n                     1..200; default\n                     50.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's execution logs\n\n  Returns the most recent `function_logs` rows for the function,\n  newest first. Each row is a single `console.log` / `console.error`\n  invocation captured from the running handler.\n\n  Page through history with the opaque `cursor` returned as\n  `next_cursor`; pass it back as the `cursor` query param on the\n  next call. `next_cursor` is `null` when there are no further\n  rows. The cursor format is an implementation detail and should\n  not be parsed by callers.\n\n\n"),
        "help\x00functions\x00list-function-secrets" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-function-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "help\x00functions\x00list-functions" => Some("List functions\n\nUSAGE\n  $ primitive functions list-functions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List functions\n\n  Returns every active (non-deleted) function in the org, newest\n  first. Each entry carries deploy status and timestamps. To\n  inspect the source code or deploy errors, use `GET /functions/{id}`.\n\n\n"),
        "help\x00functions\x00list-org-secrets" => Some("List org-level (global) secrets\n\nUSAGE\n  $ primitive functions list-org-secrets [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List org-level (global) secrets\n\n  Returns metadata for every org-level secret. Org secrets apply\n  to every function in the org and are read as `env.<KEY>` in\n  handlers. **Values are never returned.** Secret writes are\n  write-only. A function-level secret of the same name overrides\n  the org-level value for that function.\n\n\n"),
        "help\x00functions\x00list-secrets" => Some("List a function's secrets\n\nUSAGE\n  $ primitive functions list-secrets --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List a function's secrets\n\n  Returns metadata for every secret bound to the function, with\n  managed entries (provisioned by Primitive) listed first and\n  user-set entries listed alphabetically after. **Values are\n  never returned.** Secret writes are write-only.\n\n  Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,\n  `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a\n  `description` instead of `created_at` / `updated_at`. They\n  cannot be created, updated, or deleted via this API.\n\n\n"),
        "help\x00functions\x00logs" => Some("List or follow a function's execution logs\n\nUSAGE\n  $ primitive functions logs --id <value> [--api-key <value>] [--limit\n    <value>] [--cursor <value>] [-f] [--jsonl] [--poll-interval <value>]\n    [--time]\n\nFLAGS\n  -f, --follow                 Keep polling the newest logs and print rows not\n                               seen yet.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --cursor=<value>         Opaque pagination cursor from a previous logs\n                               response. Not supported with --follow.\n      --id=<value>             (required) Function id (UUID).\n      --jsonl                  Print one compact JSON object per log row.\n      --limit=<value>          [default: 50] Maximum rows to fetch per poll.\n                               Server clamps to 1..200.\n      --poll-interval=<value>  [default: 2] Seconds between polls when --follow\n                               is set.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n\nDESCRIPTION\n  List or follow a function's execution logs\n\n  List or follow function execution logs. Defaults to compact text output; use\n  --jsonl for one JSON object per log row.\n\nEXAMPLES\n  $ primitive functions logs --id <fn-id>\n\n  $ primitive functions logs --id <fn-id> --jsonl\n\n  $ primitive functions logs --id <fn-id> --follow\n\n"),
        "help\x00functions\x00redeploy" => Some("Redeploy a function from a bundled handler file\n\nUSAGE\n  $ primitive functions redeploy --id <value> --file <value> [--api-key <value>]\n    [--source-map-file <value>] [--secret <value>...] [--secret-from-env\n    <value>...] [--secret-from-file <value>...] [--secret-from-env-file\n    <value>...] [--secret-from-stdin <value>] [--wait] [--timeout <value>]\n    [--poll-interval <value>] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --file=<value>\n      (required) Path to the bundled ESM handler file. Loaded as the `code` body\n      field.\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --poll-interval=<value>\n      [default: 2] Seconds between deploy-status polls when --wait is set.\n\n  --secret=<value>...\n      Secret KEY=VALUE to write on the function before the redeploy fires.\n      Repeatable. KEY must match `^[A-Z_][A-Z0-9_]*$`; VALUE may contain `=` (only\n      the first `=` is treated as a delimiter). Each KEY may only appear once per\n      command. Passing one or more --secret flags fans out to set-secret per pair\n      then a single update-function call so the new bindings land in the same\n      redeploy. Note: values passed on the command line are visible in shell\n      history (e.g. ~/.bash_history) and to other users via `ps aux` /\n      /proc/[pid]/cmdline. For sensitive values prefer --secret-from-env,\n      --secret-from-file, --secret-from-env-file, or --secret-from-stdin.\n\n  --secret-from-env=<value>...\n      Secret KEY to read from the environment and write before the redeploy.\n      Repeatable. Example: --secret-from-env OPENAI_KEY reads\n      process.env.OPENAI_KEY.\n\n  --secret-from-env-file=<value>...\n      Secret FILE:KEY to read from a dotenv-style file and write before the\n      redeploy. Repeatable. Example: --secret-from-env-file .env.local:OPENAI_KEY.\n\n  --secret-from-file=<value>...\n      Secret KEY=PATH to read from a UTF-8 file and write before the redeploy.\n      Repeatable. The full file contents become the value.\n\n  --secret-from-stdin=<value>\n      Secret KEY to read from stdin and write before the redeploy. A single\n      trailing line ending is stripped. Stdin is consumed once, so this flag is\n      not repeatable.\n\n  --source-map-file=<value>\n      Optional path to a source map for the bundle. Used to symbolicate stack\n      traces in the function's logs.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --timeout=<value>\n      [default: 120] Seconds to wait when --wait is set before exiting non-zero.\n      Use 0 to wait forever.\n\n  --wait\n      Wait until the function deploy reaches deployed or failed. Progress is\n      written to stderr; stdout remains the final JSON payload.\n\nDESCRIPTION\n  Redeploy a function from a bundled handler file\n\n  Update or redeploy a function from a bundled handler file. Agent-grade\n  shortcut for functions:update-function.\n\n  Use to push a new bundle OR to refresh secret bindings into the\n  running handler. The same file is fine for both: the deploy reads\n  the bindings table fresh on every call, so passing the existing\n  bundle picks up any secret writes since the last deploy.\n\n  Pass secret source flags to write secrets BEFORE the redeploy fires;\n  one update-function call then refreshes every new binding. Keys must\n  match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits, underscores;\n  first character is a letter or underscore). With one or more secrets\n  the redeploy fans out to multiple API calls (set-secret per pair,\n  then update-function). Safe sources: --secret-from-env KEY reads\n  process.env[KEY], --secret-from-file KEY=PATH reads the full UTF-8 file\n  contents, --secret-from-env-file FILE:KEY reads KEY from a dotenv-style file,\n  and --secret-from-stdin KEY reads the value from stdin.\n\nEXAMPLES\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --wait\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --source-map-file ./bundle.js.map\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret OPENAI_KEY=sk-... --secret OWNER_EMAIL=me@example.com\n\n  $ primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-env OPENAI_KEY --secret-from-file PRIVATE_KEY=./private-key.pem\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions redeploy --id <fn-id> --file ./bundle.js --secret-from-stdin OPENAI_KEY\n\n"),
        "help\x00functions\x00route-get" => Some("Show a function's current route binding\n\nUSAGE\n  $ primitive functions route-get --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose route binding to show.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show a function's current route binding\n\n  Show the current route binding for a function. Returns the binding (domain or\n  fallback, with delivery counters) or null when no route is bound.\n\nEXAMPLES\n  $ primitive functions route-get --id <fn-id>\n\n"),
        "help\x00functions\x00route-set" => Some("Bind inbound mail to a function\n\nUSAGE\n  $ primitive functions route-set --id <value> [--api-key <value>] [--domain\n    <value> | --fallback] [--takeover] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Verified inbound domain id (UUID) to scope this function\n                     to. Mutually exclusive with --fallback.\n  --fallback         Bind this function as the org fallback (any active domain\n                     without a scoped binding). Mutually exclusive with\n                     --domain.\n  --id=<value>       (required) Function id (UUID) to bind a route to.\n  --takeover         Deactivate any conflicting binding before installing this\n                     one. Without this flag, the API returns a `conflict`\n                     envelope when another function is already bound at the\n                     target.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Bind inbound mail to a function\n\n  Bind inbound mail to a function by setting its route target.\n\n  Exactly one of --domain or --fallback is required. --domain scopes the\n  binding to a single verified inbound domain. --fallback binds the\n  function to any active domain that has no scoped binding of its own.\n\n  If another function is already bound at the target, the API returns a\n  conflict envelope rather than overwriting; re-run with --takeover to\n  deactivate the prior binding before installing this one.\n\nEXAMPLES\n  $ primitive functions route-set --id <fn-id> --domain <domain-id>\n\n  $ primitive functions route-set --id <fn-id> --fallback\n\n  $ primitive functions route-set --id <fn-id> --domain <domain-id> --takeover\n\n"),
        "help\x00functions\x00route-unset" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions route-unset --id <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Function id (UUID) whose routes should be\n                     unbound.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Unbind every active route from a function. The function stays deployed but\n  stops receiving inbound mail. Safe to call when no route is currently bound.\n\nEXAMPLES\n  $ primitive functions route-unset --id <fn-id>\n\n"),
        "help\x00functions\x00routing-topology" => Some("Show the org-wide function routing topology\n\nUSAGE\n  $ primitive functions routing-topology [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show the org-wide function routing topology\n\n  Show the org-wide function routing topology. Lists every active domain with\n  its bound function (if any), the org fallback function (if any), and every\n  deployed function with no route currently bound.\n\nEXAMPLES\n  $ primitive functions routing-topology\n\n"),
        "help\x00functions\x00set-function-route" => Some("Bind a route to a function\n\nUSAGE\n  $ primitive functions set-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--takeover]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --[no-]takeover      When true, deactivate any conflicting binding before\n                       installing this one.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Bind a route to a function\n\n  Binds inbound mail to this function. The route target is either\n  a specific verified domain (scoped) or the org's fallback (any\n  active domain with no scoped binding). If another function is\n  already bound at the target, returns a `conflict` envelope\n  describing the holder; re-issue with `takeover: true` to\n  deactivate that prior binding and install this one.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * target  any\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00functions\x00set-function-secret" => Some("Set a secret by key\n\nUSAGE\n  $ primitive functions set-function-secret --id <value> --key <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--value <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set a secret by key\n\n  Path-keyed companion to `POST /functions/{id}/secrets`.\n  Idempotent: returns 201 the first time the key is set, 200 on\n  subsequent updates. Same validation rules and same write-only\n  guarantees as the POST verb; the new value lands in the running\n  handler on the next deploy.\n\n\n  Tip: prefer `primitive functions set-secret --id <id> --key <KEY> --value\n  <value> [--redeploy]` for secret writes that also push the binding live. This\n  raw command exists for callers passing JSON.\n\n"),
        "help\x00functions\x00set-org-secret" => Some("Set an org secret by key\n\nUSAGE\n  $ primitive functions set-org-secret --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--value\n    <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --key=<value>        (required) Secret key. Must match `^[A-Z_][A-Z0-9_]*$`.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n  --value=<value>      value\n\nDESCRIPTION\n  Set an org secret by key\n\n  Path-keyed companion to `POST /org/secrets`. Idempotent:\n  returns 201 the first time the key is set, 200 on subsequent\n  updates. Same validation and write-only guarantees as POST.\n\n\n"),
        "help\x00functions\x00set-secret" => Some("Write a function secret (optionally redeploying to push it live)\n\nUSAGE\n  $ primitive functions set-secret --id <value> --key <value> [--api-key <value>]\n    [--value <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--redeploy] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --id=<value>\n      (required) Function id (UUID). The function must already exist.\n\n  --key=<value>\n      (required) Secret key. Uppercase letters, digits, underscores; must start\n      with a letter or underscore. System-managed keys are reserved.\n\n  --redeploy\n      Also redeploy the function with its current code so the new value lands in\n      the running handler. Without this, the secret is written but not visible to\n      the handler until the next deploy. Note: when --redeploy re-uploads the\n      function's current live code without a sourceMap field, the API preserves\n      the current stored source map. Use `functions redeploy --file <bundle.js>\n      --source-map-file <bundle.js.map>` to replace or restore a map.\n\n  --stdin\n      Read the secret value from stdin. A single trailing line ending is stripped.\n      Example: printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id\n      <fn-id> --key OPENAI_KEY --stdin\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --value=<value>\n      Secret value (up to 4096 UTF-8 bytes). Encrypted at rest. Visible in shell\n      history and process argv; prefer a non-argv source for sensitive values.\n\n  --value-file=<value>\n      UTF-8 file to read as the secret value. The full file contents become the\n      value.\n\n  --value-from-env=<value>\n      Environment variable to read as the secret value. Example: --value-from-env\n      OPENAI_KEY reads process.env.OPENAI_KEY.\n\n  --value-from-env-file=<value>\n      Dotenv-style file to read as the secret value. Use FILE to read --key from\n      that file, or FILE:KEY to read a different key.\n\nDESCRIPTION\n  Write a function secret (optionally redeploying to push it live)\n\n  Write a function secret and optionally redeploy so the new value lands in the\n  running handler. Agent-grade shortcut for functions set-function-secret +\n  functions redeploy.\n\n  Without --redeploy this is a plain secret upsert: the value is\n  encrypted at rest but is NOT visible to the running handler until\n  the next deploy. Pass --redeploy to re-run the deploy with the\n  function's current code in the same call, which refreshes the\n  binding set with the value you just wrote.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character is a letter or underscore). System-\n  managed keys are reserved and rejected. Instead of --value, use\n  --value-from-env ENV_VAR, --value-file PATH, --value-from-env-file FILE[:KEY],\n  or --stdin to avoid putting the secret value in shell history or process argv.\n  If KEY is omitted from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123\n\n  $ primitive functions set-secret --id <fn-id> --key API_TOKEN --value abc123 --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env OPENAI_KEY --redeploy\n\n  $ primitive functions set-secret --id <fn-id> --key OPENAI_KEY --value-from-env-file .env.local --redeploy\n\n  printf '%s' \"$OPENAI_KEY\" | primitive functions set-secret --id <fn-id> --key OPENAI_KEY --stdin --redeploy\n\n"),
        "help\x00functions\x00templates" => Some("List available Primitive Function templates\n\nUSAGE\n  $ primitive functions templates [--json]\n\nGLOBAL FLAGS\n  --json  Format output as json.\n\nDESCRIPTION\n  List available Primitive Function templates\n\n  List Primitive Function templates available to `primitive functions init`.\n\n  The default table is optimized for quick terminal discovery. Use\n  --json when an agent or script needs stable metadata for searching,\n  ranking, or choosing a template programmatically.\n\nEXAMPLES\n  $ primitive functions templates\n\n  $ primitive functions templates --json\n\n  $ primitive functions init my-fn --template email-reply\n\n"),
        "help\x00functions\x00test" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "help\x00functions\x00test-function" => Some("Trigger a test invocation; with --wait, watch it land\n\nUSAGE\n  $ primitive functions test-function --id <value> [--api-key <value>] [--local-part\n    <value>] [--wait] [--show-sends] [--timeout <value>] [--poll-interval\n    <value>] [--time]\n\nFLAGS\n  --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key override\n                           (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                           credentials)\n  --id=<value>             (required) Function id (UUID).\n  --local-part=<value>     Override the synthetic local-part the test inbound is\n                           addressed to. Otherwise the runtime picks\n                           `__primitive_function_test+<random>`.\n  --poll-interval=<value>  [default: 2] Seconds between polls while waiting.\n  --show-sends             When the wait resolves, also print the outbound\n                           emails the function emitted while processing the test\n                           inbound (id, status, to, subject). Implies --wait.\n  --time                   Print the wall-clock duration of this command to\n                           stderr after it completes (e.g. `[time: 1.34s]`).\n                           Useful for measuring `--wait` send latency, comparing\n                           CLI overhead, or capturing timing in scripts.\n  --timeout=<value>        [default: 60] Seconds to wait before exiting non-zero\n                           when --wait is set; 0 waits forever.\n  --wait                   Block until the function test run reaches\n                           `completed`, `failed`, or `send_failed`, or --timeout\n                           elapses. Exits non-zero on timeout or terminal\n                           failure.\n\nDESCRIPTION\n  Trigger a test invocation; with --wait, watch it land\n\n  Send a real test email through MX to trigger this function. The function must\n  have an active route bound (see `functions route-set`); without one the API\n  returns a 422 immediately so no doomed test send is queued. With --wait,\n  blocks until the function has processed the inbound; with --show-sends, also\n  prints any outbound sends the function emitted in response.\n\nEXAMPLES\n  $ primitive functions test --id <fn-id>\n\n  $ primitive functions test --id <fn-id> --local-part summarize\n\n  $ primitive functions test --id <fn-id> --wait --show-sends\n\n  $ primitive functions test --id <fn-id> --local-part summarize --wait --timeout 120\n\n"),
        "help\x00functions\x00unset-function-route" => Some("Unbind any route from a function\n\nUSAGE\n  $ primitive functions unset-function-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unbind any route from a function\n\n  Deactivates every active endpoint bound to this function. The\n  function stays deployed but stops receiving inbound mail. Safe\n  to call when no route is currently bound (no-op).\n\n\n"),
        "help\x00functions\x00update-function" => Some("Update and redeploy a function\n\nUSAGE\n  $ primitive functions update-function --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--code\n    <value>] [--sourcemap <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --code=<value>       New pre-built handler. Same rules as\n                       CreateFunctionInput.code. Provide either `code` or\n                       `files`, not both.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --sourcemap=<value>  sourceMap\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update and redeploy a function\n\n  Replaces the function's source code with the body's `code` and\n  triggers a redeploy. Same size limits as `POST /functions`.\n  Use this verb to push secret writes into the running handler:\n  passing the same `code` re-runs the deploy and refreshes the\n  binding set with the latest values from the secrets table.\n\n  On deploy failure, the previously-deployed code stays live; the\n  runtime never serves a half-built bundle. The response uses\n  `error.code` `deploy_failed`, and the function's `deploy_error`\n  field carries the latest deploy error for dashboard/API reads.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  files  object  Source files for a managed build, as a map of path to file\n  contents. Provid...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive functions redeploy --id <id> --file <bundle>` for\n  file-input ergonomics. This raw command exists for callers passing JSON.\n\n"),
        "help\x00inbox" => Some("Check inbound email setup and processing readiness. Prefer `primitive inbox setup` for a guided setup path and `primitive inbox status` for the readiness table.\n\nUSAGE\n  $ primitive inbox COMMAND\n\nCOMMANDS\n  inbox get-inbox-status  Show inbound email readiness\n  inbox setup             Guide inbound email setup\n  inbox status            Show inbound email readiness\n\n"),
        "help\x00inbox:get-inbox-status" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "help\x00inbox:setup" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "help\x00inbox:status" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "help\x00inbox\x00get-inbox-status" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "help\x00inbox\x00setup" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "help\x00inbox\x00status" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "help\x00list-operations" => Some("List all generated API operations (JSON)\n\nUSAGE\n  $ primitive list-operations\n\nDESCRIPTION\n  List all generated API operations (JSON)\n\n  List all generated API operations as JSON. Useful for piping to `jq` to\n  discover available commands, their request/response schemas, and per-field\n  descriptions. For inspecting a single operation in detail, prefer `primitive\n  describe <command-or-operation-name>`.\n\n"),
        "help\x00login" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "help\x00login:browser" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "help\x00login:confirm" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00login:otp" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "help\x00login:otp:confirm" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00login:otp:resend" => Some("Resend OTP login code\n\nUSAGE\n  $ primitive login otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP login code\n\n  Resend the verification code for a pending OTP login.\n\nEXAMPLES\n  $ primitive login otp resend user@example.com\n\n"),
        "help\x00login:resend" => Some("Resend email-code login code\n\nUSAGE\n  $ primitive login resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code login code\n\n  Resend the verification code for a pending email-code login.\n\nEXAMPLES\n  $ primitive login resend user@example.com\n\n"),
        "help\x00login\x00browser" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "help\x00login\x00confirm" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00login\x00otp" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "help\x00login\x00otp\x00confirm" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00login\x00otp\x00resend" => Some("Resend OTP login code\n\nUSAGE\n  $ primitive login otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP login code\n\n  Resend the verification code for a pending OTP login.\n\nEXAMPLES\n  $ primitive login otp resend user@example.com\n\n"),
        "help\x00login\x00resend" => Some("Resend email-code login code\n\nUSAGE\n  $ primitive login resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code login code\n\n  Resend the verification code for a pending email-code login.\n\nEXAMPLES\n  $ primitive login resend user@example.com\n\n"),
        "help\x00logout" => Some("Log out and revoke the saved CLI OAuth grant\n\nUSAGE\n  $ primitive logout [-f]\n\nFLAGS\n  -f, --force  Remove local CLI credentials, pending email-code auth state, and\n               any credential lock without revoking the server OAuth grant\n\nDESCRIPTION\n  Log out and revoke the saved CLI OAuth grant\n\n  Log out by revoking the saved Primitive CLI OAuth grant and deleting local\n  credentials. Use --force to remove local credentials, pending email-code auth\n  state, and stale credential locks without contacting Primitive.\n\nEXAMPLES\n  $ primitive logout\n\n  $ primitive logout --force\n\n"),
        "help\x00memories" => Some("Store durable JSON memories by key. Use `primitive memories set|get|delete|search`; pass `--function <function-id>` for function-scoped memories.\n\nUSAGE\n  $ primitive memories COMMAND\n\nCOMMANDS\n  memories delete           Delete a memory\n  memories delete-memory    Delete a memory\n  memories get              Get a memory\n  memories get-memory       Get a memory\n  memories search           Search memories\n  memories search-memories  Search memories\n  memories set              Set a memory\n  memories set-memory       Set a memory\n\n"),
        "help\x00memories:delete" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "help\x00memories:delete-memory" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "help\x00memories:get" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "help\x00memories:get-memory" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "help\x00memories:search" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "help\x00memories:search-memories" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "help\x00memories:set" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "help\x00memories:set-memory" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "help\x00memories\x00delete" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "help\x00memories\x00delete-memory" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "help\x00memories\x00get" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "help\x00memories\x00get-memory" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "help\x00memories\x00search" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "help\x00memories\x00search-memories" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "help\x00memories\x00set" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "help\x00memories\x00set-memory" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "help\x00org" => Some("Manage org-level (global) resources shared across functions\n\nUSAGE\n  $ primitive org COMMAND\n\nTOPICS\n  org secrets  Global secrets shared across every function. Use `primitive org\n               secrets list|set|remove`. Changes land in a function on its next\n               redeploy; a function secret of the same name overrides the\n               global.\n\n"),
        "help\x00org:secrets" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "help\x00org:secrets:delete" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "help\x00org:secrets:list" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "help\x00org:secrets:remove" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "help\x00org:secrets:set" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "help\x00org\x00secrets" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "help\x00org\x00secrets\x00delete" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "help\x00org\x00secrets\x00list" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "help\x00org\x00secrets\x00remove" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "help\x00org\x00secrets\x00set" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "help\x00otp" => Some("Start email-code auth\n\nUSAGE\n  $ primitive otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start email-code auth\n\n  Start email-code authentication, send a verification code, and save the\n  pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive otp confirm user@example.com 123456\n\nCOMMANDS\n  otp confirm  Confirm email-code auth\n  otp resend   Resend email-code auth code\n\n"),
        "help\x00otp:confirm" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00otp:resend" => Some("Resend email-code auth code\n\nUSAGE\n  $ primitive otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code auth code\n\n  Resend the verification code for pending email-code authentication.\n\nEXAMPLES\n  $ primitive otp resend user@example.com\n\n"),
        "help\x00otp\x00confirm" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00otp\x00resend" => Some("Resend email-code auth code\n\nUSAGE\n  $ primitive otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code auth code\n\n  Resend the verification code for pending email-code authentication.\n\nEXAMPLES\n  $ primitive otp resend user@example.com\n\n"),
        "help\x00payloads" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads COMMAND\n\nCOMMANDS\n  payloads pull  Stream-download and decrypt a payload to a file\n  payloads push  Stream-upload a file as an encrypted payload\n\n"),
        "help\x00payloads:pull" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "help\x00payloads:push" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "help\x00payloads\x00pull" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "help\x00payloads\x00push" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "help\x00payments" => Some("Collect and pay x402 stablecoin (USDC) payments. Start with `primitive payments register-payout-address`, then `primitive payments create-challenge` to request a payment and `primitive payments pay` to settle one. Settlement is non-custodial; signing happens locally with your wallet key. Manage caps with `primitive payments get-policy|update-spend-policy`.\n\nUSAGE\n  $ primitive payments COMMAND\n\nCOMMANDS\n  payments challenge-from-email     Print the email x402 challenge object\n                                    derived from a received payment-request\n                                    email\n  payments charge                   Request an x402 payment (create a challenge)\n  payments create-challenge         Create a payment challenge\n  payments create-email-challenge   Create an email-native payment challenge\n  payments get-challenge            Get a payment challenge\n  payments get-spend-policy         Get your spend policy\n  payments list-declined-payments   List declined payments\n  payments list-payout-addresses    List payout addresses\n  payments pay                      Sign and settle an x402 payment challenge\n  payments pay-challenge            Sign and settle an x402 payment challenge\n  payments pay-email                Sign an email x402 challenge and send the\n                                    signed interaction.json (one step; derives\n                                    the challenge from --in-reply-to)\n  payments pay-email-step           Sign an email x402 challenge into a\n                                    payment-step envelope\n  payments register-payout          Register a payout address for receiving x402\n                                    payments\n  payments register-payout-address  Register a payout address for receiving x402\n                                    payments\n  payments update-spend-policy      Update your spend policy\n\n"),
        "help\x00payments:challenge-from-email" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "help\x00payments:charge" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "help\x00payments:create-challenge" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "help\x00payments:create-email-challenge" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "help\x00payments:get-challenge" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "help\x00payments:get-spend-policy" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "help\x00payments:list-declined-payments" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "help\x00payments:list-payout-addresses" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "help\x00payments:pay" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "help\x00payments:pay-challenge" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "help\x00payments:pay-email" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "help\x00payments:pay-email-step" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "help\x00payments:register-payout" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "help\x00payments:register-payout-address" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "help\x00payments:update-spend-policy" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00payments\x00challenge-from-email" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "help\x00payments\x00charge" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "help\x00payments\x00create-challenge" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "help\x00payments\x00create-email-challenge" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "help\x00payments\x00get-challenge" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "help\x00payments\x00get-spend-policy" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "help\x00payments\x00list-declined-payments" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "help\x00payments\x00list-payout-addresses" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "help\x00payments\x00pay" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "help\x00payments\x00pay-challenge" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "help\x00payments\x00pay-email" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "help\x00payments\x00pay-email-step" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "help\x00payments\x00register-payout" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "help\x00payments\x00register-payout-address" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "help\x00payments\x00update-spend-policy" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00registries" => Some("Create and manage agent registries, publish and resolve agents by handle, and moderate publication requests\n\nUSAGE\n  $ primitive registries COMMAND\n\nCOMMANDS\n  registries agent                    Get an agent's public profile by address\n  registries agents                   List agents in a registry\n  registries create                   Create a registry\n  registries create-registry          Create a registry\n  registries decide                   Approve or reject a publication request\n  registries decide-registry-request  Approve or reject a publication request\n  registries define                   Define an agent identity\n  registries define-agent             Define an agent identity\n  registries delete-registry          Delete a registry you own\n  registries get                      Get a public registry's metadata\n  registries get-agent                Get an agent's public profile by address\n  registries get-registry             Get a public registry's metadata\n  registries list                     List the registries you own\n  registries list-registries          List the registries you own\n  registries list-registry-agents     List agents in a registry\n  registries list-registry-requests   List pending publication requests\n  registries publish                  Publish an agent into a registry\n  registries publish-agent            Publish an agent into a registry\n  registries requests                 List pending publication requests\n  registries resolve                  Resolve a registry handle to its agent\n  registries resolve-registry-handle  Resolve a registry handle to its agent\n  registries unpublish                Unpublish an agent from a registry\n  registries unpublish-agent          Unpublish an agent from a registry\n  registries update                   Update a registry you own\n  registries update-registry          Update a registry you own\n\n"),
        "help\x00registries:agent" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "help\x00registries:agents" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "help\x00registries:create" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "help\x00registries:create-registry" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "help\x00registries:decide" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "help\x00registries:decide-registry-request" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "help\x00registries:define" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00registries:define-agent" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00registries:delete-registry" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "help\x00registries:get" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "help\x00registries:get-agent" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "help\x00registries:get-registry" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "help\x00registries:list" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "help\x00registries:list-registries" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "help\x00registries:list-registry-agents" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "help\x00registries:list-registry-requests" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "help\x00registries:publish" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00registries:publish-agent" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00registries:requests" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "help\x00registries:resolve" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "help\x00registries:resolve-registry-handle" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "help\x00registries:unpublish" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "help\x00registries:unpublish-agent" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "help\x00registries:update" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "help\x00registries:update-registry" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "help\x00registries\x00agent" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "help\x00registries\x00agents" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "help\x00registries\x00create" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "help\x00registries\x00create-registry" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "help\x00registries\x00decide" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "help\x00registries\x00decide-registry-request" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "help\x00registries\x00define" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00registries\x00define-agent" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00registries\x00delete-registry" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "help\x00registries\x00get" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "help\x00registries\x00get-agent" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "help\x00registries\x00get-registry" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "help\x00registries\x00list" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "help\x00registries\x00list-registries" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "help\x00registries\x00list-registry-agents" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "help\x00registries\x00list-registry-requests" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "help\x00registries\x00publish" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00registries\x00publish-agent" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00registries\x00requests" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "help\x00registries\x00resolve" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "help\x00registries\x00resolve-registry-handle" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "help\x00registries\x00unpublish" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "help\x00registries\x00unpublish-agent" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "help\x00registries\x00update" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "help\x00registries\x00update-registry" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "help\x00reply" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "help\x00routes" => Some("Manage recipient routes. Bind an address pattern to an endpoint or function: `primitive routes add alice@d --function <id>`, `primitive routes list`, `primitive routes test <recipient>`, `primitive routes remove <id>`.\n\nUSAGE\n  $ primitive routes COMMAND\n\nCOMMANDS\n  routes add             Add a recipient route\n  routes create-route    Create a recipient route\n  routes delete-route    Delete a recipient route\n  routes list            List recipient routes\n  routes list-routes     List recipient routes\n  routes remove          Remove a recipient route\n  routes reorder         Reorder recipient routes\n  routes reorder-routes  Reorder recipient routes\n  routes simulate-route  Simulate routing for a recipient\n  routes test            Simulate routing for a recipient\n  routes update          Update a recipient route\n  routes update-route    Update a recipient route\n\n"),
        "help\x00routes:add" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "help\x00routes:create-route" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "help\x00routes:delete-route" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "help\x00routes:list" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "help\x00routes:list-routes" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "help\x00routes:remove" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "help\x00routes:reorder" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "help\x00routes:reorder-routes" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00routes:simulate-route" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "help\x00routes:test" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "help\x00routes:update" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "help\x00routes:update-route" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "help\x00routes\x00add" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "help\x00routes\x00create-route" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "help\x00routes\x00delete-route" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "help\x00routes\x00list" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "help\x00routes\x00list-routes" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "help\x00routes\x00remove" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "help\x00routes\x00reorder" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "help\x00routes\x00reorder-routes" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00routes\x00simulate-route" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "help\x00routes\x00test" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "help\x00routes\x00update" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "help\x00routes\x00update-route" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "help\x00script:autocomplete" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "help\x00script\x00autocomplete" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "help\x00search" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "help\x00search:semantic-search" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "help\x00search\x00semantic-search" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "help\x00semantic-search" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "help\x00send" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "help\x00sending" => Some("Send outbound emails. Prefer `primitive send` for fresh sends and `primitive reply --id <inbound-id>` for replies. Use `primitive domains list` or `primitive inbox status` to find usable sender domains for --from; `primitive sending permissions` lists recipient-scope destinations you may send to.\n\nUSAGE\n  $ primitive sending COMMAND\n\nCOMMANDS\n  sending get                   Get a sent email by id\n  sending get-send-permissions  List send-permission rules\n  sending get-sent-email        Get a sent email by id\n  sending list                  List outbound sent emails\n  sending list-sent-emails      List outbound sent emails\n  sending permissions           List send-permission rules\n  sending reply                 Reply to an inbound email\n  sending reply-to-email        Reply to an inbound email\n  sending send                  Send outbound email\n  sending send-email            Send outbound email\n\n"),
        "help\x00sending:get" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "help\x00sending:get-send-permissions" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "help\x00sending:get-sent-email" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "help\x00sending:list" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "help\x00sending:list-sent-emails" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "help\x00sending:permissions" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "help\x00sending:reply" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00sending:reply-to-email" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00sending:send" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "help\x00sending:send-email" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "help\x00sending\x00get" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "help\x00sending\x00get-send-permissions" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "help\x00sending\x00get-sent-email" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "help\x00sending\x00list" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "help\x00sending\x00list-sent-emails" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "help\x00sending\x00permissions" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "help\x00sending\x00reply" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00sending\x00reply-to-email" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00sending\x00send" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "help\x00sending\x00send-email" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "help\x00sent" => Some("Short aliases for outbound sent-email history: `primitive sent list` and `primitive sent get`.\n\nUSAGE\n  $ primitive sent COMMAND\n\nCOMMANDS\n  sent get   Get a sent email by id\n  sent list  List outbound sent emails\n\n"),
        "help\x00sent:get" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "help\x00sent:list" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "help\x00sent\x00get" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "help\x00sent\x00list" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "help\x00signin" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "help\x00signin:browser" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "help\x00signin:confirm" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00signin:otp" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "help\x00signin:otp:confirm" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00signin:otp:resend" => Some("Resend OTP sign-in code\n\nUSAGE\n  $ primitive signin otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP sign-in code\n\n  Resend the verification code for a pending OTP sign-in.\n\nEXAMPLES\n  $ primitive signin otp resend user@example.com\n\n"),
        "help\x00signin:resend" => Some("Resend email-code sign-in code\n\nUSAGE\n  $ primitive signin resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code sign-in code\n\n  Resend the verification code for a pending email-code sign-in.\n\nEXAMPLES\n  $ primitive signin resend user@example.com\n\n"),
        "help\x00signin\x00browser" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "help\x00signin\x00confirm" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00signin\x00otp" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "help\x00signin\x00otp\x00confirm" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "help\x00signin\x00otp\x00resend" => Some("Resend OTP sign-in code\n\nUSAGE\n  $ primitive signin otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP sign-in code\n\n  Resend the verification code for a pending OTP sign-in.\n\nEXAMPLES\n  $ primitive signin otp resend user@example.com\n\n"),
        "help\x00signin\x00resend" => Some("Resend email-code sign-in code\n\nUSAGE\n  $ primitive signin resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code sign-in code\n\n  Resend the verification code for a pending email-code sign-in.\n\nEXAMPLES\n  $ primitive signin resend user@example.com\n\n"),
        "help\x00signup" => Some("Start account signup\n\nUSAGE\n  $ primitive signup [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign up\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Start account signup\n\n  Start a Primitive account signup, send an email verification code, and save a\n  pending signup token locally.\n\nEXAMPLES\n  $ primitive signup user@example.com\n\n  $ primitive signup user@example.com --accept-terms\n\n  $ primitive signup user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signup confirm user@example.com 123456\n\nCOMMANDS\n  signup confirm      Confirm account signup\n  signup interactive  Run interactive account signup\n  signup resend       Resend signup verification code\n  signup status       Show pending signup status\n\n"),
        "help\x00signup:confirm" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "help\x00signup:interactive" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "help\x00signup:resend" => Some("Resend signup verification code\n\nUSAGE\n  $ primitive signup resend [EMAIL]\n\nARGUMENTS\n  [EMAIL]  Email address used to start signup. Defaults to the saved pending\n           signup.\n\nDESCRIPTION\n  Resend signup verification code\n\n  Resend the verification code for a pending signup.\n\nEXAMPLES\n  $ primitive signup resend\n\n  $ primitive signup resend user@example.com\n\n"),
        "help\x00signup:status" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "help\x00signup\x00confirm" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "help\x00signup\x00interactive" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "help\x00signup\x00resend" => Some("Resend signup verification code\n\nUSAGE\n  $ primitive signup resend [EMAIL]\n\nARGUMENTS\n  [EMAIL]  Email address used to start signup. Defaults to the saved pending\n           signup.\n\nDESCRIPTION\n  Resend signup verification code\n\n  Resend the verification code for a pending signup.\n\nEXAMPLES\n  $ primitive signup resend\n\n  $ primitive signup resend user@example.com\n\n"),
        "help\x00signup\x00status" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "help\x00templates" => Some("Browse and install Function templates. Use `primitive templates list-templates`, `primitive templates get-template`, `primitive templates install-template`, and `primitive templates get-template-install`.\n\nUSAGE\n  $ primitive templates COMMAND\n\nCOMMANDS\n  templates get-template          Get a function template\n  templates get-template-install  Get template install status\n  templates install-template      Install a function template\n  templates list-templates        List function templates\n\n"),
        "help\x00templates:get-template" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "help\x00templates:get-template-install" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "help\x00templates:install-template" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00templates:list-templates" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "help\x00templates\x00get-template" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "help\x00templates\x00get-template-install" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "help\x00templates\x00install-template" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00templates\x00list-templates" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "help\x00threads" => Some("Inspect conversation threads spanning received and sent emails. Prefer `primitive threads get --id <thread-id>`; generated API name `primitive threads get-thread --id <thread-id>` remains available.\n\nUSAGE\n  $ primitive threads COMMAND\n\nCOMMANDS\n  threads get         Get a conversation thread by id\n  threads get-thread  Get a conversation thread by id\n\n"),
        "help\x00threads:get" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "help\x00threads:get-thread" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "help\x00threads\x00get" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "help\x00threads\x00get-thread" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "help\x00wake" => Some("Schedule and send wake commands to your own functions over signed email, and manage which senders may wake them. Use `primitive wake schedules list|create|get|update|delete|run`, `primitive wake authorizations list|create|update|delete`, and `primitive wake dispatches list`.\n\nUSAGE\n  $ primitive wake COMMAND\n\nTOPICS\n  wake authorizations  Create a wake authorization\n  wake dispatches      List recent wake dispatches\n  wake schedules       Create a wake schedule\n\nCOMMANDS\n  wake create-wake-authorization  Create a wake authorization\n  wake create-wake-schedule       Create a wake schedule\n  wake delete-wake-authorization  Delete a wake authorization\n  wake delete-wake-schedule       Delete a wake schedule\n  wake get-wake-schedule          Get a wake schedule\n  wake list-wake-authorizations   List wake authorizations\n  wake list-wake-dispatches       List recent wake dispatches\n  wake list-wake-schedules        List wake schedules\n  wake run-wake-schedule          Run a wake schedule now\n  wake update-wake-authorization  Update a wake authorization\n  wake update-wake-schedule       Update a wake schedule\n\n"),
        "help\x00wake:authorizations" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "help\x00wake:authorizations:create" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "help\x00wake:authorizations:delete" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "help\x00wake:authorizations:list" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "help\x00wake:authorizations:update" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "help\x00wake:create-wake-authorization" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00wake:create-wake-schedule" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00wake:delete-wake-authorization" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "help\x00wake:delete-wake-schedule" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "help\x00wake:dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "help\x00wake:dispatches:list" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "help\x00wake:get-wake-schedule" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "help\x00wake:list-wake-authorizations" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "help\x00wake:list-wake-dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "help\x00wake:list-wake-schedules" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "help\x00wake:run-wake-schedule" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "help\x00wake:schedules" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "help\x00wake:schedules:create" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "help\x00wake:schedules:delete" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "help\x00wake:schedules:get" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "help\x00wake:schedules:list" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "help\x00wake:schedules:run" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "help\x00wake:schedules:update" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "help\x00wake:update-wake-authorization" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "help\x00wake:update-wake-schedule" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00wake\x00authorizations" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "help\x00wake\x00authorizations\x00create" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "help\x00wake\x00authorizations\x00delete" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "help\x00wake\x00authorizations\x00list" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "help\x00wake\x00authorizations\x00update" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "help\x00wake\x00create-wake-authorization" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00wake\x00create-wake-schedule" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00wake\x00delete-wake-authorization" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "help\x00wake\x00delete-wake-schedule" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "help\x00wake\x00dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "help\x00wake\x00dispatches\x00list" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "help\x00wake\x00get-wake-schedule" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "help\x00wake\x00list-wake-authorizations" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "help\x00wake\x00list-wake-dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "help\x00wake\x00list-wake-schedules" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "help\x00wake\x00run-wake-schedule" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "help\x00wake\x00schedules" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "help\x00wake\x00schedules\x00create" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "help\x00wake\x00schedules\x00delete" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "help\x00wake\x00schedules\x00get" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "help\x00wake\x00schedules\x00list" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "help\x00wake\x00schedules\x00run" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "help\x00wake\x00schedules\x00update" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "help\x00wake\x00update-wake-authorization" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "help\x00wake\x00update-wake-schedule" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "help\x00webhook-deliveries" => Some("View and replay webhook delivery attempts. Prefer `primitive webhook-deliveries list` and `primitive webhook-deliveries replay`.\n\nUSAGE\n  $ primitive webhook-deliveries COMMAND\n\nCOMMANDS\n  webhook-deliveries list             List webhook deliveries\n  webhook-deliveries list-deliveries  List webhook deliveries\n  webhook-deliveries replay           Replay a webhook delivery\n  webhook-deliveries replay-delivery  Replay a webhook delivery\n\n"),
        "help\x00webhook-deliveries:list" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "help\x00webhook-deliveries:list-deliveries" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "help\x00webhook-deliveries:replay" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "help\x00webhook-deliveries:replay-delivery" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "help\x00webhook-deliveries\x00list" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "help\x00webhook-deliveries\x00list-deliveries" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "help\x00webhook-deliveries\x00replay" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "help\x00webhook-deliveries\x00replay-delivery" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "help\x00whoami" => Some("Print the authenticated account (credentials smoke test)\n\nUSAGE\n  $ primitive whoami [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the full account JSON response. Default output hides\n                     setup and billing internals.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the authenticated account (credentials smoke test)\n\n  Print the account currently authenticated by saved OAuth credentials or an\n  explicit API key. Useful as a credentials smoke test: confirms auth is live\n  and shows which account it belongs to.\n\n  The default output is a concise human summary. Pass --json only when a script\n  intentionally needs the full /account response.\n\nEXAMPLES\n  $ primitive whoami\n\n  $ primitive whoami --api-key prim_...\n\n  $ primitive whoami --json | jq .id\n\n"),
        "inbox" => Some("Check inbound email setup and processing readiness. Prefer `primitive inbox setup` for a guided setup path and `primitive inbox status` for the readiness table.\n\nUSAGE\n  $ primitive inbox COMMAND\n\nCOMMANDS\n  inbox get-inbox-status  Show inbound email readiness\n  inbox setup             Guide inbound email setup\n  inbox status            Show inbound email readiness\n\n"),
        "inbox\x00--help" => Some("Check inbound email setup and processing readiness. Prefer `primitive inbox setup` for a guided setup path and `primitive inbox status` for the readiness table.\n\nUSAGE\n  $ primitive inbox COMMAND\n\nCOMMANDS\n  inbox get-inbox-status  Show inbound email readiness\n  inbox setup             Guide inbound email setup\n  inbox status            Show inbound email readiness\n\n"),
        "inbox\x00-h" => Some("Check inbound email setup and processing readiness. Prefer `primitive inbox setup` for a guided setup path and `primitive inbox status` for the readiness table.\n\nUSAGE\n  $ primitive inbox COMMAND\n\nCOMMANDS\n  inbox get-inbox-status  Show inbound email readiness\n  inbox setup             Guide inbound email setup\n  inbox status            Show inbound email readiness\n\n"),
        "inbox:get-inbox-status\x00--api-key\x00prim_test\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox:get-inbox-status\x00--domain\x00value\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox:get-inbox-status\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox:get-inbox-status\x00--json\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox:get-inbox-status\x00--time\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox:setup\x00--api-key\x00prim_test\x00--help" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "inbox:setup\x00--help" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "inbox:setup\x00--json\x00--help" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "inbox:setup\x00--time\x00--help" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "inbox:status\x00--api-key\x00prim_test\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox:status\x00--domain\x00value\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox:status\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox:status\x00--json\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox:status\x00--time\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00get-inbox-status\x00--api-key\x00prim_test\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00get-inbox-status\x00--domain\x00value\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00get-inbox-status\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00get-inbox-status\x00--json\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00get-inbox-status\x00--time\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox get-inbox-status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00setup\x00--api-key\x00prim_test\x00--help" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "inbox\x00setup\x00--help" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "inbox\x00setup\x00--json\x00--help" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "inbox\x00setup\x00--time\x00--help" => Some("Guide inbound email setup\n\nUSAGE\n  $ primitive inbox setup [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print structured readiness, receive address, commands,\n                     proof metadata, and raw status as JSON.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Guide inbound email setup\n\n  Guide inbound email setup from the server-owned inbox status API.\n\n  This command does not scaffold, deploy, or run tests. It verifies auth,\n  fetches inbox readiness, shows the first usable Primitive-managed receive\n  address/domain, explains whether inbound mail is stored-only or actively\n  processed, and prints the exact commands to add a Function processing route\n  when one is missing.\n\nEXAMPLES\n  $ primitive inbox setup\n\n  $ primitive inbox setup --json\n\n"),
        "inbox\x00status\x00--api-key\x00prim_test\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00status\x00--domain\x00value\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00status\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00status\x00--json\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "inbox\x00status\x00--time\x00--help" => Some("Show inbound email readiness\n\nUSAGE\n  $ primitive inbox status [--api-key <value>] [--domain <value>] [--json]\n    [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --domain=<value>   Focus domain readiness and recent email fields on one\n                     domain returned by the inbox status API.\n  --json             Print the raw response envelope as JSON. With --domain,\n                     domain readiness and recent email fields are focused while\n                     endpoint, function, and next-action summaries remain\n                     account-level.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Show inbound email readiness\n\n  Show consolidated inbound email readiness.\n\n  This checks the server-owned inbox status API instead of reconstructing\n  readiness locally from separate domain, endpoint, function, and email lists.\n  Use it before testing inbound email setup: it tells you whether mail can be\n  received, whether anything will process it, and which next command is most\n  useful.\n\nEXAMPLES\n  $ primitive inbox status\n\n  $ primitive inbox status --domain example.com\n\n  $ primitive inbox status --json | jq '.data.next_actions'\n\n"),
        "list-operations\x00--help" => Some("List all generated API operations (JSON)\n\nUSAGE\n  $ primitive list-operations\n\nDESCRIPTION\n  List all generated API operations (JSON)\n\n  List all generated API operations as JSON. Useful for piping to `jq` to\n  discover available commands, their request/response schemas, and per-field\n  descriptions. For inspecting a single operation in detail, prefer `primitive\n  describe <command-or-operation-name>`.\n\n"),
        "login\x00--accept-terms\x00--help" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "login\x00--device-name\x00value\x00--help" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "login\x00--force\x00--help" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "login\x00--help" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "login\x00--no-browser\x00--help" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "login\x00--signup-code\x00123456\x00--help" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "login\x00-f\x00--help" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "login:browser\x00--device-name\x00value\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login:browser\x00--force\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login:browser\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login:browser\x00--no-browser\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login:browser\x00-f\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login:confirm\x00--force\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:confirm\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:confirm\x00-f\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:otp\x00--accept-terms\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login:otp\x00--device-name\x00value\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login:otp\x00--force\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login:otp\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login:otp\x00--signup-code\x00123456\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login:otp\x00-f\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login:otp:confirm\x00--force\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:otp:confirm\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:otp:confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:otp:confirm\x00-f\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:otp:confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login:otp:resend\x00--help" => Some("Resend OTP login code\n\nUSAGE\n  $ primitive login otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP login code\n\n  Resend the verification code for a pending OTP login.\n\nEXAMPLES\n  $ primitive login otp resend user@example.com\n\n"),
        "login:otp:resend\x00user@example.com\x00--help" => Some("Resend OTP login code\n\nUSAGE\n  $ primitive login otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP login code\n\n  Resend the verification code for a pending OTP login.\n\nEXAMPLES\n  $ primitive login otp resend user@example.com\n\n"),
        "login:otp\x00user@example.com\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login:resend\x00--help" => Some("Resend email-code login code\n\nUSAGE\n  $ primitive login resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code login code\n\n  Resend the verification code for a pending email-code login.\n\nEXAMPLES\n  $ primitive login resend user@example.com\n\n"),
        "login:resend\x00user@example.com\x00--help" => Some("Resend email-code login code\n\nUSAGE\n  $ primitive login resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code login code\n\n  Resend the verification code for a pending email-code login.\n\nEXAMPLES\n  $ primitive login resend user@example.com\n\n"),
        "login\x00browser\x00--device-name\x00value\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login\x00browser\x00--force\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login\x00browser\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login\x00browser\x00--no-browser\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login\x00browser\x00-f\x00--help" => Some("Log in with browser approval\n\nUSAGE\n  $ primitive login browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Log in with browser approval\n\n  Log in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive login browser\n\n  $ primitive login browser --device-name work-laptop\n\n  $ primitive login browser --no-browser\n\n  $ primitive login browser --force\n\n"),
        "login\x00confirm\x00--force\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00confirm\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00confirm\x00-f\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm email-code login\n\nUSAGE\n  $ primitive login confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code login\n\n  Confirm a pending email-code login, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00otp\x00--accept-terms\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login\x00otp\x00--device-name\x00value\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login\x00otp\x00--force\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login\x00otp\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login\x00otp\x00--signup-code\x00123456\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login\x00otp\x00-f\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login\x00otp\x00confirm\x00--force\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00otp\x00confirm\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00otp\x00confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00otp\x00confirm\x00-f\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00otp\x00confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm OTP login\n\nUSAGE\n  $ primitive login otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP login\n\n  Confirm a pending OTP login, create an OAuth session, and save CLI credentials\n  locally.\n\nEXAMPLES\n  $ primitive login otp confirm user@example.com 123456\n\n  $ primitive login otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "login\x00otp\x00resend\x00--help" => Some("Resend OTP login code\n\nUSAGE\n  $ primitive login otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP login code\n\n  Resend the verification code for a pending OTP login.\n\nEXAMPLES\n  $ primitive login otp resend user@example.com\n\n"),
        "login\x00otp\x00resend\x00user@example.com\x00--help" => Some("Resend OTP login code\n\nUSAGE\n  $ primitive login otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP login code\n\n  Resend the verification code for a pending OTP login.\n\nEXAMPLES\n  $ primitive login otp resend user@example.com\n\n"),
        "login\x00otp\x00user@example.com\x00--help" => Some("Start OTP login\n\nUSAGE\n  $ primitive login otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP login\n\n  Start email-code login using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nCOMMANDS\n  login otp confirm  Confirm OTP login\n  login otp resend   Resend OTP login code\n\n"),
        "login\x00resend\x00--help" => Some("Resend email-code login code\n\nUSAGE\n  $ primitive login resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code login code\n\n  Resend the verification code for a pending email-code login.\n\nEXAMPLES\n  $ primitive login resend user@example.com\n\n"),
        "login\x00resend\x00user@example.com\x00--help" => Some("Resend email-code login code\n\nUSAGE\n  $ primitive login resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code login code\n\n  Resend the verification code for a pending email-code login.\n\nEXAMPLES\n  $ primitive login resend user@example.com\n\n"),
        "login\x00user@example.com\x00--help" => Some("Log in to an existing account\n\nUSAGE\n  $ primitive login [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Log in to an existing account\n\n  Log in or sign in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive login <email> --signup-code <code> --accept-terms` for\n  email-code login, then `primitive login confirm <email> <code>`. Run\n  `primitive login` with no email to use browser approval; `primitive login\n  browser` is the explicit browser form. `primitive signin` supports the same\n  flows with signin-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive login\n\n  $ primitive login browser\n\n  $ primitive login --no-browser\n\n  $ primitive login user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login confirm user@example.com 123456\n\n  $ primitive login otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive login otp confirm user@example.com 123456\n\nTOPICS\n  login otp  Start OTP login\n\nCOMMANDS\n  login browser  Log in with browser approval\n  login confirm  Confirm email-code login\n  login otp      Start OTP login\n  login resend   Resend email-code login code\n\n"),
        "logout\x00--force\x00--help" => Some("Log out and revoke the saved CLI OAuth grant\n\nUSAGE\n  $ primitive logout [-f]\n\nFLAGS\n  -f, --force  Remove local CLI credentials, pending email-code auth state, and\n               any credential lock without revoking the server OAuth grant\n\nDESCRIPTION\n  Log out and revoke the saved CLI OAuth grant\n\n  Log out by revoking the saved Primitive CLI OAuth grant and deleting local\n  credentials. Use --force to remove local credentials, pending email-code auth\n  state, and stale credential locks without contacting Primitive.\n\nEXAMPLES\n  $ primitive logout\n\n  $ primitive logout --force\n\n"),
        "logout\x00--help" => Some("Log out and revoke the saved CLI OAuth grant\n\nUSAGE\n  $ primitive logout [-f]\n\nFLAGS\n  -f, --force  Remove local CLI credentials, pending email-code auth state, and\n               any credential lock without revoking the server OAuth grant\n\nDESCRIPTION\n  Log out and revoke the saved CLI OAuth grant\n\n  Log out by revoking the saved Primitive CLI OAuth grant and deleting local\n  credentials. Use --force to remove local credentials, pending email-code auth\n  state, and stale credential locks without contacting Primitive.\n\nEXAMPLES\n  $ primitive logout\n\n  $ primitive logout --force\n\n"),
        "logout\x00-f\x00--help" => Some("Log out and revoke the saved CLI OAuth grant\n\nUSAGE\n  $ primitive logout [-f]\n\nFLAGS\n  -f, --force  Remove local CLI credentials, pending email-code auth state, and\n               any credential lock without revoking the server OAuth grant\n\nDESCRIPTION\n  Log out and revoke the saved CLI OAuth grant\n\n  Log out by revoking the saved Primitive CLI OAuth grant and deleting local\n  credentials. Use --force to remove local credentials, pending email-code auth\n  state, and stale credential locks without contacting Primitive.\n\nEXAMPLES\n  $ primitive logout\n\n  $ primitive logout --force\n\n"),
        "memories" => Some("Store durable JSON memories by key. Use `primitive memories set|get|delete|search`; pass `--function <function-id>` for function-scoped memories.\n\nUSAGE\n  $ primitive memories COMMAND\n\nCOMMANDS\n  memories delete           Delete a memory\n  memories delete-memory    Delete a memory\n  memories get              Get a memory\n  memories get-memory       Get a memory\n  memories search           Search memories\n  memories search-memories  Search memories\n  memories set              Set a memory\n  memories set-memory       Set a memory\n\n"),
        "memories\x00--help" => Some("Store durable JSON memories by key. Use `primitive memories set|get|delete|search`; pass `--function <function-id>` for function-scoped memories.\n\nUSAGE\n  $ primitive memories COMMAND\n\nCOMMANDS\n  memories delete           Delete a memory\n  memories delete-memory    Delete a memory\n  memories get              Get a memory\n  memories get-memory       Get a memory\n  memories search           Search memories\n  memories search-memories  Search memories\n  memories set              Set a memory\n  memories set-memory       Set a memory\n\n"),
        "memories\x00-h" => Some("Store durable JSON memories by key. Use `primitive memories set|get|delete|search`; pass `--function <function-id>` for function-scoped memories.\n\nUSAGE\n  $ primitive memories COMMAND\n\nCOMMANDS\n  memories delete           Delete a memory\n  memories delete-memory    Delete a memory\n  memories get              Get a memory\n  memories get-memory       Get a memory\n  memories search           Search memories\n  memories search-memories  Search memories\n  memories set              Set a memory\n  memories set-memory       Set a memory\n\n"),
        "memories:delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories:delete\x00--function\x00value\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories:delete\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories:delete\x00--if-version\x00value\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories:delete\x00--org\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories:delete\x00--time\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories:delete-memory\x00--api-key\x00prim_test\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete-memory\x00--envelope\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete-memory\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete-memory\x00--if-version\x00value\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete-memory\x00--json\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete-memory\x00--key\x00help-key\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete-memory\x00--scope-id\x00fn_123\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete-memory\x00--scope-type\x00org\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete-memory\x00--time\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete-memory\x00--x-primitive-function-id\x00fn_123\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories:delete\x00help-key\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories:get\x00--api-key\x00prim_test\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories:get\x00--function\x00value\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories:get\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories:get\x00--org\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories:get\x00--time\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories:get-memory\x00--api-key\x00prim_test\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories:get-memory\x00--envelope\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories:get-memory\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories:get-memory\x00--json\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories:get-memory\x00--key\x00help-key\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories:get-memory\x00--scope-id\x00fn_123\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories:get-memory\x00--scope-type\x00org\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories:get-memory\x00--time\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories:get-memory\x00--x-primitive-function-id\x00fn_123\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories:get\x00help-key\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories:search\x00--api-key\x00prim_test\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search\x00--cursor\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search\x00--function\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search\x00--limit\x001\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search\x00--metadata-only\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search\x00--org\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search\x00--time\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search\x00--updated-after\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search\x00--updated-before\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:search-memories\x00--api-key\x00prim_test\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--cursor\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--envelope\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--include-value\x00true\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--json\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--limit\x001\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--prefix\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--scope-id\x00fn_123\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--scope-type\x00org\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--time\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--updated-after\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--updated-before\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search-memories\x00--x-primitive-function-id\x00fn_123\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories:search\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories:set\x00--api-key\x00prim_test\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--clear-ttl\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--expires-at\x002026-01-01T00:00:00Z\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--function\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--if-absent\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--if-version\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--org\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--time\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--ttl-seconds\x001\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set\x00--value-file\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories:set-memory\x00--api-key\x00prim_test\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--body-file\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--clear-ttl\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--envelope\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--expires-at\x002026-01-01T00:00:00Z\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--if-absent\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--if-version\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--json\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--key\x00help-key\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--no-clear-ttl\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--no-if-absent\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--raw-body\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--time\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--ttl-seconds\x001\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set-memory\x00--x-primitive-function-id\x00fn_123\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories:set\x00help-key\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories\x00delete\x00--function\x00value\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories\x00delete\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories\x00delete\x00--if-version\x00value\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories\x00delete\x00--org\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories\x00delete\x00--time\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories\x00delete-memory\x00--api-key\x00prim_test\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete-memory\x00--envelope\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete-memory\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete-memory\x00--if-version\x00value\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete-memory\x00--json\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete-memory\x00--key\x00help-key\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete-memory\x00--scope-id\x00fn_123\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete-memory\x00--scope-type\x00org\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete-memory\x00--time\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete-memory\x00--x-primitive-function-id\x00fn_123\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--if-version <value>] [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --if-version=<value>\n      Optional compare-and-delete version. If the active memory exists\n      at a different version, the API returns `memory_conflict`.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete one active memory by key and scope. Deletes are idempotent when\n  `if_version` is omitted: deleting a missing key returns `deleted:\n  false`. With `if_version`, a missing key still returns `deleted: false`,\n  but a stale version returns `memory_conflict`.\n\n  A successful delete records memory write usage.\n\n\n  Tip: prefer `primitive memories delete <key> [--function <function-id>]` for\n  the common delete path.\n\n"),
        "memories\x00delete\x00help-key\x00--help" => Some("Delete a memory\n\nUSAGE\n  $ primitive memories delete KEY [--api-key <value>] [--function <value> |\n    --org] [--if-version <value>] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --function=<value>    Function id (UUID) to scope this memory to. Function\n                        names are not accepted.\n  --if-version=<value>  Compare-and-delete version. The delete fails with\n                        memory_conflict if the current version differs.\n  --org                 Force org scope. Outside a function-authenticated\n                        request this is already the default.\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Delete a memory\n\n  Delete a Primitive memory by key.\n\n  Deletes are idempotent when --if-version is omitted. With --if-version, a\n  stale version fails with memory_conflict. Pass --function <function-id> to\n  delete from a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories delete thread:latest\n\n  $ primitive memories delete state --function <fn-id> --if-version 3\n\n"),
        "memories\x00get\x00--api-key\x00prim_test\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories\x00get\x00--function\x00value\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories\x00get\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories\x00get\x00--org\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories\x00get\x00--time\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories\x00get-memory\x00--api-key\x00prim_test\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories\x00get-memory\x00--envelope\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories\x00get-memory\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories\x00get-memory\x00--json\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories\x00get-memory\x00--key\x00help-key\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories\x00get-memory\x00--scope-id\x00fn_123\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories\x00get-memory\x00--scope-type\x00org\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories\x00get-memory\x00--time\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories\x00get-memory\x00--x-primitive-function-id\x00fn_123\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get-memory --key <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --key=<value>\n      (required) Memory key. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Get a memory\n\n  Fetch one active memory by key and scope. Omit scope parameters to use\n  the automatic default: function-authenticated context, then the\n  `x-primitive-function-id` header, then org scope. Function scope uses a\n  function id UUID in `scope_id`.\n\n  A successful read records memory read usage and updates the memory's\n  read stats asynchronously.\n\n\n  Tip: prefer `primitive memories get <key> [--function <function-id>]` for the\n  common read path.\n\n"),
        "memories\x00get\x00help-key\x00--help" => Some("Get a memory\n\nUSAGE\n  $ primitive memories get KEY [--api-key <value>] [--function <value> |\n    --org] [--time]\n\nARGUMENTS\n  KEY  Memory key.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --function=<value>  Function id (UUID) to scope this memory to. Function names\n                      are not accepted.\n  --org               Force org scope. Outside a function-authenticated request\n                      this is already the default.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Get a memory\n\n  Get a Primitive memory by key.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to read a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories get thread:latest\n\n  $ primitive memories get state --function <fn-id>\n\n"),
        "memories\x00search\x00--api-key\x00prim_test\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search\x00--cursor\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search\x00--function\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search\x00--limit\x001\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search\x00--metadata-only\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search\x00--org\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search\x00--time\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search\x00--updated-after\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search\x00--updated-before\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00search-memories\x00--api-key\x00prim_test\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--cursor\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--envelope\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--include-value\x00true\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--json\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--limit\x001\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--prefix\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--scope-id\x00fn_123\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--scope-type\x00org\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--time\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--updated-after\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--updated-before\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search-memories\x00--x-primitive-function-id\x00fn_123\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search-memories [--api-key <value>] [--time] [--json]\n    [--envelope] [--prefix <value>] [--cursor <value>] [--limit <value>]\n    [--include-value true|false] [--updated-after <value>] [--updated-before\n    <value>] [--scope-type org|function] [--scope-id <value>]\n    [--x-primitive-function-id <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Key cursor from a previous response's `meta.cursor`. The next page\n      starts after this key.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --include-value=<option>\n      Pass `false` to omit the `value` field and return metadata only.\n\n      <options: true|false>\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --prefix=<value>\n      Key prefix to match. Empty string lists all active memories in the\n      selected scope. Must be at most 512 UTF-8 bytes.\n\n  --scope-id=<value>\n      Function id UUID when `scope_type=function`. Not valid with\n      `scope_type=org`.\n\n  --scope-type=<option>\n      Explicit scope type. Omit to use automatic scope resolution. Pass\n      `function` with `scope_id=<function-id>`, or `org` with no `scope_id`.\n\n      <options: org|function>\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --updated-after=<value>\n      Only include memories updated at or after this timestamp.\n\n  --updated-before=<value>\n      Only include memories updated at or before this timestamp.\n\n  --x-primitive-function-id=<value>\n      Optional function id UUID used as the default scope when query\n      scope parameters are omitted.\n\nDESCRIPTION\n  Search memories\n\n  List active memories in a scope by lexicographic key prefix. Results\n  are ordered by key ascending. The `meta.cursor` value is the next key\n  cursor; pass it back as `cursor` to continue after that key.\n\n  Search records one memory read usage event for the operation. Pass\n  `include_value=false` to return metadata only.\n\n\n  Tip: prefer `primitive memories search [prefix] [--metadata-only] [--function\n  <function-id>]` for the common search path.\n\n"),
        "memories\x00search\x00value\x00--help" => Some("Search memories\n\nUSAGE\n  $ primitive memories search [PREFIX] [--api-key <value>] [--function\n    <value> | --org] [--limit <value>] [--cursor <value>] [--metadata-only]\n    [--updated-after <value>] [--updated-before <value>] [--time]\n\nARGUMENTS\n  [PREFIX]  Key prefix. Omit to list all active memories in scope.\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --cursor=<value>          Key cursor from a previous response's meta.cursor.\n  --function=<value>        Function id (UUID) to scope this memory to. Function\n                            names are not accepted.\n  --limit=<value>           [default: 50] Maximum results to return (1-100,\n                            default 50).\n  --metadata-only           Omit memory values and return metadata only.\n  --org                     Force org scope. Outside a function-authenticated\n                            request this is already the default.\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --updated-after=<value>   Only include memories updated at or after this ISO\n                            timestamp.\n  --updated-before=<value>  Only include memories updated at or before this ISO\n                            timestamp.\n\nDESCRIPTION\n  Search memories\n\n  Search Primitive memories by key prefix.\n\n  Results are ordered by key ascending. Pass --metadata-only to omit values.\n  Pass --function <function-id> to search a function scope by function id UUID.\n\nEXAMPLES\n  $ primitive memories search thread:\n\n  $ primitive memories search --metadata-only --limit 100\n\n  $ primitive memories search state: --function <fn-id>\n\n"),
        "memories\x00set\x00--api-key\x00prim_test\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--clear-ttl\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--expires-at\x002026-01-01T00:00:00Z\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--function\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--if-absent\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--if-version\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--org\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--time\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--ttl-seconds\x001\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set\x00--value-file\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "memories\x00set-memory\x00--api-key\x00prim_test\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--body-file\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--clear-ttl\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--envelope\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--expires-at\x002026-01-01T00:00:00Z\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--if-absent\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--if-version\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--json\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--key\x00help-key\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--no-clear-ttl\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--no-if-absent\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--raw-body\x00value\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--time\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--ttl-seconds\x001\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set-memory\x00--x-primitive-function-id\x00fn_123\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set-memory [--api-key <value>] [--time] [--json]\n    [--envelope] [--x-primitive-function-id <value>] [--raw-body <value>]\n    [--body-file <value>] [--key <value>] [--clear-ttl] [--expires-at <value>]\n    [--if-absent] [--if-version <value>] [--ttl-seconds <value>]\n\nFLAGS\n  --api-key=<value>                  [env: PRIMITIVE_API_KEY] Primitive API key\n                                     override (defaults to PRIMITIVE_API_KEY or\n                                     saved OAuth login credentials)\n  --body-file=<value>                Path to a JSON file used as the request\n                                     body. Same role as --raw-body for callers\n                                     passing a saved payload.\n  --[no-]clear-ttl                   Clear any existing TTL. Mutually exclusive\n                                     with `ttl_seconds` and `expires_at`.\n  --envelope                         Print the full response envelope, including\n                                     pagination metadata such as meta.cursor.\n                                     Defaults to printing only the data payload\n                                     for backward compatibility.\n  --expires-at=<value>               Set or replace the absolute expiration\n                                     timestamp. Mutually exclusive with\n                                     `ttl_seconds` and `clear_ttl`.\n  --[no-]if-absent                   Create only when the key is absent.\n                                     Mutually exclusive with `if_version`.\n  --if-version=<value>               Bigint counter serialized as a base-10\n                                     string.\n  --json                             Accepted for consistency with task-focused\n                                     commands. Generated API commands already\n                                     print JSON by default.\n  --key=<value>                      Caller-defined key, at most 512 UTF-8\n                                     bytes.\n  --raw-body=<value>                 Full request body as raw JSON. Escape hatch\n                                     for nested or complex fields (e.g. arrays);\n                                     prefer per-field flags (e.g. --to, --from,\n                                     --body-text) when available.\n  --time                             Print the wall-clock duration of this\n                                     command to stderr after it completes (e.g.\n                                     `[time: 1.34s]`). Useful for measuring\n                                     `--wait` send latency, comparing CLI\n                                     overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>              Set or replace the TTL in seconds. Mutually\n                                     exclusive with `expires_at` and\n                                     `clear_ttl`.\n  --x-primitive-function-id=<value>  Optional function id UUID used as the\n                                     default scope when the body\n                                     does not include `scope`. Ignored when\n                                     `scope` is provided.\n\nDESCRIPTION\n  Set a memory\n\n  Create or update a durable JSON memory under an org or function scope.\n  When no explicit scope is provided, function-authenticated requests\n  use that function's id automatically; requests with\n  `x-primitive-function-id` use that function id; all other requests\n  default to org scope.\n\n  `scope.type = function` requires the function id UUID in `scope.id`.\n  Function names are not accepted as scope identifiers. Values must be\n  valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at\n  most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are\n  bigint counters serialized as strings.\n\n  Passing `if_absent` turns the write into create-only. Passing\n  `if_version` turns the write into compare-and-set. These options are\n  mutually exclusive and return `memory_conflict` on a stale version or\n  existing key.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * value  any  JSON value accepted by Primitive Memories. The server accepts\n  strings, numb...\n  scope  any  Memory scope. `org` resolves to the authenticated organization.\n  `function` ...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive memories set <key> <json-value> [--function\n  <function-id>]` for JSON value parsing and scope flags. This raw command\n  exists for callers passing full request JSON.\n\n"),
        "memories\x00set\x00help-key\x00--help" => Some("Set a memory\n\nUSAGE\n  $ primitive memories set KEY [VALUE] [--api-key <value>] [--function\n    <value> | --org] [--value-file <value>] [--ttl-seconds <value> |\n    --expires-at <value> | --clear-ttl] [--if-absent | --if-version <value>]\n    [--time]\n\nARGUMENTS\n  KEY      Memory key, at most 512 UTF-8 bytes.\n  [VALUE]  JSON value. Strings must be quoted as JSON strings.\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --clear-ttl            Clear any existing TTL. Mutually exclusive with\n                         --ttl-seconds and --expires-at.\n  --expires-at=<value>   Set or replace the absolute expiration timestamp.\n                         Mutually exclusive with --ttl-seconds and --clear-ttl.\n  --function=<value>     Function id (UUID) to scope this memory to. Function\n                         names are not accepted.\n  --if-absent            Create only when the key is absent. Mutually exclusive\n                         with --if-version.\n  --if-version=<value>   Compare-and-set version. The write fails with\n                         memory_conflict if the current version differs.\n  --org                  Force org scope. Outside a function-authenticated\n                         request this is already the default.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n  --ttl-seconds=<value>  Set or replace the TTL in seconds. Mutually exclusive\n                         with --expires-at and --clear-ttl.\n  --value-file=<value>   Read the JSON value from a UTF-8 file.\n\nDESCRIPTION\n  Set a memory\n\n  Set a Primitive memory to a JSON value.\n\n  Values must be valid JSON. Objects, arrays, numbers, booleans, null, and\n  quoted\n  JSON strings are accepted. For example, pass '\"hello\"' for a string value.\n\n  By default, CLI calls use org scope. Pass --function <function-id> to write a\n  function-scoped memory by function id UUID. Function names are not accepted.\n\nEXAMPLES\n  $ primitive memories set thread:latest '{\"email_id\":\"em_123\"}'\n\n  $ primitive memories set greeting '\"hello\"'\n\n  $ primitive memories set state '{\"step\":2}' --function <fn-id>\n\n  $ primitive memories set lock '{\"owner\":\"agent\"}' --if-absent\n\n"),
        "org" => Some("Manage org-level (global) resources shared across functions\n\nUSAGE\n  $ primitive org COMMAND\n\nTOPICS\n  org secrets  Global secrets shared across every function. Use `primitive org\n               secrets list|set|remove`. Changes land in a function on its next\n               redeploy; a function secret of the same name overrides the\n               global.\n\n"),
        "org\x00--help" => Some("Manage org-level (global) resources shared across functions\n\nUSAGE\n  $ primitive org COMMAND\n\nTOPICS\n  org secrets  Global secrets shared across every function. Use `primitive org\n               secrets list|set|remove`. Changes land in a function on its next\n               redeploy; a function secret of the same name overrides the\n               global.\n\n"),
        "org\x00-h" => Some("Manage org-level (global) resources shared across functions\n\nUSAGE\n  $ primitive org COMMAND\n\nTOPICS\n  org secrets  Global secrets shared across every function. Use `primitive org\n               secrets list|set|remove`. Changes land in a function on its next\n               redeploy; a function secret of the same name overrides the\n               global.\n\n"),
        "org:secrets" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "org:secrets\x00--help" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "org:secrets\x00-h" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "org:secrets:delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org:secrets:delete\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org:secrets:delete\x00--key\x00help-key\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org:secrets:delete\x00--time\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org:secrets:list\x00--api-key\x00prim_test\x00--help" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "org:secrets:list\x00--help" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "org:secrets:list\x00--time\x00--help" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "org:secrets:remove\x00--api-key\x00prim_test\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org:secrets:remove\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org:secrets:remove\x00--key\x00help-key\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org:secrets:remove\x00--time\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org:secrets:set\x00--api-key\x00prim_test\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org:secrets:set\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org:secrets:set\x00--key\x00help-key\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org:secrets:set\x00--stdin\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org:secrets:set\x00--time\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org:secrets:set\x00--value-file\x00value\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org:secrets:set\x00--value-from-env-file\x00value\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org:secrets:set\x00--value-from-env\x00value\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org:secrets:set\x00--value\x00hello\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org\x00secrets" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "org\x00secrets\x00--help" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "org\x00secrets\x00-h" => Some("Global secrets shared across every function. Use `primitive org secrets list|set|remove`. Changes land in a function on its next redeploy; a function secret of the same name overrides the global.\n\nUSAGE\n  $ primitive org secrets COMMAND\n\nCOMMANDS\n  org secrets delete  Delete a global secret\n  org secrets list    List global secrets (keys only; values never returned)\n  org secrets remove  Delete a global secret\n  org secrets set     Set a global secret shared across all functions\n\n"),
        "org\x00secrets\x00delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org\x00secrets\x00delete\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org\x00secrets\x00delete\x00--key\x00help-key\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org\x00secrets\x00delete\x00--time\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets delete --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org\x00secrets\x00list\x00--api-key\x00prim_test\x00--help" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "org\x00secrets\x00list\x00--help" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "org\x00secrets\x00list\x00--time\x00--help" => Some("List global secrets (keys only; values never returned)\n\nUSAGE\n  $ primitive org secrets list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List global secrets (keys only; values never returned)\n\n  List your organization's global secrets.\n\n  Global secrets apply to every function in the org and are read as\n  `env.<KEY>` in handlers. Only the keys and timestamps are returned;\n  the values are encrypted at rest and never surfaced.\n\nEXAMPLES\n  $ primitive org secrets list\n\n"),
        "org\x00secrets\x00remove\x00--api-key\x00prim_test\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org\x00secrets\x00remove\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org\x00secrets\x00remove\x00--key\x00help-key\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org\x00secrets\x00remove\x00--time\x00--help" => Some("Delete a global secret\n\nUSAGE\n  $ primitive org secrets remove --key <value> [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --key=<value>      (required) Global secret key to delete.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a global secret\n\n  Delete a global secret.\n\n  Deployed functions keep the previous value until each is redeployed. A\n  function that defines its own secret of the same name is unaffected.\n\nALIASES\n  $ primitive org secrets delete\n\nEXAMPLES\n  $ primitive org secrets remove --key STRIPE_KEY\n\n"),
        "org\x00secrets\x00set\x00--api-key\x00prim_test\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org\x00secrets\x00set\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org\x00secrets\x00set\x00--key\x00help-key\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org\x00secrets\x00set\x00--stdin\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org\x00secrets\x00set\x00--time\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org\x00secrets\x00set\x00--value-file\x00value\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org\x00secrets\x00set\x00--value-from-env-file\x00value\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org\x00secrets\x00set\x00--value-from-env\x00value\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "org\x00secrets\x00set\x00--value\x00hello\x00--help" => Some("Set a global secret shared across all functions\n\nUSAGE\n  $ primitive org secrets set --key <value> [--api-key <value>] [--value\n    <value>] [--value-from-env <value>] [--value-file <value>]\n    [--value-from-env-file <value>] [--stdin] [--time]\n\nFLAGS\n  --api-key=<value>              [env: PRIMITIVE_API_KEY] Primitive API key\n                                 override (defaults to PRIMITIVE_API_KEY or\n                                 saved OAuth login credentials)\n  --key=<value>                  (required) Secret key. Uppercase letters,\n                                 digits, underscores; must start with a letter\n                                 or underscore. System-managed keys are\n                                 reserved.\n  --stdin                        Read the secret value from stdin. A single\n                                 trailing line ending is stripped.\n  --time                         Print the wall-clock duration of this command\n                                 to stderr after it completes (e.g. `[time:\n                                 1.34s]`). Useful for measuring `--wait` send\n                                 latency, comparing CLI overhead, or capturing\n                                 timing in scripts.\n  --value=<value>                Secret value (up to 4096 UTF-8 bytes).\n                                 Encrypted at rest. Visible in shell history and\n                                 process argv; prefer a non-argv source for\n                                 sensitive values.\n  --value-file=<value>           UTF-8 file to read as the secret value. The\n                                 full file contents become the value.\n  --value-from-env=<value>       Environment variable to read as the secret\n                                 value. Example: --value-from-env OPENAI_KEY\n                                 reads process.env.OPENAI_KEY.\n  --value-from-env-file=<value>  Dotenv-style file to read as the secret value.\n                                 Use FILE to read --key from that file, or\n                                 FILE:KEY to read a different key.\n\nDESCRIPTION\n  Set a global secret shared across all functions\n\n  Set a global secret available to every function as `env.<KEY>`.\n\n  Global secrets are read into each function at deploy time, so a new or\n  changed value lands in a function only on its next redeploy. A function\n  secret with the same key overrides the global value for that function.\n\n  Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters, digits,\n  underscores; first character a letter or underscore). System-managed keys\n  are reserved and rejected. Instead of --value, use --value-from-env ENV_VAR,\n  --value-file PATH, --value-from-env-file FILE[:KEY], or --stdin to avoid\n  putting the secret value in shell history or process argv. If KEY is omitted\n  from --value-from-env-file, the command's --key is used.\n\nEXAMPLES\n  $ primitive org secrets set --key STRIPE_KEY --value sk_live_...\n\n  $ primitive org secrets set --key OPENAI_KEY --value-from-env OPENAI_KEY\n\n  printf '%s' \"$OPENAI_KEY\" | primitive org secrets set --key OPENAI_KEY --stdin\n\n"),
        "otp\x00--accept-terms\x00--help" => Some("Start email-code auth\n\nUSAGE\n  $ primitive otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start email-code auth\n\n  Start email-code authentication, send a verification code, and save the\n  pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive otp confirm user@example.com 123456\n\nCOMMANDS\n  otp confirm  Confirm email-code auth\n  otp resend   Resend email-code auth code\n\n"),
        "otp\x00--device-name\x00value\x00--help" => Some("Start email-code auth\n\nUSAGE\n  $ primitive otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start email-code auth\n\n  Start email-code authentication, send a verification code, and save the\n  pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive otp confirm user@example.com 123456\n\nCOMMANDS\n  otp confirm  Confirm email-code auth\n  otp resend   Resend email-code auth code\n\n"),
        "otp\x00--force\x00--help" => Some("Start email-code auth\n\nUSAGE\n  $ primitive otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start email-code auth\n\n  Start email-code authentication, send a verification code, and save the\n  pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive otp confirm user@example.com 123456\n\nCOMMANDS\n  otp confirm  Confirm email-code auth\n  otp resend   Resend email-code auth code\n\n"),
        "otp\x00--help" => Some("Start email-code auth\n\nUSAGE\n  $ primitive otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start email-code auth\n\n  Start email-code authentication, send a verification code, and save the\n  pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive otp confirm user@example.com 123456\n\nCOMMANDS\n  otp confirm  Confirm email-code auth\n  otp resend   Resend email-code auth code\n\n"),
        "otp\x00--signup-code\x00123456\x00--help" => Some("Start email-code auth\n\nUSAGE\n  $ primitive otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start email-code auth\n\n  Start email-code authentication, send a verification code, and save the\n  pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive otp confirm user@example.com 123456\n\nCOMMANDS\n  otp confirm  Confirm email-code auth\n  otp resend   Resend email-code auth code\n\n"),
        "otp\x00-f\x00--help" => Some("Start email-code auth\n\nUSAGE\n  $ primitive otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start email-code auth\n\n  Start email-code authentication, send a verification code, and save the\n  pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive otp confirm user@example.com 123456\n\nCOMMANDS\n  otp confirm  Confirm email-code auth\n  otp resend   Resend email-code auth code\n\n"),
        "otp:confirm\x00--force\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp:confirm\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp:confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp:confirm\x00-f\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp:confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp:resend\x00--help" => Some("Resend email-code auth code\n\nUSAGE\n  $ primitive otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code auth code\n\n  Resend the verification code for pending email-code authentication.\n\nEXAMPLES\n  $ primitive otp resend user@example.com\n\n"),
        "otp:resend\x00user@example.com\x00--help" => Some("Resend email-code auth code\n\nUSAGE\n  $ primitive otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code auth code\n\n  Resend the verification code for pending email-code authentication.\n\nEXAMPLES\n  $ primitive otp resend user@example.com\n\n"),
        "otp\x00confirm\x00--force\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp\x00confirm\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp\x00confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp\x00confirm\x00-f\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp\x00confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm email-code auth\n\nUSAGE\n  $ primitive otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code auth\n\n  Confirm pending email-code authentication, create an OAuth session, and save\n  CLI credentials locally.\n\nEXAMPLES\n  $ primitive otp confirm user@example.com 123456\n\n  $ primitive otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "otp\x00resend\x00--help" => Some("Resend email-code auth code\n\nUSAGE\n  $ primitive otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code auth code\n\n  Resend the verification code for pending email-code authentication.\n\nEXAMPLES\n  $ primitive otp resend user@example.com\n\n"),
        "otp\x00resend\x00user@example.com\x00--help" => Some("Resend email-code auth code\n\nUSAGE\n  $ primitive otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code auth code\n\n  Resend the verification code for pending email-code authentication.\n\nEXAMPLES\n  $ primitive otp resend user@example.com\n\n"),
        "otp\x00user@example.com\x00--help" => Some("Start email-code auth\n\nUSAGE\n  $ primitive otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start email-code auth\n\n  Start email-code authentication, send a verification code, and save the\n  pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive otp confirm user@example.com 123456\n\nCOMMANDS\n  otp confirm  Confirm email-code auth\n  otp resend   Resend email-code auth code\n\n"),
        "payloads" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads COMMAND\n\nCOMMANDS\n  payloads pull  Stream-download and decrypt a payload to a file\n  payloads push  Stream-upload a file as an encrypted payload\n\n"),
        "payloads\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads COMMAND\n\nCOMMANDS\n  payloads pull  Stream-download and decrypt a payload to a file\n  payloads push  Stream-upload a file as an encrypted payload\n\n"),
        "payloads\x00-h" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads COMMAND\n\nCOMMANDS\n  payloads pull  Stream-download and decrypt a payload to a file\n  payloads push  Stream-upload a file as an encrypted payload\n\n"),
        "payloads:pull\x00--api-key\x00prim_test\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads:pull\x00--cek\x00value\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads:pull\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads:pull\x00--out\x00value\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads:pull\x00--quiet\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads:pull\x00help-sweep.tmp\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads:push\x00--api-key\x00prim_test\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payloads:push\x00--concurrency\x00value\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payloads:push\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payloads:push\x00--quiet\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payloads:push\x00help-sweep.tmp\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payloads\x00pull\x00--api-key\x00prim_test\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads\x00pull\x00--cek\x00value\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads\x00pull\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads\x00pull\x00--out\x00value\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads\x00pull\x00--quiet\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads\x00pull\x00help-sweep.tmp\x00--help" => Some("Stream-download and decrypt a payload to a file\n\nUSAGE\n  $ primitive payloads pull ROOT --out <value> --cek <value> [--api-key\n    <value>] [--quiet]\n\nARGUMENTS\n  ROOT  Object content address (merkle_root)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                     PRIMITIVE_API_KEY or saved login credentials)\n  --cek=<value>      (required) Hex content-encryption key from `payloads push`\n  --out=<value>      (required) Output file path\n  --quiet            Suppress progress output\n\nDESCRIPTION\n  Stream-download and decrypt a payload to a file\n\n  Download and decrypt a Primitive Payload to a file.\n\n  Streams one chunk at a time, verifying each against its content address before\n  decryption, so a corrupt or substituted chunk fails loudly. Requires the hex\n  CEK printed by `payloads push`.\n\nEXAMPLES\n  $ primitive payloads pull <merkle_root> --cek <hex> --out ./restored.mp4\n\n"),
        "payloads\x00push\x00--api-key\x00prim_test\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payloads\x00push\x00--concurrency\x00value\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payloads\x00push\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payloads\x00push\x00--quiet\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payloads\x00push\x00help-sweep.tmp\x00--help" => Some("Stream-upload a file as an encrypted payload\n\nUSAGE\n  $ primitive payloads push FILE [--api-key <value>] [--concurrency\n    <value>] [--quiet]\n\nARGUMENTS\n  FILE  Path to the file to upload\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key (defaults to\n                         PRIMITIVE_API_KEY or saved login credentials)\n  --concurrency=<value>  [default: 3] Parallel chunk uploads\n  --quiet                Suppress progress output\n\nDESCRIPTION\n  Stream-upload a file as an encrypted payload\n\n  Upload a file as a Primitive Payload \u{2014} a large, content-addressed,\n  end-to-end-encrypted object.\n\n  The file is chunked and encrypted client-side and streamed up in bounded\n  memory (multi-GB files never load fully into RAM). Prints the object's content\n  address (merkle_root) and the hex CEK required to download it \u{2014} keep the CEK\n  secret; without it the object cannot be decrypted.\n\nEXAMPLES\n  $ primitive payloads push ./big-video.mp4\n\n"),
        "payments" => Some("Collect and pay x402 stablecoin (USDC) payments. Start with `primitive payments register-payout-address`, then `primitive payments create-challenge` to request a payment and `primitive payments pay` to settle one. Settlement is non-custodial; signing happens locally with your wallet key. Manage caps with `primitive payments get-policy|update-spend-policy`.\n\nUSAGE\n  $ primitive payments COMMAND\n\nCOMMANDS\n  payments challenge-from-email     Print the email x402 challenge object\n                                    derived from a received payment-request\n                                    email\n  payments charge                   Request an x402 payment (create a challenge)\n  payments create-challenge         Create a payment challenge\n  payments create-email-challenge   Create an email-native payment challenge\n  payments get-challenge            Get a payment challenge\n  payments get-spend-policy         Get your spend policy\n  payments list-declined-payments   List declined payments\n  payments list-payout-addresses    List payout addresses\n  payments pay                      Sign and settle an x402 payment challenge\n  payments pay-challenge            Sign and settle an x402 payment challenge\n  payments pay-email                Sign an email x402 challenge and send the\n                                    signed interaction.json (one step; derives\n                                    the challenge from --in-reply-to)\n  payments pay-email-step           Sign an email x402 challenge into a\n                                    payment-step envelope\n  payments register-payout          Register a payout address for receiving x402\n                                    payments\n  payments register-payout-address  Register a payout address for receiving x402\n                                    payments\n  payments update-spend-policy      Update your spend policy\n\n"),
        "payments\x00--help" => Some("Collect and pay x402 stablecoin (USDC) payments. Start with `primitive payments register-payout-address`, then `primitive payments create-challenge` to request a payment and `primitive payments pay` to settle one. Settlement is non-custodial; signing happens locally with your wallet key. Manage caps with `primitive payments get-policy|update-spend-policy`.\n\nUSAGE\n  $ primitive payments COMMAND\n\nCOMMANDS\n  payments challenge-from-email     Print the email x402 challenge object\n                                    derived from a received payment-request\n                                    email\n  payments charge                   Request an x402 payment (create a challenge)\n  payments create-challenge         Create a payment challenge\n  payments create-email-challenge   Create an email-native payment challenge\n  payments get-challenge            Get a payment challenge\n  payments get-spend-policy         Get your spend policy\n  payments list-declined-payments   List declined payments\n  payments list-payout-addresses    List payout addresses\n  payments pay                      Sign and settle an x402 payment challenge\n  payments pay-challenge            Sign and settle an x402 payment challenge\n  payments pay-email                Sign an email x402 challenge and send the\n                                    signed interaction.json (one step; derives\n                                    the challenge from --in-reply-to)\n  payments pay-email-step           Sign an email x402 challenge into a\n                                    payment-step envelope\n  payments register-payout          Register a payout address for receiving x402\n                                    payments\n  payments register-payout-address  Register a payout address for receiving x402\n                                    payments\n  payments update-spend-policy      Update your spend policy\n\n"),
        "payments\x00-h" => Some("Collect and pay x402 stablecoin (USDC) payments. Start with `primitive payments register-payout-address`, then `primitive payments create-challenge` to request a payment and `primitive payments pay` to settle one. Settlement is non-custodial; signing happens locally with your wallet key. Manage caps with `primitive payments get-policy|update-spend-policy`.\n\nUSAGE\n  $ primitive payments COMMAND\n\nCOMMANDS\n  payments challenge-from-email     Print the email x402 challenge object\n                                    derived from a received payment-request\n                                    email\n  payments charge                   Request an x402 payment (create a challenge)\n  payments create-challenge         Create a payment challenge\n  payments create-email-challenge   Create an email-native payment challenge\n  payments get-challenge            Get a payment challenge\n  payments get-spend-policy         Get your spend policy\n  payments list-declined-payments   List declined payments\n  payments list-payout-addresses    List payout addresses\n  payments pay                      Sign and settle an x402 payment challenge\n  payments pay-challenge            Sign and settle an x402 payment challenge\n  payments pay-email                Sign an email x402 challenge and send the\n                                    signed interaction.json (one step; derives\n                                    the challenge from --in-reply-to)\n  payments pay-email-step           Sign an email x402 challenge into a\n                                    payment-step envelope\n  payments register-payout          Register a payout address for receiving x402\n                                    payments\n  payments register-payout-address  Register a payout address for receiving x402\n                                    payments\n  payments update-spend-policy      Update your spend policy\n\n"),
        "payments:challenge-from-email\x00--api-key\x00prim_test\x00--help" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "payments:challenge-from-email\x00--help" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "payments:challenge-from-email\x00--id\x00fn_123\x00--help" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "payments:challenge-from-email\x00--time\x00--help" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "payments:charge\x00--amount-usdc\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--amount\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--api-key\x00prim_test\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--description\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--expires-in\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--idempotency-key\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--network\x00base\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--payer-org\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--resource\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:charge\x00--time\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments:create-challenge\x00--amount\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--api-key\x00prim_test\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--body-file\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--description\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--envelope\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--expires-in\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--json\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--network\x00base\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--payer-org\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--raw-body\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--resource\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-challenge\x00--time\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--amount\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--api-key\x00prim_test\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--body-file\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--description\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--envelope\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--expires-in\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--from\x00user@example.com\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--idempotency-key\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--json\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--network\x00base\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--raw-body\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--resource\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--time\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:create-email-challenge\x00--to\x00user@example.com\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments:get-challenge\x00--api-key\x00prim_test\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments:get-challenge\x00--envelope\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments:get-challenge\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments:get-challenge\x00--id\x00fn_123\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments:get-challenge\x00--json\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments:get-challenge\x00--time\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments:get-spend-policy\x00--api-key\x00prim_test\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments:get-spend-policy\x00--envelope\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments:get-spend-policy\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments:get-spend-policy\x00--json\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments:get-spend-policy\x00--time\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments:list-declined-payments\x00--api-key\x00prim_test\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments:list-declined-payments\x00--envelope\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments:list-declined-payments\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments:list-declined-payments\x00--json\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments:list-declined-payments\x00--time\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments:list-payout-addresses\x00--api-key\x00prim_test\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments:list-payout-addresses\x00--envelope\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments:list-payout-addresses\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments:list-payout-addresses\x00--json\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments:list-payout-addresses\x00--time\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments:pay\x00--api-key\x00prim_test\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay\x00--challenge-file\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay\x00--challenge\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay\x00--json\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay\x00--private-key\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay\x00--time\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay-challenge\x00--api-key\x00prim_test\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay-challenge\x00--challenge-file\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay-challenge\x00--challenge\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay-challenge\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay-challenge\x00--json\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay-challenge\x00--private-key\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay-challenge\x00--time\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments:pay-email\x00--api-key\x00prim_test\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--body\x00hello\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--challenge-file\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--challenge\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--from\x00user@example.com\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--in-reply-to\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--json\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--private-key\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--settle-interval\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--settle-timeout\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--time\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--wait\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email\x00--wait-settle\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments:pay-email-step\x00--api-key\x00prim_test\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments:pay-email-step\x00--challenge-file\x00value\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments:pay-email-step\x00--challenge\x00value\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments:pay-email-step\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments:pay-email-step\x00--json\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments:pay-email-step\x00--private-key\x00value\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments:pay-email-step\x00--time\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments:register-payout\x00--api-key\x00prim_test\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout\x00--issued-at\x002026-01-01T00:00:00Z\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout\x00--json\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout\x00--label\x00value\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout\x00--network\x00base\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout\x00--private-key\x00value\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout\x00--time\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout-address\x00--api-key\x00prim_test\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout-address\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout-address\x00--issued-at\x002026-01-01T00:00:00Z\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout-address\x00--json\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout-address\x00--label\x00value\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout-address\x00--network\x00base\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout-address\x00--private-key\x00value\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:register-payout-address\x00--time\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments:update-spend-policy\x00--api-key\x00prim_test\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--body-file\x00value\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--envelope\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--json\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--max-per-day\x00value\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--max-per-payment\x00value\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--no-paused\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--paused\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--raw-body\x00value\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments:update-spend-policy\x00--time\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00challenge-from-email\x00--api-key\x00prim_test\x00--help" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "payments\x00challenge-from-email\x00--help" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "payments\x00challenge-from-email\x00--id\x00fn_123\x00--help" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "payments\x00challenge-from-email\x00--time\x00--help" => Some("Print the email x402 challenge object derived from a received payment-request email\n\nUSAGE\n  $ primitive payments challenge-from-email --id <value> [--api-key <value>]\n  [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --id=<value>       (required) Id of the inbound payment-request email whose\n                     interaction.json attachment carries the challenge.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the email x402 challenge object derived from a received payment-request\n  email\n\n  Derive and print the email x402 challenge object from a received\n  payment-request email.\n\n  A payer receives an x402 payment request as an email carrying an\n  `interaction.json` attachment (the wire envelope). The signing commands,\n  however, take the challenge object the payee's `create-email-challenge` API\n  returns, which has a different field layout. This command downloads the\n  inbound\n  email's `interaction.json` attachment and reshapes it into that challenge\n  object, printing it to stdout.\n\n  Use it when you want the challenge as a portable artifact (e.g. to feed\n  `pay-email-step` for offline signing, or an SDK `payEmailChallenge` call).\n  For the simple one-shot, prefer `pay-email --in-reply-to <id>`, which derives\n  the challenge internally and needs no intermediate file.\n\n  primitive payments challenge-from-email --id <inbound-id> > challenge.json\n  primitive payments challenge-from-email --id <inbound-id> \\\n  | primitive payments pay-email-step > interaction.json\n\nEXAMPLES\n  $ primitive payments challenge-from-email --id <inbound-email-id>\n\n  $ primitive payments challenge-from-email --id <inbound-email-id> | primitive payments pay-email-step > interaction.json\n\n"),
        "payments\x00charge\x00--amount-usdc\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--amount\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--api-key\x00prim_test\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--description\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--expires-in\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--idempotency-key\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--network\x00base\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--payer-org\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--resource\x00value\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00charge\x00--time\x00--help" => Some("Request an x402 payment (create a challenge)\n\nUSAGE\n  $ primitive payments charge [--api-key <value>] [--amount-usdc <value> |\n    --amount <value>] [--network base|base-sepolia] [--payer-org <value>]\n    [--description <value>] [--resource <value>] [--expires-in <value>]\n    [--idempotency-key <value>] [--time]\n\nFLAGS\n  --amount=<value>           Amount to collect in token base units, e.g. 10000\n                             (0.01 USDC).\n  --amount-usdc=<value>      Amount to collect in USDC, e.g. 0.01. Converted to\n                             base units.\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --description=<value>      Human-readable description of what the payment is\n                             for.\n  --expires-in=<value>       Seconds until the challenge expires (60-86400;\n                             default 3600).\n  --idempotency-key=<value>  Retry-safe key: charging again with the same key\n                             returns the original challenge instead of a\n                             duplicate.\n  --network=<option>         [default: base-sepolia] Chain to collect on.\n                             <options: base|base-sepolia>\n  --payer-org=<value>        Restrict who can pay to this organization id\n                             (on-net binding).\n  --resource=<value>         A URL identifying the thing being paid for.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Request an x402 payment (create a challenge)\n\n  Request an x402 payment by creating a challenge (payee side).\n\n  Give the amount as human USDC with --amount-usdc (e.g. 0.01) or as token base\n  units with --amount (e.g. 10000). The challenge JSON is printed to stdout;\n  hand it to the payer (for example in an email reply) so they can pay it. The\n  payee must have a registered payout address first.\n\nEXAMPLES\n  $ primitive payments charge --amount-usdc 0.01\n\n  $ primitive payments charge --amount 10000 --network base\n\n  $ primitive payments charge --amount-usdc 1.50 > challenge.json\n\n"),
        "payments\x00create-challenge\x00--amount\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--api-key\x00prim_test\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--body-file\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--description\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--envelope\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--expires-in\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--json\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--network\x00base\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--payer-org\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--raw-body\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--resource\x00value\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-challenge\x00--time\x00--help" => Some("Create a payment challenge\n\nUSAGE\n  $ primitive payments create-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--amount <value>]\n    [--network base|base-sepolia] [--description <value>] [--expires-in <value>]\n    [--payer-org <value>] [--resource <value>]\n\nFLAGS\n  --amount=<value>       Amount to collect, in token base units. USDC has 6\n                         decimals, so `\"10000\"` is 0.01 USDC.\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --description=<value>  Optional human-readable description of the payment.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --expires-in=<value>   Seconds until the challenge expires. Defaults to 3600.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --network=<option>     network\n                         <options: base|base-sepolia>\n  --payer-org=<value>    The org id allowed to pay this challenge (on-net\n                         binding). Optional.\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --resource=<value>     Optional URL identifying what is being paid for.\n                         Defaults to a synthetic `x402:challenge:<id>`\n                         identifier.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a payment challenge\n\n  Create an x402 payment challenge (the payee side of a payment). The\n  `pay_to` address is resolved server-side from your registered default\n  payout address for the network, never from the request. The response\n  carries the `nonce_binding` and `payment_requirements` the payer needs to\n  sign; hand the whole challenge object to the payer (for example in an\n  email reply). Amounts are in token base units (USDC has 6 decimals, so\n  `\"10000\"` is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--amount\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--api-key\x00prim_test\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--body-file\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--description\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--envelope\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--expires-in\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--from\x00user@example.com\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--idempotency-key\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--json\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--network\x00base\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--raw-body\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--resource\x00value\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--time\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00create-email-challenge\x00--to\x00user@example.com\x00--help" => Some("Create an email-native payment challenge\n\nUSAGE\n  $ primitive payments create-email-challenge [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--amount <value>] [--from <value>] [--network base|base-sepolia]\n    [--to <value>] [--description <value>] [--expires-in <value>] [--resource\n    <value>]\n\nFLAGS\n  --amount=<value>\n      Amount to collect, in token base units (unlike the `charge` CLI command,\n      which also accepts `--amount-usdc`, this field takes base units only). USDC\n      has 6 decimals, so `\"10000\"` is 0.01 USDC: multiply a human USDC amount by\n      1,000,000 (0.01 USDC -> `\"10000\"`).\n\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --description=<value>\n      Optional human-readable description of the payment.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --expires-in=<value>\n      Seconds until the challenge expires. Defaults to 300.\n\n  --from=<value>\n      Your sending address (the payee / funds receiver). Must be an address your\n      org is allowed to send from.\n\n  --idempotency-key=<value>\n      Optional idempotency key. Retrying a request with the same key returns\n      the original result instead of repeating the side effect (for\n      `createEmailChallenge`, re-sending the email).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --network=<option>\n      network\n      <options: base|base-sepolia>\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --resource=<value>\n      Optional URL identifying what is being paid for.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      The payer's email address the challenge is sent to.\n\nDESCRIPTION\n  Create an email-native payment challenge\n\n  Issue an x402 payment challenge over a real email thread (the payee\n  side). Unlike `createChallenge` (which mints a synthetic challenge id),\n  this sends the challenge as an email from `from` to `to` and binds the\n  payment to that DKIM-authenticated thread. The `pay_to` address and the\n  token asset are resolved server-side from your registered default payout\n  address for the network, never from the request. The response carries\n  the thread's `interaction_id` plus the `challenge` (the\n  `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer\n  needs to sign; the payer replies with a signed `payment` interaction\n  step. Amounts are in token base units (USDC has 6 decimals, so `\"10000\"`\n  is 0.01 USDC).\n\n\n"),
        "payments\x00get-challenge\x00--api-key\x00prim_test\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments\x00get-challenge\x00--envelope\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments\x00get-challenge\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments\x00get-challenge\x00--id\x00fn_123\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments\x00get-challenge\x00--json\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments\x00get-challenge\x00--time\x00--help" => Some("Get a payment challenge\n\nUSAGE\n  $ primitive payments get-challenge --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a payment challenge\n\n  Fetch a challenge you created, to poll its `status` and settlement\n  receipt (`settle_tx`). Scoped to the challenger org that created it.\n\n\n"),
        "payments\x00get-spend-policy\x00--api-key\x00prim_test\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments\x00get-spend-policy\x00--envelope\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments\x00get-spend-policy\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments\x00get-spend-policy\x00--json\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments\x00get-spend-policy\x00--time\x00--help" => Some("Get your spend policy\n\nUSAGE\n  $ primitive payments get-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get your spend policy\n\n  Read your org's outbound spend policy: the kill-switch, per-payment and\n  per-day caps, and the payee allowlist. Returns the defaults (no limits,\n  not paused) when no policy has been set.\n\n\n"),
        "payments\x00list-declined-payments\x00--api-key\x00prim_test\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments\x00list-declined-payments\x00--envelope\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments\x00list-declined-payments\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments\x00list-declined-payments\x00--json\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments\x00list-declined-payments\x00--time\x00--help" => Some("List declined payments\n\nUSAGE\n  $ primitive payments list-declined-payments [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List declined payments\n\n  The 50 most recent payments your org's spend policy declined, newest\n  first. Use this to see why an outbound payment was refused (a cap, the\n  payee allowlist, or the kill-switch) instead of only reading the\n  dashboard.\n\n\n"),
        "payments\x00list-payout-addresses\x00--api-key\x00prim_test\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments\x00list-payout-addresses\x00--envelope\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments\x00list-payout-addresses\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments\x00list-payout-addresses\x00--json\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments\x00list-payout-addresses\x00--time\x00--help" => Some("List payout addresses\n\nUSAGE\n  $ primitive payments list-payout-addresses [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List payout addresses\n\n  List your org's registered payout addresses, newest first.\n\n"),
        "payments\x00pay\x00--api-key\x00prim_test\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay\x00--challenge-file\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay\x00--challenge\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay\x00--json\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay\x00--private-key\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay\x00--time\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay-challenge\x00--api-key\x00prim_test\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay-challenge\x00--challenge-file\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay-challenge\x00--challenge\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay-challenge\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay-challenge\x00--json\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay-challenge\x00--private-key\x00value\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay-challenge\x00--time\x00--help" => Some("Sign and settle an x402 payment challenge\n\nUSAGE\n  $ primitive payments pay-challenge [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the challenge JSON.\n\n  --json\n      Print the raw receipt JSON instead of a human summary.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign and settle an x402 payment challenge\n\n  Pay an x402 payment challenge.\n\n  Reads the challenge object the payee gave you, derives and signs the\n  interaction-bound EIP-3009 authorization locally with your wallet key (read\n  from PRIMITIVE_X402_PRIVATE_KEY by default), and submits it for non-custodial\n  on-chain settlement. The key never leaves your machine. Provide the challenge\n  inline with --challenge, from a file with --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay --challenge-file challenge.json\n\n  $ primitive payments pay --challenge '{\"id\":\"...\",\"network\":\"base-sepolia\",...}'\n\n  cat challenge.json | primitive payments pay\n\n"),
        "payments\x00pay-email\x00--api-key\x00prim_test\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--body\x00hello\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--challenge-file\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--challenge\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--from\x00user@example.com\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--in-reply-to\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--json\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--private-key\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--settle-interval\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--settle-timeout\x00value\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--time\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--wait\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email\x00--wait-settle\x00--help" => Some("Sign an email x402 challenge and send the signed interaction.json (one step; derives the challenge from --in-reply-to)\n\nUSAGE\n  $ primitive payments pay-email --in-reply-to <value> [--api-key <value>]\n    [--private-key <value>] [--challenge <value> | --challenge-file <value>]\n    [--from <value>] [--body <value>] [--wait] [--wait-settle] [--settle-timeout\n    <value>] [--settle-interval <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body=<value>\n      Plain-text body. The signed authorization travels in the interaction.json\n      attachment; this is the human-readable accompanying note. Defaults to \"x402\n      payment authorization attached (interaction.json).\".\n\n  --challenge=<value>\n      OVERRIDE: the payee-side email challenge object (the create-email-challenge\n      response shape, NOT the inbound interaction.json attachment) as a JSON\n      string. Optional: omit it and the challenge is auto-derived from the\n      --in-reply-to email's attachment.\n\n  --challenge-file=<value>\n      OVERRIDE: path to a file containing the payee-side email challenge JSON (the\n      create-email-challenge response shape). Optional: omit it and the challenge\n      is auto-derived from the --in-reply-to email's attachment.\n\n  --from=<value>\n      Optional From header override. Defaults to the inbound's recipient (the\n      payer the challenge was addressed to).\n\n  --in-reply-to=<value>\n      (required) Id of the inbound challenge email you received. With no\n      --challenge / --challenge-file, its interaction.json attachment is\n      downloaded and reshaped into the challenge to sign. It is also fetched to\n      derive the payee recipient and the payer From. The outgoing send is not\n      threaded under the challenge; the payment associates by interaction_id.\n\n  --json\n      Print a JSON object with the signed interaction step and the send result.\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --settle-interval=<value>\n      [default: 5] With --wait-settle: seconds between settlement-email polls.\n\n  --settle-timeout=<value>\n      [default: 180] With --wait-settle: seconds to wait for the settlement email\n      before giving up (0 waits forever).\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --wait\n      Block until the receiving MTA returns a delivery outcome (SMTP 250). This\n      confirms email DELIVERY only, NOT on-chain settlement. Without --wait, the\n      call returns once Primitive has accepted the message for delivery.\n\n  --wait-settle\n      After sending, poll the inbox for the follow-up x402 settlement interaction\n      email from the payee and print the settlement receipt (including settle_tx\n      when present). Settlement is async, so this can take a while; tune with\n      --settle-timeout / --settle-interval.\n\nDESCRIPTION\n  Sign an email x402 challenge and send the signed interaction.json (one step;\n  derives the challenge from --in-reply-to)\n\n  Pay an email-native x402 challenge in one step: sign it and send the signed\n  interaction.json.\n\n  THE ONE-COMMAND PAYER FLOW. You received a payment-request email carrying an\n  `interaction.json` attachment. Run:\n\n  primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  with your wallet key in PRIMITIVE_X402_PRIVATE_KEY, and this command\n  auto-derives the\n  challenge from that inbound email's attachment, signs the interaction-bound\n  EIP-3009 authorization locally, and sends the signed `interaction.json` back\n  to the payee. The key never leaves your machine. You do NOT need --challenge\n  or\n  --challenge-file: with just --in-reply-to the challenge is derived for you.\n\n  THE CHALLENGE OBJECT (only needed for the --challenge / --challenge-file\n  override). `--challenge` expects the object the PAYEE's\n  `create-email-challenge` API returns, NOT the inbound email's\n  `interaction.json` attachment (those are different shapes). Its fields are:\n\n  {\n  \"interaction_id\": \"<uuid@domain>\",     // the email thread id\n  \"challenge\": {\n  \"payment_requirements\": { ... },     // scheme, network, payTo, asset, ...\n  \"nonce_binding\": {\n  \"interaction_id\": \"<uuid@domain>\",  // must equal the outer interaction_id\n  \"challenge_step_id\": \"<uuid>\",      // the challenge step id\n  \"challenge_nonce\": \"<64 hex chars>\"\n  },\n  \"expires_at\": \"<ISO-8601>\"\n  }\n  }\n\n  As a real payer you usually have only the inbound email's attachment, whose\n  layout differs (top-level `step`, `step_id`, `expires_at`, and a nested\n  `payload.payment_requirements` / `payload.challenge_nonce`). You do NOT\n  reshape it by hand: omit --challenge and let --in-reply-to derive it, or run\n  `payments challenge-from-email --id <inbound-id>` to print the correctly\n  shaped challenge object. Pass --challenge / --challenge-file only when you\n  have\n  the payee-side challenge from another source; it is used as-is and overrides\n  the auto-derive.\n\n  SETTLEMENT IS ASYNC. --wait only confirms the receiving MTA accepted the\n  message (SMTP 250); it does NOT confirm on-chain settlement. The `settle_tx`\n  hash arrives later in a follow-up inbound x402 settlement interaction email\n  from the payee. Use --wait-settle to poll for that settlement email after\n  sending and surface the receipt.\n\n  ADDRESSING. --in-reply-to is the inbound challenge email's id. It is fetched\n  to\n  derive the recipient (the payee, from the inbound's sender) and the From (you,\n  the payer, from the inbound's recipient). The outgoing send does NOT thread\n  under the challenge: threading would trigger the send endpoint's parent-thread\n  dedup and the payment would never settle. The interaction associates by\n  interaction_id instead. Pass --from only to override the derived payer From.\n\nEXAMPLES\n  $ primitive payments pay-email --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --wait-settle\n\n  $ primitive payments pay-email --challenge-file challenge.json --in-reply-to <inbound-email-id>\n\n  $ primitive payments pay-email --in-reply-to <inbound-email-id> --from 'Payer <payer@your-domain.example>'\n\n"),
        "payments\x00pay-email-step\x00--api-key\x00prim_test\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments\x00pay-email-step\x00--challenge-file\x00value\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments\x00pay-email-step\x00--challenge\x00value\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments\x00pay-email-step\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments\x00pay-email-step\x00--json\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments\x00pay-email-step\x00--private-key\x00value\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments\x00pay-email-step\x00--time\x00--help" => Some("Sign an email x402 challenge into a payment-step envelope\n\nUSAGE\n  $ primitive payments pay-email-step [--api-key <value>] [--private-key <value>]\n    [--challenge <value> | --challenge-file <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --challenge=<value>\n      The email challenge object as a JSON string.\n\n  --challenge-file=<value>\n      Path to a file containing the email challenge JSON.\n\n  --json\n      Print the full signed envelope object (default prints the interaction.json\n      bytes).\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Sign an email x402 challenge into a payment-step envelope\n\n  Sign an email-native x402 challenge into a payment-step interaction.json.\n\n  Reads the email challenge the payee issued (the JSON from\n  `payments create-email-challenge`), derives and signs the interaction-bound\n  EIP-3009 authorization locally with your wallet key (read from\n  PRIMITIVE_X402_PRIVATE_KEY by default), and prints the signed\n  `interaction.json`\n  payment-step envelope. The key never leaves your machine.\n\n  This command does NOT send the payment. Attach the printed envelope as the\n  `interaction.json` part of your reply to the challenge email, for example:\n  primitive payments pay-email-step --challenge-file challenge.json >\n  interaction.json\n  primitive reply --attachment interaction.json\n\n  Provide the challenge inline with --challenge, from a file with\n  --challenge-file, or piped on stdin.\n\nEXAMPLES\n  $ primitive payments pay-email-step --challenge-file challenge.json > interaction.json\n\n  $ primitive payments pay-email-step --challenge '{\"interaction_id\":\"...\",\"challenge\":{...}}'\n\n  cat challenge.json | primitive payments pay-email-step\n\n"),
        "payments\x00register-payout\x00--api-key\x00prim_test\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout\x00--issued-at\x002026-01-01T00:00:00Z\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout\x00--json\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout\x00--label\x00value\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout\x00--network\x00base\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout\x00--private-key\x00value\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout\x00--time\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout-address\x00--api-key\x00prim_test\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout-address\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout-address\x00--issued-at\x002026-01-01T00:00:00Z\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout-address\x00--json\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout-address\x00--label\x00value\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout-address\x00--network\x00base\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout-address\x00--private-key\x00value\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00register-payout-address\x00--time\x00--help" => Some("Register a payout address for receiving x402 payments\n\nUSAGE\n  $ primitive payments register-payout-address [--api-key <value>] [--private-key <value>]\n    [--network base|base-sepolia] [--label <value>] [--issued-at <value>]\n    [--json] [--time]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --issued-at=<value>\n      ISO-8601 timestamp embedded in the signed message. Defaults to now. Must be\n      within ~10 minutes of server time.\n\n  --json\n      Print the raw payout-address JSON instead of a human summary.\n\n  --label=<value>\n      Optional human-readable label for the address.\n\n  --network=<option>\n      [default: base-sepolia] Chain the address receives on.\n      <options: base|base-sepolia>\n\n  --private-key=<value>\n      [env: PRIMITIVE_X402_PRIVATE_KEY] Hex private key (0x-prefixed) of the\n      wallet that signs. Prefer the PRIMITIVE_X402_PRIVATE_KEY environment\n      variable so the key is not exposed in shell history or the process list. The\n      key is used locally to sign and never sent to Primitive.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Register a payout address for receiving x402 payments\n\n  Register the default payout address your org receives x402 payments at.\n\n  Proves control of the address by signing an org-bound message locally with\n  your wallet key (read from PRIMITIVE_X402_PRIVATE_KEY by default), then\n  registers the\n  recovered address as the default for the chosen network. Registering again\n  replaces the existing default. Run this before `payments create-challenge`.\n\nEXAMPLES\n  $ primitive payments register-payout-address\n\n  $ primitive payments register-payout-address --network base --label treasury\n\n"),
        "payments\x00update-spend-policy\x00--api-key\x00prim_test\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--body-file\x00value\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--envelope\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--json\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--max-per-day\x00value\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--max-per-payment\x00value\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--no-paused\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--paused\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--raw-body\x00value\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "payments\x00update-spend-policy\x00--time\x00--help" => Some("Update your spend policy\n\nUSAGE\n  $ primitive payments update-spend-policy [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--max-per-day\n    <value>] [--max-per-payment <value>] [--paused]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --max-per-day=<value>      max_per_day\n  --max-per-payment=<value>  max_per_payment\n  --[no-]paused              paused\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update your spend policy\n\n  Update your org's spend policy. Applied as a merge: only the fields you\n  include change, and omitted fields keep their current value, so a partial\n  update can't silently reset the kill-switch. Send an explicit `null` to\n  clear a cap. Caps are in token base units.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowlist  array?\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries" => Some("Create and manage agent registries, publish and resolve agents by handle, and moderate publication requests\n\nUSAGE\n  $ primitive registries COMMAND\n\nCOMMANDS\n  registries agent                    Get an agent's public profile by address\n  registries agents                   List agents in a registry\n  registries create                   Create a registry\n  registries create-registry          Create a registry\n  registries decide                   Approve or reject a publication request\n  registries decide-registry-request  Approve or reject a publication request\n  registries define                   Define an agent identity\n  registries define-agent             Define an agent identity\n  registries delete-registry          Delete a registry you own\n  registries get                      Get a public registry's metadata\n  registries get-agent                Get an agent's public profile by address\n  registries get-registry             Get a public registry's metadata\n  registries list                     List the registries you own\n  registries list-registries          List the registries you own\n  registries list-registry-agents     List agents in a registry\n  registries list-registry-requests   List pending publication requests\n  registries publish                  Publish an agent into a registry\n  registries publish-agent            Publish an agent into a registry\n  registries requests                 List pending publication requests\n  registries resolve                  Resolve a registry handle to its agent\n  registries resolve-registry-handle  Resolve a registry handle to its agent\n  registries unpublish                Unpublish an agent from a registry\n  registries unpublish-agent          Unpublish an agent from a registry\n  registries update                   Update a registry you own\n  registries update-registry          Update a registry you own\n\n"),
        "registries\x00--help" => Some("Create and manage agent registries, publish and resolve agents by handle, and moderate publication requests\n\nUSAGE\n  $ primitive registries COMMAND\n\nCOMMANDS\n  registries agent                    Get an agent's public profile by address\n  registries agents                   List agents in a registry\n  registries create                   Create a registry\n  registries create-registry          Create a registry\n  registries decide                   Approve or reject a publication request\n  registries decide-registry-request  Approve or reject a publication request\n  registries define                   Define an agent identity\n  registries define-agent             Define an agent identity\n  registries delete-registry          Delete a registry you own\n  registries get                      Get a public registry's metadata\n  registries get-agent                Get an agent's public profile by address\n  registries get-registry             Get a public registry's metadata\n  registries list                     List the registries you own\n  registries list-registries          List the registries you own\n  registries list-registry-agents     List agents in a registry\n  registries list-registry-requests   List pending publication requests\n  registries publish                  Publish an agent into a registry\n  registries publish-agent            Publish an agent into a registry\n  registries requests                 List pending publication requests\n  registries resolve                  Resolve a registry handle to its agent\n  registries resolve-registry-handle  Resolve a registry handle to its agent\n  registries unpublish                Unpublish an agent from a registry\n  registries unpublish-agent          Unpublish an agent from a registry\n  registries update                   Update a registry you own\n  registries update-registry          Update a registry you own\n\n"),
        "registries\x00-h" => Some("Create and manage agent registries, publish and resolve agents by handle, and moderate publication requests\n\nUSAGE\n  $ primitive registries COMMAND\n\nCOMMANDS\n  registries agent                    Get an agent's public profile by address\n  registries agents                   List agents in a registry\n  registries create                   Create a registry\n  registries create-registry          Create a registry\n  registries decide                   Approve or reject a publication request\n  registries decide-registry-request  Approve or reject a publication request\n  registries define                   Define an agent identity\n  registries define-agent             Define an agent identity\n  registries delete-registry          Delete a registry you own\n  registries get                      Get a public registry's metadata\n  registries get-agent                Get an agent's public profile by address\n  registries get-registry             Get a public registry's metadata\n  registries list                     List the registries you own\n  registries list-registries          List the registries you own\n  registries list-registry-agents     List agents in a registry\n  registries list-registry-requests   List pending publication requests\n  registries publish                  Publish an agent into a registry\n  registries publish-agent            Publish an agent into a registry\n  registries requests                 List pending publication requests\n  registries resolve                  Resolve a registry handle to its agent\n  registries resolve-registry-handle  Resolve a registry handle to its agent\n  registries unpublish                Unpublish an agent from a registry\n  registries unpublish-agent          Unpublish an agent from a registry\n  registries update                   Update a registry you own\n  registries update-registry          Update a registry you own\n\n"),
        "registries:agent\x00--address\x00value\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:agent\x00--api-key\x00prim_test\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:agent\x00--envelope\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:agent\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:agent\x00--json\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:agent\x00--time\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:agents\x00--api-key\x00prim_test\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:agents\x00--cursor\x00value\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:agents\x00--envelope\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:agents\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:agents\x00--json\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:agents\x00--limit\x001\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:agents\x00--slug\x00value\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:agents\x00--time\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:create\x00--api-key\x00prim_test\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--body-file\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--description\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--envelope\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--is-public\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--json\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--name\x00help-name\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--no-is-public\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--publish-policy\x00owner_only\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--raw-body\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--slug\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create\x00--time\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--api-key\x00prim_test\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--body-file\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--description\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--envelope\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--is-public\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--json\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--name\x00help-name\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--no-is-public\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--publish-policy\x00owner_only\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--raw-body\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--slug\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:create-registry\x00--time\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries:decide\x00--api-key\x00prim_test\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide\x00--body-file\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide\x00--decision\x00approved\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide\x00--envelope\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide\x00--id\x00fn_123\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide\x00--json\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide\x00--raw-body\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide\x00--slug\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide\x00--time\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--api-key\x00prim_test\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--body-file\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--decision\x00approved\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--envelope\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--id\x00fn_123\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--json\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--raw-body\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--slug\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:decide-registry-request\x00--time\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries:define\x00--address\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--api-key\x00prim_test\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--body-file\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--description\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--display-name\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--endpoint-id\x00fn_123\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--envelope\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--json\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--raw-body\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--time\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define\x00--title\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--address\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--api-key\x00prim_test\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--body-file\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--description\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--display-name\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--endpoint-id\x00fn_123\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--envelope\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--json\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--raw-body\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--time\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:define-agent\x00--title\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:delete-registry\x00--api-key\x00prim_test\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries:delete-registry\x00--envelope\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries:delete-registry\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries:delete-registry\x00--json\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries:delete-registry\x00--slug\x00value\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries:delete-registry\x00--time\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries:get\x00--api-key\x00prim_test\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get\x00--envelope\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get\x00--json\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get\x00--slug\x00value\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get\x00--time\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get-agent\x00--address\x00value\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:get-agent\x00--api-key\x00prim_test\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:get-agent\x00--envelope\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:get-agent\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:get-agent\x00--json\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:get-agent\x00--time\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries:get-registry\x00--api-key\x00prim_test\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get-registry\x00--envelope\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get-registry\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get-registry\x00--json\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get-registry\x00--slug\x00value\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:get-registry\x00--time\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries:list\x00--api-key\x00prim_test\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list\x00--envelope\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list\x00--json\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list\x00--time\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list-registries\x00--api-key\x00prim_test\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list-registries\x00--envelope\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list-registries\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list-registries\x00--json\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list-registries\x00--time\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries:list-registry-agents\x00--api-key\x00prim_test\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:list-registry-agents\x00--cursor\x00value\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:list-registry-agents\x00--envelope\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:list-registry-agents\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:list-registry-agents\x00--json\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:list-registry-agents\x00--limit\x001\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:list-registry-agents\x00--slug\x00value\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:list-registry-agents\x00--time\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries:list-registry-requests\x00--api-key\x00prim_test\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:list-registry-requests\x00--envelope\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:list-registry-requests\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:list-registry-requests\x00--json\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:list-registry-requests\x00--limit\x001\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:list-registry-requests\x00--slug\x00value\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:list-registry-requests\x00--time\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:publish\x00--address\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--api-key\x00prim_test\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--body-file\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--description\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--display-name\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--endpoint-id\x00fn_123\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--envelope\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--handle\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--json\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--raw-body\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--slug\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--time\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish\x00--title\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--address\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--api-key\x00prim_test\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--body-file\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--description\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--display-name\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--endpoint-id\x00fn_123\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--envelope\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--handle\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--json\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--raw-body\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--slug\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--time\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:publish-agent\x00--title\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries:requests\x00--api-key\x00prim_test\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:requests\x00--envelope\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:requests\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:requests\x00--json\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:requests\x00--limit\x001\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:requests\x00--slug\x00value\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:requests\x00--time\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries:resolve\x00--api-key\x00prim_test\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve\x00--envelope\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve\x00--handle\x00value\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve\x00--json\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve\x00--slug\x00value\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve\x00--time\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve-registry-handle\x00--api-key\x00prim_test\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve-registry-handle\x00--envelope\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve-registry-handle\x00--handle\x00value\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve-registry-handle\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve-registry-handle\x00--json\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve-registry-handle\x00--slug\x00value\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:resolve-registry-handle\x00--time\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish\x00--api-key\x00prim_test\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish\x00--envelope\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish\x00--handle\x00value\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish\x00--json\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish\x00--slug\x00value\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish\x00--time\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish-agent\x00--api-key\x00prim_test\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish-agent\x00--envelope\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish-agent\x00--handle\x00value\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish-agent\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish-agent\x00--json\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish-agent\x00--slug\x00value\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:unpublish-agent\x00--time\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries:update\x00--api-key\x00prim_test\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--body-file\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--description\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--envelope\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--is-public\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--json\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--name\x00help-name\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--no-is-public\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--publish-policy\x00owner_only\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--raw-body\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--slug\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update\x00--time\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--api-key\x00prim_test\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--body-file\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--description\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--envelope\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--is-public\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--json\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--name\x00help-name\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--no-is-public\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--publish-policy\x00owner_only\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--raw-body\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--slug\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries:update-registry\x00--time\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00agent\x00--address\x00value\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00agent\x00--api-key\x00prim_test\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00agent\x00--envelope\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00agent\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00agent\x00--json\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00agent\x00--time\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00agents\x00--api-key\x00prim_test\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00agents\x00--cursor\x00value\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00agents\x00--envelope\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00agents\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00agents\x00--json\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00agents\x00--limit\x001\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00agents\x00--slug\x00value\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00agents\x00--time\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00create\x00--api-key\x00prim_test\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--body-file\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--description\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--envelope\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--is-public\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--json\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--name\x00help-name\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--no-is-public\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--publish-policy\x00owner_only\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--raw-body\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--slug\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create\x00--time\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--api-key\x00prim_test\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--body-file\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--description\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--envelope\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--is-public\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--json\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--name\x00help-name\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--no-is-public\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--publish-policy\x00owner_only\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--raw-body\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--slug\x00value\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00create-registry\x00--time\x00--help" => Some("Create a registry\n\nUSAGE\n  $ primitive registries create-registry [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--name <value>]\n    [--slug <value>] [--description <value>] [--is-public] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             Lowercase slug, unique across registries.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Create a registry\n\n  POST /registries\n\n"),
        "registries\x00decide\x00--api-key\x00prim_test\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide\x00--body-file\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide\x00--decision\x00approved\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide\x00--envelope\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide\x00--id\x00fn_123\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide\x00--json\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide\x00--raw-body\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide\x00--slug\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide\x00--time\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--api-key\x00prim_test\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--body-file\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--decision\x00approved\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--envelope\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--id\x00fn_123\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--json\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--raw-body\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--slug\x00value\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00decide-registry-request\x00--time\x00--help" => Some("Approve or reject a publication request\n\nUSAGE\n  $ primitive registries decide-registry-request --slug <value> --id <value> [--api-key <value>]\n    [--time] [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--decision approved|rejected]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --decision=<option>  decision\n                       <options: approved|rejected>\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --slug=<value>       (required) The registry slug.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Approve or reject a publication request\n\n  POST /registries/{slug}/requests/{id}\n\n"),
        "registries\x00define\x00--address\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--api-key\x00prim_test\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--body-file\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--description\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--display-name\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--endpoint-id\x00fn_123\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--envelope\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--json\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--raw-body\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--time\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define\x00--title\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--address\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--api-key\x00prim_test\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--body-file\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--description\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--display-name\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--endpoint-id\x00fn_123\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--envelope\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--json\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--raw-body\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--time\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00define-agent\x00--title\x00value\x00--help" => Some("Define an agent identity\n\nUSAGE\n  $ primitive registries define-agent [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--address <value>]\n    [--display-name <value>] [--description <value>] [--endpoint-id <value>]\n    [--title <value>]\n\nFLAGS\n  --address=<value>       The agent's globally unique email address; mail to it\n                          must route to an endpoint the account controls.\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  display_name\n  --endpoint-id=<value>   Optional. The endpoint the agent runs on. Omit it to\n                          resolve the endpoint from the address's routing\n                          automatically; supply it to pin a specific endpoint,\n                          which is then validated against the address's route.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Define an agent identity\n\n  POST /agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00delete-registry\x00--api-key\x00prim_test\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries\x00delete-registry\x00--envelope\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries\x00delete-registry\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries\x00delete-registry\x00--json\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries\x00delete-registry\x00--slug\x00value\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries\x00delete-registry\x00--time\x00--help" => Some("Delete a registry you own\n\nUSAGE\n  $ primitive registries delete-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a registry you own\n\n  Removes the registry from discovery and frees its slug for re-creation.\n\n"),
        "registries\x00get\x00--api-key\x00prim_test\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get\x00--envelope\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get\x00--json\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get\x00--slug\x00value\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get\x00--time\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get-agent\x00--address\x00value\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00get-agent\x00--api-key\x00prim_test\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00get-agent\x00--envelope\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00get-agent\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00get-agent\x00--json\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00get-agent\x00--time\x00--help" => Some("Get an agent's public profile by address\n\nUSAGE\n  $ primitive registries get-agent --address <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --address=<value>  (required) The agent's email address (URL-encoded).\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get an agent's public profile by address\n\n  GET /agents/{address}\n\n"),
        "registries\x00get-registry\x00--api-key\x00prim_test\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get-registry\x00--envelope\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get-registry\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get-registry\x00--json\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get-registry\x00--slug\x00value\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00get-registry\x00--time\x00--help" => Some("Get a public registry's metadata\n\nUSAGE\n  $ primitive registries get-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a public registry's metadata\n\n  GET /registries/{slug}\n\n"),
        "registries\x00list\x00--api-key\x00prim_test\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list\x00--envelope\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list\x00--json\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list\x00--time\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list-registries\x00--api-key\x00prim_test\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list-registries\x00--envelope\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list-registries\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list-registries\x00--json\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list-registries\x00--time\x00--help" => Some("List the registries you own\n\nUSAGE\n  $ primitive registries list-registries [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List the registries you own\n\n  GET /registries\n\n"),
        "registries\x00list-registry-agents\x00--api-key\x00prim_test\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00list-registry-agents\x00--cursor\x00value\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00list-registry-agents\x00--envelope\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00list-registry-agents\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00list-registry-agents\x00--json\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00list-registry-agents\x00--limit\x001\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00list-registry-agents\x00--slug\x00value\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00list-registry-agents\x00--time\x00--help" => Some("List agents in a registry\n\nUSAGE\n  $ primitive registries list-registry-agents --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>] [--cursor <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   The address of the last agent from the previous page.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List agents in a registry\n\n  GET /registries/{slug}/agents\n\n"),
        "registries\x00list-registry-requests\x00--api-key\x00prim_test\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00list-registry-requests\x00--envelope\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00list-registry-requests\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00list-registry-requests\x00--json\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00list-registry-requests\x00--limit\x001\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00list-registry-requests\x00--slug\x00value\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00list-registry-requests\x00--time\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries list-registry-requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00publish\x00--address\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--api-key\x00prim_test\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--body-file\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--description\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--display-name\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--endpoint-id\x00fn_123\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--envelope\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--handle\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--json\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--raw-body\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--slug\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--time\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish\x00--title\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--address\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--api-key\x00prim_test\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--body-file\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--description\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--display-name\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--endpoint-id\x00fn_123\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--envelope\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--handle\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--json\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--raw-body\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--slug\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--time\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00publish-agent\x00--title\x00value\x00--help" => Some("Publish an agent into a registry\n\nUSAGE\n  $ primitive registries publish-agent --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--handle <value>] [--description <value>] [--display-name <value>]\n    [--endpoint-id <value>] [--title <value>]\n\nFLAGS\n  --address=<value>       address\n  --api-key=<value>       [env: PRIMITIVE_API_KEY] Primitive API key override\n                          (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                          credentials)\n  --body-file=<value>     Path to a JSON file used as the request body. Same\n                          role as --raw-body for callers passing a saved\n                          payload.\n  --description=<value>   description\n  --display-name=<value>  Present to define the agent identity before publishing\n                          (define + publish in one call).\n  --endpoint-id=<value>   Optional, only used when defining. Omit to resolve the\n                          endpoint from the address's routing.\n  --envelope              Print the full response envelope, including pagination\n                          metadata such as meta.cursor. Defaults to printing\n                          only the data payload for backward compatibility.\n  --handle=<value>        The registry-scoped name to list the agent under.\n  --json                  Accepted for consistency with task-focused commands.\n                          Generated API commands already print JSON by default.\n  --raw-body=<value>      Full request body as raw JSON. Escape hatch for nested\n                          or complex fields (e.g. arrays); prefer per-field\n                          flags (e.g. --to, --from, --body-text) when available.\n  --slug=<value>          (required) The registry slug.\n  --time                  Print the wall-clock duration of this command to\n                          stderr after it completes (e.g. `[time: 1.34s]`).\n                          Useful for measuring `--wait` send latency, comparing\n                          CLI overhead, or capturing timing in scripts.\n  --title=<value>         title\n\nDESCRIPTION\n  Publish an agent into a registry\n\n  POST /registries/{slug}/agents\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  tags  array<string>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "registries\x00requests\x00--api-key\x00prim_test\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00requests\x00--envelope\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00requests\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00requests\x00--json\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00requests\x00--limit\x001\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00requests\x00--slug\x00value\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00requests\x00--time\x00--help" => Some("List pending publication requests\n\nUSAGE\n  $ primitive registries requests --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    Maximum number of items to return (1-200).\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List pending publication requests\n\n  GET /registries/{slug}/requests\n\n"),
        "registries\x00resolve\x00--api-key\x00prim_test\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve\x00--envelope\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve\x00--handle\x00value\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve\x00--json\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve\x00--slug\x00value\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve\x00--time\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve-registry-handle\x00--api-key\x00prim_test\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve-registry-handle\x00--envelope\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve-registry-handle\x00--handle\x00value\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve-registry-handle\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve-registry-handle\x00--json\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve-registry-handle\x00--slug\x00value\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00resolve-registry-handle\x00--time\x00--help" => Some("Resolve a registry handle to its agent\n\nUSAGE\n  $ primitive registries resolve-registry-handle --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Resolve a registry handle to its agent\n\n  GET /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish\x00--api-key\x00prim_test\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish\x00--envelope\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish\x00--handle\x00value\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish\x00--json\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish\x00--slug\x00value\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish\x00--time\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish-agent\x00--api-key\x00prim_test\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish-agent\x00--envelope\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish-agent\x00--handle\x00value\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish-agent\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish-agent\x00--json\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish-agent\x00--slug\x00value\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00unpublish-agent\x00--time\x00--help" => Some("Unpublish an agent from a registry\n\nUSAGE\n  $ primitive registries unpublish-agent --slug <value> --handle <value> [--api-key\n    <value>] [--time] [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --handle=<value>   (required) The registry-scoped handle the agent is\n                     published under.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --slug=<value>     (required) The registry slug.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Unpublish an agent from a registry\n\n  DELETE /registries/{slug}/agents/{handle}\n\n"),
        "registries\x00update\x00--api-key\x00prim_test\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--body-file\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--description\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--envelope\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--is-public\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--json\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--name\x00help-name\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--no-is-public\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--publish-policy\x00owner_only\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--raw-body\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--slug\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update\x00--time\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--api-key\x00prim_test\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--body-file\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--description\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--envelope\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--is-public\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--json\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--name\x00help-name\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--no-is-public\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--publish-policy\x00owner_only\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--raw-body\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--slug\x00value\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "registries\x00update-registry\x00--time\x00--help" => Some("Update a registry you own\n\nUSAGE\n  $ primitive registries update-registry --slug <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--description <value>] [--is-public] [--name <value>] [--publish-policy\n    owner_only|request|open]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body-file=<value>        Path to a JSON file used as the request body. Same\n                             role as --raw-body for callers passing a saved\n                             payload.\n  --description=<value>      description\n  --envelope                 Print the full response envelope, including\n                             pagination metadata such as meta.cursor. Defaults\n                             to printing only the data payload for backward\n                             compatibility.\n  --[no-]is-public           is_public\n  --json                     Accepted for consistency with task-focused\n                             commands. Generated API commands already print JSON\n                             by default.\n  --name=<value>             name\n  --publish-policy=<option>  Who may publish into a registry. owner_only: only\n                             the registry owner. request: anyone may request and\n                             the owner approves. open: anyone may publish and it\n                             lists immediately (no approval step).\n                             <options: owner_only|request|open>\n  --raw-body=<value>         Full request body as raw JSON. Escape hatch for\n                             nested or complex fields (e.g. arrays); prefer\n                             per-field flags (e.g. --to, --from, --body-text)\n                             when available.\n  --slug=<value>             (required) The registry slug\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n\nDESCRIPTION\n  Update a registry you own\n\n  PATCH /registries/{slug}\n\n"),
        "reply\x00--api-key\x00prim_test\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--attachment\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--body-file\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--body-stdin\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--body\x00hello\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--from\x00user@example.com\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--html-file\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--html-stdin\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--html\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--id\x00fn_123\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--time\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00--wait\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "reply\x00-a\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive reply --id <value> [--api-key <value>] [--body\n    <value>] [--body-file <value>] [--body-stdin] [--html <value>] [--html-file\n    <value>] [--html-stdin] [--from <value>] [-a <value>...] [--wait] [--time]\n\nFLAGS\n  -a, --attachment=<value>...  Attach a file to the reply. Repeat --attachment\n                               to attach multiple files.\n      --api-key=<value>        [env: PRIMITIVE_API_KEY] Primitive API key\n                               override (defaults to PRIMITIVE_API_KEY or saved\n                               OAuth login credentials)\n      --body=<value>           Plain-text reply body. Either --body or --html\n                               (or both) is required.\n      --body-file=<value>      Read the plain-text reply body from a UTF-8 file;\n                               this does not attach the file. Use --attachment\n                               for attachments. Mutually exclusive with --body\n                               and --body-stdin.\n      --body-stdin             Read the plain-text reply body from stdin.\n                               Mutually exclusive with --body and --body-file.\n                               Stdin can only be consumed once.\n      --from=<value>           Optional From header override. Defaults to the\n                               inbound recipient.\n      --html=<value>           HTML reply body. Either --body or --html (or\n                               both) is required.\n      --html-file=<value>      Read the HTML reply body from a UTF-8 file; this\n                               does not attach the file. Use --attachment for\n                               attachments. Mutually exclusive with --html and\n                               --html-stdin.\n      --html-stdin             Read the HTML reply body from stdin. Mutually\n                               exclusive with --html and --html-file. Stdin can\n                               only be consumed once.\n      --id=<value>             (required) Inbound email id to reply to.\n      --time                   Print the wall-clock duration of this command to\n                               stderr after it completes (e.g. `[time: 1.34s]`).\n                               Useful for measuring `--wait` send latency,\n                               comparing CLI overhead, or capturing timing in\n                               scripts.\n      --wait                   Block until the receiving MTA returns an outcome.\n                               Without --wait, the call returns once Primitive\n                               has accepted the reply for delivery.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Reply to an inbound email.\n\n  The API derives recipients, the Re: subject, and threading headers from the\n  inbound email id. Use `primitive send --in-reply-to <message-id>` only when\n  you need to thread against a raw Message-Id instead of an inbound email stored\n  by Primitive.\n\nEXAMPLES\n  $ primitive reply --id <inbound-email-id> --body 'Thanks, got it.'\n\n  $ primitive reply --id <inbound-email-id> --body-file ./reply.txt\n\n  $ primitive reply --id <inbound-email-id> --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive reply --id <inbound-email-id> --html '<p>Thanks, got it.</p>' --wait\n\n  $ primitive reply --id <inbound-email-id> --from 'Support <support@example.com>' --body 'Thanks!'\n\n"),
        "routes" => Some("Manage recipient routes. Bind an address pattern to an endpoint or function: `primitive routes add alice@d --function <id>`, `primitive routes list`, `primitive routes test <recipient>`, `primitive routes remove <id>`.\n\nUSAGE\n  $ primitive routes COMMAND\n\nCOMMANDS\n  routes add             Add a recipient route\n  routes create-route    Create a recipient route\n  routes delete-route    Delete a recipient route\n  routes list            List recipient routes\n  routes list-routes     List recipient routes\n  routes remove          Remove a recipient route\n  routes reorder         Reorder recipient routes\n  routes reorder-routes  Reorder recipient routes\n  routes simulate-route  Simulate routing for a recipient\n  routes test            Simulate routing for a recipient\n  routes update          Update a recipient route\n  routes update-route    Update a recipient route\n\n"),
        "routes\x00--help" => Some("Manage recipient routes. Bind an address pattern to an endpoint or function: `primitive routes add alice@d --function <id>`, `primitive routes list`, `primitive routes test <recipient>`, `primitive routes remove <id>`.\n\nUSAGE\n  $ primitive routes COMMAND\n\nCOMMANDS\n  routes add             Add a recipient route\n  routes create-route    Create a recipient route\n  routes delete-route    Delete a recipient route\n  routes list            List recipient routes\n  routes list-routes     List recipient routes\n  routes remove          Remove a recipient route\n  routes reorder         Reorder recipient routes\n  routes reorder-routes  Reorder recipient routes\n  routes simulate-route  Simulate routing for a recipient\n  routes test            Simulate routing for a recipient\n  routes update          Update a recipient route\n  routes update-route    Update a recipient route\n\n"),
        "routes\x00-h" => Some("Manage recipient routes. Bind an address pattern to an endpoint or function: `primitive routes add alice@d --function <id>`, `primitive routes list`, `primitive routes test <recipient>`, `primitive routes remove <id>`.\n\nUSAGE\n  $ primitive routes COMMAND\n\nCOMMANDS\n  routes add             Add a recipient route\n  routes create-route    Create a recipient route\n  routes delete-route    Delete a recipient route\n  routes list            List recipient routes\n  routes list-routes     List recipient routes\n  routes remove          Remove a recipient route\n  routes reorder         Reorder recipient routes\n  routes reorder-routes  Reorder recipient routes\n  routes simulate-route  Simulate routing for a recipient\n  routes test            Simulate routing for a recipient\n  routes update          Update a recipient route\n  routes update-route    Update a recipient route\n\n"),
        "routes:add\x00--api-key\x00prim_test\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:add\x00--disabled\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:add\x00--domain\x00value\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:add\x00--endpoint\x00value\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:add\x00--function\x00value\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:add\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:add\x00--match\x00exact\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:add\x00--priority\x001\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:add\x00--time\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:add\x00b@example.com\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes:create-route\x00--api-key\x00prim_test\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--body-file\x00value\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--domain-id\x00fn_123\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--enabled\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--endpoint-id\x00fn_123\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--envelope\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--function-id\x00fn_123\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--json\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--match-type\x00exact\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--no-enabled\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--pattern\x00b@example.com\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--priority\x001\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--raw-body\x00value\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:create-route\x00--time\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes:delete-route\x00--api-key\x00prim_test\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes:delete-route\x00--envelope\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes:delete-route\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes:delete-route\x00--id\x00fn_123\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes:delete-route\x00--json\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes:delete-route\x00--time\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes:list\x00--api-key\x00prim_test\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "routes:list\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "routes:list\x00--time\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "routes:list-routes\x00--api-key\x00prim_test\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes:list-routes\x00--envelope\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes:list-routes\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes:list-routes\x00--json\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes:list-routes\x00--time\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes:remove\x00--api-key\x00prim_test\x00--help" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "routes:remove\x00--help" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "routes:remove\x00--time\x00--help" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "routes:remove\x00fn_123\x00--help" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "routes:reorder\x00--api-key\x00prim_test\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "routes:reorder\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "routes:reorder\x00--set\x00value\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "routes:reorder\x00--time\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "routes:reorder-routes\x00--api-key\x00prim_test\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes:reorder-routes\x00--body-file\x00value\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes:reorder-routes\x00--envelope\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes:reorder-routes\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes:reorder-routes\x00--json\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes:reorder-routes\x00--raw-body\x00value\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes:reorder-routes\x00--time\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes:simulate-route\x00--api-key\x00prim_test\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes:simulate-route\x00--body-file\x00value\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes:simulate-route\x00--envelope\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes:simulate-route\x00--event-type\x00value\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes:simulate-route\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes:simulate-route\x00--json\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes:simulate-route\x00--raw-body\x00value\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes:simulate-route\x00--recipient\x00user@example.com\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes:simulate-route\x00--time\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes:test\x00--api-key\x00prim_test\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes:test\x00--event-type\x00value\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes:test\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes:test\x00--time\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes:test\x00user@example.com\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes:update\x00--api-key\x00prim_test\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update\x00--disable\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update\x00--domain\x00value\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update\x00--enable\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update\x00--endpoint\x00value\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update\x00--match\x00exact\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update\x00--pattern\x00b@example.com\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update\x00--priority\x001\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update\x00--time\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes:update-route\x00--api-key\x00prim_test\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--body-file\x00value\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--domain-id\x00fn_123\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--enabled\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--endpoint-id\x00fn_123\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--envelope\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--id\x00fn_123\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--json\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--match-type\x00exact\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--no-enabled\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--pattern\x00b@example.com\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--priority\x001\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--raw-body\x00value\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update-route\x00--time\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes:update\x00fn_123\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00add\x00--api-key\x00prim_test\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00add\x00--disabled\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00add\x00--domain\x00value\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00add\x00--endpoint\x00value\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00add\x00--function\x00value\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00add\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00add\x00--match\x00exact\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00add\x00--priority\x001\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00add\x00--time\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00add\x00b@example.com\x00--help" => Some("Add a recipient route\n\nUSAGE\n  $ primitive routes add PATTERN [--api-key <value>] [--time]\n    [--function <value> | --endpoint <value>] [--match exact|wildcard|regex]\n    [--domain <value>] [--priority <value>] [--disabled]\n\nARGUMENTS\n  PATTERN  Recipient address or wildcard pattern (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disabled          Create the route disabled.\n  --domain=<value>    Scope the route to this domain id (defaults to the\n                      pattern's domain).\n  --endpoint=<value>  Existing endpoint id to route to. Mutually exclusive with\n                      --function.\n  --function=<value>  Function id to route this address to (its route-target\n                      endpoint is minted if needed). Mutually exclusive with\n                      --endpoint.\n  --match=<option>    [default: exact] Match type for the pattern. regex\n                      requires the Power plan and may be rejected if not\n                      enabled.\n                      <options: exact|wildcard|regex>\n  --priority=<value>  Evaluation order within a scope; lower is checked first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Add a recipient route\n\n  Create a recipient route binding an address pattern to a destination.\n\n  Provide exactly one of --function (route to a function; its route-target\n  endpoint is created in the same transaction) or --endpoint (an existing\n  endpoint). The match type defaults to exact; use --match wildcard for\n  patterns like 'support+*@acme.com'.\n\nEXAMPLES\n  $ primitive routes add alice@acme.com --function <fn-id>\n\n  $ primitive routes add 'support+*@acme.com' --match wildcard --function <fn-id>\n\n  $ primitive routes add billing@acme.com --endpoint <endpoint-id> --priority 10\n\n"),
        "routes\x00create-route\x00--api-key\x00prim_test\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--body-file\x00value\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--domain-id\x00fn_123\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--enabled\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--endpoint-id\x00fn_123\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--envelope\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--function-id\x00fn_123\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--json\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--match-type\x00exact\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--no-enabled\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--pattern\x00b@example.com\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--priority\x001\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--raw-body\x00value\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00create-route\x00--time\x00--help" => Some("Create a recipient route\n\nUSAGE\n  $ primitive routes create-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--domain-id <value>] [--enabled]\n    [--endpoint-id <value>] [--function-id <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    Scope the route to a domain; defaults to the pattern's\n                         domain.\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  An existing endpoint to route to. Mutually exclusive\n                         with function_id.\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --function-id=<value>  Route to this function, minting its route-target\n                         endpoint if needed. Mutually exclusive with\n                         endpoint_id.\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a recipient route\n\n  Binds a recipient pattern to a destination. Provide exactly one of\n  `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,\n  a dedicated route-target endpoint is minted for that function in the same\n  transaction, enabling per-address function routing (e.g.\n  `alice@acme.com -> functionA`).\n\n\n"),
        "routes\x00delete-route\x00--api-key\x00prim_test\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes\x00delete-route\x00--envelope\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes\x00delete-route\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes\x00delete-route\x00--id\x00fn_123\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes\x00delete-route\x00--json\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes\x00delete-route\x00--time\x00--help" => Some("Delete a recipient route\n\nUSAGE\n  $ primitive routes delete-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a recipient route\n\n  DELETE /routes/{id}\n\n"),
        "routes\x00list\x00--api-key\x00prim_test\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "routes\x00list\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "routes\x00list\x00--time\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  List the org's recipient routes in evaluation order.\n\nEXAMPLES\n  $ primitive routes list\n\n"),
        "routes\x00list-routes\x00--api-key\x00prim_test\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes\x00list-routes\x00--envelope\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes\x00list-routes\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes\x00list-routes\x00--json\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes\x00list-routes\x00--time\x00--help" => Some("List recipient routes\n\nUSAGE\n  $ primitive routes list-routes [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recipient routes\n\n  Returns the org's recipient routing rules in evaluation order. Each rule\n  binds a recipient address pattern to one endpoint; inbound mail resolves\n  to a single destination at delivery time.\n\n\n"),
        "routes\x00remove\x00--api-key\x00prim_test\x00--help" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "routes\x00remove\x00--help" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "routes\x00remove\x00--time\x00--help" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "routes\x00remove\x00fn_123\x00--help" => Some("Remove a recipient route\n\nUSAGE\n  $ primitive routes remove ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Route id (UUID) to delete.\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Remove a recipient route\n\n  Delete a recipient route by id. Mail matching its pattern then falls back to\n  the domain or organization default.\n\nEXAMPLES\n  $ primitive routes remove <route-id>\n\n"),
        "routes\x00reorder\x00--api-key\x00prim_test\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "routes\x00reorder\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "routes\x00reorder\x00--set\x00value\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "routes\x00reorder\x00--time\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder --set <value>... [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --set=<value>...   (required) A route and its new priority as\n                     <route-id>=<priority>. Repeatable.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Set the priority of one or more recipient routes in a single call.\n\n  Priority controls evaluation order within a scope; lower is checked first.\n  Pass --set <route-id>=<priority> once per route you want to move.\n\nEXAMPLES\n  $ primitive routes reorder --set <route-id>=10\n\n  $ primitive routes reorder --set <id-a>=10 --set <id-b>=20\n\n"),
        "routes\x00reorder-routes\x00--api-key\x00prim_test\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes\x00reorder-routes\x00--body-file\x00value\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes\x00reorder-routes\x00--envelope\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes\x00reorder-routes\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes\x00reorder-routes\x00--json\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes\x00reorder-routes\x00--raw-body\x00value\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes\x00reorder-routes\x00--time\x00--help" => Some("Reorder recipient routes\n\nUSAGE\n  $ primitive routes reorder-routes [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Reorder recipient routes\n\n  Update the priority of one or more routes in a single call.\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  * updates  array<object>\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "routes\x00simulate-route\x00--api-key\x00prim_test\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes\x00simulate-route\x00--body-file\x00value\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes\x00simulate-route\x00--envelope\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes\x00simulate-route\x00--event-type\x00value\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes\x00simulate-route\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes\x00simulate-route\x00--json\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes\x00simulate-route\x00--raw-body\x00value\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes\x00simulate-route\x00--recipient\x00user@example.com\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes\x00simulate-route\x00--time\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes simulate-route [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--recipient\n    <value>] [--event-type <value>]\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --body-file=<value>   Path to a JSON file used as the request body. Same role\n                        as --raw-body for callers passing a saved payload.\n  --envelope            Print the full response envelope, including pagination\n                        metadata such as meta.cursor. Defaults to printing only\n                        the data payload for backward compatibility.\n  --event-type=<value>  Event type to model; defaults to email.received.\n  --json                Accepted for consistency with task-focused commands.\n                        Generated API commands already print JSON by default.\n  --raw-body=<value>    Full request body as raw JSON. Escape hatch for nested\n                        or complex fields (e.g. arrays); prefer per-field flags\n                        (e.g. --to, --from, --body-text) when available.\n  --recipient=<value>   recipient\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Resolves where an inbound email to `recipient` would be delivered, with a\n  trace of every rule evaluated and why. Read-only; creates nothing.\n\n\n"),
        "routes\x00test\x00--api-key\x00prim_test\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes\x00test\x00--event-type\x00value\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes\x00test\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes\x00test\x00--time\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes\x00test\x00user@example.com\x00--help" => Some("Simulate routing for a recipient\n\nUSAGE\n  $ primitive routes test RECIPIENT [--api-key <value>] [--time]\n    [--event-type <value>]\n\nARGUMENTS\n  RECIPIENT  Recipient address to simulate (e.g. alice@acme.com).\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --event-type=<value>  Event type to model (defaults to email.received).\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Simulate routing for a recipient\n\n  Simulate routing for a recipient address.\n\n  Shows the resolved destination and a trace of every rule evaluated (which\n  matched or missed, and why). Does not create or change anything.\n\nEXAMPLES\n  $ primitive routes test alice@acme.com\n\n  $ primitive routes test bounce@acme.com --event-type email.bounced\n\n"),
        "routes\x00update\x00--api-key\x00prim_test\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update\x00--disable\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update\x00--domain\x00value\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update\x00--enable\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update\x00--endpoint\x00value\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update\x00--match\x00exact\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update\x00--pattern\x00b@example.com\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update\x00--priority\x001\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update\x00--time\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "routes\x00update-route\x00--api-key\x00prim_test\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--body-file\x00value\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--domain-id\x00fn_123\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--enabled\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--endpoint-id\x00fn_123\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--envelope\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--id\x00fn_123\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--json\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--match-type\x00exact\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--no-enabled\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--pattern\x00b@example.com\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--priority\x001\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--raw-body\x00value\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update-route\x00--time\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update-route --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--domain-id <value>] [--enabled] [--endpoint-id <value>] [--match-type\n    exact|wildcard|regex] [--pattern <value>] [--priority <value>]\n\nFLAGS\n  --api-key=<value>      [env: PRIMITIVE_API_KEY] Primitive API key override\n                         (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                         credentials)\n  --body-file=<value>    Path to a JSON file used as the request body. Same role\n                         as --raw-body for callers passing a saved payload.\n  --domain-id=<value>    domain_id\n  --[no-]enabled         enabled\n  --endpoint-id=<value>  endpoint_id\n  --envelope             Print the full response envelope, including pagination\n                         metadata such as meta.cursor. Defaults to printing only\n                         the data payload for backward compatibility.\n  --id=<value>           (required) Resource UUID\n  --json                 Accepted for consistency with task-focused commands.\n                         Generated API commands already print JSON by default.\n  --match-type=<option>  match_type\n                         <options: exact|wildcard|regex>\n  --pattern=<value>      pattern\n  --priority=<value>     priority\n  --raw-body=<value>     Full request body as raw JSON. Escape hatch for nested\n                         or complex fields (e.g. arrays); prefer per-field flags\n                         (e.g. --to, --from, --body-text) when available.\n  --time                 Print the wall-clock duration of this command to stderr\n                         after it completes (e.g. `[time: 1.34s]`). Useful for\n                         measuring `--wait` send latency, comparing CLI\n                         overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  PATCH /routes/{id}\n\n"),
        "routes\x00update\x00fn_123\x00--help" => Some("Update a recipient route\n\nUSAGE\n  $ primitive routes update ID [--api-key <value>] [--time] [--match\n    exact|wildcard|regex] [--pattern <value>] [--endpoint <value>] [--domain\n    <value>] [--priority <value>] [--enable | --disable]\n\nARGUMENTS\n  ID  Route id (UUID) to update.\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --disable           Disable the route.\n  --domain=<value>    New domain scope id (UUID).\n  --enable            Enable the route.\n  --endpoint=<value>  New target endpoint id (UUID).\n  --match=<option>    New match type for the pattern.\n                      <options: exact|wildcard|regex>\n  --pattern=<value>   New recipient address or wildcard pattern.\n  --priority=<value>  New evaluation priority within a scope; lower is checked\n                      first.\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  Update a recipient route\n\n  Update an existing recipient route.\n\n  Change the pattern, match type, target endpoint, domain scope, priority, or\n  enabled state. Provide at least one field; anything omitted is left as-is.\n\nEXAMPLES\n  $ primitive routes update <route-id> --priority 5\n\n  $ primitive routes update <route-id> --pattern alice@acme.com\n\n  $ primitive routes update <route-id> --disable\n\n"),
        "script:autocomplete\x00--help" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "script:autocomplete\x00bash\x00--help" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "script\x00autocomplete\x00--help" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "script\x00autocomplete\x00bash\x00--help" => Some("outputs autocomplete config script for shells\n\nUSAGE\n  $ primitive autocomplete script [SHELL]\n\nARGUMENTS\n  [SHELL]  (zsh|bash|powershell) Shell type\n\nDESCRIPTION\n  outputs autocomplete config script for shells\n\n"),
        "search\x00--api-key\x00prim_test\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--body\x00hello\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--corpus\x00inbound\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--cursor\x00value\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--domain-id\x00fn_123\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--domain\x00value\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--envelope\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--from\x00user@example.com\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--has-attachment\x00true\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--include-facets\x00true\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--json\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--limit\x001\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--mode\x00hybrid\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--snippet\x00true\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--sort\x00relevance\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--status\x00pending\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--subject\x00hello\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--time\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00--to\x00user@example.com\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search:semantic-search\x00--api-key\x00prim_test\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00--corpus\x00inbound\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00--cursor\x00value\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00--json\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00--limit\x001\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00--mode\x00hybrid\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00--time\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search:semantic-search\x00hello\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00hello\x00--help" => Some("Search mail (lexical by default; --mode for semantic)\n\nUSAGE\n  $ primitive search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--from <value>] [--to <value>] [--subject <value>]\n    [--body <value>] [--domain <value>] [--domain-id <value>] [--has-attachment\n    true|false] [--status pending|accepted|completed|rejected] [--sort\n    relevance|received_at_desc|received_at_asc] [--snippet true|false]\n    [--include-facets true|false] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--envelope] [--time]\n\nARGUMENTS\n  QUERY  The search query. Matched against every indexed field (subject, body,\n         sender, recipient) when lexical; against the embedding when semantic.\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --body=<value>             Lexical only. Full-text search restricted to the\n                             parsed text body.\n  --corpus=<option>...       Semantic only. Restrict to inbound or outbound\n                             mail. Pass twice to include both (the default).\n                             <options: inbound|outbound>\n  --cursor=<value>           Opaque pagination cursor from a prior response's\n                             meta.cursor.\n  --date-from=<value>        Only include mail at or after this ISO-8601\n                             timestamp.\n  --date-to=<value>          Only include mail at or before this ISO-8601\n                             timestamp.\n  --domain=<value>           Lexical only. Filter by the recipient's mail\n                             domain.\n  --domain-id=<value>        Lexical only. Filter by domain ID.\n  --envelope                 Lexical text-table only. Surface the next\n                             pagination cursor (if any) on STDERR below the\n                             table. Has no effect with --json; --json already\n                             prints the full envelope including meta.\n  --from=<value>             Lexical only. Filter by sender address or sender\n                             domain.\n  --has-attachment=<option>  Lexical only. Filter by whether the email has one\n                             or more attachments.\n                             <options: true|false>\n  --include-facets=<option>  Lexical only. Include facet counts for sender,\n                             domain, status, and attachment presence.\n                             <options: true|false>\n  --json                     Print the raw response envelope as JSON on STDOUT\n                             instead of the text table.\n  --limit=<value>            [default: 10] Maximum results to return (1-100,\n                             default 10).\n  --mode=<option>            Switch to the cross-corpus semantic backend. Omit\n                             for the default lexical backend.\n                             <options: hybrid|semantic|keyword>\n  --snippet=<option>         Lexical only. Include match-centered subject/body\n                             highlights on each row.\n                             <options: true|false>\n  --sort=<option>            Lexical only. Result ordering.\n                             <options:\n                             relevance|received_at_desc|received_at_asc>\n  --status=<option>          Lexical only. Filter by parse status.\n                             <options: pending|accepted|completed|rejected>\n  --subject=<value>          Lexical only. Full-text search restricted to the\n                             subject.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               Lexical only. Filter by recipient address or\n                             recipient domain.\n\nDESCRIPTION\n  Search mail (lexical by default; --mode for semantic)\n\n  Search received (default) or both received and sent mail (with --mode).\n\n  Default behavior is lexical full-text matching: the positional query is sent\n  as `q=<query>` to the inbound search endpoint, which matches against subject,\n  body, sender, and recipient in a single pass. Structured filters (--from,\n  --to, --subject, --body, --domain, --has-attachment, --date-from, --date-to,\n  --status) AND with the text query.\n\n  Pass --mode to switch to the cross-corpus semantic backend (covers inbound and\n  outbound). `--mode keyword` is plain full-text; `--mode semantic` is\n  embedding-only; `--mode hybrid` blends both. Semantic modes require the Pro\n  plan with the semantic_search_enabled entitlement.\n\n  Output is a fixed-width text table by default (header on STDERR so rows stay\n  grep/awk-friendly). Use --json for the raw envelope.\n\nEXAMPLES\n  $ primitive search \"invoice\"\n\n  $ primitive search \"renewal\" --from acme.com\n\n  $ primitive search \"kickoff\" --mode hybrid\n\n  $ primitive search \"shipping\" --mode keyword --corpus outbound\n\n  $ primitive search \"needle\" --json | jq '.data[0].id'\n\nCOMMANDS\n  search semantic-search  Semantic / hybrid / keyword search across received and\n                          sent mail\n\n"),
        "search\x00semantic-search\x00--api-key\x00prim_test\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00--corpus\x00inbound\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00--cursor\x00value\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00--json\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00--limit\x001\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00--mode\x00hybrid\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00--time\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "search\x00semantic-search\x00hello\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive search semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--api-key\x00prim_test\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--corpus\x00inbound\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--cursor\x00value\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--json\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--limit\x001\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--mode\x00hybrid\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00--time\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "semantic-search\x00hello\x00--help" => Some("Semantic / hybrid / keyword search across received and sent mail\n\nUSAGE\n  $ primitive semantic-search QUERY [--api-key <value>] [--mode\n    hybrid|semantic|keyword] [--corpus inbound|outbound...] [--date-from\n    <value>] [--date-to <value>] [--limit <value>] [--cursor <value>] [--json]\n    [--time]\n\nARGUMENTS\n  QUERY  The search query.\n\nFLAGS\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --corpus=<option>...  Restrict to inbound or outbound. Pass twice to include\n                        both (the default).\n                        <options: inbound|outbound>\n  --cursor=<value>      Opaque pagination cursor from a prior response's\n                        meta.cursor.\n  --date-from=<value>   Only include mail at or after this ISO-8601 timestamp.\n  --date-to=<value>     Only include mail at or before this ISO-8601 timestamp.\n  --json                Print the raw response envelope as JSON on STDOUT\n                        instead of the text table.\n  --limit=<value>       [default: 10] Maximum results to return (1-100, default\n                        10).\n  --mode=<option>       [default: hybrid] Ranking strategy.\n                        <options: hybrid|semantic|keyword>\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Semantic / hybrid / keyword search across received and sent mail\n\n  Search received and sent mail by meaning or keywords.\n\n  Returns ranked rows. Each row carries a relevance score, the fields it\n  matched, and a match-centered excerpt. Defaults to hybrid mode (blends\n  semantic and keyword signals); use `--mode keyword` for plain\n  full-text matching and `--mode semantic` for embedding-only.\n\n  Requires the Pro plan with the semantic_search_enabled entitlement.\n\nEXAMPLES\n  $ primitive semantic-search \"invoice from acme\"\n\n  $ primitive semantic-search \"shipping update\" --mode keyword\n\n  $ primitive semantic-search \"kickoff\" --corpus inbound --limit 25\n\n  $ primitive semantic-search renewal --json | jq '.data[].id'\n\n"),
        "send\x00--api-key\x00prim_test\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--attachment\x00value\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--bcc\x00value\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--body-file\x00value\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--body-stdin\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--body\x00hello\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--cc\x00value\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--from\x00user@example.com\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--html-file\x00value\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--html-stdin\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--html\x00value\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--in-reply-to\x00value\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--subject\x00hello\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--time\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--to\x00user@example.com\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--wait\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "send\x00--wait-timeout-ms\x001\x00--help" => Some("Send an email (simplified, agent-friendly)\n\nUSAGE\n  $ primitive send --to <value> [--api-key <value>] [--from\n    <value>] [--subject <value>] [--body <value>] [--body-file <value>]\n    [--body-stdin] [--html <value>] [--html-file <value>] [--html-stdin]\n    [--attachment <value>...] [--cc <value>...] [--bcc <value>...]\n    [--in-reply-to <value>] [--wait] [--wait-timeout-ms <value>] [--time]\n\nFLAGS\n  --api-key=<value>          [env: PRIMITIVE_API_KEY] Primitive API key override\n                             (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                             credentials)\n  --attachment=<value>...    Attach a file to the email. Repeatable. Sends file\n                             bytes as a MIME attachment; use --body-file only\n                             for message body text.\n  --bcc=<value>...           Blind-carbon-copy recipient. Repeatable for\n                             multiple. Bcc recipients receive the message but\n                             are not disclosed to the other recipients.\n  --body=<value>             Plain-text message body. Either --body or --html\n                             (or both) is required.\n  --body-file=<value>        Read the plain-text message body from a UTF-8 file.\n                             This does not attach the file; use --attachment for\n                             file attachments. Mutually exclusive with --body\n                             and --body-stdin.\n  --body-stdin               Read the plain-text message body from stdin.\n                             Mutually exclusive with --body and --body-file.\n                             Stdin can only be consumed once.\n  --cc=<value>...            Carbon-copy recipient. Repeatable for multiple. Cc\n                             recipients are visible to everyone who receives the\n                             message.\n  --from=<value>             Sender address. Defaults to\n                             agent@<your-first-verified-outbound-domain>.\n  --html=<value>             HTML message body. Either --body or --html (or\n                             both) is required.\n  --html-file=<value>        Read the HTML message body from a UTF-8 file.\n                             Mutually exclusive with --html and --html-stdin.\n  --html-stdin               Read the HTML message body from stdin. Mutually\n                             exclusive with --html and --html-file. Stdin can\n                             only be consumed once.\n  --in-reply-to=<value>      Message-Id of the parent email when threading a\n                             reply on the wire. For replying to an inbound\n                             message you received, prefer `primitive reply --id\n                             <inbound-id>`.\n  --subject=<value>          Subject line. Defaults to the first line of --body\n                             / --html when omitted.\n  --time                     Print the wall-clock duration of this command to\n                             stderr after it completes (e.g. `[time: 1.34s]`).\n                             Useful for measuring `--wait` send latency,\n                             comparing CLI overhead, or capturing timing in\n                             scripts.\n  --to=<value>               (required) Recipient address (e.g.\n                             alice@example.com).\n  --wait                     Block until the receiving MTA returns an outcome.\n                             Without --wait, the call returns once Primitive has\n                             accepted the message for delivery.\n  --wait-timeout-ms=<value>  Maximum time to wait when --wait is set. Defaults\n                             to 30000ms.\n\nDESCRIPTION\n  Send an email (simplified, agent-friendly)\n\n  Send an outbound email. Agent-grade shortcut for `sending send` with sensible\n  defaults.\n\n  --from defaults to agent@<your-first-verified-outbound-domain> when omitted.\n  --subject defaults to the first line of the body when omitted.\n  --attachment attaches a file; repeat it to attach multiple files.\n\n  For the full flag set (custom message-id threading on the wire,\n  references arrays, etc.), use `primitive sending send`.\n\nEXAMPLES\n  $ primitive send --to alice@example.com --body 'Hi Alice!'\n\n  $ primitive send --to alice@example.com --body-file ./message.txt\n\n  $ primitive send --to alice@example.com --body 'See attached.' --attachment ./report.pdf\n\n  $ primitive send --to alice@example.com --from support@yourcompany.com --subject 'Quick question' --body 'Are you free Thursday?'\n\n  $ primitive send --to alice@example.com --html '<p>Hello!</p>'\n\n  $ primitive send --to alice@example.com --cc bob@example.com --bcc audit@example.com --body 'Loop bob in; audit copy stays hidden.'\n\n  $ primitive send --to alice@example.com --body 'Confirmed' --wait\n\n  $ primitive send --to inbox@your-managed-domain.primitive.email --body 'self-loop smoke test' --wait  # any *.primitive.email address routes back to the sending account; useful for proving outbound + inbound work end-to-end\n\n"),
        "sending" => Some("Send outbound emails. Prefer `primitive send` for fresh sends and `primitive reply --id <inbound-id>` for replies. Use `primitive domains list` or `primitive inbox status` to find usable sender domains for --from; `primitive sending permissions` lists recipient-scope destinations you may send to.\n\nUSAGE\n  $ primitive sending COMMAND\n\nCOMMANDS\n  sending get                   Get a sent email by id\n  sending get-send-permissions  List send-permission rules\n  sending get-sent-email        Get a sent email by id\n  sending list                  List outbound sent emails\n  sending list-sent-emails      List outbound sent emails\n  sending permissions           List send-permission rules\n  sending reply                 Reply to an inbound email\n  sending reply-to-email        Reply to an inbound email\n  sending send                  Send outbound email\n  sending send-email            Send outbound email\n\n"),
        "sending\x00--help" => Some("Send outbound emails. Prefer `primitive send` for fresh sends and `primitive reply --id <inbound-id>` for replies. Use `primitive domains list` or `primitive inbox status` to find usable sender domains for --from; `primitive sending permissions` lists recipient-scope destinations you may send to.\n\nUSAGE\n  $ primitive sending COMMAND\n\nCOMMANDS\n  sending get                   Get a sent email by id\n  sending get-send-permissions  List send-permission rules\n  sending get-sent-email        Get a sent email by id\n  sending list                  List outbound sent emails\n  sending list-sent-emails      List outbound sent emails\n  sending permissions           List send-permission rules\n  sending reply                 Reply to an inbound email\n  sending reply-to-email        Reply to an inbound email\n  sending send                  Send outbound email\n  sending send-email            Send outbound email\n\n"),
        "sending\x00-h" => Some("Send outbound emails. Prefer `primitive send` for fresh sends and `primitive reply --id <inbound-id>` for replies. Use `primitive domains list` or `primitive inbox status` to find usable sender domains for --from; `primitive sending permissions` lists recipient-scope destinations you may send to.\n\nUSAGE\n  $ primitive sending COMMAND\n\nCOMMANDS\n  sending get                   Get a sent email by id\n  sending get-send-permissions  List send-permission rules\n  sending get-sent-email        Get a sent email by id\n  sending list                  List outbound sent emails\n  sending list-sent-emails      List outbound sent emails\n  sending permissions           List send-permission rules\n  sending reply                 Reply to an inbound email\n  sending reply-to-email        Reply to an inbound email\n  sending send                  Send outbound email\n  sending send-email            Send outbound email\n\n"),
        "sending:get\x00--api-key\x00prim_test\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get\x00--envelope\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get\x00--id\x00fn_123\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get\x00--json\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get\x00--time\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get-send-permissions\x00--api-key\x00prim_test\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:get-send-permissions\x00--envelope\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:get-send-permissions\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:get-send-permissions\x00--json\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:get-send-permissions\x00--time\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:get-sent-email\x00--api-key\x00prim_test\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get-sent-email\x00--envelope\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get-sent-email\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get-sent-email\x00--id\x00fn_123\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get-sent-email\x00--json\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:get-sent-email\x00--time\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending:list\x00--api-key\x00prim_test\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--cursor\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--envelope\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--idempotency-key\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--json\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--limit\x001\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--request-id\x00fn_123\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--status\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list\x00--time\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--api-key\x00prim_test\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--cursor\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--envelope\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--idempotency-key\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--json\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--limit\x001\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--request-id\x00fn_123\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--status\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:list-sent-emails\x00--time\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending:permissions\x00--api-key\x00prim_test\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:permissions\x00--envelope\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:permissions\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:permissions\x00--json\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:permissions\x00--time\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending:reply\x00--api-key\x00prim_test\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--body-file\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--body-html\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--body-text\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--envelope\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--from\x00user@example.com\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--id\x00fn_123\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--json\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--no-wait\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--raw-body\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--time\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply\x00--wait\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--api-key\x00prim_test\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--body-file\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--body-html\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--body-text\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--envelope\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--from\x00user@example.com\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--id\x00fn_123\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--json\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--no-wait\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--raw-body\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--time\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:reply-to-email\x00--wait\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending:send\x00--api-key\x00prim_test\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--body-file\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--body-html\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--body-text\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--envelope\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--from\x00user@example.com\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--idempotency-key\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--in-reply-to\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--json\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--no-wait\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--raw-body\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--subject\x00hello\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--time\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--to\x00user@example.com\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--wait\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send\x00--wait-timeout-ms\x001\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--api-key\x00prim_test\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--body-file\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--body-html\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--body-text\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--envelope\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--from\x00user@example.com\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--idempotency-key\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--in-reply-to\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--json\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--no-wait\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--raw-body\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--subject\x00hello\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--time\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--to\x00user@example.com\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--wait\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending:send-email\x00--wait-timeout-ms\x001\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00get\x00--api-key\x00prim_test\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get\x00--envelope\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get\x00--id\x00fn_123\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get\x00--json\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get\x00--time\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get-send-permissions\x00--api-key\x00prim_test\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00get-send-permissions\x00--envelope\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00get-send-permissions\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00get-send-permissions\x00--json\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00get-send-permissions\x00--time\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending get-send-permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00get-sent-email\x00--api-key\x00prim_test\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get-sent-email\x00--envelope\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get-sent-email\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get-sent-email\x00--id\x00fn_123\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get-sent-email\x00--json\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00get-sent-email\x00--time\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sending get-sent-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sending\x00list\x00--api-key\x00prim_test\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--cursor\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--envelope\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--idempotency-key\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--json\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--limit\x001\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--request-id\x00fn_123\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--status\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list\x00--time\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--api-key\x00prim_test\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--cursor\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--envelope\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--idempotency-key\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--json\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--limit\x001\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--request-id\x00fn_123\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--status\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00list-sent-emails\x00--time\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sending list-sent-emails [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sending\x00permissions\x00--api-key\x00prim_test\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00permissions\x00--envelope\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00permissions\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00permissions\x00--json\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00permissions\x00--time\x00--help" => Some("List send-permission rules\n\nUSAGE\n  $ primitive sending permissions [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List send-permission rules\n\n  Returns a flat list of rules describing every recipient the\n  caller may send to. Each rule has a `type`, a kind-specific\n  payload, and a human-readable `description`. If any rule\n  matches the recipient, /send-mail will accept the send under\n  the recipient-scope check.\n\n  The endpoint is the answer to \"where can I send\" without\n  exposing internal entitlement names. Agents that don't\n  recognize a `type` can still read the `description` prose\n  and act on it.\n\n  Rule kinds, ordered broadest-first so an agent can stop\n  scanning at the first match:\n\n  1. `any_recipient` (one entry, only when the org can send\n  anywhere): every other rule below it is redundant.\n  2. `managed_zone` (always emitted, one per Primitive-managed\n  zone): sends to any address at *.primitive.email or\n  *.email.works always succeed; no entitlement required.\n  3. `your_domain` (one per active verified outbound domain\n  owned by the org): sends to that domain are approved.\n  4. `address` (one per address that has authenticated\n  inbound mail to the org, capped at `meta.address_cap`):\n  sends to that exact address are approved.\n\n  The list is informational, not an authorization check.\n  /send-mail remains the source of truth on whether an\n  individual send will succeed (it also enforces the\n  from-address and the `send_mail` entitlement, which are\n  not recipient-scope concerns and are not represented here).\n\n\n  Tip: this command answers where you may send mail to. To find usable sender\n  domains for --from, run `primitive domains list` or `primitive inbox status`\n  and use an address at an active verified domain.\n\n"),
        "sending\x00reply\x00--api-key\x00prim_test\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--body-file\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--body-html\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--body-text\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--envelope\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--from\x00user@example.com\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--id\x00fn_123\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--json\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--no-wait\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--raw-body\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--time\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply\x00--wait\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--api-key\x00prim_test\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--body-file\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--body-html\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--body-text\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--envelope\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--from\x00user@example.com\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--id\x00fn_123\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--json\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--no-wait\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--raw-body\x00value\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--time\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00reply-to-email\x00--wait\x00--help" => Some("Reply to an inbound email\n\nUSAGE\n  $ primitive sending reply-to-email --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--body-html <value>] [--body-text <value>] [--from <value>] [--wait]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML reply body. At least one of body_text or body_html is required.\n\n  --body-text=<value>\n      Plain-text reply body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes (same cap as send-mail).\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      Optional override for the reply's From header. Defaults to the inbound's\n      recipient. Use to add a display name (`\"Acme Support\" <agent@company.com>`)\n      or to reply from a different verified outbound address (e.g. multi-team\n      routing where support@ triages to billing@). The from-domain must be a\n      verified outbound domain for your org, same as send-mail.\n\n  --id=<value>\n      (required) Resource UUID\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning, mirroring the send-mail `wait` semantics.\n\nDESCRIPTION\n  Reply to an inbound email\n\n  Sends an outbound reply to the inbound email identified by `id`.\n  Threading headers (`In-Reply-To`, `References`), recipient\n  derivation (Reply-To, then From, then bare sender), and the\n  `Re:` subject prefix are all derived server-side from the\n  stored inbound row. The request body carries only the message\n  body, optional From override, optional attachments, and optional\n  `wait` flag; passing any header or recipient override is\n  rejected by the schema (`additionalProperties: false`).\n\n  Forwards through the same gates as `/send-mail`: the response\n  status, error envelope, and `idempotent_replay` flag mirror\n  the send-mail contract verbatim.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments  array<object>  Inline attachments for this reply. Use\n  https://api.primitive.dev/v1 for rep...\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "sending\x00send\x00--api-key\x00prim_test\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--body-file\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--body-html\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--body-text\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--envelope\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--from\x00user@example.com\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--idempotency-key\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--in-reply-to\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--json\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--no-wait\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--raw-body\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--subject\x00hello\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--time\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--to\x00user@example.com\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--wait\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send\x00--wait-timeout-ms\x001\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--api-key\x00prim_test\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--body-file\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--body-html\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--body-text\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--envelope\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--from\x00user@example.com\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--idempotency-key\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--in-reply-to\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--json\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--no-wait\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--raw-body\x00value\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--subject\x00hello\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--time\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--to\x00user@example.com\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--wait\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sending\x00send-email\x00--wait-timeout-ms\x001\x00--help" => Some("Send outbound email\n\nUSAGE\n  $ primitive sending send-email [--api-key <value>] [--time] [--json]\n    [--envelope] [--idempotency-key <value>] [--raw-body <value>] [--body-file\n    <value>] [--from <value>] [--subject <value>] [--to <value>] [--body-html\n    <value>] [--body-text <value>] [--in-reply-to <value>] [--wait]\n    [--wait-timeout-ms <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --body-file=<value>\n      Path to a JSON file used as the request body. Same role as --raw-body for\n      callers passing a saved payload.\n\n  --body-html=<value>\n      HTML message body. At least one of body_text or body_html is required. The\n      combined UTF-8 byte length of body_text and body_html must be at most 262144\n      bytes.\n\n  --body-text=<value>\n      Plain-text message body. At least one of body_text or body_html is required.\n      The combined UTF-8 byte length of body_text and body_html must be at most\n      262144 bytes.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --from=<value>\n      RFC 5322 From header. The sender domain must be a verified outbound domain\n      for your organization.\n\n  --idempotency-key=<value>\n      Optional customer-supplied idempotency key. If omitted, Primitive\n      derives one from the canonical request payload and echoes the\n      effective value in the `Idempotency-Key` response header.\n\n  --in-reply-to=<value>\n      Message-ID of the direct parent email when sending a threaded reply.\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --raw-body=<value>\n      Full request body as raw JSON. Escape hatch for nested or complex fields\n      (e.g. arrays); prefer per-field flags (e.g. --to, --from, --body-text) when\n      available.\n\n  --subject=<value>\n      Subject line for the outbound message\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\n  --to=<value>\n      Recipient address. Recipient eligibility depends on your account's outbound\n      entitlements.\n\n  --[no-]wait\n      When true, wait for the first downstream SMTP delivery outcome before\n      returning.\n\n  --wait-timeout-ms=<value>\n      Maximum time to wait for a delivery outcome when wait is true. Defaults to\n      30000.\n\nDESCRIPTION\n  Send outbound email\n\n  Sends an outbound email through Primitive's outbound relay. By default\n  the request returns once the relay accepts the message for delivery.\n  Set `wait: true` to wait for the first downstream SMTP delivery outcome.\n\n  **Host routing.** /send-mail is served by the canonical API host\n  (`https://api.primitive.dev/v1`) so the request body can carry\n  inline attachments up to ~30 MiB raw. The legacy dashboard\n  compatibility host (`https://www.primitive.dev/api/v1`) also accepts\n  /send-mail, but Vercel request body limits apply before proxying.\n  The typed SDKs route /send-mail to the canonical API host\n  automatically.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  attachments          array<object>  Inline attachments. Send requests with\n  attachments to https://api.primitive...\n  bcc                  any  Blind-carbon-copy recipients. Either a single\n  address or an array of addres...\n  cc                   any  Carbon-copy recipients. Either a single address or\n  an array of addresses. E...\n  payload_attachments  array<object>  Deliver an already-uploaded Primitive\n  Payloads object as an attachment by r...\n  references           array<string>  Full ordered message-id chain for the\n  thread.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n  Tip: prefer `primitive send --to <address> --body <text> --attachment <file>`\n  for file attachments. This raw command exists for callers passing JSON.\n\n"),
        "sent" => Some("Short aliases for outbound sent-email history: `primitive sent list` and `primitive sent get`.\n\nUSAGE\n  $ primitive sent COMMAND\n\nCOMMANDS\n  sent get   Get a sent email by id\n  sent list  List outbound sent emails\n\n"),
        "sent\x00--help" => Some("Short aliases for outbound sent-email history: `primitive sent list` and `primitive sent get`.\n\nUSAGE\n  $ primitive sent COMMAND\n\nCOMMANDS\n  sent get   Get a sent email by id\n  sent list  List outbound sent emails\n\n"),
        "sent\x00-h" => Some("Short aliases for outbound sent-email history: `primitive sent list` and `primitive sent get`.\n\nUSAGE\n  $ primitive sent COMMAND\n\nCOMMANDS\n  sent get   Get a sent email by id\n  sent list  List outbound sent emails\n\n"),
        "sent:get\x00--api-key\x00prim_test\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent:get\x00--envelope\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent:get\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent:get\x00--id\x00fn_123\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent:get\x00--json\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent:get\x00--time\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent:list\x00--api-key\x00prim_test\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--cursor\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--envelope\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--idempotency-key\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--json\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--limit\x001\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--request-id\x00fn_123\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--status\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent:list\x00--time\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00get\x00--api-key\x00prim_test\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent\x00get\x00--envelope\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent\x00get\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent\x00get\x00--id\x00fn_123\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent\x00get\x00--json\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent\x00get\x00--time\x00--help" => Some("Get a sent email by id\n\nUSAGE\n  $ primitive sent get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a sent email by id\n\n  Returns the full sent-email record by id, including\n  `body_text` and `body_html` (omitted from the listing\n  endpoint to keep paginated responses small). Use this when\n  diagnosing a specific send, e.g. inspecting the receiver's\n  SMTP response on a `bounced` row or pulling the gate\n  denial detail on a `gate_denied` row.\n\n\n"),
        "sent\x00list\x00--api-key\x00prim_test\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--cursor\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--envelope\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--idempotency-key\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--json\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--limit\x001\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--request-id\x00fn_123\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--status\x00value\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "sent\x00list\x00--time\x00--help" => Some("List outbound sent emails\n\nUSAGE\n  $ primitive sent list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--status <value>]\n    [--request-id <value>] [--idempotency-key <value>] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>\n      [env: PRIMITIVE_API_KEY] Primitive API key override (defaults to\n      PRIMITIVE_API_KEY or saved OAuth login credentials)\n\n  --cursor=<value>\n      Pagination cursor from a previous response's `meta.cursor` field.\n      Format: `{ISO-datetime}|{id}`\n\n  --date-from=<value>\n      Inclusive lower bound on `created_at`.\n\n  --date-to=<value>\n      Inclusive upper bound on `created_at`.\n\n  --envelope\n      Print the full response envelope, including pagination metadata such as\n      meta.cursor. Defaults to printing only the data payload for backward\n      compatibility.\n\n  --idempotency-key=<value>\n      Filter to rows with the given `client_idempotency_key`.\n      Multiple rows can share a key (a retry that hit the\n      idempotent-replay path returns the same row, but a\n      retry with a DIFFERENT canonical payload under the\n      same key is rejected by /send-mail before the row is\n      written, so duplicates are bounded).\n\n  --json\n      Accepted for consistency with task-focused commands. Generated API commands\n      already print JSON by default.\n\n  --limit=<value>\n      [default: 50] Number of results per page\n\n  --request-id=<value>\n      Filter to the row matching a specific server-issued\n      `request_id`. The /send-mail response surfaces\n      `request_id` on every send; this lookup lets the\n      caller find the historical row for a given live call\n      without remembering its `id`.\n\n  --status=<value>\n      Filter to rows in this status. Useful for polling\n      queued rows that haven't transitioned, auditing\n      gate-denied attempts, or listing only successful\n      deliveries.\n\n  --time\n      Print the wall-clock duration of this command to stderr after it completes\n      (e.g. `[time: 1.34s]`). Useful for measuring `--wait` send latency,\n      comparing CLI overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  List outbound sent emails\n\n  Returns a paginated list of OUTBOUND emails the caller's\n  org has sent via /send-mail (and /emails/{id}/reply, which\n  forwards through /send-mail). Includes every recorded\n  attempt, including gate-denied attempts that the agent\n  never called and rows still in `queued` state.\n\n  For inbound mail received at your verified domains, see\n  /emails. There is no unified send/receive history endpoint;\n  the two surfaces are intentionally separate because the\n  underlying tables, statuses, and lifecycle differ.\n\n  Email bodies (`body_text`, `body_html`) are NOT included on\n  list rows so a 50-row page can't balloon into a multi-MB\n  response when sends are near the 5MB body cap. Use\n  /sent-emails/{id} to fetch a single row with bodies, or\n  cross-reference by `client_idempotency_key` if the caller\n  already has the body locally.\n\n\n"),
        "signin\x00--accept-terms\x00--help" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "signin\x00--device-name\x00value\x00--help" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "signin\x00--force\x00--help" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "signin\x00--help" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "signin\x00--no-browser\x00--help" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "signin\x00--signup-code\x00123456\x00--help" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "signin\x00-f\x00--help" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "signin:browser\x00--device-name\x00value\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin:browser\x00--force\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin:browser\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin:browser\x00--no-browser\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin:browser\x00-f\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin:confirm\x00--force\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:confirm\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:confirm\x00-f\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:otp\x00--accept-terms\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin:otp\x00--device-name\x00value\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin:otp\x00--force\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin:otp\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin:otp\x00--signup-code\x00123456\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin:otp\x00-f\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin:otp:confirm\x00--force\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:otp:confirm\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:otp:confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:otp:confirm\x00-f\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:otp:confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin:otp:resend\x00--help" => Some("Resend OTP sign-in code\n\nUSAGE\n  $ primitive signin otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP sign-in code\n\n  Resend the verification code for a pending OTP sign-in.\n\nEXAMPLES\n  $ primitive signin otp resend user@example.com\n\n"),
        "signin:otp:resend\x00user@example.com\x00--help" => Some("Resend OTP sign-in code\n\nUSAGE\n  $ primitive signin otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP sign-in code\n\n  Resend the verification code for a pending OTP sign-in.\n\nEXAMPLES\n  $ primitive signin otp resend user@example.com\n\n"),
        "signin:otp\x00user@example.com\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin:resend\x00--help" => Some("Resend email-code sign-in code\n\nUSAGE\n  $ primitive signin resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code sign-in code\n\n  Resend the verification code for a pending email-code sign-in.\n\nEXAMPLES\n  $ primitive signin resend user@example.com\n\n"),
        "signin:resend\x00user@example.com\x00--help" => Some("Resend email-code sign-in code\n\nUSAGE\n  $ primitive signin resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code sign-in code\n\n  Resend the verification code for a pending email-code sign-in.\n\nEXAMPLES\n  $ primitive signin resend user@example.com\n\n"),
        "signin\x00browser\x00--device-name\x00value\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin\x00browser\x00--force\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin\x00browser\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin\x00browser\x00--no-browser\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin\x00browser\x00-f\x00--help" => Some("Sign in with browser approval\n\nUSAGE\n  $ primitive signin browser [--device-name <value>] [--no-browser] [-f]\n\nFLAGS\n  -f, --force                Replace saved credentials without first verifying\n                             the existing session\n      --device-name=<value>  Device name shown in the browser approval screen\n      --no-browser           Do not attempt to open the browser automatically\n\nDESCRIPTION\n  Sign in with browser approval\n\n  Sign in to an existing Primitive account by opening Primitive in your browser\n  and saving an org-scoped OAuth session locally.\n\nEXAMPLES\n  $ primitive signin browser\n\n  $ primitive signin browser --device-name work-laptop\n\n  $ primitive signin browser --no-browser\n\n  $ primitive signin browser --force\n\n"),
        "signin\x00confirm\x00--force\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00confirm\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00confirm\x00-f\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm email-code sign-in\n\nUSAGE\n  $ primitive signin confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm email-code sign-in\n\n  Confirm a pending email-code sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00otp\x00--accept-terms\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin\x00otp\x00--device-name\x00value\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin\x00otp\x00--force\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin\x00otp\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin\x00otp\x00--signup-code\x00123456\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin\x00otp\x00-f\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin\x00otp\x00confirm\x00--force\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00otp\x00confirm\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00otp\x00confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00otp\x00confirm\x00-f\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00otp\x00confirm\x00user@example.com\x00123456\x00--help" => Some("Confirm OTP sign-in\n\nUSAGE\n  $ primitive signin otp confirm EMAIL CODE [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n  CODE   Verification code from the sign-in email\n\nFLAGS\n  -f, --force           Replace saved credentials after verification\n      --org-id=<value>  Workspace id to target when the email belongs to\n                        multiple workspaces\n\nDESCRIPTION\n  Confirm OTP sign-in\n\n  Confirm a pending OTP sign-in, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signin otp confirm user@example.com 123456\n\n  $ primitive signin otp confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n"),
        "signin\x00otp\x00resend\x00--help" => Some("Resend OTP sign-in code\n\nUSAGE\n  $ primitive signin otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP sign-in code\n\n  Resend the verification code for a pending OTP sign-in.\n\nEXAMPLES\n  $ primitive signin otp resend user@example.com\n\n"),
        "signin\x00otp\x00resend\x00user@example.com\x00--help" => Some("Resend OTP sign-in code\n\nUSAGE\n  $ primitive signin otp resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend OTP sign-in code\n\n  Resend the verification code for a pending OTP sign-in.\n\nEXAMPLES\n  $ primitive signin otp resend user@example.com\n\n"),
        "signin\x00otp\x00user@example.com\x00--help" => Some("Start OTP sign-in\n\nUSAGE\n  $ primitive signin otp [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign in with\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Start OTP sign-in\n\n  Start email-code sign-in using Primitive's signup/auth OTP flow, send a\n  verification code, and save the pending token locally. Requires a signup code.\n\nEXAMPLES\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nCOMMANDS\n  signin otp confirm  Confirm OTP sign-in\n  signin otp resend   Resend OTP sign-in code\n\n"),
        "signin\x00resend\x00--help" => Some("Resend email-code sign-in code\n\nUSAGE\n  $ primitive signin resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code sign-in code\n\n  Resend the verification code for a pending email-code sign-in.\n\nEXAMPLES\n  $ primitive signin resend user@example.com\n\n"),
        "signin\x00resend\x00user@example.com\x00--help" => Some("Resend email-code sign-in code\n\nUSAGE\n  $ primitive signin resend EMAIL\n\nARGUMENTS\n  EMAIL  Email address used to start OTP sign-in\n\nDESCRIPTION\n  Resend email-code sign-in code\n\n  Resend the verification code for a pending email-code sign-in.\n\nEXAMPLES\n  $ primitive signin resend user@example.com\n\n"),
        "signin\x00user@example.com\x00--help" => Some("Sign in to an existing account\n\nUSAGE\n  $ primitive signin [EMAIL] [--device-name <value>] [--no-browser]\n    [-f] [--accept-terms] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address for email-code sign-in. Omit it to use browser\n           approval.\n\nFLAGS\n  -f, --force                Replace saved credentials or pending email-code\n                             auth state when needed, without first verifying the\n                             existing session\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --no-browser           Do not attempt to open the browser automatically\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Signup code required\n                             to start email-code sign-in\n\nDESCRIPTION\n  Sign in to an existing account\n\n  Sign in or log in to an existing Primitive account and save an org-scoped\n  OAuth session locally.\n\n  Run `primitive signin <email> --signup-code <code> --accept-terms` for\n  email-code sign-in, then `primitive signin confirm <email> <code>`. Run\n  `primitive signin` with no email to use browser approval; `primitive signin\n  browser` is the explicit browser form. `primitive login` supports the same\n  flows with login-shaped commands. `primitive otp <email>` is the shortest\n  email-code auth form. For new account creation, use `primitive signup\n  <email>`.\n\nEXAMPLES\n  $ primitive signin\n\n  $ primitive signin browser\n\n  $ primitive signin --no-browser\n\n  $ primitive signin user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin confirm user@example.com 123456\n\n  $ primitive signin otp user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signin otp confirm user@example.com 123456\n\nTOPICS\n  signin otp  Start OTP sign-in\n\nCOMMANDS\n  signin browser  Sign in with browser approval\n  signin confirm  Confirm email-code sign-in\n  signin otp      Start OTP sign-in\n  signin resend   Resend email-code sign-in code\n\n"),
        "signup\x00--accept-terms\x00--help" => Some("Start account signup\n\nUSAGE\n  $ primitive signup [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign up\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Start account signup\n\n  Start a Primitive account signup, send an email verification code, and save a\n  pending signup token locally.\n\nEXAMPLES\n  $ primitive signup user@example.com\n\n  $ primitive signup user@example.com --accept-terms\n\n  $ primitive signup user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signup confirm user@example.com 123456\n\nCOMMANDS\n  signup confirm      Confirm account signup\n  signup interactive  Run interactive account signup\n  signup resend       Resend signup verification code\n  signup status       Show pending signup status\n\n"),
        "signup\x00--device-name\x00value\x00--help" => Some("Start account signup\n\nUSAGE\n  $ primitive signup [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign up\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Start account signup\n\n  Start a Primitive account signup, send an email verification code, and save a\n  pending signup token locally.\n\nEXAMPLES\n  $ primitive signup user@example.com\n\n  $ primitive signup user@example.com --accept-terms\n\n  $ primitive signup user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signup confirm user@example.com 123456\n\nCOMMANDS\n  signup confirm      Confirm account signup\n  signup interactive  Run interactive account signup\n  signup resend       Resend signup verification code\n  signup status       Show pending signup status\n\n"),
        "signup\x00--force\x00--help" => Some("Start account signup\n\nUSAGE\n  $ primitive signup [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign up\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Start account signup\n\n  Start a Primitive account signup, send an email verification code, and save a\n  pending signup token locally.\n\nEXAMPLES\n  $ primitive signup user@example.com\n\n  $ primitive signup user@example.com --accept-terms\n\n  $ primitive signup user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signup confirm user@example.com 123456\n\nCOMMANDS\n  signup confirm      Confirm account signup\n  signup interactive  Run interactive account signup\n  signup resend       Resend signup verification code\n  signup status       Show pending signup status\n\n"),
        "signup\x00--help" => Some("Start account signup\n\nUSAGE\n  $ primitive signup [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign up\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Start account signup\n\n  Start a Primitive account signup, send an email verification code, and save a\n  pending signup token locally.\n\nEXAMPLES\n  $ primitive signup user@example.com\n\n  $ primitive signup user@example.com --accept-terms\n\n  $ primitive signup user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signup confirm user@example.com 123456\n\nCOMMANDS\n  signup confirm      Confirm account signup\n  signup interactive  Run interactive account signup\n  signup resend       Resend signup verification code\n  signup status       Show pending signup status\n\n"),
        "signup\x00--signup-code\x00123456\x00--help" => Some("Start account signup\n\nUSAGE\n  $ primitive signup [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign up\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Start account signup\n\n  Start a Primitive account signup, send an email verification code, and save a\n  pending signup token locally.\n\nEXAMPLES\n  $ primitive signup user@example.com\n\n  $ primitive signup user@example.com --accept-terms\n\n  $ primitive signup user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signup confirm user@example.com 123456\n\nCOMMANDS\n  signup confirm      Confirm account signup\n  signup interactive  Run interactive account signup\n  signup resend       Resend signup verification code\n  signup status       Show pending signup status\n\n"),
        "signup\x00-f\x00--help" => Some("Start account signup\n\nUSAGE\n  $ primitive signup [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign up\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Start account signup\n\n  Start a Primitive account signup, send an email verification code, and save a\n  pending signup token locally.\n\nEXAMPLES\n  $ primitive signup user@example.com\n\n  $ primitive signup user@example.com --accept-terms\n\n  $ primitive signup user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signup confirm user@example.com 123456\n\nCOMMANDS\n  signup confirm      Confirm account signup\n  signup interactive  Run interactive account signup\n  signup resend       Resend signup verification code\n  signup status       Show pending signup status\n\n"),
        "signup:confirm\x00--code-from-env\x00value\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup:confirm\x00--code-from-file\x00value\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup:confirm\x00--code-from-stdin\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup:confirm\x00--force\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup:confirm\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup:confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup:confirm\x00-f\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup:confirm\x00user@example.com\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup:interactive\x00--accept-terms\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup:interactive\x00--device-name\x00value\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup:interactive\x00--force\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup:interactive\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup:interactive\x00--signup-code\x00123456\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup:interactive\x00-f\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup:resend\x00--help" => Some("Resend signup verification code\n\nUSAGE\n  $ primitive signup resend [EMAIL]\n\nARGUMENTS\n  [EMAIL]  Email address used to start signup. Defaults to the saved pending\n           signup.\n\nDESCRIPTION\n  Resend signup verification code\n\n  Resend the verification code for a pending signup.\n\nEXAMPLES\n  $ primitive signup resend\n\n  $ primitive signup resend user@example.com\n\n"),
        "signup:resend\x00user@example.com\x00--help" => Some("Resend signup verification code\n\nUSAGE\n  $ primitive signup resend [EMAIL]\n\nARGUMENTS\n  [EMAIL]  Email address used to start signup. Defaults to the saved pending\n           signup.\n\nDESCRIPTION\n  Resend signup verification code\n\n  Resend the verification code for a pending signup.\n\nEXAMPLES\n  $ primitive signup resend\n\n  $ primitive signup resend user@example.com\n\n"),
        "signup:status\x00--help" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "signup:status\x00--json\x00--help" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "signup:status\x00user@example.com\x00--help" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "signup\x00confirm\x00--code-from-env\x00value\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup\x00confirm\x00--code-from-file\x00value\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup\x00confirm\x00--code-from-stdin\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup\x00confirm\x00--force\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup\x00confirm\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup\x00confirm\x00--org-id\x00fn_123\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup\x00confirm\x00-f\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup\x00confirm\x00user@example.com\x00--help" => Some("Confirm account signup\n\nUSAGE\n  $ primitive signup confirm EMAIL [CODE] [--code-from-stdin]\n    [--code-from-file <value>] [--code-from-env <value>] [-f] [--org-id <value>]\n\nARGUMENTS\n  EMAIL   Email address used to start signup\n  [CODE]  Verification code from the signup email. Optional when one of\n          --code-from-stdin / --code-from-file / --code-from-env is passed;\n          exactly one source must be set.\n\nFLAGS\n  -f, --force\n      Replace saved credentials after verification\n\n  --code-from-env=<value>\n      Read the verification code from this environment variable. Pair with `read\n      -rs CODE && CODE=\"$CODE\" primitive signup confirm <email> --code-from-env\n      CODE && unset CODE` so the value never appears on the command line or in\n      shell history. Plain `read` creates a shell-local variable that child\n      processes cannot see; the inline `CODE=\"$CODE\"` exports it for just the one\n      command.\n\n  --code-from-file=<value>\n      Read the verification code from a UTF-8 file at this path. Trailing newlines\n      are stripped.\n\n  --code-from-stdin\n      Read the verification code from stdin instead of the positional argument.\n      Use when an agent is constructing the command for the user to run, so the\n      code never enters the agent's prompt context.\n\n  --org-id=<value>\n      Workspace id to target when the email belongs to multiple workspaces\n\nDESCRIPTION\n  Confirm account signup\n\n  Confirm a pending Primitive signup, create an OAuth session, and save CLI\n  credentials locally.\n\nEXAMPLES\n  $ primitive signup confirm user@example.com 123456\n\n  $ primitive signup confirm user@example.com 123456 --org-id 00000000-0000-4000-8000-000000000000\n\n  read -rs CODE && CODE=\"$CODE\" primitive signup confirm user@example.com --code-from-env CODE && unset CODE\n\n  read -rs CODE && printf '%s' \"$CODE\" | primitive signup confirm user@example.com --code-from-stdin && unset CODE\n\n  $ primitive signup confirm user@example.com --code-from-file /run/user/$(id -u)/verification-code\n\n"),
        "signup\x00interactive\x00--accept-terms\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup\x00interactive\x00--device-name\x00value\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup\x00interactive\x00--force\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup\x00interactive\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup\x00interactive\x00--signup-code\x00123456\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup\x00interactive\x00-f\x00--help" => Some("Run interactive account signup\n\nUSAGE\n  $ primitive signup interactive [--accept-terms] [--device-name <value>] [-f]\n    [--signup-code <value>]\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Run interactive account signup\n\n  Run the full signup flow in one interactive terminal session.\n\nEXAMPLES\n  $ primitive signup interactive\n\n"),
        "signup\x00resend\x00--help" => Some("Resend signup verification code\n\nUSAGE\n  $ primitive signup resend [EMAIL]\n\nARGUMENTS\n  [EMAIL]  Email address used to start signup. Defaults to the saved pending\n           signup.\n\nDESCRIPTION\n  Resend signup verification code\n\n  Resend the verification code for a pending signup.\n\nEXAMPLES\n  $ primitive signup resend\n\n  $ primitive signup resend user@example.com\n\n"),
        "signup\x00resend\x00user@example.com\x00--help" => Some("Resend signup verification code\n\nUSAGE\n  $ primitive signup resend [EMAIL]\n\nARGUMENTS\n  [EMAIL]  Email address used to start signup. Defaults to the saved pending\n           signup.\n\nDESCRIPTION\n  Resend signup verification code\n\n  Resend the verification code for a pending signup.\n\nEXAMPLES\n  $ primitive signup resend\n\n  $ primitive signup resend user@example.com\n\n"),
        "signup\x00status\x00--help" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "signup\x00status\x00--json\x00--help" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "signup\x00status\x00user@example.com\x00--help" => Some("Show pending signup status\n\nUSAGE\n  $ primitive signup status [EMAIL] [--json]\n\nARGUMENTS\n  [EMAIL]  Email address expected in the pending signup\n\nFLAGS\n  --json  Print pending signup status as JSON\n\nDESCRIPTION\n  Show pending signup status\n\n  Inspect the locally saved pending Primitive signup state.\n\nEXAMPLES\n  $ primitive signup status\n\n  $ primitive signup status user@example.com\n\n  $ primitive signup status --json\n\n"),
        "signup\x00user@example.com\x00--help" => Some("Start account signup\n\nUSAGE\n  $ primitive signup [EMAIL] [--accept-terms] [--device-name\n    <value>] [-f] [--signup-code <value>]\n\nARGUMENTS\n  [EMAIL]  Email address to sign up\n\nFLAGS\n  -f, --force                Replace saved credentials or pending signup state\n                             when needed\n      --accept-terms         Confirm acceptance of Primitive's Terms of Service\n                             and Privacy Policy\n      --device-name=<value>  Device name used for the created CLI OAuth session\n      --signup-code=<value>  [env: PRIMITIVE_SIGNUP_CODE] Optional signup code.\n                             Omit if you do not have one.\n\nDESCRIPTION\n  Start account signup\n\n  Start a Primitive account signup, send an email verification code, and save a\n  pending signup token locally.\n\nEXAMPLES\n  $ primitive signup user@example.com\n\n  $ primitive signup user@example.com --accept-terms\n\n  $ primitive signup user@example.com --signup-code invite-code --accept-terms\n\n  $ primitive signup confirm user@example.com 123456\n\nCOMMANDS\n  signup confirm      Confirm account signup\n  signup interactive  Run interactive account signup\n  signup resend       Resend signup verification code\n  signup status       Show pending signup status\n\n"),
        "templates" => Some("Browse and install Function templates. Use `primitive templates list-templates`, `primitive templates get-template`, `primitive templates install-template`, and `primitive templates get-template-install`.\n\nUSAGE\n  $ primitive templates COMMAND\n\nCOMMANDS\n  templates get-template          Get a function template\n  templates get-template-install  Get template install status\n  templates install-template      Install a function template\n  templates list-templates        List function templates\n\n"),
        "templates\x00--help" => Some("Browse and install Function templates. Use `primitive templates list-templates`, `primitive templates get-template`, `primitive templates install-template`, and `primitive templates get-template-install`.\n\nUSAGE\n  $ primitive templates COMMAND\n\nCOMMANDS\n  templates get-template          Get a function template\n  templates get-template-install  Get template install status\n  templates install-template      Install a function template\n  templates list-templates        List function templates\n\n"),
        "templates\x00-h" => Some("Browse and install Function templates. Use `primitive templates list-templates`, `primitive templates get-template`, `primitive templates install-template`, and `primitive templates get-template-install`.\n\nUSAGE\n  $ primitive templates COMMAND\n\nCOMMANDS\n  templates get-template          Get a function template\n  templates get-template-install  Get template install status\n  templates install-template      Install a function template\n  templates list-templates        List function templates\n\n"),
        "templates:get-template\x00--api-key\x00prim_test\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates:get-template\x00--envelope\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates:get-template\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates:get-template\x00--id\x00fn_123\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates:get-template\x00--json\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates:get-template\x00--time\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates:get-template-install\x00--api-key\x00prim_test\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates:get-template-install\x00--envelope\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates:get-template-install\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates:get-template-install\x00--id\x00fn_123\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates:get-template-install\x00--json\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates:get-template-install\x00--time\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates:install-template\x00--address\x00value\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:install-template\x00--api-key\x00prim_test\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:install-template\x00--body-file\x00value\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:install-template\x00--domain\x00value\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:install-template\x00--envelope\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:install-template\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:install-template\x00--id\x00fn_123\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:install-template\x00--json\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:install-template\x00--raw-body\x00value\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:install-template\x00--time\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates:list-templates\x00--api-key\x00prim_test\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates:list-templates\x00--cursor\x00value\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates:list-templates\x00--envelope\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates:list-templates\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates:list-templates\x00--json\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates:list-templates\x00--limit\x001\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates:list-templates\x00--q\x00hello\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates:list-templates\x00--tag\x00value\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates:list-templates\x00--time\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates\x00get-template\x00--api-key\x00prim_test\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates\x00get-template\x00--envelope\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates\x00get-template\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates\x00get-template\x00--id\x00fn_123\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates\x00get-template\x00--json\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates\x00get-template\x00--time\x00--help" => Some("Get a function template\n\nUSAGE\n  $ primitive templates get-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template slug from the template manifest.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a function template\n\n  Fetch one approved Function template by slug, including its manifest\n  snapshot and README. The stored source files used for install are not\n  returned.\n\n\n"),
        "templates\x00get-template-install\x00--api-key\x00prim_test\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates\x00get-template-install\x00--envelope\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates\x00get-template-install\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates\x00get-template-install\x00--id\x00fn_123\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates\x00get-template-install\x00--json\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates\x00get-template-install\x00--time\x00--help" => Some("Get template install status\n\nUSAGE\n  $ primitive templates get-template-install --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Template install UUID.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get template install status\n\n  Fetch the current state of a template install. Reads may advance the\n  self-test phase when a reply effect has been observed.\n\n\n"),
        "templates\x00install-template\x00--address\x00value\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00install-template\x00--api-key\x00prim_test\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00install-template\x00--body-file\x00value\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00install-template\x00--domain\x00value\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00install-template\x00--envelope\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00install-template\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00install-template\x00--id\x00fn_123\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00install-template\x00--json\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00install-template\x00--raw-body\x00value\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00install-template\x00--time\x00--help" => Some("Install a function template\n\nUSAGE\n  $ primitive templates install-template --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--address\n    <value>] [--domain <value>]\n\nFLAGS\n  --address=<value>    Localpart to claim on the selected inbound domain.\n                       Defaults to the template slug.\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --domain=<value>     Org domain name to claim the address on. Defaults to the\n                       org's newest active domain.\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Template slug from the template manifest.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Install a function template\n\n  Start a one-shot deploy of an approved deploy-mode Function template.\n  The response returns an install record immediately; poll\n  `GET /templates/installs/{id}` for progress.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  secrets    object  Secret values keyed by manifest secret key.\n  variables  object  Template variable values keyed by manifest variable key.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "templates\x00list-templates\x00--api-key\x00prim_test\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates\x00list-templates\x00--cursor\x00value\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates\x00list-templates\x00--envelope\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates\x00list-templates\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates\x00list-templates\x00--json\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates\x00list-templates\x00--limit\x001\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates\x00list-templates\x00--q\x00hello\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates\x00list-templates\x00--tag\x00value\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "templates\x00list-templates\x00--time\x00--help" => Some("List function templates\n\nUSAGE\n  $ primitive templates list-templates [--api-key <value>] [--time] [--json]\n    [--envelope] [--q <value>] [--tag <value>] [--cursor <value>] [--limit\n    <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --cursor=<value>   Cursor from a previous response `data.next_cursor` value.\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of templates to return. The\n                     server caps this at 100.\n  --q=<value>        Search templates by title, summary, or slug.\n  --tag=<value>      Filter templates by an exact tag.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List function templates\n\n  List approved Function templates available for browsing and\n  installation. Results are cacheable and paginated with\n  `data.next_cursor`.\n\n\n"),
        "threads" => Some("Inspect conversation threads spanning received and sent emails. Prefer `primitive threads get --id <thread-id>`; generated API name `primitive threads get-thread --id <thread-id>` remains available.\n\nUSAGE\n  $ primitive threads COMMAND\n\nCOMMANDS\n  threads get         Get a conversation thread by id\n  threads get-thread  Get a conversation thread by id\n\n"),
        "threads\x00--help" => Some("Inspect conversation threads spanning received and sent emails. Prefer `primitive threads get --id <thread-id>`; generated API name `primitive threads get-thread --id <thread-id>` remains available.\n\nUSAGE\n  $ primitive threads COMMAND\n\nCOMMANDS\n  threads get         Get a conversation thread by id\n  threads get-thread  Get a conversation thread by id\n\n"),
        "threads\x00-h" => Some("Inspect conversation threads spanning received and sent emails. Prefer `primitive threads get --id <thread-id>`; generated API name `primitive threads get-thread --id <thread-id>` remains available.\n\nUSAGE\n  $ primitive threads COMMAND\n\nCOMMANDS\n  threads get         Get a conversation thread by id\n  threads get-thread  Get a conversation thread by id\n\n"),
        "threads:get\x00--api-key\x00prim_test\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get\x00--envelope\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get\x00--id\x00fn_123\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get\x00--json\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get\x00--time\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get-thread\x00--api-key\x00prim_test\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get-thread\x00--envelope\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get-thread\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get-thread\x00--id\x00fn_123\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get-thread\x00--json\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads:get-thread\x00--time\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get\x00--api-key\x00prim_test\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get\x00--envelope\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get\x00--id\x00fn_123\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get\x00--json\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get\x00--time\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get-thread\x00--api-key\x00prim_test\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get-thread\x00--envelope\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get-thread\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get-thread\x00--id\x00fn_123\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get-thread\x00--json\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "threads\x00get-thread\x00--time\x00--help" => Some("Get a conversation thread by id\n\nUSAGE\n  $ primitive threads get-thread --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a conversation thread by id\n\n  Returns a conversation thread: its metadata plus the inbound\n  and outbound messages that belong to it, interleaved in time\n  order (oldest first). A thread spans both received emails and\n  your sends, so an agent can reconstruct an entire back-and-forth\n  from one call instead of walking reply headers.\n\n  Each message carries a `direction` (`inbound` | `outbound`) and\n  an `id`; fetch the full message via `/emails/{id}` or\n  `/sent-emails/{id}` accordingly. Bodies are omitted here to keep\n  the thread view lightweight.\n\n  Discover a thread id from the `thread_id` field on any email or\n  sent-email (list or detail). The message list is capped; compare\n  `message_count` against `messages.length` to detect truncation.\n\n\n"),
        "wake" => Some("Schedule and send wake commands to your own functions over signed email, and manage which senders may wake them. Use `primitive wake schedules list|create|get|update|delete|run`, `primitive wake authorizations list|create|update|delete`, and `primitive wake dispatches list`.\n\nUSAGE\n  $ primitive wake COMMAND\n\nTOPICS\n  wake authorizations  Create a wake authorization\n  wake dispatches      List recent wake dispatches\n  wake schedules       Create a wake schedule\n\nCOMMANDS\n  wake create-wake-authorization  Create a wake authorization\n  wake create-wake-schedule       Create a wake schedule\n  wake delete-wake-authorization  Delete a wake authorization\n  wake delete-wake-schedule       Delete a wake schedule\n  wake get-wake-schedule          Get a wake schedule\n  wake list-wake-authorizations   List wake authorizations\n  wake list-wake-dispatches       List recent wake dispatches\n  wake list-wake-schedules        List wake schedules\n  wake run-wake-schedule          Run a wake schedule now\n  wake update-wake-authorization  Update a wake authorization\n  wake update-wake-schedule       Update a wake schedule\n\n"),
        "wake\x00--help" => Some("Schedule and send wake commands to your own functions over signed email, and manage which senders may wake them. Use `primitive wake schedules list|create|get|update|delete|run`, `primitive wake authorizations list|create|update|delete`, and `primitive wake dispatches list`.\n\nUSAGE\n  $ primitive wake COMMAND\n\nTOPICS\n  wake authorizations  Create a wake authorization\n  wake dispatches      List recent wake dispatches\n  wake schedules       Create a wake schedule\n\nCOMMANDS\n  wake create-wake-authorization  Create a wake authorization\n  wake create-wake-schedule       Create a wake schedule\n  wake delete-wake-authorization  Delete a wake authorization\n  wake delete-wake-schedule       Delete a wake schedule\n  wake get-wake-schedule          Get a wake schedule\n  wake list-wake-authorizations   List wake authorizations\n  wake list-wake-dispatches       List recent wake dispatches\n  wake list-wake-schedules        List wake schedules\n  wake run-wake-schedule          Run a wake schedule now\n  wake update-wake-authorization  Update a wake authorization\n  wake update-wake-schedule       Update a wake schedule\n\n"),
        "wake\x00-h" => Some("Schedule and send wake commands to your own functions over signed email, and manage which senders may wake them. Use `primitive wake schedules list|create|get|update|delete|run`, `primitive wake authorizations list|create|update|delete`, and `primitive wake dispatches list`.\n\nUSAGE\n  $ primitive wake COMMAND\n\nTOPICS\n  wake authorizations  Create a wake authorization\n  wake dispatches      List recent wake dispatches\n  wake schedules       Create a wake schedule\n\nCOMMANDS\n  wake create-wake-authorization  Create a wake authorization\n  wake create-wake-schedule       Create a wake schedule\n  wake delete-wake-authorization  Delete a wake authorization\n  wake delete-wake-schedule       Delete a wake schedule\n  wake get-wake-schedule          Get a wake schedule\n  wake list-wake-authorizations   List wake authorizations\n  wake list-wake-dispatches       List recent wake dispatches\n  wake list-wake-schedules        List wake schedules\n  wake run-wake-schedule          Run a wake schedule now\n  wake update-wake-authorization  Update a wake authorization\n  wake update-wake-schedule       Update a wake schedule\n\n"),
        "wake:authorizations" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "wake:authorizations\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "wake:authorizations\x00-h" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "wake:authorizations:create\x00--address\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake:authorizations:create\x00--api-key\x00prim_test\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake:authorizations:create\x00--command\x00send\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake:authorizations:create\x00--domain\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake:authorizations:create\x00--endpoint\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake:authorizations:create\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake:authorizations:create\x00--note\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake:authorizations:create\x00--time\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake:authorizations:delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "wake:authorizations:delete\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "wake:authorizations:delete\x00--time\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "wake:authorizations:delete\x00fn_123\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "wake:authorizations:list\x00--api-key\x00prim_test\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "wake:authorizations:list\x00--endpoint\x00value\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "wake:authorizations:list\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "wake:authorizations:list\x00--time\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "wake:authorizations:update\x00--api-key\x00prim_test\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake:authorizations:update\x00--enabled\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake:authorizations:update\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake:authorizations:update\x00--no-enabled\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake:authorizations:update\x00--time\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake:authorizations:update\x00fn_123\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake:create-wake-authorization\x00--allowed-sender-address\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--allowed-sender-domain\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--api-key\x00prim_test\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--body-file\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--envelope\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--json\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--note\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--raw-body\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--recipient-endpoint-id\x00fn_123\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-authorization\x00--time\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--body-file\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--command\x00send\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--cron-expr\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--envelope\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--from-address\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--json\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--note\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--raw-body\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--target-address\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--time\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:create-wake-schedule\x00--timezone\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:delete-wake-authorization\x00--api-key\x00prim_test\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake:delete-wake-authorization\x00--envelope\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake:delete-wake-authorization\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake:delete-wake-authorization\x00--id\x00fn_123\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake:delete-wake-authorization\x00--json\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake:delete-wake-authorization\x00--time\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake:delete-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake:delete-wake-schedule\x00--envelope\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake:delete-wake-schedule\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake:delete-wake-schedule\x00--id\x00fn_123\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake:delete-wake-schedule\x00--json\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake:delete-wake-schedule\x00--time\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake:dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "wake:dispatches\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "wake:dispatches\x00-h" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "wake:dispatches:list\x00--api-key\x00prim_test\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "wake:dispatches:list\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "wake:dispatches:list\x00--limit\x001\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "wake:dispatches:list\x00--time\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "wake:get-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake:get-wake-schedule\x00--envelope\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake:get-wake-schedule\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake:get-wake-schedule\x00--id\x00fn_123\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake:get-wake-schedule\x00--json\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake:get-wake-schedule\x00--time\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake:list-wake-authorizations\x00--api-key\x00prim_test\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake:list-wake-authorizations\x00--envelope\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake:list-wake-authorizations\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake:list-wake-authorizations\x00--json\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake:list-wake-authorizations\x00--recipient-endpoint-id\x00fn_123\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake:list-wake-authorizations\x00--time\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake:list-wake-dispatches\x00--api-key\x00prim_test\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake:list-wake-dispatches\x00--envelope\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake:list-wake-dispatches\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake:list-wake-dispatches\x00--json\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake:list-wake-dispatches\x00--limit\x001\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake:list-wake-dispatches\x00--time\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake:list-wake-schedules\x00--api-key\x00prim_test\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake:list-wake-schedules\x00--envelope\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake:list-wake-schedules\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake:list-wake-schedules\x00--json\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake:list-wake-schedules\x00--time\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake:run-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake:run-wake-schedule\x00--envelope\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake:run-wake-schedule\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake:run-wake-schedule\x00--id\x00fn_123\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake:run-wake-schedule\x00--json\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake:run-wake-schedule\x00--time\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake:schedules" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "wake:schedules\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "wake:schedules\x00-h" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "wake:schedules:create\x00--api-key\x00prim_test\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:create\x00--args\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:create\x00--command\x00send\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:create\x00--cron\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:create\x00--from\x00user@example.com\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:create\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:create\x00--note\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:create\x00--time\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:create\x00--timezone\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:create\x00--to\x00user@example.com\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake:schedules:delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "wake:schedules:delete\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "wake:schedules:delete\x00--time\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "wake:schedules:delete\x00fn_123\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "wake:schedules:get\x00--api-key\x00prim_test\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "wake:schedules:get\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "wake:schedules:get\x00--time\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "wake:schedules:get\x00fn_123\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "wake:schedules:list\x00--api-key\x00prim_test\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "wake:schedules:list\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "wake:schedules:list\x00--time\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "wake:schedules:run\x00--api-key\x00prim_test\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "wake:schedules:run\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "wake:schedules:run\x00--time\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "wake:schedules:run\x00fn_123\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "wake:schedules:update\x00--api-key\x00prim_test\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--args\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--command\x00send\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--cron\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--enabled\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--from\x00user@example.com\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--no-enabled\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--note\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--time\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--timezone\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00--to\x00user@example.com\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:schedules:update\x00fn_123\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake:update-wake-authorization\x00--api-key\x00prim_test\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-authorization\x00--body-file\x00value\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-authorization\x00--enabled\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-authorization\x00--envelope\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-authorization\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-authorization\x00--id\x00fn_123\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-authorization\x00--json\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-authorization\x00--no-enabled\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-authorization\x00--raw-body\x00value\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-authorization\x00--time\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake:update-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--body-file\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--command\x00send\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--cron-expr\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--enabled\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--envelope\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--from-address\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--id\x00fn_123\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--json\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--no-enabled\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--note\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--raw-body\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--target-address\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--time\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake:update-wake-schedule\x00--timezone\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00authorizations" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "wake\x00authorizations\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "wake\x00authorizations\x00-h" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations COMMAND\n\nCOMMANDS\n  wake authorizations create  Create a wake authorization\n  wake authorizations delete  Delete a wake authorization\n  wake authorizations list    List wake authorizations\n  wake authorizations update  Update a wake authorization\n\n"),
        "wake\x00authorizations\x00create\x00--address\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake\x00authorizations\x00create\x00--api-key\x00prim_test\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake\x00authorizations\x00create\x00--command\x00send\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake\x00authorizations\x00create\x00--domain\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake\x00authorizations\x00create\x00--endpoint\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake\x00authorizations\x00create\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake\x00authorizations\x00create\x00--note\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake\x00authorizations\x00create\x00--time\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake authorizations create --endpoint <value> --domain <value> [--api-key\n    <value>] [--time] [--address <value>] [--command <value>...] [--note\n    <value>]\n\nFLAGS\n  --address=<value>     Optional specific allowed sender address\n  --api-key=<value>     [env: PRIMITIVE_API_KEY] Primitive API key override\n                        (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                        credentials)\n  --command=<value>...  Allowed command (repeatable); omit for any\n  --domain=<value>      (required) Allowed sender domain (fully-qualified)\n  --endpoint=<value>    (required) Target endpoint id (recipient_endpoint_id)\n  --note=<value>        Optional note\n  --time                Print the wall-clock duration of this command to stderr\n                        after it completes (e.g. `[time: 1.34s]`). Useful for\n                        measuring `--wait` send latency, comparing CLI overhead,\n                        or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally an address and command set) permission\n  to wake a target function.\n\nEXAMPLES\n  $ primitive wake authorizations create --endpoint <endpoint-id> --domain agents.acme.dev --command process_inbox\n\n"),
        "wake\x00authorizations\x00delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "wake\x00authorizations\x00delete\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "wake\x00authorizations\x00delete\x00--time\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "wake\x00authorizations\x00delete\x00fn_123\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake authorizations delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  Delete a wake authorization by id.\n\nEXAMPLES\n  $ primitive wake authorizations delete <authorization-id>\n\n"),
        "wake\x00authorizations\x00list\x00--api-key\x00prim_test\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "wake\x00authorizations\x00list\x00--endpoint\x00value\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "wake\x00authorizations\x00list\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "wake\x00authorizations\x00list\x00--time\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake authorizations list [--api-key <value>] [--time] [--endpoint\n    <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --endpoint=<value>  Only return grants for this target endpoint id\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  List wake authorizations (the per-target allowlist). Optionally filter by\n  target endpoint.\n\nEXAMPLES\n  $ primitive wake authorizations list\n\n  $ primitive wake authorizations list --endpoint <endpoint-id>\n\n"),
        "wake\x00authorizations\x00update\x00--api-key\x00prim_test\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake\x00authorizations\x00update\x00--enabled\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake\x00authorizations\x00update\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake\x00authorizations\x00update\x00--no-enabled\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake\x00authorizations\x00update\x00--time\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake\x00authorizations\x00update\x00fn_123\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake authorizations update ID [--api-key <value>] [--time]\n  [--enabled]\n\nARGUMENTS\n  ID  Wake authorization id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --[no-]enabled     Enable the authorization (use --no-enabled to disable)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Set a wake authorization's enabled state. Pass --enabled or --no-enabled.\n\nEXAMPLES\n  $ primitive wake authorizations update <authorization-id> --no-enabled\n\n  $ primitive wake authorizations update <authorization-id> --enabled\n\n"),
        "wake\x00create-wake-authorization\x00--allowed-sender-address\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--allowed-sender-domain\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--api-key\x00prim_test\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--body-file\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--envelope\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--json\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--note\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--raw-body\x00value\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--recipient-endpoint-id\x00fn_123\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-authorization\x00--time\x00--help" => Some("Create a wake authorization\n\nUSAGE\n  $ primitive wake create-wake-authorization [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>]\n    [--allowed-sender-domain <value>] [--recipient-endpoint-id <value>]\n    [--allowed-sender-address <value>] [--note <value>]\n\nFLAGS\n  --allowed-sender-address=<value>  Optional specific sender address to pin the\n                                    grant to.\n  --allowed-sender-domain=<value>   Fully-qualified sender domain (at least two\n                                    labels).\n  --api-key=<value>                 [env: PRIMITIVE_API_KEY] Primitive API key\n                                    override (defaults to PRIMITIVE_API_KEY or\n                                    saved OAuth login credentials)\n  --body-file=<value>               Path to a JSON file used as the request\n                                    body. Same role as --raw-body for callers\n                                    passing a saved payload.\n  --envelope                        Print the full response envelope, including\n                                    pagination metadata such as meta.cursor.\n                                    Defaults to printing only the data payload\n                                    for backward compatibility.\n  --json                            Accepted for consistency with task-focused\n                                    commands. Generated API commands already\n                                    print JSON by default.\n  --note=<value>                    note\n  --raw-body=<value>                Full request body as raw JSON. Escape hatch\n                                    for nested or complex fields (e.g. arrays);\n                                    prefer per-field flags (e.g. --to, --from,\n                                    --body-text) when available.\n  --recipient-endpoint-id=<value>   recipient_endpoint_id\n  --time                            Print the wall-clock duration of this\n                                    command to stderr after it completes (e.g.\n                                    `[time: 1.34s]`). Useful for measuring\n                                    `--wait` send latency, comparing CLI\n                                    overhead, or capturing timing in scripts.\n\nDESCRIPTION\n  Create a wake authorization\n\n  Grant a sender domain (and optionally a specific address and command set)\n  permission to wake a target function. The domain must be fully-qualified.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  allowed_commands  array?  Optional command allowlist; null = any command.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--body-file\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--command\x00send\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--cron-expr\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--envelope\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--from-address\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--json\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--note\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--raw-body\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--target-address\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--time\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00create-wake-schedule\x00--timezone\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake create-wake-schedule [--api-key <value>] [--time] [--json]\n    [--envelope] [--raw-body <value>] [--body-file <value>] [--command <value>]\n    [--cron-expr <value>] [--from-address <value>] [--target-address <value>]\n    [--note <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    Sending identity (must be a domain the org can\n                            sign).\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  Your function address (must differ from\n                            from_address).\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your\n  own function addresses. `from` and `to` must differ (no self-dispatch);\n  the cron expression and IANA timezone are validated and the first fire\n  time is computed without firing immediately.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object  Optional JSON object passed through to the woken function.\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00delete-wake-authorization\x00--api-key\x00prim_test\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake\x00delete-wake-authorization\x00--envelope\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake\x00delete-wake-authorization\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake\x00delete-wake-authorization\x00--id\x00fn_123\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake\x00delete-wake-authorization\x00--json\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake\x00delete-wake-authorization\x00--time\x00--help" => Some("Delete a wake authorization\n\nUSAGE\n  $ primitive wake delete-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake authorization\n\n  DELETE /wake/authorizations/{id}\n\n"),
        "wake\x00delete-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake\x00delete-wake-schedule\x00--envelope\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake\x00delete-wake-schedule\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake\x00delete-wake-schedule\x00--id\x00fn_123\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake\x00delete-wake-schedule\x00--json\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake\x00delete-wake-schedule\x00--time\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake delete-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  DELETE /wake/schedules/{id}\n\n"),
        "wake\x00dispatches" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "wake\x00dispatches\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "wake\x00dispatches\x00-h" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches COMMAND\n\nCOMMANDS\n  wake dispatches list  List recent wake dispatches\n\n"),
        "wake\x00dispatches\x00list\x00--api-key\x00prim_test\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "wake\x00dispatches\x00list\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "wake\x00dispatches\x00list\x00--limit\x001\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "wake\x00dispatches\x00list\x00--time\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake dispatches list [--api-key <value>] [--time] [--limit\n  <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --limit=<value>    Max rows to return (1-200, default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  List recent wake.dispatch interactions for the org (audit).\n\nEXAMPLES\n  $ primitive wake dispatches list\n\n  $ primitive wake dispatches list --limit 20\n\n"),
        "wake\x00get-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake\x00get-wake-schedule\x00--envelope\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake\x00get-wake-schedule\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake\x00get-wake-schedule\x00--id\x00fn_123\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake\x00get-wake-schedule\x00--json\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake\x00get-wake-schedule\x00--time\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake get-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  GET /wake/schedules/{id}\n\n"),
        "wake\x00list-wake-authorizations\x00--api-key\x00prim_test\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake\x00list-wake-authorizations\x00--envelope\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake\x00list-wake-authorizations\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake\x00list-wake-authorizations\x00--json\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake\x00list-wake-authorizations\x00--recipient-endpoint-id\x00fn_123\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake\x00list-wake-authorizations\x00--time\x00--help" => Some("List wake authorizations\n\nUSAGE\n  $ primitive wake list-wake-authorizations [--api-key <value>] [--time] [--json]\n    [--envelope] [--recipient-endpoint-id <value>]\n\nFLAGS\n  --api-key=<value>                [env: PRIMITIVE_API_KEY] Primitive API key\n                                   override (defaults to PRIMITIVE_API_KEY or\n                                   saved OAuth login credentials)\n  --envelope                       Print the full response envelope, including\n                                   pagination metadata such as meta.cursor.\n                                   Defaults to printing only the data payload\n                                   for backward compatibility.\n  --json                           Accepted for consistency with task-focused\n                                   commands. Generated API commands already\n                                   print JSON by default.\n  --recipient-endpoint-id=<value>  Only return grants for this target endpoint\n  --time                           Print the wall-clock duration of this command\n                                   to stderr after it completes (e.g. `[time:\n                                   1.34s]`). Useful for measuring `--wait` send\n                                   latency, comparing CLI overhead, or capturing\n                                   timing in scripts.\n\nDESCRIPTION\n  List wake authorizations\n\n  Returns the per-target allowlist grants that authorize which senders may\n  wake a function. Optionally filter by the target endpoint.\n\n\n"),
        "wake\x00list-wake-dispatches\x00--api-key\x00prim_test\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake\x00list-wake-dispatches\x00--envelope\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake\x00list-wake-dispatches\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake\x00list-wake-dispatches\x00--json\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake\x00list-wake-dispatches\x00--limit\x001\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake\x00list-wake-dispatches\x00--time\x00--help" => Some("List recent wake dispatches\n\nUSAGE\n  $ primitive wake list-wake-dispatches [--api-key <value>] [--time] [--json]\n    [--envelope] [--limit <value>]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --limit=<value>    [default: 50] Maximum number of rows to return (1-200,\n                     default 50)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List recent wake dispatches\n\n  Read-only audit of recent wake.dispatch interactions for the org.\n\n"),
        "wake\x00list-wake-schedules\x00--api-key\x00prim_test\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake\x00list-wake-schedules\x00--envelope\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake\x00list-wake-schedules\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake\x00list-wake-schedules\x00--json\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake\x00list-wake-schedules\x00--time\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake list-wake-schedules [--api-key <value>] [--time] [--json]\n    [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  Returns the org's wake.dispatch schedules.\n\n"),
        "wake\x00run-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake\x00run-wake-schedule\x00--envelope\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake\x00run-wake-schedule\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake\x00run-wake-schedule\x00--id\x00fn_123\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake\x00run-wake-schedule\x00--json\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake\x00run-wake-schedule\x00--time\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake run-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Resource UUID\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire the schedule immediately, sending one wake.dispatch via the same\n  signed-send path as a scheduled fire. Does not change the schedule's next\n  fire time.\n\n\n"),
        "wake\x00schedules" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "wake\x00schedules\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "wake\x00schedules\x00-h" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules COMMAND\n\nCOMMANDS\n  wake schedules create  Create a wake schedule\n  wake schedules delete  Delete a wake schedule\n  wake schedules get     Get a wake schedule\n  wake schedules list    List wake schedules\n  wake schedules run     Run a wake schedule now\n  wake schedules update  Update a wake schedule\n\n"),
        "wake\x00schedules\x00create\x00--api-key\x00prim_test\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00create\x00--args\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00create\x00--command\x00send\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00create\x00--cron\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00create\x00--from\x00user@example.com\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00create\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00create\x00--note\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00create\x00--time\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00create\x00--timezone\x00value\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00create\x00--to\x00user@example.com\x00--help" => Some("Create a wake schedule\n\nUSAGE\n  $ primitive wake schedules create --from <value> --to <value> --command <value>\n    --cron <value> [--api-key <value>] [--time] [--timezone <value>] [--args\n    <value>] [--note <value>]\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   (required) Wake command name\n  --cron=<value>      (required) 5-field cron expression\n  --from=<value>      (required) Sending identity address (must differ from\n                      --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone (default UTC)\n  --to=<value>        (required) Target function address the wake is delivered\n                      to\n\nDESCRIPTION\n  Create a wake schedule\n\n  Create a cron schedule that sends a wake.dispatch command to one of your own\n  function addresses.\n\nEXAMPLES\n  $ primitive wake schedules create --from scheduler@acme.dev --to alice@acme.dev --command process_inbox --cron \"0 9 * * *\" --timezone America/New_York\n\n"),
        "wake\x00schedules\x00delete\x00--api-key\x00prim_test\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "wake\x00schedules\x00delete\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "wake\x00schedules\x00delete\x00--time\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "wake\x00schedules\x00delete\x00fn_123\x00--help" => Some("Delete a wake schedule\n\nUSAGE\n  $ primitive wake schedules delete ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Delete a wake schedule\n\n  Delete a wake schedule by id. Future fires stop; past dispatches are kept.\n\nEXAMPLES\n  $ primitive wake schedules delete <schedule-id>\n\n"),
        "wake\x00schedules\x00get\x00--api-key\x00prim_test\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "wake\x00schedules\x00get\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "wake\x00schedules\x00get\x00--time\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "wake\x00schedules\x00get\x00fn_123\x00--help" => Some("Get a wake schedule\n\nUSAGE\n  $ primitive wake schedules get ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Get a wake schedule\n\n  Get a single wake schedule by id.\n\nEXAMPLES\n  $ primitive wake schedules get <schedule-id>\n\n"),
        "wake\x00schedules\x00list\x00--api-key\x00prim_test\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "wake\x00schedules\x00list\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "wake\x00schedules\x00list\x00--time\x00--help" => Some("List wake schedules\n\nUSAGE\n  $ primitive wake schedules list [--api-key <value>] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  List wake schedules\n\n  List the org's wake.dispatch schedules.\n\nEXAMPLES\n  $ primitive wake schedules list\n\n"),
        "wake\x00schedules\x00run\x00--api-key\x00prim_test\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "wake\x00schedules\x00run\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "wake\x00schedules\x00run\x00--time\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "wake\x00schedules\x00run\x00fn_123\x00--help" => Some("Run a wake schedule now\n\nUSAGE\n  $ primitive wake schedules run ID [--api-key <value>] [--time]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Run a wake schedule now\n\n  Fire a wake schedule immediately. Does not change the next fire time.\n\nEXAMPLES\n  $ primitive wake schedules run <schedule-id>\n\n"),
        "wake\x00schedules\x00update\x00--api-key\x00prim_test\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--args\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--command\x00send\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--cron\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--enabled\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--from\x00user@example.com\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--no-enabled\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--note\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--time\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--timezone\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00--to\x00user@example.com\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00schedules\x00update\x00fn_123\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake schedules update ID [--api-key <value>] [--time] [--from\n    <value>] [--to <value>] [--command <value>] [--cron <value>] [--timezone\n    <value>] [--args <value>] [--note <value>] [--enabled]\n\nARGUMENTS\n  ID  Wake schedule id (UUID)\n\nFLAGS\n  --api-key=<value>   [env: PRIMITIVE_API_KEY] Primitive API key override\n                      (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                      credentials)\n  --args=<value>      Args as a JSON object\n  --command=<value>   Wake command name\n  --cron=<value>      5-field cron expression\n  --[no-]enabled      Enable the schedule (use --no-enabled to pause)\n  --from=<value>      Sending identity address (must differ from --to)\n  --note=<value>      Optional note\n  --time              Print the wall-clock duration of this command to stderr\n                      after it completes (e.g. `[time: 1.34s]`). Useful for\n                      measuring `--wait` send latency, comparing CLI overhead,\n                      or capturing timing in scripts.\n  --timezone=<value>  IANA timezone\n  --to=<value>        Target function address the wake is delivered to\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a wake schedule's command, args, cadence, addresses, note, or enabled\n  state.\n\nEXAMPLES\n  $ primitive wake schedules update <schedule-id> --cron \"*/15 * * * *\"\n\n  $ primitive wake schedules update <schedule-id> --no-enabled\n\n"),
        "wake\x00update-wake-authorization\x00--api-key\x00prim_test\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-authorization\x00--body-file\x00value\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-authorization\x00--enabled\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-authorization\x00--envelope\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-authorization\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-authorization\x00--id\x00fn_123\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-authorization\x00--json\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-authorization\x00--no-enabled\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-authorization\x00--raw-body\x00value\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-authorization\x00--time\x00--help" => Some("Update a wake authorization\n\nUSAGE\n  $ primitive wake update-wake-authorization --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--enabled]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --body-file=<value>  Path to a JSON file used as the request body. Same role\n                       as --raw-body for callers passing a saved payload.\n  --[no-]enabled       enabled\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --id=<value>         (required) Resource UUID\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --raw-body=<value>   Full request body as raw JSON. Escape hatch for nested or\n                       complex fields (e.g. arrays); prefer per-field flags\n                       (e.g. --to, --from, --body-text) when available.\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  Update a wake authorization\n\n  Toggle a wake authorization's enabled state.\n\n"),
        "wake\x00update-wake-schedule\x00--api-key\x00prim_test\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--body-file\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--command\x00send\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--cron-expr\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--enabled\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--envelope\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--from-address\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--id\x00fn_123\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--json\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--no-enabled\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--note\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--raw-body\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--target-address\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--time\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "wake\x00update-wake-schedule\x00--timezone\x00value\x00--help" => Some("Update a wake schedule\n\nUSAGE\n  $ primitive wake update-wake-schedule --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope] [--raw-body <value>] [--body-file <value>] [--command\n    <value>] [--cron-expr <value>] [--enabled] [--from-address <value>] [--note\n    <value>] [--target-address <value>] [--timezone <value>]\n\nFLAGS\n  --api-key=<value>         [env: PRIMITIVE_API_KEY] Primitive API key override\n                            (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                            credentials)\n  --body-file=<value>       Path to a JSON file used as the request body. Same\n                            role as --raw-body for callers passing a saved\n                            payload.\n  --command=<value>         command\n  --cron-expr=<value>       cron_expr\n  --[no-]enabled            enabled\n  --envelope                Print the full response envelope, including\n                            pagination metadata such as meta.cursor. Defaults to\n                            printing only the data payload for backward\n                            compatibility.\n  --from-address=<value>    from_address\n  --id=<value>              (required) Resource UUID\n  --json                    Accepted for consistency with task-focused commands.\n                            Generated API commands already print JSON by\n                            default.\n  --note=<value>            note\n  --raw-body=<value>        Full request body as raw JSON. Escape hatch for\n                            nested or complex fields (e.g. arrays); prefer\n                            per-field flags (e.g. --to, --from, --body-text)\n                            when available.\n  --target-address=<value>  target_address\n  --time                    Print the wall-clock duration of this command to\n                            stderr after it completes (e.g. `[time: 1.34s]`).\n                            Useful for measuring `--wait` send latency,\n                            comparing CLI overhead, or capturing timing in\n                            scripts.\n  --timezone=<value>        timezone\n\nDESCRIPTION\n  Update a wake schedule\n\n  Update a schedule's command, args, cadence, addresses, note, or enabled\n  state. Changing the cadence (or re-enabling) recomputes the next fire time.\n\n\n  Body fields requiring --raw-body JSON (these are not exposed as flags):\n  args  object\n  (* = required. Scalar body fields are exposed as individual --flag-name flags;\n  see FLAGS above.)\n\n"),
        "webhook-deliveries" => Some("View and replay webhook delivery attempts. Prefer `primitive webhook-deliveries list` and `primitive webhook-deliveries replay`.\n\nUSAGE\n  $ primitive webhook-deliveries COMMAND\n\nCOMMANDS\n  webhook-deliveries list             List webhook deliveries\n  webhook-deliveries list-deliveries  List webhook deliveries\n  webhook-deliveries replay           Replay a webhook delivery\n  webhook-deliveries replay-delivery  Replay a webhook delivery\n\n"),
        "webhook-deliveries\x00--help" => Some("View and replay webhook delivery attempts. Prefer `primitive webhook-deliveries list` and `primitive webhook-deliveries replay`.\n\nUSAGE\n  $ primitive webhook-deliveries COMMAND\n\nCOMMANDS\n  webhook-deliveries list             List webhook deliveries\n  webhook-deliveries list-deliveries  List webhook deliveries\n  webhook-deliveries replay           Replay a webhook delivery\n  webhook-deliveries replay-delivery  Replay a webhook delivery\n\n"),
        "webhook-deliveries\x00-h" => Some("View and replay webhook delivery attempts. Prefer `primitive webhook-deliveries list` and `primitive webhook-deliveries replay`.\n\nUSAGE\n  $ primitive webhook-deliveries COMMAND\n\nCOMMANDS\n  webhook-deliveries list             List webhook deliveries\n  webhook-deliveries list-deliveries  List webhook deliveries\n  webhook-deliveries replay           Replay a webhook delivery\n  webhook-deliveries replay-delivery  Replay a webhook delivery\n\n"),
        "webhook-deliveries:list\x00--api-key\x00prim_test\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--cursor\x00value\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--email-id\x00user@example.com\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--envelope\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--json\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--limit\x001\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--status\x00pending\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list\x00--time\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--api-key\x00prim_test\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--cursor\x00value\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--email-id\x00user@example.com\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--envelope\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--json\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--limit\x001\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--status\x00pending\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:list-deliveries\x00--time\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries:replay\x00--api-key\x00prim_test\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay\x00--envelope\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay\x00--id\x00fn_123\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay\x00--json\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay\x00--time\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay-delivery\x00--api-key\x00prim_test\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay-delivery\x00--envelope\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay-delivery\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay-delivery\x00--id\x00fn_123\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay-delivery\x00--json\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries:replay-delivery\x00--time\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00list\x00--api-key\x00prim_test\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--cursor\x00value\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--email-id\x00user@example.com\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--envelope\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--json\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--limit\x001\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--status\x00pending\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list\x00--time\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--api-key\x00prim_test\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--cursor\x00value\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--date-from\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--date-to\x002026-01-01T00:00:00Z\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--email-id\x00user@example.com\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--envelope\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--json\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--limit\x001\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--status\x00pending\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00list-deliveries\x00--time\x00--help" => Some("List webhook deliveries\n\nUSAGE\n  $ primitive webhook-deliveries list-deliveries [--api-key <value>] [--time] [--json]\n    [--envelope] [--cursor <value>] [--limit <value>] [--email-id <value>]\n    [--status pending|delivered|header_confirmed|failed] [--date-from <value>]\n    [--date-to <value>]\n\nFLAGS\n  --api-key=<value>    [env: PRIMITIVE_API_KEY] Primitive API key override\n                       (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                       credentials)\n  --cursor=<value>     Pagination cursor from a previous response's\n                       `meta.cursor` field.\n                       Format: `{ISO-datetime}|{id}`\n  --date-from=<value>  Filter deliveries created on or after this timestamp\n  --date-to=<value>    Filter deliveries created on or before this timestamp\n  --email-id=<value>   Filter by email ID\n  --envelope           Print the full response envelope, including pagination\n                       metadata such as meta.cursor. Defaults to printing only\n                       the data payload for backward compatibility.\n  --json               Accepted for consistency with task-focused commands.\n                       Generated API commands already print JSON by default.\n  --limit=<value>      [default: 50] Number of results per page\n  --status=<option>    Filter by delivery status\n                       <options: pending|delivered|header_confirmed|failed>\n  --time               Print the wall-clock duration of this command to stderr\n                       after it completes (e.g. `[time: 1.34s]`). Useful for\n                       measuring `--wait` send latency, comparing CLI overhead,\n                       or capturing timing in scripts.\n\nDESCRIPTION\n  List webhook deliveries\n\n  Returns a paginated list of webhook delivery attempts. Each delivery\n  includes a nested `email` object with sender, recipient, and subject.\n\n\n"),
        "webhook-deliveries\x00replay\x00--api-key\x00prim_test\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay\x00--envelope\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay\x00--id\x00fn_123\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay\x00--json\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay\x00--time\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay-delivery\x00--api-key\x00prim_test\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay-delivery\x00--envelope\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay-delivery\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay-delivery\x00--id\x00fn_123\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay-delivery\x00--json\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "webhook-deliveries\x00replay-delivery\x00--time\x00--help" => Some("Replay a webhook delivery\n\nUSAGE\n  $ primitive webhook-deliveries replay-delivery --id <value> [--api-key <value>] [--time]\n    [--json] [--envelope]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --envelope         Print the full response envelope, including pagination\n                     metadata such as meta.cursor. Defaults to printing only the\n                     data payload for backward compatibility.\n  --id=<value>       (required) Delivery ID (numeric)\n  --json             Accepted for consistency with task-focused commands.\n                     Generated API commands already print JSON by default.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Replay a webhook delivery\n\n  Re-sends the stored webhook payload from a previous delivery attempt.\n  If the original endpoint is still active, it is targeted. If the\n  original endpoint was deleted, the oldest active endpoint is used.\n  Deactivated endpoints cannot be replayed to. Rate limited per-org,\n  sharing an org-wide budget with email replays.\n\n\n"),
        "whoami\x00--api-key\x00prim_test\x00--help" => Some("Print the authenticated account (credentials smoke test)\n\nUSAGE\n  $ primitive whoami [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the full account JSON response. Default output hides\n                     setup and billing internals.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the authenticated account (credentials smoke test)\n\n  Print the account currently authenticated by saved OAuth credentials or an\n  explicit API key. Useful as a credentials smoke test: confirms auth is live\n  and shows which account it belongs to.\n\n  The default output is a concise human summary. Pass --json only when a script\n  intentionally needs the full /account response.\n\nEXAMPLES\n  $ primitive whoami\n\n  $ primitive whoami --api-key prim_...\n\n  $ primitive whoami --json | jq .id\n\n"),
        "whoami\x00--help" => Some("Print the authenticated account (credentials smoke test)\n\nUSAGE\n  $ primitive whoami [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the full account JSON response. Default output hides\n                     setup and billing internals.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the authenticated account (credentials smoke test)\n\n  Print the account currently authenticated by saved OAuth credentials or an\n  explicit API key. Useful as a credentials smoke test: confirms auth is live\n  and shows which account it belongs to.\n\n  The default output is a concise human summary. Pass --json only when a script\n  intentionally needs the full /account response.\n\nEXAMPLES\n  $ primitive whoami\n\n  $ primitive whoami --api-key prim_...\n\n  $ primitive whoami --json | jq .id\n\n"),
        "whoami\x00--json\x00--help" => Some("Print the authenticated account (credentials smoke test)\n\nUSAGE\n  $ primitive whoami [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the full account JSON response. Default output hides\n                     setup and billing internals.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the authenticated account (credentials smoke test)\n\n  Print the account currently authenticated by saved OAuth credentials or an\n  explicit API key. Useful as a credentials smoke test: confirms auth is live\n  and shows which account it belongs to.\n\n  The default output is a concise human summary. Pass --json only when a script\n  intentionally needs the full /account response.\n\nEXAMPLES\n  $ primitive whoami\n\n  $ primitive whoami --api-key prim_...\n\n  $ primitive whoami --json | jq .id\n\n"),
        "whoami\x00--time\x00--help" => Some("Print the authenticated account (credentials smoke test)\n\nUSAGE\n  $ primitive whoami [--api-key <value>] [--json] [--time]\n\nFLAGS\n  --api-key=<value>  [env: PRIMITIVE_API_KEY] Primitive API key override\n                     (defaults to PRIMITIVE_API_KEY or saved OAuth login\n                     credentials)\n  --json             Print the full account JSON response. Default output hides\n                     setup and billing internals.\n  --time             Print the wall-clock duration of this command to stderr\n                     after it completes (e.g. `[time: 1.34s]`). Useful for\n                     measuring `--wait` send latency, comparing CLI overhead, or\n                     capturing timing in scripts.\n\nDESCRIPTION\n  Print the authenticated account (credentials smoke test)\n\n  Print the account currently authenticated by saved OAuth credentials or an\n  explicit API key. Useful as a credentials smoke test: confirms auth is live\n  and shows which account it belongs to.\n\n  The default output is a concise human summary. Pass --json only when a script\n  intentionally needs the full /account response.\n\nEXAMPLES\n  $ primitive whoami\n\n  $ primitive whoami --api-key prim_...\n\n  $ primitive whoami --json | jq .id\n\n"),
        _ => None,
    }
}
